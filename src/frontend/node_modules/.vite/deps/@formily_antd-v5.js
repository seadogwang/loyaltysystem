import {
  Keyframes_default,
  ResizeObserver_es_default,
  badge_default,
  button_default,
  card_default,
  cascader_default,
  checkbox_default,
  collapse_default,
  config_provider_default,
  date_picker_default,
  drawer_default,
  empty_default,
  input_default,
  input_number_default,
  message_default,
  modal_default,
  pagination_default,
  popover_default,
  radio_default,
  require_dayjs_min,
  select_default,
  space_default,
  steps_default,
  switch_default,
  table_default,
  tabs_default,
  tag_default,
  theme_default,
  time_picker_default,
  tooltip_default,
  transfer_default,
  tree_select_default,
  upload_default,
  useStyleRegister
} from "./chunk-4B57VB56.js";
import {
  CheckCircleOutlined_default,
  CloseCircleOutlined_default,
  ExclamationCircleOutlined_default,
  InboxOutlined_default,
  MenuOutlined_default,
  MessageOutlined_default,
  UploadOutlined_default
} from "./chunk-WR7ZMQY7.js";
import {
  CloseOutlined_default,
  CopyOutlined_default,
  DeleteOutlined_default,
  DownOutlined_default,
  EditOutlined_default,
  LoadingOutlined_default,
  PlusOutlined_default,
  QuestionCircleOutlined_default,
  UpOutlined_default,
  require_classnames
} from "./chunk-7CCXRJIP.js";
import "./chunk-WRG7CURK.js";
import {
  FormProvider,
  Observer,
  RecordScope,
  RecordsScope,
  RecursionField,
  connect,
  mapProps,
  mapReadPretty,
  observer,
  useField,
  useFieldSchema,
  useParentForm
} from "./chunk-XMBEV4LG.js";
import {
  require_react_dom
} from "./chunk-PJEEZAML.js";
import {
  action,
  applyMiddleware,
  batch,
  clone,
  createForm,
  define,
  isArr,
  isBool,
  isEmpty,
  isFn,
  isForm,
  isNum,
  isStr,
  isUndef,
  isValid,
  isVoidField,
  markRaw,
  merge,
  model,
  observable,
  onFormSubmitSuccess,
  reaction,
  toArr,
  toJS
} from "./chunk-6L5D6D5S.js";
import {
  require_react
} from "./chunk-DRWLMN53.js";
import {
  __toESM
} from "./chunk-G3PMV62Z.js";

// node_modules/@formily/antd-v5/esm/array-base/index.js
var import_classnames = __toESM(require_classnames());
var import_react12 = __toESM(require_react());

// node_modules/@formily/antd-v5/esm/__builtins__/dayjs.js
var import_dayjs = __toESM(require_dayjs_min());
function dayjsable(value, format) {
  if (!value)
    return value;
  if (Array.isArray(value)) {
    return value.map(function(val) {
      var date2 = (0, import_dayjs.default)(val, format);
      if (date2.isValid())
        return date2;
      var _date2 = (0, import_dayjs.default)(val);
      return _date2.isValid() ? _date2 : val;
    });
  } else {
    var date = (0, import_dayjs.default)(value, format);
    if (date.isValid())
      return date;
    var _date = (0, import_dayjs.default)(value);
    return _date.isValid() ? _date : value;
  }
}
var formatDayjsValue = function(value, format, placeholder) {
  var validFormatDate = function(date, format2) {
    if (typeof date === "number") {
      return (0, import_dayjs.default)(date).format(format2);
    }
    var _date = (0, import_dayjs.default)(date, format2);
    return _date.isValid() ? _date.format(format2) : date;
  };
  var formatDate = function(date, format2, i) {
    if (i === void 0) {
      i = 0;
    }
    if (!date)
      return placeholder;
    if (isArr(format2)) {
      var _format = format2[i];
      if (isFn(_format)) {
        return _format(date);
      }
      if (isEmpty(_format)) {
        return date;
      }
      return validFormatDate(date, _format);
    } else {
      if (isFn(format2)) {
        return format2(date);
      }
      if (isEmpty(format2)) {
        return date;
      }
      return validFormatDate(date, format2);
    }
  };
  if (isArr(value)) {
    return value.map(function(val, index) {
      return formatDate(val, format, index);
    });
  } else {
    return value ? formatDate(value, format) : value || placeholder;
  }
};

// node_modules/@formily/antd-v5/esm/__builtins__/hooks/useClickAway.js
var import_react = __toESM(require_react());
var defaultEvent = "click";
function getTargetElement(target, defaultElement) {
  if (!target) {
    return defaultElement;
  }
  var targetElement;
  if (typeof target === "function") {
    targetElement = target();
  } else if ("current" in target) {
    targetElement = target.current;
  } else {
    targetElement = target;
  }
  return targetElement;
}
var useClickAway = function(onClickAway, target, eventName) {
  if (eventName === void 0) {
    eventName = defaultEvent;
  }
  var onClickAwayRef = (0, import_react.useRef)(onClickAway);
  onClickAwayRef.current = onClickAway;
  (0, import_react.useEffect)(function() {
    var handler = function(event) {
      var targets = Array.isArray(target) ? target : [target];
      if (targets.some(function(targetItem) {
        var targetElement = getTargetElement(targetItem);
        return !targetElement || (targetElement === null || targetElement === void 0 ? void 0 : targetElement.contains(event.target));
      })) {
        return;
      }
      onClickAwayRef.current(event);
    };
    document.addEventListener(eventName, handler);
    return function() {
      document.removeEventListener(eventName, handler);
    };
  }, [target, eventName]);
};

// node_modules/@formily/antd-v5/esm/__builtins__/hooks/useConfig.js
var import_react2 = __toESM(require_react());
var ConfigContext = config_provider_default.ConfigContext;
var useConfig = function() {
  return (0, import_react2.useContext)(ConfigContext);
};

// node_modules/@formily/antd-v5/esm/__builtins__/hooks/usePrefixCls.js
var import_react3 = __toESM(require_react());
var usePrefixCls = function(tag, props) {
  var _a;
  if ("ConfigContext" in config_provider_default) {
    var getPrefixCls = (0, import_react3.useContext)(config_provider_default.ConfigContext).getPrefixCls;
    return getPrefixCls(tag, props === null || props === void 0 ? void 0 : props.prefixCls);
  } else {
    var prefix = (_a = props === null || props === void 0 ? void 0 : props.prefixCls) !== null && _a !== void 0 ? _a : "ant-";
    return "".concat(prefix).concat(tag !== null && tag !== void 0 ? tag : "");
  }
};

// node_modules/@formily/antd-v5/esm/__builtins__/hooks/useToken.js
var useToken = theme_default.useToken;

// node_modules/@formily/antd-v5/esm/__builtins__/loading.js
var __awaiter = function(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var __generator = function(thisArg, body) {
  var _ = { label: 0, sent: function() {
    if (t[0] & 1) throw t[1];
    return t[1];
  }, trys: [], ops: [] }, f, y, t, g;
  return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() {
    return this;
  }), g;
  function verb(n) {
    return function(v) {
      return step([n, v]);
    };
  }
  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");
    while (g && (g = 0, op[0] && (_ = 0)), _) try {
      if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
      if (y = 0, t) op = [op[0] & 2, t.value];
      switch (op[0]) {
        case 0:
        case 1:
          t = op;
          break;
        case 4:
          _.label++;
          return { value: op[1], done: false };
        case 5:
          _.label++;
          y = op[1];
          op = [0];
          continue;
        case 7:
          op = _.ops.pop();
          _.trys.pop();
          continue;
        default:
          if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
            _ = 0;
            continue;
          }
          if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
            _.label = op[1];
            break;
          }
          if (op[0] === 6 && _.label < t[1]) {
            _.label = t[1];
            t = op;
            break;
          }
          if (t && _.label < t[2]) {
            _.label = t[2];
            _.ops.push(op);
            break;
          }
          if (t[2]) _.ops.pop();
          _.trys.pop();
          continue;
      }
      op = body.call(thisArg, _);
    } catch (e) {
      op = [6, e];
      y = 0;
    } finally {
      f = t = 0;
    }
    if (op[0] & 5) throw op[1];
    return { value: op[0] ? op[1] : void 0, done: true };
  }
};
var loading = function(title, processor) {
  if (title === void 0) {
    title = "Loading...";
  }
  return __awaiter(void 0, void 0, void 0, function() {
    var hide, loading2;
    return __generator(this, function(_a) {
      switch (_a.label) {
        case 0:
          hide = null;
          loading2 = setTimeout(function() {
            hide = message_default.loading(title);
          }, 100);
          _a.label = 1;
        case 1:
          _a.trys.push([1, , 3, 4]);
          return [4, processor()];
        case 2:
          return [2, _a.sent()];
        case 3:
          hide === null || hide === void 0 ? void 0 : hide();
          clearTimeout(loading2);
          return [
            7
            /*endfinally*/
          ];
        case 4:
          return [
            2
            /*return*/
          ];
      }
    });
  });
};

// node_modules/@formily/antd-v5/esm/__builtins__/pickDataProps.js
var pickDataProps = function(props) {
  if (props === void 0) {
    props = {};
  }
  var results = {};
  for (var key2 in props) {
    if (key2.indexOf("data-") > -1) {
      results[key2] = props[key2];
    }
  }
  return results;
};

// node_modules/@formily/antd-v5/esm/__builtins__/portal.js
var import_react5 = __toESM(require_react());
var import_react_dom = __toESM(require_react_dom());

// node_modules/@formily/antd-v5/esm/__builtins__/render.js
var ReactDOM = __toESM(require_react_dom());
var __assign = function() {
  __assign = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign.apply(this, arguments);
};
var __awaiter2 = function(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var __generator2 = function(thisArg, body) {
  var _ = { label: 0, sent: function() {
    if (t[0] & 1) throw t[1];
    return t[1];
  }, trys: [], ops: [] }, f, y, t, g;
  return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() {
    return this;
  }), g;
  function verb(n) {
    return function(v) {
      return step([n, v]);
    };
  }
  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");
    while (g && (g = 0, op[0] && (_ = 0)), _) try {
      if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
      if (y = 0, t) op = [op[0] & 2, t.value];
      switch (op[0]) {
        case 0:
        case 1:
          t = op;
          break;
        case 4:
          _.label++;
          return { value: op[1], done: false };
        case 5:
          _.label++;
          y = op[1];
          op = [0];
          continue;
        case 7:
          op = _.ops.pop();
          _.trys.pop();
          continue;
        default:
          if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
            _ = 0;
            continue;
          }
          if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
            _.label = op[1];
            break;
          }
          if (op[0] === 6 && _.label < t[1]) {
            _.label = t[1];
            t = op;
            break;
          }
          if (t && _.label < t[2]) {
            _.label = t[2];
            _.ops.push(op);
            break;
          }
          if (t[2]) _.ops.pop();
          _.trys.pop();
          continue;
      }
      op = body.call(thisArg, _);
    } catch (e) {
      op = [6, e];
      y = 0;
    } finally {
      f = t = 0;
    }
    if (op[0] & 5) throw op[1];
    return { value: op[0] ? op[1] : void 0, done: true };
  }
};
var fullClone = __assign({}, ReactDOM);
var version = fullClone.version;
var reactRender = fullClone.render;
var unmountComponentAtNode = fullClone.unmountComponentAtNode;
var createRoot;
try {
  mainVersion = Number((version || "").split(".")[0]);
  if (mainVersion >= 18 && fullClone.createRoot) {
    createRoot = fullClone.createRoot;
  }
} catch (e) {
}
var mainVersion;
function toggleWarning(skip) {
  var __SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED = fullClone.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
  if (__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED && typeof __SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED === "object") {
    __SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED.usingClientEntryPoint = skip;
  }
}
var MARK = "__antd_mobile_root__";
function legacyRender(node, container) {
  reactRender(node, container);
}
function concurrentRender(node, container) {
  toggleWarning(true);
  var root = container[MARK] || createRoot(container);
  toggleWarning(false);
  root.render(node);
  container[MARK] = root;
}
function render(node, container) {
  if (createRoot) {
    concurrentRender(node, container);
    return;
  }
  legacyRender(node, container);
}
function legacyUnmount(container) {
  return unmountComponentAtNode(container);
}
function concurrentUnmount(container) {
  return __awaiter2(this, void 0, void 0, function() {
    return __generator2(this, function(_a) {
      return [2, Promise.resolve().then(function() {
        var _a2;
        (_a2 = container[MARK]) === null || _a2 === void 0 ? void 0 : _a2.unmount();
        delete container[MARK];
      })];
    });
  });
}
function unmount(container) {
  if (createRoot) {
    return concurrentUnmount(container);
  }
  return legacyUnmount(container);
}

// node_modules/@formily/antd-v5/esm/__builtins__/portal.js
var PortalMap = observable(/* @__PURE__ */ new Map());
var createPortalProvider = function(id) {
  var Portal = function(props) {
    var _a;
    var portalId = (_a = props.id) !== null && _a !== void 0 ? _a : id;
    if (portalId && !PortalMap.has(portalId)) {
      PortalMap.set(portalId, null);
    }
    return import_react5.default.createElement(
      import_react5.Fragment,
      null,
      props.children,
      import_react5.default.createElement(Observer, null, function() {
        if (portalId)
          return import_react5.default.createElement(import_react5.default.Fragment, null);
        var portal = PortalMap.get(portalId);
        if (portal)
          return (0, import_react_dom.createPortal)(portal, document.body);
        return import_react5.default.createElement(import_react5.default.Fragment, null);
      })
    );
  };
  return Portal;
};
function createPortalRoot(host, id) {
  function render2(renderer) {
    if (PortalMap.has(id)) {
      PortalMap.set(id, renderer === null || renderer === void 0 ? void 0 : renderer());
    } else if (host) {
      render(import_react5.default.createElement(import_react5.Fragment, null, renderer === null || renderer === void 0 ? void 0 : renderer()), host);
    }
  }
  function unmount2() {
    var _a;
    if (PortalMap.has(id)) {
      PortalMap.set(id, null);
    }
    if (host) {
      var unmountResult = unmount(host);
      if (unmountResult && host.parentNode) {
        (_a = host.parentNode) === null || _a === void 0 ? void 0 : _a.removeChild(host);
      }
    }
  }
  return {
    render: render2,
    unmount: unmount2
  };
}

// node_modules/@dnd-kit/core/dist/core.esm.js
var import_react8 = __toESM(require_react());
var import_react_dom2 = __toESM(require_react_dom());

// node_modules/@dnd-kit/utilities/dist/utilities.esm.js
var import_react6 = __toESM(require_react());
function useCombinedRefs() {
  for (var _len = arguments.length, refs = new Array(_len), _key = 0; _key < _len; _key++) {
    refs[_key] = arguments[_key];
  }
  return (0, import_react6.useMemo)(
    () => (node) => {
      refs.forEach((ref) => ref(node));
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    refs
  );
}
var canUseDOM = typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined";
function isWindow(element) {
  const elementString = Object.prototype.toString.call(element);
  return elementString === "[object Window]" || // In Electron context the Window object serializes to [object global]
  elementString === "[object global]";
}
function isNode(node) {
  return "nodeType" in node;
}
function getWindow(target) {
  var _target$ownerDocument, _target$ownerDocument2;
  if (!target) {
    return window;
  }
  if (isWindow(target)) {
    return target;
  }
  if (!isNode(target)) {
    return window;
  }
  return (_target$ownerDocument = (_target$ownerDocument2 = target.ownerDocument) == null ? void 0 : _target$ownerDocument2.defaultView) != null ? _target$ownerDocument : window;
}
function isDocument(node) {
  const {
    Document
  } = getWindow(node);
  return node instanceof Document;
}
function isHTMLElement(node) {
  if (isWindow(node)) {
    return false;
  }
  return node instanceof getWindow(node).HTMLElement;
}
function isSVGElement(node) {
  return node instanceof getWindow(node).SVGElement;
}
function getOwnerDocument(target) {
  if (!target) {
    return document;
  }
  if (isWindow(target)) {
    return target.document;
  }
  if (!isNode(target)) {
    return document;
  }
  if (isDocument(target)) {
    return target;
  }
  if (isHTMLElement(target) || isSVGElement(target)) {
    return target.ownerDocument;
  }
  return document;
}
var useIsomorphicLayoutEffect = canUseDOM ? import_react6.useLayoutEffect : import_react6.useEffect;
function useEvent(handler) {
  const handlerRef = (0, import_react6.useRef)(handler);
  useIsomorphicLayoutEffect(() => {
    handlerRef.current = handler;
  });
  return (0, import_react6.useCallback)(function() {
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    return handlerRef.current == null ? void 0 : handlerRef.current(...args);
  }, []);
}
function useInterval() {
  const intervalRef = (0, import_react6.useRef)(null);
  const set = (0, import_react6.useCallback)((listener, duration) => {
    intervalRef.current = setInterval(listener, duration);
  }, []);
  const clear = (0, import_react6.useCallback)(() => {
    if (intervalRef.current !== null) {
      clearInterval(intervalRef.current);
      intervalRef.current = null;
    }
  }, []);
  return [set, clear];
}
function useLatestValue(value, dependencies) {
  if (dependencies === void 0) {
    dependencies = [value];
  }
  const valueRef = (0, import_react6.useRef)(value);
  useIsomorphicLayoutEffect(() => {
    if (valueRef.current !== value) {
      valueRef.current = value;
    }
  }, dependencies);
  return valueRef;
}
function useLazyMemo(callback, dependencies) {
  const valueRef = (0, import_react6.useRef)();
  return (0, import_react6.useMemo)(
    () => {
      const newValue = callback(valueRef.current);
      valueRef.current = newValue;
      return newValue;
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [...dependencies]
  );
}
function useNodeRef(onChange) {
  const onChangeHandler = useEvent(onChange);
  const node = (0, import_react6.useRef)(null);
  const setNodeRef = (0, import_react6.useCallback)(
    (element) => {
      if (element !== node.current) {
        onChangeHandler == null ? void 0 : onChangeHandler(element, node.current);
      }
      node.current = element;
    },
    //eslint-disable-next-line
    []
  );
  return [node, setNodeRef];
}
function usePrevious(value) {
  const ref = (0, import_react6.useRef)();
  (0, import_react6.useEffect)(() => {
    ref.current = value;
  }, [value]);
  return ref.current;
}
var ids = {};
function useUniqueId(prefix, value) {
  return (0, import_react6.useMemo)(() => {
    if (value) {
      return value;
    }
    const id = ids[prefix] == null ? 0 : ids[prefix] + 1;
    ids[prefix] = id;
    return prefix + "-" + id;
  }, [prefix, value]);
}
function createAdjustmentFn(modifier) {
  return function(object) {
    for (var _len = arguments.length, adjustments = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      adjustments[_key - 1] = arguments[_key];
    }
    return adjustments.reduce((accumulator, adjustment) => {
      const entries = Object.entries(adjustment);
      for (const [key2, valueAdjustment] of entries) {
        const value = accumulator[key2];
        if (value != null) {
          accumulator[key2] = value + modifier * valueAdjustment;
        }
      }
      return accumulator;
    }, {
      ...object
    });
  };
}
var add = createAdjustmentFn(1);
var subtract = createAdjustmentFn(-1);
function hasViewportRelativeCoordinates(event) {
  return "clientX" in event && "clientY" in event;
}
function isKeyboardEvent(event) {
  if (!event) {
    return false;
  }
  const {
    KeyboardEvent
  } = getWindow(event.target);
  return KeyboardEvent && event instanceof KeyboardEvent;
}
function isTouchEvent(event) {
  if (!event) {
    return false;
  }
  const {
    TouchEvent
  } = getWindow(event.target);
  return TouchEvent && event instanceof TouchEvent;
}
function getEventCoordinates(event) {
  if (isTouchEvent(event)) {
    if (event.touches && event.touches.length) {
      const {
        clientX: x,
        clientY: y
      } = event.touches[0];
      return {
        x,
        y
      };
    } else if (event.changedTouches && event.changedTouches.length) {
      const {
        clientX: x,
        clientY: y
      } = event.changedTouches[0];
      return {
        x,
        y
      };
    }
  }
  if (hasViewportRelativeCoordinates(event)) {
    return {
      x: event.clientX,
      y: event.clientY
    };
  }
  return null;
}
var CSS2 = Object.freeze({
  Translate: {
    toString(transform) {
      if (!transform) {
        return;
      }
      const {
        x,
        y
      } = transform;
      return "translate3d(" + (x ? Math.round(x) : 0) + "px, " + (y ? Math.round(y) : 0) + "px, 0)";
    }
  },
  Scale: {
    toString(transform) {
      if (!transform) {
        return;
      }
      const {
        scaleX,
        scaleY
      } = transform;
      return "scaleX(" + scaleX + ") scaleY(" + scaleY + ")";
    }
  },
  Transform: {
    toString(transform) {
      if (!transform) {
        return;
      }
      return [CSS2.Translate.toString(transform), CSS2.Scale.toString(transform)].join(" ");
    }
  },
  Transition: {
    toString(_ref) {
      let {
        property,
        duration,
        easing
      } = _ref;
      return property + " " + duration + "ms " + easing;
    }
  }
});
var SELECTOR = "a,frame,iframe,input:not([type=hidden]):not(:disabled),select:not(:disabled),textarea:not(:disabled),button:not(:disabled),*[tabindex]";
function findFirstFocusableNode(element) {
  if (element.matches(SELECTOR)) {
    return element;
  }
  return element.querySelector(SELECTOR);
}

// node_modules/@dnd-kit/accessibility/dist/accessibility.esm.js
var import_react7 = __toESM(require_react());
var hiddenStyles = {
  display: "none"
};
function HiddenText(_ref) {
  let {
    id,
    value
  } = _ref;
  return import_react7.default.createElement("div", {
    id,
    style: hiddenStyles
  }, value);
}
function LiveRegion(_ref) {
  let {
    id,
    announcement,
    ariaLiveType = "assertive"
  } = _ref;
  const visuallyHidden = {
    position: "fixed",
    top: 0,
    left: 0,
    width: 1,
    height: 1,
    margin: -1,
    border: 0,
    padding: 0,
    overflow: "hidden",
    clip: "rect(0 0 0 0)",
    clipPath: "inset(100%)",
    whiteSpace: "nowrap"
  };
  return import_react7.default.createElement("div", {
    id,
    style: visuallyHidden,
    role: "status",
    "aria-live": ariaLiveType,
    "aria-atomic": true
  }, announcement);
}
function useAnnouncement() {
  const [announcement, setAnnouncement] = (0, import_react7.useState)("");
  const announce = (0, import_react7.useCallback)((value) => {
    if (value != null) {
      setAnnouncement(value);
    }
  }, []);
  return {
    announce,
    announcement
  };
}

// node_modules/@dnd-kit/core/dist/core.esm.js
var DndMonitorContext = (0, import_react8.createContext)(null);
function useDndMonitor(listener) {
  const registerListener = (0, import_react8.useContext)(DndMonitorContext);
  (0, import_react8.useEffect)(() => {
    if (!registerListener) {
      throw new Error("useDndMonitor must be used within a children of <DndContext>");
    }
    const unsubscribe = registerListener(listener);
    return unsubscribe;
  }, [listener, registerListener]);
}
function useDndMonitorProvider() {
  const [listeners] = (0, import_react8.useState)(() => /* @__PURE__ */ new Set());
  const registerListener = (0, import_react8.useCallback)((listener) => {
    listeners.add(listener);
    return () => listeners.delete(listener);
  }, [listeners]);
  const dispatch = (0, import_react8.useCallback)((_ref) => {
    let {
      type,
      event
    } = _ref;
    listeners.forEach((listener) => {
      var _listener$type;
      return (_listener$type = listener[type]) == null ? void 0 : _listener$type.call(listener, event);
    });
  }, [listeners]);
  return [dispatch, registerListener];
}
var defaultScreenReaderInstructions = {
  draggable: "\n    To pick up a draggable item, press the space bar.\n    While dragging, use the arrow keys to move the item.\n    Press space again to drop the item in its new position, or press escape to cancel.\n  "
};
var defaultAnnouncements = {
  onDragStart(_ref) {
    let {
      active
    } = _ref;
    return "Picked up draggable item " + active.id + ".";
  },
  onDragOver(_ref2) {
    let {
      active,
      over
    } = _ref2;
    if (over) {
      return "Draggable item " + active.id + " was moved over droppable area " + over.id + ".";
    }
    return "Draggable item " + active.id + " is no longer over a droppable area.";
  },
  onDragEnd(_ref3) {
    let {
      active,
      over
    } = _ref3;
    if (over) {
      return "Draggable item " + active.id + " was dropped over droppable area " + over.id;
    }
    return "Draggable item " + active.id + " was dropped.";
  },
  onDragCancel(_ref4) {
    let {
      active
    } = _ref4;
    return "Dragging was cancelled. Draggable item " + active.id + " was dropped.";
  }
};
function Accessibility(_ref) {
  let {
    announcements = defaultAnnouncements,
    container,
    hiddenTextDescribedById,
    screenReaderInstructions = defaultScreenReaderInstructions
  } = _ref;
  const {
    announce,
    announcement
  } = useAnnouncement();
  const liveRegionId = useUniqueId("DndLiveRegion");
  const [mounted, setMounted] = (0, import_react8.useState)(false);
  (0, import_react8.useEffect)(() => {
    setMounted(true);
  }, []);
  useDndMonitor((0, import_react8.useMemo)(() => ({
    onDragStart(_ref2) {
      let {
        active
      } = _ref2;
      announce(announcements.onDragStart({
        active
      }));
    },
    onDragMove(_ref3) {
      let {
        active,
        over
      } = _ref3;
      if (announcements.onDragMove) {
        announce(announcements.onDragMove({
          active,
          over
        }));
      }
    },
    onDragOver(_ref4) {
      let {
        active,
        over
      } = _ref4;
      announce(announcements.onDragOver({
        active,
        over
      }));
    },
    onDragEnd(_ref5) {
      let {
        active,
        over
      } = _ref5;
      announce(announcements.onDragEnd({
        active,
        over
      }));
    },
    onDragCancel(_ref6) {
      let {
        active,
        over
      } = _ref6;
      announce(announcements.onDragCancel({
        active,
        over
      }));
    }
  }), [announce, announcements]));
  if (!mounted) {
    return null;
  }
  const markup = import_react8.default.createElement(import_react8.default.Fragment, null, import_react8.default.createElement(HiddenText, {
    id: hiddenTextDescribedById,
    value: screenReaderInstructions.draggable
  }), import_react8.default.createElement(LiveRegion, {
    id: liveRegionId,
    announcement
  }));
  return container ? (0, import_react_dom2.createPortal)(markup, container) : markup;
}
var Action;
(function(Action2) {
  Action2["DragStart"] = "dragStart";
  Action2["DragMove"] = "dragMove";
  Action2["DragEnd"] = "dragEnd";
  Action2["DragCancel"] = "dragCancel";
  Action2["DragOver"] = "dragOver";
  Action2["RegisterDroppable"] = "registerDroppable";
  Action2["SetDroppableDisabled"] = "setDroppableDisabled";
  Action2["UnregisterDroppable"] = "unregisterDroppable";
})(Action || (Action = {}));
function noop() {
}
var defaultCoordinates = Object.freeze({
  x: 0,
  y: 0
});
function getRelativeTransformOrigin(event, rect) {
  const eventCoordinates = getEventCoordinates(event);
  if (!eventCoordinates) {
    return "0 0";
  }
  const transformOrigin = {
    x: (eventCoordinates.x - rect.left) / rect.width * 100,
    y: (eventCoordinates.y - rect.top) / rect.height * 100
  };
  return transformOrigin.x + "% " + transformOrigin.y + "%";
}
function sortCollisionsDesc(_ref3, _ref4) {
  let {
    data: {
      value: a
    }
  } = _ref3;
  let {
    data: {
      value: b
    }
  } = _ref4;
  return b - a;
}
function getFirstCollision(collisions, property) {
  if (!collisions || collisions.length === 0) {
    return null;
  }
  const [firstCollision] = collisions;
  return property ? firstCollision[property] : firstCollision;
}
function getIntersectionRatio(entry, target) {
  const top = Math.max(target.top, entry.top);
  const left = Math.max(target.left, entry.left);
  const right = Math.min(target.left + target.width, entry.left + entry.width);
  const bottom = Math.min(target.top + target.height, entry.top + entry.height);
  const width = right - left;
  const height = bottom - top;
  if (left < right && top < bottom) {
    const targetArea = target.width * target.height;
    const entryArea = entry.width * entry.height;
    const intersectionArea = width * height;
    const intersectionRatio = intersectionArea / (targetArea + entryArea - intersectionArea);
    return Number(intersectionRatio.toFixed(4));
  }
  return 0;
}
var rectIntersection = (_ref) => {
  let {
    collisionRect,
    droppableRects,
    droppableContainers
  } = _ref;
  const collisions = [];
  for (const droppableContainer of droppableContainers) {
    const {
      id
    } = droppableContainer;
    const rect = droppableRects.get(id);
    if (rect) {
      const intersectionRatio = getIntersectionRatio(rect, collisionRect);
      if (intersectionRatio > 0) {
        collisions.push({
          id,
          data: {
            droppableContainer,
            value: intersectionRatio
          }
        });
      }
    }
  }
  return collisions.sort(sortCollisionsDesc);
};
function adjustScale(transform, rect1, rect2) {
  return {
    ...transform,
    scaleX: rect1 && rect2 ? rect1.width / rect2.width : 1,
    scaleY: rect1 && rect2 ? rect1.height / rect2.height : 1
  };
}
function getRectDelta(rect1, rect2) {
  return rect1 && rect2 ? {
    x: rect1.left - rect2.left,
    y: rect1.top - rect2.top
  } : defaultCoordinates;
}
function createRectAdjustmentFn(modifier) {
  return function adjustClientRect(rect) {
    for (var _len = arguments.length, adjustments = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      adjustments[_key - 1] = arguments[_key];
    }
    return adjustments.reduce((acc, adjustment) => ({
      ...acc,
      top: acc.top + modifier * adjustment.y,
      bottom: acc.bottom + modifier * adjustment.y,
      left: acc.left + modifier * adjustment.x,
      right: acc.right + modifier * adjustment.x
    }), {
      ...rect
    });
  };
}
var getAdjustedRect = createRectAdjustmentFn(1);
function parseTransform(transform) {
  if (transform.startsWith("matrix3d(")) {
    const transformArray = transform.slice(9, -1).split(/, /);
    return {
      x: +transformArray[12],
      y: +transformArray[13],
      scaleX: +transformArray[0],
      scaleY: +transformArray[5]
    };
  } else if (transform.startsWith("matrix(")) {
    const transformArray = transform.slice(7, -1).split(/, /);
    return {
      x: +transformArray[4],
      y: +transformArray[5],
      scaleX: +transformArray[0],
      scaleY: +transformArray[3]
    };
  }
  return null;
}
function inverseTransform(rect, transform, transformOrigin) {
  const parsedTransform = parseTransform(transform);
  if (!parsedTransform) {
    return rect;
  }
  const {
    scaleX,
    scaleY,
    x: translateX,
    y: translateY
  } = parsedTransform;
  const x = rect.left - translateX - (1 - scaleX) * parseFloat(transformOrigin);
  const y = rect.top - translateY - (1 - scaleY) * parseFloat(transformOrigin.slice(transformOrigin.indexOf(" ") + 1));
  const w = scaleX ? rect.width / scaleX : rect.width;
  const h = scaleY ? rect.height / scaleY : rect.height;
  return {
    width: w,
    height: h,
    top: y,
    right: x + w,
    bottom: y + h,
    left: x
  };
}
var defaultOptions = {
  ignoreTransform: false
};
function getClientRect(element, options) {
  if (options === void 0) {
    options = defaultOptions;
  }
  let rect = element.getBoundingClientRect();
  if (options.ignoreTransform) {
    const {
      transform,
      transformOrigin
    } = getWindow(element).getComputedStyle(element);
    if (transform) {
      rect = inverseTransform(rect, transform, transformOrigin);
    }
  }
  const {
    top,
    left,
    width,
    height,
    bottom,
    right
  } = rect;
  return {
    top,
    left,
    width,
    height,
    bottom,
    right
  };
}
function getTransformAgnosticClientRect(element) {
  return getClientRect(element, {
    ignoreTransform: true
  });
}
function getWindowClientRect(element) {
  const width = element.innerWidth;
  const height = element.innerHeight;
  return {
    top: 0,
    left: 0,
    right: width,
    bottom: height,
    width,
    height
  };
}
function isFixed(node, computedStyle) {
  if (computedStyle === void 0) {
    computedStyle = getWindow(node).getComputedStyle(node);
  }
  return computedStyle.position === "fixed";
}
function isScrollable(element, computedStyle) {
  if (computedStyle === void 0) {
    computedStyle = getWindow(element).getComputedStyle(element);
  }
  const overflowRegex = /(auto|scroll|overlay)/;
  const properties2 = ["overflow", "overflowX", "overflowY"];
  return properties2.some((property) => {
    const value = computedStyle[property];
    return typeof value === "string" ? overflowRegex.test(value) : false;
  });
}
function getScrollableAncestors(element, limit) {
  const scrollParents = [];
  function findScrollableAncestors(node) {
    if (limit != null && scrollParents.length >= limit) {
      return scrollParents;
    }
    if (!node) {
      return scrollParents;
    }
    if (isDocument(node) && node.scrollingElement != null && !scrollParents.includes(node.scrollingElement)) {
      scrollParents.push(node.scrollingElement);
      return scrollParents;
    }
    if (!isHTMLElement(node) || isSVGElement(node)) {
      return scrollParents;
    }
    if (scrollParents.includes(node)) {
      return scrollParents;
    }
    const computedStyle = getWindow(element).getComputedStyle(node);
    if (node !== element) {
      if (isScrollable(node, computedStyle)) {
        scrollParents.push(node);
      }
    }
    if (isFixed(node, computedStyle)) {
      return scrollParents;
    }
    return findScrollableAncestors(node.parentNode);
  }
  if (!element) {
    return scrollParents;
  }
  return findScrollableAncestors(element);
}
function getFirstScrollableAncestor(node) {
  const [firstScrollableAncestor] = getScrollableAncestors(node, 1);
  return firstScrollableAncestor != null ? firstScrollableAncestor : null;
}
function getScrollableElement(element) {
  if (!canUseDOM || !element) {
    return null;
  }
  if (isWindow(element)) {
    return element;
  }
  if (!isNode(element)) {
    return null;
  }
  if (isDocument(element) || element === getOwnerDocument(element).scrollingElement) {
    return window;
  }
  if (isHTMLElement(element)) {
    return element;
  }
  return null;
}
function getScrollXCoordinate(element) {
  if (isWindow(element)) {
    return element.scrollX;
  }
  return element.scrollLeft;
}
function getScrollYCoordinate(element) {
  if (isWindow(element)) {
    return element.scrollY;
  }
  return element.scrollTop;
}
function getScrollCoordinates(element) {
  return {
    x: getScrollXCoordinate(element),
    y: getScrollYCoordinate(element)
  };
}
var Direction;
(function(Direction2) {
  Direction2[Direction2["Forward"] = 1] = "Forward";
  Direction2[Direction2["Backward"] = -1] = "Backward";
})(Direction || (Direction = {}));
function isDocumentScrollingElement(element) {
  if (!canUseDOM || !element) {
    return false;
  }
  return element === document.scrollingElement;
}
function getScrollPosition(scrollingContainer) {
  const minScroll = {
    x: 0,
    y: 0
  };
  const dimensions = isDocumentScrollingElement(scrollingContainer) ? {
    height: window.innerHeight,
    width: window.innerWidth
  } : {
    height: scrollingContainer.clientHeight,
    width: scrollingContainer.clientWidth
  };
  const maxScroll = {
    x: scrollingContainer.scrollWidth - dimensions.width,
    y: scrollingContainer.scrollHeight - dimensions.height
  };
  const isTop = scrollingContainer.scrollTop <= minScroll.y;
  const isLeft = scrollingContainer.scrollLeft <= minScroll.x;
  const isBottom = scrollingContainer.scrollTop >= maxScroll.y;
  const isRight = scrollingContainer.scrollLeft >= maxScroll.x;
  return {
    isTop,
    isLeft,
    isBottom,
    isRight,
    maxScroll,
    minScroll
  };
}
var defaultThreshold = {
  x: 0.2,
  y: 0.2
};
function getScrollDirectionAndSpeed(scrollContainer, scrollContainerRect, _ref, acceleration, thresholdPercentage) {
  let {
    top,
    left,
    right,
    bottom
  } = _ref;
  if (acceleration === void 0) {
    acceleration = 10;
  }
  if (thresholdPercentage === void 0) {
    thresholdPercentage = defaultThreshold;
  }
  const {
    isTop,
    isBottom,
    isLeft,
    isRight
  } = getScrollPosition(scrollContainer);
  const direction = {
    x: 0,
    y: 0
  };
  const speed = {
    x: 0,
    y: 0
  };
  const threshold = {
    height: scrollContainerRect.height * thresholdPercentage.y,
    width: scrollContainerRect.width * thresholdPercentage.x
  };
  if (!isTop && top <= scrollContainerRect.top + threshold.height) {
    direction.y = Direction.Backward;
    speed.y = acceleration * Math.abs((scrollContainerRect.top + threshold.height - top) / threshold.height);
  } else if (!isBottom && bottom >= scrollContainerRect.bottom - threshold.height) {
    direction.y = Direction.Forward;
    speed.y = acceleration * Math.abs((scrollContainerRect.bottom - threshold.height - bottom) / threshold.height);
  }
  if (!isRight && right >= scrollContainerRect.right - threshold.width) {
    direction.x = Direction.Forward;
    speed.x = acceleration * Math.abs((scrollContainerRect.right - threshold.width - right) / threshold.width);
  } else if (!isLeft && left <= scrollContainerRect.left + threshold.width) {
    direction.x = Direction.Backward;
    speed.x = acceleration * Math.abs((scrollContainerRect.left + threshold.width - left) / threshold.width);
  }
  return {
    direction,
    speed
  };
}
function getScrollElementRect(element) {
  if (element === document.scrollingElement) {
    const {
      innerWidth,
      innerHeight
    } = window;
    return {
      top: 0,
      left: 0,
      right: innerWidth,
      bottom: innerHeight,
      width: innerWidth,
      height: innerHeight
    };
  }
  const {
    top,
    left,
    right,
    bottom
  } = element.getBoundingClientRect();
  return {
    top,
    left,
    right,
    bottom,
    width: element.clientWidth,
    height: element.clientHeight
  };
}
function getScrollOffsets(scrollableAncestors) {
  return scrollableAncestors.reduce((acc, node) => {
    return add(acc, getScrollCoordinates(node));
  }, defaultCoordinates);
}
function getScrollXOffset(scrollableAncestors) {
  return scrollableAncestors.reduce((acc, node) => {
    return acc + getScrollXCoordinate(node);
  }, 0);
}
function getScrollYOffset(scrollableAncestors) {
  return scrollableAncestors.reduce((acc, node) => {
    return acc + getScrollYCoordinate(node);
  }, 0);
}
function scrollIntoViewIfNeeded(element, measure) {
  if (measure === void 0) {
    measure = getClientRect;
  }
  if (!element) {
    return;
  }
  const {
    top,
    left,
    bottom,
    right
  } = measure(element);
  const firstScrollableAncestor = getFirstScrollableAncestor(element);
  if (!firstScrollableAncestor) {
    return;
  }
  if (bottom <= 0 || right <= 0 || top >= window.innerHeight || left >= window.innerWidth) {
    element.scrollIntoView({
      block: "center",
      inline: "center"
    });
  }
}
var properties = [["x", ["left", "right"], getScrollXOffset], ["y", ["top", "bottom"], getScrollYOffset]];
var Rect = class {
  constructor(rect, element) {
    this.rect = void 0;
    this.width = void 0;
    this.height = void 0;
    this.top = void 0;
    this.bottom = void 0;
    this.right = void 0;
    this.left = void 0;
    const scrollableAncestors = getScrollableAncestors(element);
    const scrollOffsets = getScrollOffsets(scrollableAncestors);
    this.rect = {
      ...rect
    };
    this.width = rect.width;
    this.height = rect.height;
    for (const [axis, keys, getScrollOffset] of properties) {
      for (const key2 of keys) {
        Object.defineProperty(this, key2, {
          get: () => {
            const currentOffsets = getScrollOffset(scrollableAncestors);
            const scrollOffsetsDeltla = scrollOffsets[axis] - currentOffsets;
            return this.rect[key2] + scrollOffsetsDeltla;
          },
          enumerable: true
        });
      }
    }
    Object.defineProperty(this, "rect", {
      enumerable: false
    });
  }
};
var Listeners = class {
  constructor(target) {
    this.target = void 0;
    this.listeners = [];
    this.removeAll = () => {
      this.listeners.forEach((listener) => {
        var _this$target;
        return (_this$target = this.target) == null ? void 0 : _this$target.removeEventListener(...listener);
      });
    };
    this.target = target;
  }
  add(eventName, handler, options) {
    var _this$target2;
    (_this$target2 = this.target) == null ? void 0 : _this$target2.addEventListener(eventName, handler, options);
    this.listeners.push([eventName, handler, options]);
  }
};
function getEventListenerTarget(target) {
  const {
    EventTarget
  } = getWindow(target);
  return target instanceof EventTarget ? target : getOwnerDocument(target);
}
function hasExceededDistance(delta, measurement) {
  const dx = Math.abs(delta.x);
  const dy = Math.abs(delta.y);
  if (typeof measurement === "number") {
    return Math.sqrt(dx ** 2 + dy ** 2) > measurement;
  }
  if ("x" in measurement && "y" in measurement) {
    return dx > measurement.x && dy > measurement.y;
  }
  if ("x" in measurement) {
    return dx > measurement.x;
  }
  if ("y" in measurement) {
    return dy > measurement.y;
  }
  return false;
}
var EventName;
(function(EventName2) {
  EventName2["Click"] = "click";
  EventName2["DragStart"] = "dragstart";
  EventName2["Keydown"] = "keydown";
  EventName2["ContextMenu"] = "contextmenu";
  EventName2["Resize"] = "resize";
  EventName2["SelectionChange"] = "selectionchange";
  EventName2["VisibilityChange"] = "visibilitychange";
})(EventName || (EventName = {}));
function preventDefault(event) {
  event.preventDefault();
}
function stopPropagation(event) {
  event.stopPropagation();
}
var KeyboardCode;
(function(KeyboardCode2) {
  KeyboardCode2["Space"] = "Space";
  KeyboardCode2["Down"] = "ArrowDown";
  KeyboardCode2["Right"] = "ArrowRight";
  KeyboardCode2["Left"] = "ArrowLeft";
  KeyboardCode2["Up"] = "ArrowUp";
  KeyboardCode2["Esc"] = "Escape";
  KeyboardCode2["Enter"] = "Enter";
  KeyboardCode2["Tab"] = "Tab";
})(KeyboardCode || (KeyboardCode = {}));
var defaultKeyboardCodes = {
  start: [KeyboardCode.Space, KeyboardCode.Enter],
  cancel: [KeyboardCode.Esc],
  end: [KeyboardCode.Space, KeyboardCode.Enter, KeyboardCode.Tab]
};
var defaultKeyboardCoordinateGetter = (event, _ref) => {
  let {
    currentCoordinates
  } = _ref;
  switch (event.code) {
    case KeyboardCode.Right:
      return {
        ...currentCoordinates,
        x: currentCoordinates.x + 25
      };
    case KeyboardCode.Left:
      return {
        ...currentCoordinates,
        x: currentCoordinates.x - 25
      };
    case KeyboardCode.Down:
      return {
        ...currentCoordinates,
        y: currentCoordinates.y + 25
      };
    case KeyboardCode.Up:
      return {
        ...currentCoordinates,
        y: currentCoordinates.y - 25
      };
  }
  return void 0;
};
var KeyboardSensor = class {
  constructor(props) {
    this.props = void 0;
    this.autoScrollEnabled = false;
    this.referenceCoordinates = void 0;
    this.listeners = void 0;
    this.windowListeners = void 0;
    this.props = props;
    const {
      event: {
        target
      }
    } = props;
    this.props = props;
    this.listeners = new Listeners(getOwnerDocument(target));
    this.windowListeners = new Listeners(getWindow(target));
    this.handleKeyDown = this.handleKeyDown.bind(this);
    this.handleCancel = this.handleCancel.bind(this);
    this.attach();
  }
  attach() {
    this.handleStart();
    this.windowListeners.add(EventName.Resize, this.handleCancel);
    this.windowListeners.add(EventName.VisibilityChange, this.handleCancel);
    setTimeout(() => this.listeners.add(EventName.Keydown, this.handleKeyDown));
  }
  handleStart() {
    const {
      activeNode,
      onStart
    } = this.props;
    const node = activeNode.node.current;
    if (node) {
      scrollIntoViewIfNeeded(node);
    }
    onStart(defaultCoordinates);
  }
  handleKeyDown(event) {
    if (isKeyboardEvent(event)) {
      const {
        active,
        context,
        options
      } = this.props;
      const {
        keyboardCodes = defaultKeyboardCodes,
        coordinateGetter = defaultKeyboardCoordinateGetter,
        scrollBehavior = "smooth"
      } = options;
      const {
        code
      } = event;
      if (keyboardCodes.end.includes(code)) {
        this.handleEnd(event);
        return;
      }
      if (keyboardCodes.cancel.includes(code)) {
        this.handleCancel(event);
        return;
      }
      const {
        collisionRect
      } = context.current;
      const currentCoordinates = collisionRect ? {
        x: collisionRect.left,
        y: collisionRect.top
      } : defaultCoordinates;
      if (!this.referenceCoordinates) {
        this.referenceCoordinates = currentCoordinates;
      }
      const newCoordinates = coordinateGetter(event, {
        active,
        context: context.current,
        currentCoordinates
      });
      if (newCoordinates) {
        const coordinatesDelta = subtract(newCoordinates, currentCoordinates);
        const scrollDelta = {
          x: 0,
          y: 0
        };
        const {
          scrollableAncestors
        } = context.current;
        for (const scrollContainer of scrollableAncestors) {
          const direction = event.code;
          const {
            isTop,
            isRight,
            isLeft,
            isBottom,
            maxScroll,
            minScroll
          } = getScrollPosition(scrollContainer);
          const scrollElementRect = getScrollElementRect(scrollContainer);
          const clampedCoordinates = {
            x: Math.min(direction === KeyboardCode.Right ? scrollElementRect.right - scrollElementRect.width / 2 : scrollElementRect.right, Math.max(direction === KeyboardCode.Right ? scrollElementRect.left : scrollElementRect.left + scrollElementRect.width / 2, newCoordinates.x)),
            y: Math.min(direction === KeyboardCode.Down ? scrollElementRect.bottom - scrollElementRect.height / 2 : scrollElementRect.bottom, Math.max(direction === KeyboardCode.Down ? scrollElementRect.top : scrollElementRect.top + scrollElementRect.height / 2, newCoordinates.y))
          };
          const canScrollX = direction === KeyboardCode.Right && !isRight || direction === KeyboardCode.Left && !isLeft;
          const canScrollY = direction === KeyboardCode.Down && !isBottom || direction === KeyboardCode.Up && !isTop;
          if (canScrollX && clampedCoordinates.x !== newCoordinates.x) {
            const newScrollCoordinates = scrollContainer.scrollLeft + coordinatesDelta.x;
            const canScrollToNewCoordinates = direction === KeyboardCode.Right && newScrollCoordinates <= maxScroll.x || direction === KeyboardCode.Left && newScrollCoordinates >= minScroll.x;
            if (canScrollToNewCoordinates && !coordinatesDelta.y) {
              scrollContainer.scrollTo({
                left: newScrollCoordinates,
                behavior: scrollBehavior
              });
              return;
            }
            if (canScrollToNewCoordinates) {
              scrollDelta.x = scrollContainer.scrollLeft - newScrollCoordinates;
            } else {
              scrollDelta.x = direction === KeyboardCode.Right ? scrollContainer.scrollLeft - maxScroll.x : scrollContainer.scrollLeft - minScroll.x;
            }
            if (scrollDelta.x) {
              scrollContainer.scrollBy({
                left: -scrollDelta.x,
                behavior: scrollBehavior
              });
            }
            break;
          } else if (canScrollY && clampedCoordinates.y !== newCoordinates.y) {
            const newScrollCoordinates = scrollContainer.scrollTop + coordinatesDelta.y;
            const canScrollToNewCoordinates = direction === KeyboardCode.Down && newScrollCoordinates <= maxScroll.y || direction === KeyboardCode.Up && newScrollCoordinates >= minScroll.y;
            if (canScrollToNewCoordinates && !coordinatesDelta.x) {
              scrollContainer.scrollTo({
                top: newScrollCoordinates,
                behavior: scrollBehavior
              });
              return;
            }
            if (canScrollToNewCoordinates) {
              scrollDelta.y = scrollContainer.scrollTop - newScrollCoordinates;
            } else {
              scrollDelta.y = direction === KeyboardCode.Down ? scrollContainer.scrollTop - maxScroll.y : scrollContainer.scrollTop - minScroll.y;
            }
            if (scrollDelta.y) {
              scrollContainer.scrollBy({
                top: -scrollDelta.y,
                behavior: scrollBehavior
              });
            }
            break;
          }
        }
        this.handleMove(event, add(subtract(newCoordinates, this.referenceCoordinates), scrollDelta));
      }
    }
  }
  handleMove(event, coordinates) {
    const {
      onMove
    } = this.props;
    event.preventDefault();
    onMove(coordinates);
  }
  handleEnd(event) {
    const {
      onEnd
    } = this.props;
    event.preventDefault();
    this.detach();
    onEnd();
  }
  handleCancel(event) {
    const {
      onCancel
    } = this.props;
    event.preventDefault();
    this.detach();
    onCancel();
  }
  detach() {
    this.listeners.removeAll();
    this.windowListeners.removeAll();
  }
};
KeyboardSensor.activators = [{
  eventName: "onKeyDown",
  handler: (event, _ref, _ref2) => {
    let {
      keyboardCodes = defaultKeyboardCodes,
      onActivation
    } = _ref;
    let {
      active
    } = _ref2;
    const {
      code
    } = event.nativeEvent;
    if (keyboardCodes.start.includes(code)) {
      const activator = active.activatorNode.current;
      if (activator && event.target !== activator) {
        return false;
      }
      event.preventDefault();
      onActivation == null ? void 0 : onActivation({
        event: event.nativeEvent
      });
      return true;
    }
    return false;
  }
}];
function isDistanceConstraint(constraint) {
  return Boolean(constraint && "distance" in constraint);
}
function isDelayConstraint(constraint) {
  return Boolean(constraint && "delay" in constraint);
}
var AbstractPointerSensor = class {
  constructor(props, events3, listenerTarget) {
    var _getEventCoordinates;
    if (listenerTarget === void 0) {
      listenerTarget = getEventListenerTarget(props.event.target);
    }
    this.props = void 0;
    this.events = void 0;
    this.autoScrollEnabled = true;
    this.document = void 0;
    this.activated = false;
    this.initialCoordinates = void 0;
    this.timeoutId = null;
    this.listeners = void 0;
    this.documentListeners = void 0;
    this.windowListeners = void 0;
    this.props = props;
    this.events = events3;
    const {
      event
    } = props;
    const {
      target
    } = event;
    this.props = props;
    this.events = events3;
    this.document = getOwnerDocument(target);
    this.documentListeners = new Listeners(this.document);
    this.listeners = new Listeners(listenerTarget);
    this.windowListeners = new Listeners(getWindow(target));
    this.initialCoordinates = (_getEventCoordinates = getEventCoordinates(event)) != null ? _getEventCoordinates : defaultCoordinates;
    this.handleStart = this.handleStart.bind(this);
    this.handleMove = this.handleMove.bind(this);
    this.handleEnd = this.handleEnd.bind(this);
    this.handleCancel = this.handleCancel.bind(this);
    this.handleKeydown = this.handleKeydown.bind(this);
    this.removeTextSelection = this.removeTextSelection.bind(this);
    this.attach();
  }
  attach() {
    const {
      events: events3,
      props: {
        options: {
          activationConstraint,
          bypassActivationConstraint
        }
      }
    } = this;
    this.listeners.add(events3.move.name, this.handleMove, {
      passive: false
    });
    this.listeners.add(events3.end.name, this.handleEnd);
    if (events3.cancel) {
      this.listeners.add(events3.cancel.name, this.handleCancel);
    }
    this.windowListeners.add(EventName.Resize, this.handleCancel);
    this.windowListeners.add(EventName.DragStart, preventDefault);
    this.windowListeners.add(EventName.VisibilityChange, this.handleCancel);
    this.windowListeners.add(EventName.ContextMenu, preventDefault);
    this.documentListeners.add(EventName.Keydown, this.handleKeydown);
    if (activationConstraint) {
      if (bypassActivationConstraint != null && bypassActivationConstraint({
        event: this.props.event,
        activeNode: this.props.activeNode,
        options: this.props.options
      })) {
        return this.handleStart();
      }
      if (isDelayConstraint(activationConstraint)) {
        this.timeoutId = setTimeout(this.handleStart, activationConstraint.delay);
        this.handlePending(activationConstraint);
        return;
      }
      if (isDistanceConstraint(activationConstraint)) {
        this.handlePending(activationConstraint);
        return;
      }
    }
    this.handleStart();
  }
  detach() {
    this.listeners.removeAll();
    this.windowListeners.removeAll();
    setTimeout(this.documentListeners.removeAll, 50);
    if (this.timeoutId !== null) {
      clearTimeout(this.timeoutId);
      this.timeoutId = null;
    }
  }
  handlePending(constraint, offset) {
    const {
      active,
      onPending
    } = this.props;
    onPending(active, constraint, this.initialCoordinates, offset);
  }
  handleStart() {
    const {
      initialCoordinates
    } = this;
    const {
      onStart
    } = this.props;
    if (initialCoordinates) {
      this.activated = true;
      this.documentListeners.add(EventName.Click, stopPropagation, {
        capture: true
      });
      this.removeTextSelection();
      this.documentListeners.add(EventName.SelectionChange, this.removeTextSelection);
      onStart(initialCoordinates);
    }
  }
  handleMove(event) {
    var _getEventCoordinates2;
    const {
      activated,
      initialCoordinates,
      props
    } = this;
    const {
      onMove,
      options: {
        activationConstraint
      }
    } = props;
    if (!initialCoordinates) {
      return;
    }
    const coordinates = (_getEventCoordinates2 = getEventCoordinates(event)) != null ? _getEventCoordinates2 : defaultCoordinates;
    const delta = subtract(initialCoordinates, coordinates);
    if (!activated && activationConstraint) {
      if (isDistanceConstraint(activationConstraint)) {
        if (activationConstraint.tolerance != null && hasExceededDistance(delta, activationConstraint.tolerance)) {
          return this.handleCancel();
        }
        if (hasExceededDistance(delta, activationConstraint.distance)) {
          return this.handleStart();
        }
      }
      if (isDelayConstraint(activationConstraint)) {
        if (hasExceededDistance(delta, activationConstraint.tolerance)) {
          return this.handleCancel();
        }
      }
      this.handlePending(activationConstraint, delta);
      return;
    }
    if (event.cancelable) {
      event.preventDefault();
    }
    onMove(coordinates);
  }
  handleEnd() {
    const {
      onAbort,
      onEnd
    } = this.props;
    this.detach();
    if (!this.activated) {
      onAbort(this.props.active);
    }
    onEnd();
  }
  handleCancel() {
    const {
      onAbort,
      onCancel
    } = this.props;
    this.detach();
    if (!this.activated) {
      onAbort(this.props.active);
    }
    onCancel();
  }
  handleKeydown(event) {
    if (event.code === KeyboardCode.Esc) {
      this.handleCancel();
    }
  }
  removeTextSelection() {
    var _this$document$getSel;
    (_this$document$getSel = this.document.getSelection()) == null ? void 0 : _this$document$getSel.removeAllRanges();
  }
};
var events = {
  cancel: {
    name: "pointercancel"
  },
  move: {
    name: "pointermove"
  },
  end: {
    name: "pointerup"
  }
};
var PointerSensor = class extends AbstractPointerSensor {
  constructor(props) {
    const {
      event
    } = props;
    const listenerTarget = getOwnerDocument(event.target);
    super(props, events, listenerTarget);
  }
};
PointerSensor.activators = [{
  eventName: "onPointerDown",
  handler: (_ref, _ref2) => {
    let {
      nativeEvent: event
    } = _ref;
    let {
      onActivation
    } = _ref2;
    if (!event.isPrimary || event.button !== 0) {
      return false;
    }
    onActivation == null ? void 0 : onActivation({
      event
    });
    return true;
  }
}];
var events$1 = {
  move: {
    name: "mousemove"
  },
  end: {
    name: "mouseup"
  }
};
var MouseButton;
(function(MouseButton2) {
  MouseButton2[MouseButton2["RightClick"] = 2] = "RightClick";
})(MouseButton || (MouseButton = {}));
var MouseSensor = class extends AbstractPointerSensor {
  constructor(props) {
    super(props, events$1, getOwnerDocument(props.event.target));
  }
};
MouseSensor.activators = [{
  eventName: "onMouseDown",
  handler: (_ref, _ref2) => {
    let {
      nativeEvent: event
    } = _ref;
    let {
      onActivation
    } = _ref2;
    if (event.button === MouseButton.RightClick) {
      return false;
    }
    onActivation == null ? void 0 : onActivation({
      event
    });
    return true;
  }
}];
var events$2 = {
  cancel: {
    name: "touchcancel"
  },
  move: {
    name: "touchmove"
  },
  end: {
    name: "touchend"
  }
};
var TouchSensor = class extends AbstractPointerSensor {
  constructor(props) {
    super(props, events$2);
  }
  static setup() {
    window.addEventListener(events$2.move.name, noop2, {
      capture: false,
      passive: false
    });
    return function teardown() {
      window.removeEventListener(events$2.move.name, noop2);
    };
    function noop2() {
    }
  }
};
TouchSensor.activators = [{
  eventName: "onTouchStart",
  handler: (_ref, _ref2) => {
    let {
      nativeEvent: event
    } = _ref;
    let {
      onActivation
    } = _ref2;
    const {
      touches
    } = event;
    if (touches.length > 1) {
      return false;
    }
    onActivation == null ? void 0 : onActivation({
      event
    });
    return true;
  }
}];
var AutoScrollActivator;
(function(AutoScrollActivator2) {
  AutoScrollActivator2[AutoScrollActivator2["Pointer"] = 0] = "Pointer";
  AutoScrollActivator2[AutoScrollActivator2["DraggableRect"] = 1] = "DraggableRect";
})(AutoScrollActivator || (AutoScrollActivator = {}));
var TraversalOrder;
(function(TraversalOrder2) {
  TraversalOrder2[TraversalOrder2["TreeOrder"] = 0] = "TreeOrder";
  TraversalOrder2[TraversalOrder2["ReversedTreeOrder"] = 1] = "ReversedTreeOrder";
})(TraversalOrder || (TraversalOrder = {}));
function useAutoScroller(_ref) {
  let {
    acceleration,
    activator = AutoScrollActivator.Pointer,
    canScroll,
    draggingRect,
    enabled,
    interval = 5,
    order = TraversalOrder.TreeOrder,
    pointerCoordinates,
    scrollableAncestors,
    scrollableAncestorRects,
    delta,
    threshold
  } = _ref;
  const scrollIntent = useScrollIntent({
    delta,
    disabled: !enabled
  });
  const [setAutoScrollInterval, clearAutoScrollInterval] = useInterval();
  const scrollSpeed = (0, import_react8.useRef)({
    x: 0,
    y: 0
  });
  const scrollDirection = (0, import_react8.useRef)({
    x: 0,
    y: 0
  });
  const rect = (0, import_react8.useMemo)(() => {
    switch (activator) {
      case AutoScrollActivator.Pointer:
        return pointerCoordinates ? {
          top: pointerCoordinates.y,
          bottom: pointerCoordinates.y,
          left: pointerCoordinates.x,
          right: pointerCoordinates.x
        } : null;
      case AutoScrollActivator.DraggableRect:
        return draggingRect;
    }
  }, [activator, draggingRect, pointerCoordinates]);
  const scrollContainerRef = (0, import_react8.useRef)(null);
  const autoScroll = (0, import_react8.useCallback)(() => {
    const scrollContainer = scrollContainerRef.current;
    if (!scrollContainer) {
      return;
    }
    const scrollLeft = scrollSpeed.current.x * scrollDirection.current.x;
    const scrollTop = scrollSpeed.current.y * scrollDirection.current.y;
    scrollContainer.scrollBy(scrollLeft, scrollTop);
  }, []);
  const sortedScrollableAncestors = (0, import_react8.useMemo)(() => order === TraversalOrder.TreeOrder ? [...scrollableAncestors].reverse() : scrollableAncestors, [order, scrollableAncestors]);
  (0, import_react8.useEffect)(
    () => {
      if (!enabled || !scrollableAncestors.length || !rect) {
        clearAutoScrollInterval();
        return;
      }
      for (const scrollContainer of sortedScrollableAncestors) {
        if ((canScroll == null ? void 0 : canScroll(scrollContainer)) === false) {
          continue;
        }
        const index = scrollableAncestors.indexOf(scrollContainer);
        const scrollContainerRect = scrollableAncestorRects[index];
        if (!scrollContainerRect) {
          continue;
        }
        const {
          direction,
          speed
        } = getScrollDirectionAndSpeed(scrollContainer, scrollContainerRect, rect, acceleration, threshold);
        for (const axis of ["x", "y"]) {
          if (!scrollIntent[axis][direction[axis]]) {
            speed[axis] = 0;
            direction[axis] = 0;
          }
        }
        if (speed.x > 0 || speed.y > 0) {
          clearAutoScrollInterval();
          scrollContainerRef.current = scrollContainer;
          setAutoScrollInterval(autoScroll, interval);
          scrollSpeed.current = speed;
          scrollDirection.current = direction;
          return;
        }
      }
      scrollSpeed.current = {
        x: 0,
        y: 0
      };
      scrollDirection.current = {
        x: 0,
        y: 0
      };
      clearAutoScrollInterval();
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [
      acceleration,
      autoScroll,
      canScroll,
      clearAutoScrollInterval,
      enabled,
      interval,
      // eslint-disable-next-line react-hooks/exhaustive-deps
      JSON.stringify(rect),
      // eslint-disable-next-line react-hooks/exhaustive-deps
      JSON.stringify(scrollIntent),
      setAutoScrollInterval,
      scrollableAncestors,
      sortedScrollableAncestors,
      scrollableAncestorRects,
      // eslint-disable-next-line react-hooks/exhaustive-deps
      JSON.stringify(threshold)
    ]
  );
}
var defaultScrollIntent = {
  x: {
    [Direction.Backward]: false,
    [Direction.Forward]: false
  },
  y: {
    [Direction.Backward]: false,
    [Direction.Forward]: false
  }
};
function useScrollIntent(_ref2) {
  let {
    delta,
    disabled
  } = _ref2;
  const previousDelta = usePrevious(delta);
  return useLazyMemo((previousIntent) => {
    if (disabled || !previousDelta || !previousIntent) {
      return defaultScrollIntent;
    }
    const direction = {
      x: Math.sign(delta.x - previousDelta.x),
      y: Math.sign(delta.y - previousDelta.y)
    };
    return {
      x: {
        [Direction.Backward]: previousIntent.x[Direction.Backward] || direction.x === -1,
        [Direction.Forward]: previousIntent.x[Direction.Forward] || direction.x === 1
      },
      y: {
        [Direction.Backward]: previousIntent.y[Direction.Backward] || direction.y === -1,
        [Direction.Forward]: previousIntent.y[Direction.Forward] || direction.y === 1
      }
    };
  }, [disabled, delta, previousDelta]);
}
function useCachedNode(draggableNodes, id) {
  const draggableNode = id != null ? draggableNodes.get(id) : void 0;
  const node = draggableNode ? draggableNode.node.current : null;
  return useLazyMemo((cachedNode) => {
    var _ref;
    if (id == null) {
      return null;
    }
    return (_ref = node != null ? node : cachedNode) != null ? _ref : null;
  }, [node, id]);
}
function useCombineActivators(sensors, getSyntheticHandler) {
  return (0, import_react8.useMemo)(() => sensors.reduce((accumulator, sensor) => {
    const {
      sensor: Sensor
    } = sensor;
    const sensorActivators = Sensor.activators.map((activator) => ({
      eventName: activator.eventName,
      handler: getSyntheticHandler(activator.handler, sensor)
    }));
    return [...accumulator, ...sensorActivators];
  }, []), [sensors, getSyntheticHandler]);
}
var MeasuringStrategy;
(function(MeasuringStrategy2) {
  MeasuringStrategy2[MeasuringStrategy2["Always"] = 0] = "Always";
  MeasuringStrategy2[MeasuringStrategy2["BeforeDragging"] = 1] = "BeforeDragging";
  MeasuringStrategy2[MeasuringStrategy2["WhileDragging"] = 2] = "WhileDragging";
})(MeasuringStrategy || (MeasuringStrategy = {}));
var MeasuringFrequency;
(function(MeasuringFrequency2) {
  MeasuringFrequency2["Optimized"] = "optimized";
})(MeasuringFrequency || (MeasuringFrequency = {}));
var defaultValue = /* @__PURE__ */ new Map();
function useDroppableMeasuring(containers, _ref) {
  let {
    dragging,
    dependencies,
    config
  } = _ref;
  const [queue, setQueue] = (0, import_react8.useState)(null);
  const {
    frequency,
    measure,
    strategy
  } = config;
  const containersRef = (0, import_react8.useRef)(containers);
  const disabled = isDisabled();
  const disabledRef = useLatestValue(disabled);
  const measureDroppableContainers = (0, import_react8.useCallback)(function(ids2) {
    if (ids2 === void 0) {
      ids2 = [];
    }
    if (disabledRef.current) {
      return;
    }
    setQueue((value) => {
      if (value === null) {
        return ids2;
      }
      return value.concat(ids2.filter((id) => !value.includes(id)));
    });
  }, [disabledRef]);
  const timeoutId = (0, import_react8.useRef)(null);
  const droppableRects = useLazyMemo((previousValue) => {
    if (disabled && !dragging) {
      return defaultValue;
    }
    if (!previousValue || previousValue === defaultValue || containersRef.current !== containers || queue != null) {
      const map = /* @__PURE__ */ new Map();
      for (let container of containers) {
        if (!container) {
          continue;
        }
        if (queue && queue.length > 0 && !queue.includes(container.id) && container.rect.current) {
          map.set(container.id, container.rect.current);
          continue;
        }
        const node = container.node.current;
        const rect = node ? new Rect(measure(node), node) : null;
        container.rect.current = rect;
        if (rect) {
          map.set(container.id, rect);
        }
      }
      return map;
    }
    return previousValue;
  }, [containers, queue, dragging, disabled, measure]);
  (0, import_react8.useEffect)(() => {
    containersRef.current = containers;
  }, [containers]);
  (0, import_react8.useEffect)(
    () => {
      if (disabled) {
        return;
      }
      measureDroppableContainers();
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [dragging, disabled]
  );
  (0, import_react8.useEffect)(
    () => {
      if (queue && queue.length > 0) {
        setQueue(null);
      }
    },
    //eslint-disable-next-line react-hooks/exhaustive-deps
    [JSON.stringify(queue)]
  );
  (0, import_react8.useEffect)(
    () => {
      if (disabled || typeof frequency !== "number" || timeoutId.current !== null) {
        return;
      }
      timeoutId.current = setTimeout(() => {
        measureDroppableContainers();
        timeoutId.current = null;
      }, frequency);
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [frequency, disabled, measureDroppableContainers, ...dependencies]
  );
  return {
    droppableRects,
    measureDroppableContainers,
    measuringScheduled: queue != null
  };
  function isDisabled() {
    switch (strategy) {
      case MeasuringStrategy.Always:
        return false;
      case MeasuringStrategy.BeforeDragging:
        return dragging;
      default:
        return !dragging;
    }
  }
}
function useInitialValue(value, computeFn) {
  return useLazyMemo((previousValue) => {
    if (!value) {
      return null;
    }
    if (previousValue) {
      return previousValue;
    }
    return typeof computeFn === "function" ? computeFn(value) : value;
  }, [computeFn, value]);
}
function useInitialRect(node, measure) {
  return useInitialValue(node, measure);
}
function useMutationObserver(_ref) {
  let {
    callback,
    disabled
  } = _ref;
  const handleMutations = useEvent(callback);
  const mutationObserver = (0, import_react8.useMemo)(() => {
    if (disabled || typeof window === "undefined" || typeof window.MutationObserver === "undefined") {
      return void 0;
    }
    const {
      MutationObserver: MutationObserver2
    } = window;
    return new MutationObserver2(handleMutations);
  }, [handleMutations, disabled]);
  (0, import_react8.useEffect)(() => {
    return () => mutationObserver == null ? void 0 : mutationObserver.disconnect();
  }, [mutationObserver]);
  return mutationObserver;
}
function useResizeObserver(_ref) {
  let {
    callback,
    disabled
  } = _ref;
  const handleResize = useEvent(callback);
  const resizeObserver = (0, import_react8.useMemo)(
    () => {
      if (disabled || typeof window === "undefined" || typeof window.ResizeObserver === "undefined") {
        return void 0;
      }
      const {
        ResizeObserver: ResizeObserver3
      } = window;
      return new ResizeObserver3(handleResize);
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [disabled]
  );
  (0, import_react8.useEffect)(() => {
    return () => resizeObserver == null ? void 0 : resizeObserver.disconnect();
  }, [resizeObserver]);
  return resizeObserver;
}
function defaultMeasure(element) {
  return new Rect(getClientRect(element), element);
}
function useRect(element, measure, fallbackRect) {
  if (measure === void 0) {
    measure = defaultMeasure;
  }
  const [rect, setRect] = (0, import_react8.useState)(null);
  function measureRect() {
    setRect((currentRect) => {
      if (!element) {
        return null;
      }
      if (element.isConnected === false) {
        var _ref;
        return (_ref = currentRect != null ? currentRect : fallbackRect) != null ? _ref : null;
      }
      const newRect = measure(element);
      if (JSON.stringify(currentRect) === JSON.stringify(newRect)) {
        return currentRect;
      }
      return newRect;
    });
  }
  const mutationObserver = useMutationObserver({
    callback(records) {
      if (!element) {
        return;
      }
      for (const record of records) {
        const {
          type,
          target
        } = record;
        if (type === "childList" && target instanceof HTMLElement && target.contains(element)) {
          measureRect();
          break;
        }
      }
    }
  });
  const resizeObserver = useResizeObserver({
    callback: measureRect
  });
  useIsomorphicLayoutEffect(() => {
    measureRect();
    if (element) {
      resizeObserver == null ? void 0 : resizeObserver.observe(element);
      mutationObserver == null ? void 0 : mutationObserver.observe(document.body, {
        childList: true,
        subtree: true
      });
    } else {
      resizeObserver == null ? void 0 : resizeObserver.disconnect();
      mutationObserver == null ? void 0 : mutationObserver.disconnect();
    }
  }, [element]);
  return rect;
}
function useRectDelta(rect) {
  const initialRect = useInitialValue(rect);
  return getRectDelta(rect, initialRect);
}
var defaultValue$1 = [];
function useScrollableAncestors(node) {
  const previousNode = (0, import_react8.useRef)(node);
  const ancestors = useLazyMemo((previousValue) => {
    if (!node) {
      return defaultValue$1;
    }
    if (previousValue && previousValue !== defaultValue$1 && node && previousNode.current && node.parentNode === previousNode.current.parentNode) {
      return previousValue;
    }
    return getScrollableAncestors(node);
  }, [node]);
  (0, import_react8.useEffect)(() => {
    previousNode.current = node;
  }, [node]);
  return ancestors;
}
function useScrollOffsets(elements) {
  const [scrollCoordinates, setScrollCoordinates] = (0, import_react8.useState)(null);
  const prevElements = (0, import_react8.useRef)(elements);
  const handleScroll = (0, import_react8.useCallback)((event) => {
    const scrollingElement = getScrollableElement(event.target);
    if (!scrollingElement) {
      return;
    }
    setScrollCoordinates((scrollCoordinates2) => {
      if (!scrollCoordinates2) {
        return null;
      }
      scrollCoordinates2.set(scrollingElement, getScrollCoordinates(scrollingElement));
      return new Map(scrollCoordinates2);
    });
  }, []);
  (0, import_react8.useEffect)(() => {
    const previousElements = prevElements.current;
    if (elements !== previousElements) {
      cleanup(previousElements);
      const entries = elements.map((element) => {
        const scrollableElement = getScrollableElement(element);
        if (scrollableElement) {
          scrollableElement.addEventListener("scroll", handleScroll, {
            passive: true
          });
          return [scrollableElement, getScrollCoordinates(scrollableElement)];
        }
        return null;
      }).filter((entry) => entry != null);
      setScrollCoordinates(entries.length ? new Map(entries) : null);
      prevElements.current = elements;
    }
    return () => {
      cleanup(elements);
      cleanup(previousElements);
    };
    function cleanup(elements2) {
      elements2.forEach((element) => {
        const scrollableElement = getScrollableElement(element);
        scrollableElement == null ? void 0 : scrollableElement.removeEventListener("scroll", handleScroll);
      });
    }
  }, [handleScroll, elements]);
  return (0, import_react8.useMemo)(() => {
    if (elements.length) {
      return scrollCoordinates ? Array.from(scrollCoordinates.values()).reduce((acc, coordinates) => add(acc, coordinates), defaultCoordinates) : getScrollOffsets(elements);
    }
    return defaultCoordinates;
  }, [elements, scrollCoordinates]);
}
function useScrollOffsetsDelta(scrollOffsets, dependencies) {
  if (dependencies === void 0) {
    dependencies = [];
  }
  const initialScrollOffsets = (0, import_react8.useRef)(null);
  (0, import_react8.useEffect)(
    () => {
      initialScrollOffsets.current = null;
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    dependencies
  );
  (0, import_react8.useEffect)(() => {
    const hasScrollOffsets = scrollOffsets !== defaultCoordinates;
    if (hasScrollOffsets && !initialScrollOffsets.current) {
      initialScrollOffsets.current = scrollOffsets;
    }
    if (!hasScrollOffsets && initialScrollOffsets.current) {
      initialScrollOffsets.current = null;
    }
  }, [scrollOffsets]);
  return initialScrollOffsets.current ? subtract(scrollOffsets, initialScrollOffsets.current) : defaultCoordinates;
}
function useSensorSetup(sensors) {
  (0, import_react8.useEffect)(
    () => {
      if (!canUseDOM) {
        return;
      }
      const teardownFns = sensors.map((_ref) => {
        let {
          sensor
        } = _ref;
        return sensor.setup == null ? void 0 : sensor.setup();
      });
      return () => {
        for (const teardown of teardownFns) {
          teardown == null ? void 0 : teardown();
        }
      };
    },
    // TO-DO: Sensors length could theoretically change which would not be a valid dependency
    // eslint-disable-next-line react-hooks/exhaustive-deps
    sensors.map((_ref2) => {
      let {
        sensor
      } = _ref2;
      return sensor;
    })
  );
}
function useSyntheticListeners(listeners, id) {
  return (0, import_react8.useMemo)(() => {
    return listeners.reduce((acc, _ref) => {
      let {
        eventName,
        handler
      } = _ref;
      acc[eventName] = (event) => {
        handler(event, id);
      };
      return acc;
    }, {});
  }, [listeners, id]);
}
function useWindowRect(element) {
  return (0, import_react8.useMemo)(() => element ? getWindowClientRect(element) : null, [element]);
}
var defaultValue$2 = [];
function useRects(elements, measure) {
  if (measure === void 0) {
    measure = getClientRect;
  }
  const [firstElement] = elements;
  const windowRect = useWindowRect(firstElement ? getWindow(firstElement) : null);
  const [rects, setRects] = (0, import_react8.useState)(defaultValue$2);
  function measureRects() {
    setRects(() => {
      if (!elements.length) {
        return defaultValue$2;
      }
      return elements.map((element) => isDocumentScrollingElement(element) ? windowRect : new Rect(measure(element), element));
    });
  }
  const resizeObserver = useResizeObserver({
    callback: measureRects
  });
  useIsomorphicLayoutEffect(() => {
    resizeObserver == null ? void 0 : resizeObserver.disconnect();
    measureRects();
    elements.forEach((element) => resizeObserver == null ? void 0 : resizeObserver.observe(element));
  }, [elements]);
  return rects;
}
function getMeasurableNode(node) {
  if (!node) {
    return null;
  }
  if (node.children.length > 1) {
    return node;
  }
  const firstChild = node.children[0];
  return isHTMLElement(firstChild) ? firstChild : node;
}
function useDragOverlayMeasuring(_ref) {
  let {
    measure
  } = _ref;
  const [rect, setRect] = (0, import_react8.useState)(null);
  const handleResize = (0, import_react8.useCallback)((entries) => {
    for (const {
      target
    } of entries) {
      if (isHTMLElement(target)) {
        setRect((rect2) => {
          const newRect = measure(target);
          return rect2 ? {
            ...rect2,
            width: newRect.width,
            height: newRect.height
          } : newRect;
        });
        break;
      }
    }
  }, [measure]);
  const resizeObserver = useResizeObserver({
    callback: handleResize
  });
  const handleNodeChange = (0, import_react8.useCallback)((element) => {
    const node = getMeasurableNode(element);
    resizeObserver == null ? void 0 : resizeObserver.disconnect();
    if (node) {
      resizeObserver == null ? void 0 : resizeObserver.observe(node);
    }
    setRect(node ? measure(node) : null);
  }, [measure, resizeObserver]);
  const [nodeRef, setRef] = useNodeRef(handleNodeChange);
  return (0, import_react8.useMemo)(() => ({
    nodeRef,
    rect,
    setRef
  }), [rect, nodeRef, setRef]);
}
var defaultSensors = [{
  sensor: PointerSensor,
  options: {}
}, {
  sensor: KeyboardSensor,
  options: {}
}];
var defaultData = {
  current: {}
};
var defaultMeasuringConfiguration = {
  draggable: {
    measure: getTransformAgnosticClientRect
  },
  droppable: {
    measure: getTransformAgnosticClientRect,
    strategy: MeasuringStrategy.WhileDragging,
    frequency: MeasuringFrequency.Optimized
  },
  dragOverlay: {
    measure: getClientRect
  }
};
var DroppableContainersMap = class extends Map {
  get(id) {
    var _super$get;
    return id != null ? (_super$get = super.get(id)) != null ? _super$get : void 0 : void 0;
  }
  toArray() {
    return Array.from(this.values());
  }
  getEnabled() {
    return this.toArray().filter((_ref) => {
      let {
        disabled
      } = _ref;
      return !disabled;
    });
  }
  getNodeFor(id) {
    var _this$get$node$curren, _this$get;
    return (_this$get$node$curren = (_this$get = this.get(id)) == null ? void 0 : _this$get.node.current) != null ? _this$get$node$curren : void 0;
  }
};
var defaultPublicContext = {
  activatorEvent: null,
  active: null,
  activeNode: null,
  activeNodeRect: null,
  collisions: null,
  containerNodeRect: null,
  draggableNodes: /* @__PURE__ */ new Map(),
  droppableRects: /* @__PURE__ */ new Map(),
  droppableContainers: new DroppableContainersMap(),
  over: null,
  dragOverlay: {
    nodeRef: {
      current: null
    },
    rect: null,
    setRef: noop
  },
  scrollableAncestors: [],
  scrollableAncestorRects: [],
  measuringConfiguration: defaultMeasuringConfiguration,
  measureDroppableContainers: noop,
  windowRect: null,
  measuringScheduled: false
};
var defaultInternalContext = {
  activatorEvent: null,
  activators: [],
  active: null,
  activeNodeRect: null,
  ariaDescribedById: {
    draggable: ""
  },
  dispatch: noop,
  draggableNodes: /* @__PURE__ */ new Map(),
  over: null,
  measureDroppableContainers: noop
};
var InternalContext = (0, import_react8.createContext)(defaultInternalContext);
var PublicContext = (0, import_react8.createContext)(defaultPublicContext);
function getInitialState() {
  return {
    draggable: {
      active: null,
      initialCoordinates: {
        x: 0,
        y: 0
      },
      nodes: /* @__PURE__ */ new Map(),
      translate: {
        x: 0,
        y: 0
      }
    },
    droppable: {
      containers: new DroppableContainersMap()
    }
  };
}
function reducer(state, action2) {
  switch (action2.type) {
    case Action.DragStart:
      return {
        ...state,
        draggable: {
          ...state.draggable,
          initialCoordinates: action2.initialCoordinates,
          active: action2.active
        }
      };
    case Action.DragMove:
      if (state.draggable.active == null) {
        return state;
      }
      return {
        ...state,
        draggable: {
          ...state.draggable,
          translate: {
            x: action2.coordinates.x - state.draggable.initialCoordinates.x,
            y: action2.coordinates.y - state.draggable.initialCoordinates.y
          }
        }
      };
    case Action.DragEnd:
    case Action.DragCancel:
      return {
        ...state,
        draggable: {
          ...state.draggable,
          active: null,
          initialCoordinates: {
            x: 0,
            y: 0
          },
          translate: {
            x: 0,
            y: 0
          }
        }
      };
    case Action.RegisterDroppable: {
      const {
        element
      } = action2;
      const {
        id
      } = element;
      const containers = new DroppableContainersMap(state.droppable.containers);
      containers.set(id, element);
      return {
        ...state,
        droppable: {
          ...state.droppable,
          containers
        }
      };
    }
    case Action.SetDroppableDisabled: {
      const {
        id,
        key: key2,
        disabled
      } = action2;
      const element = state.droppable.containers.get(id);
      if (!element || key2 !== element.key) {
        return state;
      }
      const containers = new DroppableContainersMap(state.droppable.containers);
      containers.set(id, {
        ...element,
        disabled
      });
      return {
        ...state,
        droppable: {
          ...state.droppable,
          containers
        }
      };
    }
    case Action.UnregisterDroppable: {
      const {
        id,
        key: key2
      } = action2;
      const element = state.droppable.containers.get(id);
      if (!element || key2 !== element.key) {
        return state;
      }
      const containers = new DroppableContainersMap(state.droppable.containers);
      containers.delete(id);
      return {
        ...state,
        droppable: {
          ...state.droppable,
          containers
        }
      };
    }
    default: {
      return state;
    }
  }
}
function RestoreFocus(_ref) {
  let {
    disabled
  } = _ref;
  const {
    active,
    activatorEvent,
    draggableNodes
  } = (0, import_react8.useContext)(InternalContext);
  const previousActivatorEvent = usePrevious(activatorEvent);
  const previousActiveId = usePrevious(active == null ? void 0 : active.id);
  (0, import_react8.useEffect)(() => {
    if (disabled) {
      return;
    }
    if (!activatorEvent && previousActivatorEvent && previousActiveId != null) {
      if (!isKeyboardEvent(previousActivatorEvent)) {
        return;
      }
      if (document.activeElement === previousActivatorEvent.target) {
        return;
      }
      const draggableNode = draggableNodes.get(previousActiveId);
      if (!draggableNode) {
        return;
      }
      const {
        activatorNode,
        node
      } = draggableNode;
      if (!activatorNode.current && !node.current) {
        return;
      }
      requestAnimationFrame(() => {
        for (const element of [activatorNode.current, node.current]) {
          if (!element) {
            continue;
          }
          const focusableNode = findFirstFocusableNode(element);
          if (focusableNode) {
            focusableNode.focus();
            break;
          }
        }
      });
    }
  }, [activatorEvent, disabled, draggableNodes, previousActiveId, previousActivatorEvent]);
  return null;
}
function applyModifiers(modifiers, _ref) {
  let {
    transform,
    ...args
  } = _ref;
  return modifiers != null && modifiers.length ? modifiers.reduce((accumulator, modifier) => {
    return modifier({
      transform: accumulator,
      ...args
    });
  }, transform) : transform;
}
function useMeasuringConfiguration(config) {
  return (0, import_react8.useMemo)(
    () => ({
      draggable: {
        ...defaultMeasuringConfiguration.draggable,
        ...config == null ? void 0 : config.draggable
      },
      droppable: {
        ...defaultMeasuringConfiguration.droppable,
        ...config == null ? void 0 : config.droppable
      },
      dragOverlay: {
        ...defaultMeasuringConfiguration.dragOverlay,
        ...config == null ? void 0 : config.dragOverlay
      }
    }),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [config == null ? void 0 : config.draggable, config == null ? void 0 : config.droppable, config == null ? void 0 : config.dragOverlay]
  );
}
function useLayoutShiftScrollCompensation(_ref) {
  let {
    activeNode,
    measure,
    initialRect,
    config = true
  } = _ref;
  const initialized = (0, import_react8.useRef)(false);
  const {
    x,
    y
  } = typeof config === "boolean" ? {
    x: config,
    y: config
  } : config;
  useIsomorphicLayoutEffect(() => {
    const disabled = !x && !y;
    if (disabled || !activeNode) {
      initialized.current = false;
      return;
    }
    if (initialized.current || !initialRect) {
      return;
    }
    const node = activeNode == null ? void 0 : activeNode.node.current;
    if (!node || node.isConnected === false) {
      return;
    }
    const rect = measure(node);
    const rectDelta = getRectDelta(rect, initialRect);
    if (!x) {
      rectDelta.x = 0;
    }
    if (!y) {
      rectDelta.y = 0;
    }
    initialized.current = true;
    if (Math.abs(rectDelta.x) > 0 || Math.abs(rectDelta.y) > 0) {
      const firstScrollableAncestor = getFirstScrollableAncestor(node);
      if (firstScrollableAncestor) {
        firstScrollableAncestor.scrollBy({
          top: rectDelta.y,
          left: rectDelta.x
        });
      }
    }
  }, [activeNode, x, y, initialRect, measure]);
}
var ActiveDraggableContext = (0, import_react8.createContext)({
  ...defaultCoordinates,
  scaleX: 1,
  scaleY: 1
});
var Status;
(function(Status2) {
  Status2[Status2["Uninitialized"] = 0] = "Uninitialized";
  Status2[Status2["Initializing"] = 1] = "Initializing";
  Status2[Status2["Initialized"] = 2] = "Initialized";
})(Status || (Status = {}));
var DndContext = (0, import_react8.memo)(function DndContext2(_ref) {
  var _sensorContext$curren, _dragOverlay$nodeRef$, _dragOverlay$rect, _over$rect;
  let {
    id,
    accessibility,
    autoScroll = true,
    children,
    sensors = defaultSensors,
    collisionDetection = rectIntersection,
    measuring,
    modifiers,
    ...props
  } = _ref;
  const store = (0, import_react8.useReducer)(reducer, void 0, getInitialState);
  const [state, dispatch] = store;
  const [dispatchMonitorEvent, registerMonitorListener] = useDndMonitorProvider();
  const [status, setStatus] = (0, import_react8.useState)(Status.Uninitialized);
  const isInitialized = status === Status.Initialized;
  const {
    draggable: {
      active: activeId,
      nodes: draggableNodes,
      translate
    },
    droppable: {
      containers: droppableContainers
    }
  } = state;
  const node = activeId != null ? draggableNodes.get(activeId) : null;
  const activeRects = (0, import_react8.useRef)({
    initial: null,
    translated: null
  });
  const active = (0, import_react8.useMemo)(() => {
    var _node$data;
    return activeId != null ? {
      id: activeId,
      // It's possible for the active node to unmount while dragging
      data: (_node$data = node == null ? void 0 : node.data) != null ? _node$data : defaultData,
      rect: activeRects
    } : null;
  }, [activeId, node]);
  const activeRef = (0, import_react8.useRef)(null);
  const [activeSensor, setActiveSensor] = (0, import_react8.useState)(null);
  const [activatorEvent, setActivatorEvent] = (0, import_react8.useState)(null);
  const latestProps = useLatestValue(props, Object.values(props));
  const draggableDescribedById = useUniqueId("DndDescribedBy", id);
  const enabledDroppableContainers = (0, import_react8.useMemo)(() => droppableContainers.getEnabled(), [droppableContainers]);
  const measuringConfiguration = useMeasuringConfiguration(measuring);
  const {
    droppableRects,
    measureDroppableContainers,
    measuringScheduled
  } = useDroppableMeasuring(enabledDroppableContainers, {
    dragging: isInitialized,
    dependencies: [translate.x, translate.y],
    config: measuringConfiguration.droppable
  });
  const activeNode = useCachedNode(draggableNodes, activeId);
  const activationCoordinates = (0, import_react8.useMemo)(() => activatorEvent ? getEventCoordinates(activatorEvent) : null, [activatorEvent]);
  const autoScrollOptions = getAutoScrollerOptions();
  const initialActiveNodeRect = useInitialRect(activeNode, measuringConfiguration.draggable.measure);
  useLayoutShiftScrollCompensation({
    activeNode: activeId != null ? draggableNodes.get(activeId) : null,
    config: autoScrollOptions.layoutShiftCompensation,
    initialRect: initialActiveNodeRect,
    measure: measuringConfiguration.draggable.measure
  });
  const activeNodeRect = useRect(activeNode, measuringConfiguration.draggable.measure, initialActiveNodeRect);
  const containerNodeRect = useRect(activeNode ? activeNode.parentElement : null);
  const sensorContext = (0, import_react8.useRef)({
    activatorEvent: null,
    active: null,
    activeNode,
    collisionRect: null,
    collisions: null,
    droppableRects,
    draggableNodes,
    draggingNode: null,
    draggingNodeRect: null,
    droppableContainers,
    over: null,
    scrollableAncestors: [],
    scrollAdjustedTranslate: null
  });
  const overNode = droppableContainers.getNodeFor((_sensorContext$curren = sensorContext.current.over) == null ? void 0 : _sensorContext$curren.id);
  const dragOverlay = useDragOverlayMeasuring({
    measure: measuringConfiguration.dragOverlay.measure
  });
  const draggingNode = (_dragOverlay$nodeRef$ = dragOverlay.nodeRef.current) != null ? _dragOverlay$nodeRef$ : activeNode;
  const draggingNodeRect = isInitialized ? (_dragOverlay$rect = dragOverlay.rect) != null ? _dragOverlay$rect : activeNodeRect : null;
  const usesDragOverlay = Boolean(dragOverlay.nodeRef.current && dragOverlay.rect);
  const nodeRectDelta = useRectDelta(usesDragOverlay ? null : activeNodeRect);
  const windowRect = useWindowRect(draggingNode ? getWindow(draggingNode) : null);
  const scrollableAncestors = useScrollableAncestors(isInitialized ? overNode != null ? overNode : activeNode : null);
  const scrollableAncestorRects = useRects(scrollableAncestors);
  const modifiedTranslate = applyModifiers(modifiers, {
    transform: {
      x: translate.x - nodeRectDelta.x,
      y: translate.y - nodeRectDelta.y,
      scaleX: 1,
      scaleY: 1
    },
    activatorEvent,
    active,
    activeNodeRect,
    containerNodeRect,
    draggingNodeRect,
    over: sensorContext.current.over,
    overlayNodeRect: dragOverlay.rect,
    scrollableAncestors,
    scrollableAncestorRects,
    windowRect
  });
  const pointerCoordinates = activationCoordinates ? add(activationCoordinates, translate) : null;
  const scrollOffsets = useScrollOffsets(scrollableAncestors);
  const scrollAdjustment = useScrollOffsetsDelta(scrollOffsets);
  const activeNodeScrollDelta = useScrollOffsetsDelta(scrollOffsets, [activeNodeRect]);
  const scrollAdjustedTranslate = add(modifiedTranslate, scrollAdjustment);
  const collisionRect = draggingNodeRect ? getAdjustedRect(draggingNodeRect, modifiedTranslate) : null;
  const collisions = active && collisionRect ? collisionDetection({
    active,
    collisionRect,
    droppableRects,
    droppableContainers: enabledDroppableContainers,
    pointerCoordinates
  }) : null;
  const overId = getFirstCollision(collisions, "id");
  const [over, setOver] = (0, import_react8.useState)(null);
  const appliedTranslate = usesDragOverlay ? modifiedTranslate : add(modifiedTranslate, activeNodeScrollDelta);
  const transform = adjustScale(appliedTranslate, (_over$rect = over == null ? void 0 : over.rect) != null ? _over$rect : null, activeNodeRect);
  const activeSensorRef = (0, import_react8.useRef)(null);
  const instantiateSensor = (0, import_react8.useCallback)(
    (event, _ref2) => {
      let {
        sensor: Sensor,
        options
      } = _ref2;
      if (activeRef.current == null) {
        return;
      }
      const activeNode2 = draggableNodes.get(activeRef.current);
      if (!activeNode2) {
        return;
      }
      const activatorEvent2 = event.nativeEvent;
      const sensorInstance = new Sensor({
        active: activeRef.current,
        activeNode: activeNode2,
        event: activatorEvent2,
        options,
        // Sensors need to be instantiated with refs for arguments that change over time
        // otherwise they are frozen in time with the stale arguments
        context: sensorContext,
        onAbort(id2) {
          const draggableNode = draggableNodes.get(id2);
          if (!draggableNode) {
            return;
          }
          const {
            onDragAbort
          } = latestProps.current;
          const event2 = {
            id: id2
          };
          onDragAbort == null ? void 0 : onDragAbort(event2);
          dispatchMonitorEvent({
            type: "onDragAbort",
            event: event2
          });
        },
        onPending(id2, constraint, initialCoordinates, offset) {
          const draggableNode = draggableNodes.get(id2);
          if (!draggableNode) {
            return;
          }
          const {
            onDragPending
          } = latestProps.current;
          const event2 = {
            id: id2,
            constraint,
            initialCoordinates,
            offset
          };
          onDragPending == null ? void 0 : onDragPending(event2);
          dispatchMonitorEvent({
            type: "onDragPending",
            event: event2
          });
        },
        onStart(initialCoordinates) {
          const id2 = activeRef.current;
          if (id2 == null) {
            return;
          }
          const draggableNode = draggableNodes.get(id2);
          if (!draggableNode) {
            return;
          }
          const {
            onDragStart
          } = latestProps.current;
          const event2 = {
            activatorEvent: activatorEvent2,
            active: {
              id: id2,
              data: draggableNode.data,
              rect: activeRects
            }
          };
          (0, import_react_dom2.unstable_batchedUpdates)(() => {
            onDragStart == null ? void 0 : onDragStart(event2);
            setStatus(Status.Initializing);
            dispatch({
              type: Action.DragStart,
              initialCoordinates,
              active: id2
            });
            dispatchMonitorEvent({
              type: "onDragStart",
              event: event2
            });
            setActiveSensor(activeSensorRef.current);
            setActivatorEvent(activatorEvent2);
          });
        },
        onMove(coordinates) {
          dispatch({
            type: Action.DragMove,
            coordinates
          });
        },
        onEnd: createHandler(Action.DragEnd),
        onCancel: createHandler(Action.DragCancel)
      });
      activeSensorRef.current = sensorInstance;
      function createHandler(type) {
        return async function handler() {
          const {
            active: active2,
            collisions: collisions2,
            over: over2,
            scrollAdjustedTranslate: scrollAdjustedTranslate2
          } = sensorContext.current;
          let event2 = null;
          if (active2 && scrollAdjustedTranslate2) {
            const {
              cancelDrop
            } = latestProps.current;
            event2 = {
              activatorEvent: activatorEvent2,
              active: active2,
              collisions: collisions2,
              delta: scrollAdjustedTranslate2,
              over: over2
            };
            if (type === Action.DragEnd && typeof cancelDrop === "function") {
              const shouldCancel = await Promise.resolve(cancelDrop(event2));
              if (shouldCancel) {
                type = Action.DragCancel;
              }
            }
          }
          activeRef.current = null;
          (0, import_react_dom2.unstable_batchedUpdates)(() => {
            dispatch({
              type
            });
            setStatus(Status.Uninitialized);
            setOver(null);
            setActiveSensor(null);
            setActivatorEvent(null);
            activeSensorRef.current = null;
            const eventName = type === Action.DragEnd ? "onDragEnd" : "onDragCancel";
            if (event2) {
              const handler2 = latestProps.current[eventName];
              handler2 == null ? void 0 : handler2(event2);
              dispatchMonitorEvent({
                type: eventName,
                event: event2
              });
            }
          });
        };
      }
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [draggableNodes]
  );
  const bindActivatorToSensorInstantiator = (0, import_react8.useCallback)((handler, sensor) => {
    return (event, active2) => {
      const nativeEvent = event.nativeEvent;
      const activeDraggableNode = draggableNodes.get(active2);
      if (
        // Another sensor is already instantiating
        activeRef.current !== null || // No active draggable
        !activeDraggableNode || // Event has already been captured
        nativeEvent.dndKit || nativeEvent.defaultPrevented
      ) {
        return;
      }
      const activationContext = {
        active: activeDraggableNode
      };
      const shouldActivate = handler(event, sensor.options, activationContext);
      if (shouldActivate === true) {
        nativeEvent.dndKit = {
          capturedBy: sensor.sensor
        };
        activeRef.current = active2;
        instantiateSensor(event, sensor);
      }
    };
  }, [draggableNodes, instantiateSensor]);
  const activators = useCombineActivators(sensors, bindActivatorToSensorInstantiator);
  useSensorSetup(sensors);
  useIsomorphicLayoutEffect(() => {
    if (activeNodeRect && status === Status.Initializing) {
      setStatus(Status.Initialized);
    }
  }, [activeNodeRect, status]);
  (0, import_react8.useEffect)(
    () => {
      const {
        onDragMove
      } = latestProps.current;
      const {
        active: active2,
        activatorEvent: activatorEvent2,
        collisions: collisions2,
        over: over2
      } = sensorContext.current;
      if (!active2 || !activatorEvent2) {
        return;
      }
      const event = {
        active: active2,
        activatorEvent: activatorEvent2,
        collisions: collisions2,
        delta: {
          x: scrollAdjustedTranslate.x,
          y: scrollAdjustedTranslate.y
        },
        over: over2
      };
      (0, import_react_dom2.unstable_batchedUpdates)(() => {
        onDragMove == null ? void 0 : onDragMove(event);
        dispatchMonitorEvent({
          type: "onDragMove",
          event
        });
      });
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [scrollAdjustedTranslate.x, scrollAdjustedTranslate.y]
  );
  (0, import_react8.useEffect)(
    () => {
      const {
        active: active2,
        activatorEvent: activatorEvent2,
        collisions: collisions2,
        droppableContainers: droppableContainers2,
        scrollAdjustedTranslate: scrollAdjustedTranslate2
      } = sensorContext.current;
      if (!active2 || activeRef.current == null || !activatorEvent2 || !scrollAdjustedTranslate2) {
        return;
      }
      const {
        onDragOver
      } = latestProps.current;
      const overContainer = droppableContainers2.get(overId);
      const over2 = overContainer && overContainer.rect.current ? {
        id: overContainer.id,
        rect: overContainer.rect.current,
        data: overContainer.data,
        disabled: overContainer.disabled
      } : null;
      const event = {
        active: active2,
        activatorEvent: activatorEvent2,
        collisions: collisions2,
        delta: {
          x: scrollAdjustedTranslate2.x,
          y: scrollAdjustedTranslate2.y
        },
        over: over2
      };
      (0, import_react_dom2.unstable_batchedUpdates)(() => {
        setOver(over2);
        onDragOver == null ? void 0 : onDragOver(event);
        dispatchMonitorEvent({
          type: "onDragOver",
          event
        });
      });
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [overId]
  );
  useIsomorphicLayoutEffect(() => {
    sensorContext.current = {
      activatorEvent,
      active,
      activeNode,
      collisionRect,
      collisions,
      droppableRects,
      draggableNodes,
      draggingNode,
      draggingNodeRect,
      droppableContainers,
      over,
      scrollableAncestors,
      scrollAdjustedTranslate
    };
    activeRects.current = {
      initial: draggingNodeRect,
      translated: collisionRect
    };
  }, [active, activeNode, collisions, collisionRect, draggableNodes, draggingNode, draggingNodeRect, droppableRects, droppableContainers, over, scrollableAncestors, scrollAdjustedTranslate]);
  useAutoScroller({
    ...autoScrollOptions,
    delta: translate,
    draggingRect: collisionRect,
    pointerCoordinates,
    scrollableAncestors,
    scrollableAncestorRects
  });
  const publicContext = (0, import_react8.useMemo)(() => {
    const context = {
      active,
      activeNode,
      activeNodeRect,
      activatorEvent,
      collisions,
      containerNodeRect,
      dragOverlay,
      draggableNodes,
      droppableContainers,
      droppableRects,
      over,
      measureDroppableContainers,
      scrollableAncestors,
      scrollableAncestorRects,
      measuringConfiguration,
      measuringScheduled,
      windowRect
    };
    return context;
  }, [active, activeNode, activeNodeRect, activatorEvent, collisions, containerNodeRect, dragOverlay, draggableNodes, droppableContainers, droppableRects, over, measureDroppableContainers, scrollableAncestors, scrollableAncestorRects, measuringConfiguration, measuringScheduled, windowRect]);
  const internalContext = (0, import_react8.useMemo)(() => {
    const context = {
      activatorEvent,
      activators,
      active,
      activeNodeRect,
      ariaDescribedById: {
        draggable: draggableDescribedById
      },
      dispatch,
      draggableNodes,
      over,
      measureDroppableContainers
    };
    return context;
  }, [activatorEvent, activators, active, activeNodeRect, dispatch, draggableDescribedById, draggableNodes, over, measureDroppableContainers]);
  return import_react8.default.createElement(DndMonitorContext.Provider, {
    value: registerMonitorListener
  }, import_react8.default.createElement(InternalContext.Provider, {
    value: internalContext
  }, import_react8.default.createElement(PublicContext.Provider, {
    value: publicContext
  }, import_react8.default.createElement(ActiveDraggableContext.Provider, {
    value: transform
  }, children)), import_react8.default.createElement(RestoreFocus, {
    disabled: (accessibility == null ? void 0 : accessibility.restoreFocus) === false
  })), import_react8.default.createElement(Accessibility, {
    ...accessibility,
    hiddenTextDescribedById: draggableDescribedById
  }));
  function getAutoScrollerOptions() {
    const activeSensorDisablesAutoscroll = (activeSensor == null ? void 0 : activeSensor.autoScrollEnabled) === false;
    const autoScrollGloballyDisabled = typeof autoScroll === "object" ? autoScroll.enabled === false : autoScroll === false;
    const enabled = isInitialized && !activeSensorDisablesAutoscroll && !autoScrollGloballyDisabled;
    if (typeof autoScroll === "object") {
      return {
        ...autoScroll,
        enabled
      };
    }
    return {
      enabled
    };
  }
});
var NullContext = (0, import_react8.createContext)(null);
var defaultRole = "button";
var ID_PREFIX = "Draggable";
function useDraggable(_ref) {
  let {
    id,
    data,
    disabled = false,
    attributes
  } = _ref;
  const key2 = useUniqueId(ID_PREFIX);
  const {
    activators,
    activatorEvent,
    active,
    activeNodeRect,
    ariaDescribedById,
    draggableNodes,
    over
  } = (0, import_react8.useContext)(InternalContext);
  const {
    role = defaultRole,
    roleDescription = "draggable",
    tabIndex = 0
  } = attributes != null ? attributes : {};
  const isDragging = (active == null ? void 0 : active.id) === id;
  const transform = (0, import_react8.useContext)(isDragging ? ActiveDraggableContext : NullContext);
  const [node, setNodeRef] = useNodeRef();
  const [activatorNode, setActivatorNodeRef] = useNodeRef();
  const listeners = useSyntheticListeners(activators, id);
  const dataRef = useLatestValue(data);
  useIsomorphicLayoutEffect(
    () => {
      draggableNodes.set(id, {
        id,
        key: key2,
        node,
        activatorNode,
        data: dataRef
      });
      return () => {
        const node2 = draggableNodes.get(id);
        if (node2 && node2.key === key2) {
          draggableNodes.delete(id);
        }
      };
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [draggableNodes, id]
  );
  const memoizedAttributes = (0, import_react8.useMemo)(() => ({
    role,
    tabIndex,
    "aria-disabled": disabled,
    "aria-pressed": isDragging && role === defaultRole ? true : void 0,
    "aria-roledescription": roleDescription,
    "aria-describedby": ariaDescribedById.draggable
  }), [disabled, role, tabIndex, isDragging, roleDescription, ariaDescribedById.draggable]);
  return {
    active,
    activatorEvent,
    activeNodeRect,
    attributes: memoizedAttributes,
    isDragging,
    listeners: disabled ? void 0 : listeners,
    node,
    over,
    setNodeRef,
    setActivatorNodeRef,
    transform
  };
}
function useDndContext() {
  return (0, import_react8.useContext)(PublicContext);
}
var ID_PREFIX$1 = "Droppable";
var defaultResizeObserverConfig = {
  timeout: 25
};
function useDroppable(_ref) {
  let {
    data,
    disabled = false,
    id,
    resizeObserverConfig
  } = _ref;
  const key2 = useUniqueId(ID_PREFIX$1);
  const {
    active,
    dispatch,
    over,
    measureDroppableContainers
  } = (0, import_react8.useContext)(InternalContext);
  const previous = (0, import_react8.useRef)({
    disabled
  });
  const resizeObserverConnected = (0, import_react8.useRef)(false);
  const rect = (0, import_react8.useRef)(null);
  const callbackId = (0, import_react8.useRef)(null);
  const {
    disabled: resizeObserverDisabled,
    updateMeasurementsFor,
    timeout: resizeObserverTimeout
  } = {
    ...defaultResizeObserverConfig,
    ...resizeObserverConfig
  };
  const ids2 = useLatestValue(updateMeasurementsFor != null ? updateMeasurementsFor : id);
  const handleResize = (0, import_react8.useCallback)(
    () => {
      if (!resizeObserverConnected.current) {
        resizeObserverConnected.current = true;
        return;
      }
      if (callbackId.current != null) {
        clearTimeout(callbackId.current);
      }
      callbackId.current = setTimeout(() => {
        measureDroppableContainers(Array.isArray(ids2.current) ? ids2.current : [ids2.current]);
        callbackId.current = null;
      }, resizeObserverTimeout);
    },
    //eslint-disable-next-line react-hooks/exhaustive-deps
    [resizeObserverTimeout]
  );
  const resizeObserver = useResizeObserver({
    callback: handleResize,
    disabled: resizeObserverDisabled || !active
  });
  const handleNodeChange = (0, import_react8.useCallback)((newElement, previousElement) => {
    if (!resizeObserver) {
      return;
    }
    if (previousElement) {
      resizeObserver.unobserve(previousElement);
      resizeObserverConnected.current = false;
    }
    if (newElement) {
      resizeObserver.observe(newElement);
    }
  }, [resizeObserver]);
  const [nodeRef, setNodeRef] = useNodeRef(handleNodeChange);
  const dataRef = useLatestValue(data);
  (0, import_react8.useEffect)(() => {
    if (!resizeObserver || !nodeRef.current) {
      return;
    }
    resizeObserver.disconnect();
    resizeObserverConnected.current = false;
    resizeObserver.observe(nodeRef.current);
  }, [nodeRef, resizeObserver]);
  (0, import_react8.useEffect)(
    () => {
      dispatch({
        type: Action.RegisterDroppable,
        element: {
          id,
          key: key2,
          disabled,
          node: nodeRef,
          rect,
          data: dataRef
        }
      });
      return () => dispatch({
        type: Action.UnregisterDroppable,
        key: key2,
        id
      });
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [id]
  );
  (0, import_react8.useEffect)(() => {
    if (disabled !== previous.current.disabled) {
      dispatch({
        type: Action.SetDroppableDisabled,
        id,
        key: key2,
        disabled
      });
      previous.current.disabled = disabled;
    }
  }, [id, key2, disabled, dispatch]);
  return {
    active,
    rect,
    isOver: (over == null ? void 0 : over.id) === id,
    node: nodeRef,
    over,
    setNodeRef
  };
}
function AnimationManager(_ref) {
  let {
    animation,
    children
  } = _ref;
  const [clonedChildren, setClonedChildren] = (0, import_react8.useState)(null);
  const [element, setElement] = (0, import_react8.useState)(null);
  const previousChildren = usePrevious(children);
  if (!children && !clonedChildren && previousChildren) {
    setClonedChildren(previousChildren);
  }
  useIsomorphicLayoutEffect(() => {
    if (!element) {
      return;
    }
    const key2 = clonedChildren == null ? void 0 : clonedChildren.key;
    const id = clonedChildren == null ? void 0 : clonedChildren.props.id;
    if (key2 == null || id == null) {
      setClonedChildren(null);
      return;
    }
    Promise.resolve(animation(id, element)).then(() => {
      setClonedChildren(null);
    });
  }, [animation, clonedChildren, element]);
  return import_react8.default.createElement(import_react8.default.Fragment, null, children, clonedChildren ? (0, import_react8.cloneElement)(clonedChildren, {
    ref: setElement
  }) : null);
}
var defaultTransform = {
  x: 0,
  y: 0,
  scaleX: 1,
  scaleY: 1
};
function NullifiedContextProvider(_ref) {
  let {
    children
  } = _ref;
  return import_react8.default.createElement(InternalContext.Provider, {
    value: defaultInternalContext
  }, import_react8.default.createElement(ActiveDraggableContext.Provider, {
    value: defaultTransform
  }, children));
}
var baseStyles = {
  position: "fixed",
  touchAction: "none"
};
var defaultTransition = (activatorEvent) => {
  const isKeyboardActivator = isKeyboardEvent(activatorEvent);
  return isKeyboardActivator ? "transform 250ms ease" : void 0;
};
var PositionedOverlay = (0, import_react8.forwardRef)((_ref, ref) => {
  let {
    as,
    activatorEvent,
    adjustScale: adjustScale2,
    children,
    className,
    rect,
    style,
    transform,
    transition = defaultTransition
  } = _ref;
  if (!rect) {
    return null;
  }
  const scaleAdjustedTransform = adjustScale2 ? transform : {
    ...transform,
    scaleX: 1,
    scaleY: 1
  };
  const styles = {
    ...baseStyles,
    width: rect.width,
    height: rect.height,
    top: rect.top,
    left: rect.left,
    transform: CSS2.Transform.toString(scaleAdjustedTransform),
    transformOrigin: adjustScale2 && activatorEvent ? getRelativeTransformOrigin(activatorEvent, rect) : void 0,
    transition: typeof transition === "function" ? transition(activatorEvent) : transition,
    ...style
  };
  return import_react8.default.createElement(as, {
    className,
    style: styles,
    ref
  }, children);
});
var defaultDropAnimationSideEffects = (options) => (_ref) => {
  let {
    active,
    dragOverlay
  } = _ref;
  const originalStyles = {};
  const {
    styles,
    className
  } = options;
  if (styles != null && styles.active) {
    for (const [key2, value] of Object.entries(styles.active)) {
      if (value === void 0) {
        continue;
      }
      originalStyles[key2] = active.node.style.getPropertyValue(key2);
      active.node.style.setProperty(key2, value);
    }
  }
  if (styles != null && styles.dragOverlay) {
    for (const [key2, value] of Object.entries(styles.dragOverlay)) {
      if (value === void 0) {
        continue;
      }
      dragOverlay.node.style.setProperty(key2, value);
    }
  }
  if (className != null && className.active) {
    active.node.classList.add(className.active);
  }
  if (className != null && className.dragOverlay) {
    dragOverlay.node.classList.add(className.dragOverlay);
  }
  return function cleanup() {
    for (const [key2, value] of Object.entries(originalStyles)) {
      active.node.style.setProperty(key2, value);
    }
    if (className != null && className.active) {
      active.node.classList.remove(className.active);
    }
  };
};
var defaultKeyframeResolver = (_ref2) => {
  let {
    transform: {
      initial,
      final
    }
  } = _ref2;
  return [{
    transform: CSS2.Transform.toString(initial)
  }, {
    transform: CSS2.Transform.toString(final)
  }];
};
var defaultDropAnimationConfiguration = {
  duration: 250,
  easing: "ease",
  keyframes: defaultKeyframeResolver,
  sideEffects: defaultDropAnimationSideEffects({
    styles: {
      active: {
        opacity: "0"
      }
    }
  })
};
function useDropAnimation(_ref3) {
  let {
    config,
    draggableNodes,
    droppableContainers,
    measuringConfiguration
  } = _ref3;
  return useEvent((id, node) => {
    if (config === null) {
      return;
    }
    const activeDraggable = draggableNodes.get(id);
    if (!activeDraggable) {
      return;
    }
    const activeNode = activeDraggable.node.current;
    if (!activeNode) {
      return;
    }
    const measurableNode = getMeasurableNode(node);
    if (!measurableNode) {
      return;
    }
    const {
      transform
    } = getWindow(node).getComputedStyle(node);
    const parsedTransform = parseTransform(transform);
    if (!parsedTransform) {
      return;
    }
    const animation = typeof config === "function" ? config : createDefaultDropAnimation(config);
    scrollIntoViewIfNeeded(activeNode, measuringConfiguration.draggable.measure);
    return animation({
      active: {
        id,
        data: activeDraggable.data,
        node: activeNode,
        rect: measuringConfiguration.draggable.measure(activeNode)
      },
      draggableNodes,
      dragOverlay: {
        node,
        rect: measuringConfiguration.dragOverlay.measure(measurableNode)
      },
      droppableContainers,
      measuringConfiguration,
      transform: parsedTransform
    });
  });
}
function createDefaultDropAnimation(options) {
  const {
    duration,
    easing,
    sideEffects,
    keyframes
  } = {
    ...defaultDropAnimationConfiguration,
    ...options
  };
  return (_ref4) => {
    let {
      active,
      dragOverlay,
      transform,
      ...rest
    } = _ref4;
    if (!duration) {
      return;
    }
    const delta = {
      x: dragOverlay.rect.left - active.rect.left,
      y: dragOverlay.rect.top - active.rect.top
    };
    const scale = {
      scaleX: transform.scaleX !== 1 ? active.rect.width * transform.scaleX / dragOverlay.rect.width : 1,
      scaleY: transform.scaleY !== 1 ? active.rect.height * transform.scaleY / dragOverlay.rect.height : 1
    };
    const finalTransform = {
      x: transform.x - delta.x,
      y: transform.y - delta.y,
      ...scale
    };
    const animationKeyframes = keyframes({
      ...rest,
      active,
      dragOverlay,
      transform: {
        initial: transform,
        final: finalTransform
      }
    });
    const [firstKeyframe] = animationKeyframes;
    const lastKeyframe = animationKeyframes[animationKeyframes.length - 1];
    if (JSON.stringify(firstKeyframe) === JSON.stringify(lastKeyframe)) {
      return;
    }
    const cleanup = sideEffects == null ? void 0 : sideEffects({
      active,
      dragOverlay,
      ...rest
    });
    const animation = dragOverlay.node.animate(animationKeyframes, {
      duration,
      easing,
      fill: "forwards"
    });
    return new Promise((resolve) => {
      animation.onfinish = () => {
        cleanup == null ? void 0 : cleanup();
        resolve();
      };
    });
  };
}
var key = 0;
function useKey(id) {
  return (0, import_react8.useMemo)(() => {
    if (id == null) {
      return;
    }
    key++;
    return key;
  }, [id]);
}
var DragOverlay = import_react8.default.memo((_ref) => {
  let {
    adjustScale: adjustScale2 = false,
    children,
    dropAnimation: dropAnimationConfig,
    style,
    transition,
    modifiers,
    wrapperElement = "div",
    className,
    zIndex = 999
  } = _ref;
  const {
    activatorEvent,
    active,
    activeNodeRect,
    containerNodeRect,
    draggableNodes,
    droppableContainers,
    dragOverlay,
    over,
    measuringConfiguration,
    scrollableAncestors,
    scrollableAncestorRects,
    windowRect
  } = useDndContext();
  const transform = (0, import_react8.useContext)(ActiveDraggableContext);
  const key2 = useKey(active == null ? void 0 : active.id);
  const modifiedTransform = applyModifiers(modifiers, {
    activatorEvent,
    active,
    activeNodeRect,
    containerNodeRect,
    draggingNodeRect: dragOverlay.rect,
    over,
    overlayNodeRect: dragOverlay.rect,
    scrollableAncestors,
    scrollableAncestorRects,
    transform,
    windowRect
  });
  const initialRect = useInitialValue(activeNodeRect);
  const dropAnimation = useDropAnimation({
    config: dropAnimationConfig,
    draggableNodes,
    droppableContainers,
    measuringConfiguration
  });
  const ref = initialRect ? dragOverlay.setRef : void 0;
  return import_react8.default.createElement(NullifiedContextProvider, null, import_react8.default.createElement(AnimationManager, {
    animation: dropAnimation
  }, active && key2 ? import_react8.default.createElement(PositionedOverlay, {
    key: key2,
    id: active.id,
    ref,
    as: wrapperElement,
    activatorEvent,
    adjustScale: adjustScale2,
    className,
    transition,
    rect: initialRect,
    style: {
      zIndex,
      ...style
    },
    transform: modifiedTransform
  }, children) : null));
});

// node_modules/@dnd-kit/sortable/dist/sortable.esm.js
var import_react9 = __toESM(require_react());
function arrayMove(array, from, to) {
  const newArray = array.slice();
  newArray.splice(to < 0 ? newArray.length + to : to, 0, newArray.splice(from, 1)[0]);
  return newArray;
}
function getSortedRects(items, rects) {
  return items.reduce((accumulator, id, index) => {
    const rect = rects.get(id);
    if (rect) {
      accumulator[index] = rect;
    }
    return accumulator;
  }, Array(items.length));
}
function isValidIndex(index) {
  return index !== null && index >= 0;
}
function itemsEqual(a, b) {
  if (a === b) {
    return true;
  }
  if (a.length !== b.length) {
    return false;
  }
  for (let i = 0; i < a.length; i++) {
    if (a[i] !== b[i]) {
      return false;
    }
  }
  return true;
}
function normalizeDisabled(disabled) {
  if (typeof disabled === "boolean") {
    return {
      draggable: disabled,
      droppable: disabled
    };
  }
  return disabled;
}
var rectSortingStrategy = (_ref) => {
  let {
    rects,
    activeIndex,
    overIndex,
    index
  } = _ref;
  const newRects = arrayMove(rects, overIndex, activeIndex);
  const oldRect = rects[index];
  const newRect = newRects[index];
  if (!newRect || !oldRect) {
    return null;
  }
  return {
    x: newRect.left - oldRect.left,
    y: newRect.top - oldRect.top,
    scaleX: newRect.width / oldRect.width,
    scaleY: newRect.height / oldRect.height
  };
};
var defaultScale$1 = {
  scaleX: 1,
  scaleY: 1
};
var verticalListSortingStrategy = (_ref) => {
  var _rects$activeIndex;
  let {
    activeIndex,
    activeNodeRect: fallbackActiveRect,
    index,
    rects,
    overIndex
  } = _ref;
  const activeNodeRect = (_rects$activeIndex = rects[activeIndex]) != null ? _rects$activeIndex : fallbackActiveRect;
  if (!activeNodeRect) {
    return null;
  }
  if (index === activeIndex) {
    const overIndexRect = rects[overIndex];
    if (!overIndexRect) {
      return null;
    }
    return {
      x: 0,
      y: activeIndex < overIndex ? overIndexRect.top + overIndexRect.height - (activeNodeRect.top + activeNodeRect.height) : overIndexRect.top - activeNodeRect.top,
      ...defaultScale$1
    };
  }
  const itemGap = getItemGap$1(rects, index, activeIndex);
  if (index > activeIndex && index <= overIndex) {
    return {
      x: 0,
      y: -activeNodeRect.height - itemGap,
      ...defaultScale$1
    };
  }
  if (index < activeIndex && index >= overIndex) {
    return {
      x: 0,
      y: activeNodeRect.height + itemGap,
      ...defaultScale$1
    };
  }
  return {
    x: 0,
    y: 0,
    ...defaultScale$1
  };
};
function getItemGap$1(clientRects, index, activeIndex) {
  const currentRect = clientRects[index];
  const previousRect = clientRects[index - 1];
  const nextRect = clientRects[index + 1];
  if (!currentRect) {
    return 0;
  }
  if (activeIndex < index) {
    return previousRect ? currentRect.top - (previousRect.top + previousRect.height) : nextRect ? nextRect.top - (currentRect.top + currentRect.height) : 0;
  }
  return nextRect ? nextRect.top - (currentRect.top + currentRect.height) : previousRect ? currentRect.top - (previousRect.top + previousRect.height) : 0;
}
var ID_PREFIX2 = "Sortable";
var Context = import_react9.default.createContext({
  activeIndex: -1,
  containerId: ID_PREFIX2,
  disableTransforms: false,
  items: [],
  overIndex: -1,
  useDragOverlay: false,
  sortedRects: [],
  strategy: rectSortingStrategy,
  disabled: {
    draggable: false,
    droppable: false
  }
});
function SortableContext(_ref) {
  let {
    children,
    id,
    items: userDefinedItems,
    strategy = rectSortingStrategy,
    disabled: disabledProp = false
  } = _ref;
  const {
    active,
    dragOverlay,
    droppableRects,
    over,
    measureDroppableContainers
  } = useDndContext();
  const containerId = useUniqueId(ID_PREFIX2, id);
  const useDragOverlay = Boolean(dragOverlay.rect !== null);
  const items = (0, import_react9.useMemo)(() => userDefinedItems.map((item) => typeof item === "object" && "id" in item ? item.id : item), [userDefinedItems]);
  const isDragging = active != null;
  const activeIndex = active ? items.indexOf(active.id) : -1;
  const overIndex = over ? items.indexOf(over.id) : -1;
  const previousItemsRef = (0, import_react9.useRef)(items);
  const itemsHaveChanged = !itemsEqual(items, previousItemsRef.current);
  const disableTransforms = overIndex !== -1 && activeIndex === -1 || itemsHaveChanged;
  const disabled = normalizeDisabled(disabledProp);
  useIsomorphicLayoutEffect(() => {
    if (itemsHaveChanged && isDragging) {
      measureDroppableContainers(items);
    }
  }, [itemsHaveChanged, items, isDragging, measureDroppableContainers]);
  (0, import_react9.useEffect)(() => {
    previousItemsRef.current = items;
  }, [items]);
  const contextValue = (0, import_react9.useMemo)(
    () => ({
      activeIndex,
      containerId,
      disabled,
      disableTransforms,
      items,
      overIndex,
      useDragOverlay,
      sortedRects: getSortedRects(items, droppableRects),
      strategy
    }),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [activeIndex, containerId, disabled.draggable, disabled.droppable, disableTransforms, items, overIndex, droppableRects, useDragOverlay, strategy]
  );
  return import_react9.default.createElement(Context.Provider, {
    value: contextValue
  }, children);
}
var defaultNewIndexGetter = (_ref) => {
  let {
    id,
    items,
    activeIndex,
    overIndex
  } = _ref;
  return arrayMove(items, activeIndex, overIndex).indexOf(id);
};
var defaultAnimateLayoutChanges = (_ref2) => {
  let {
    containerId,
    isSorting,
    wasDragging,
    index,
    items,
    newIndex,
    previousItems,
    previousContainerId,
    transition
  } = _ref2;
  if (!transition || !wasDragging) {
    return false;
  }
  if (previousItems !== items && index === newIndex) {
    return false;
  }
  if (isSorting) {
    return true;
  }
  return newIndex !== index && containerId === previousContainerId;
};
var defaultTransition2 = {
  duration: 200,
  easing: "ease"
};
var transitionProperty = "transform";
var disabledTransition = CSS2.Transition.toString({
  property: transitionProperty,
  duration: 0,
  easing: "linear"
});
var defaultAttributes = {
  roleDescription: "sortable"
};
function useDerivedTransform(_ref) {
  let {
    disabled,
    index,
    node,
    rect
  } = _ref;
  const [derivedTransform, setDerivedtransform] = (0, import_react9.useState)(null);
  const previousIndex = (0, import_react9.useRef)(index);
  useIsomorphicLayoutEffect(() => {
    if (!disabled && index !== previousIndex.current && node.current) {
      const initial = rect.current;
      if (initial) {
        const current = getClientRect(node.current, {
          ignoreTransform: true
        });
        const delta = {
          x: initial.left - current.left,
          y: initial.top - current.top,
          scaleX: initial.width / current.width,
          scaleY: initial.height / current.height
        };
        if (delta.x || delta.y) {
          setDerivedtransform(delta);
        }
      }
    }
    if (index !== previousIndex.current) {
      previousIndex.current = index;
    }
  }, [disabled, index, node, rect]);
  (0, import_react9.useEffect)(() => {
    if (derivedTransform) {
      setDerivedtransform(null);
    }
  }, [derivedTransform]);
  return derivedTransform;
}
function useSortable(_ref) {
  let {
    animateLayoutChanges = defaultAnimateLayoutChanges,
    attributes: userDefinedAttributes,
    disabled: localDisabled,
    data: customData,
    getNewIndex = defaultNewIndexGetter,
    id,
    strategy: localStrategy,
    resizeObserverConfig,
    transition = defaultTransition2
  } = _ref;
  const {
    items,
    containerId,
    activeIndex,
    disabled: globalDisabled,
    disableTransforms,
    sortedRects,
    overIndex,
    useDragOverlay,
    strategy: globalStrategy
  } = (0, import_react9.useContext)(Context);
  const disabled = normalizeLocalDisabled(localDisabled, globalDisabled);
  const index = items.indexOf(id);
  const data = (0, import_react9.useMemo)(() => ({
    sortable: {
      containerId,
      index,
      items
    },
    ...customData
  }), [containerId, customData, index, items]);
  const itemsAfterCurrentSortable = (0, import_react9.useMemo)(() => items.slice(items.indexOf(id)), [items, id]);
  const {
    rect,
    node,
    isOver,
    setNodeRef: setDroppableNodeRef
  } = useDroppable({
    id,
    data,
    disabled: disabled.droppable,
    resizeObserverConfig: {
      updateMeasurementsFor: itemsAfterCurrentSortable,
      ...resizeObserverConfig
    }
  });
  const {
    active,
    activatorEvent,
    activeNodeRect,
    attributes,
    setNodeRef: setDraggableNodeRef,
    listeners,
    isDragging,
    over,
    setActivatorNodeRef,
    transform
  } = useDraggable({
    id,
    data,
    attributes: {
      ...defaultAttributes,
      ...userDefinedAttributes
    },
    disabled: disabled.draggable
  });
  const setNodeRef = useCombinedRefs(setDroppableNodeRef, setDraggableNodeRef);
  const isSorting = Boolean(active);
  const displaceItem = isSorting && !disableTransforms && isValidIndex(activeIndex) && isValidIndex(overIndex);
  const shouldDisplaceDragSource = !useDragOverlay && isDragging;
  const dragSourceDisplacement = shouldDisplaceDragSource && displaceItem ? transform : null;
  const strategy = localStrategy != null ? localStrategy : globalStrategy;
  const finalTransform = displaceItem ? dragSourceDisplacement != null ? dragSourceDisplacement : strategy({
    rects: sortedRects,
    activeNodeRect,
    activeIndex,
    overIndex,
    index
  }) : null;
  const newIndex = isValidIndex(activeIndex) && isValidIndex(overIndex) ? getNewIndex({
    id,
    items,
    activeIndex,
    overIndex
  }) : index;
  const activeId = active == null ? void 0 : active.id;
  const previous = (0, import_react9.useRef)({
    activeId,
    items,
    newIndex,
    containerId
  });
  const itemsHaveChanged = items !== previous.current.items;
  const shouldAnimateLayoutChanges = animateLayoutChanges({
    active,
    containerId,
    isDragging,
    isSorting,
    id,
    index,
    items,
    newIndex: previous.current.newIndex,
    previousItems: previous.current.items,
    previousContainerId: previous.current.containerId,
    transition,
    wasDragging: previous.current.activeId != null
  });
  const derivedTransform = useDerivedTransform({
    disabled: !shouldAnimateLayoutChanges,
    index,
    node,
    rect
  });
  (0, import_react9.useEffect)(() => {
    if (isSorting && previous.current.newIndex !== newIndex) {
      previous.current.newIndex = newIndex;
    }
    if (containerId !== previous.current.containerId) {
      previous.current.containerId = containerId;
    }
    if (items !== previous.current.items) {
      previous.current.items = items;
    }
  }, [isSorting, newIndex, containerId, items]);
  (0, import_react9.useEffect)(() => {
    if (activeId === previous.current.activeId) {
      return;
    }
    if (activeId && !previous.current.activeId) {
      previous.current.activeId = activeId;
      return;
    }
    const timeoutId = setTimeout(() => {
      previous.current.activeId = activeId;
    }, 50);
    return () => clearTimeout(timeoutId);
  }, [activeId]);
  return {
    active,
    activeIndex,
    attributes,
    data,
    rect,
    index,
    newIndex,
    items,
    isOver,
    isSorting,
    isDragging,
    listeners,
    node,
    overIndex,
    over,
    setNodeRef,
    setActivatorNodeRef,
    setDroppableNodeRef,
    setDraggableNodeRef,
    transform: derivedTransform != null ? derivedTransform : finalTransform,
    transition: getTransition()
  };
  function getTransition() {
    if (
      // Temporarily disable transitions for a single frame to set up derived transforms
      derivedTransform || // Or to prevent items jumping to back to their "new" position when items change
      itemsHaveChanged && previous.current.newIndex === index
    ) {
      return disabledTransition;
    }
    if (shouldDisplaceDragSource && !isKeyboardEvent(activatorEvent) || !transition) {
      return void 0;
    }
    if (isSorting || shouldAnimateLayoutChanges) {
      return CSS2.Transition.toString({
        ...transition,
        property: transitionProperty
      });
    }
    return void 0;
  }
}
function normalizeLocalDisabled(localDisabled, globalDisabled) {
  var _localDisabled$dragga, _localDisabled$droppa;
  if (typeof localDisabled === "boolean") {
    return {
      draggable: localDisabled,
      // Backwards compatibility
      droppable: false
    };
  }
  return {
    draggable: (_localDisabled$dragga = localDisabled == null ? void 0 : localDisabled.draggable) != null ? _localDisabled$dragga : globalDisabled.draggable,
    droppable: (_localDisabled$droppa = localDisabled == null ? void 0 : localDisabled.droppable) != null ? _localDisabled$droppa : globalDisabled.droppable
  };
}
var directions = [KeyboardCode.Down, KeyboardCode.Right, KeyboardCode.Up, KeyboardCode.Left];

// node_modules/@formily/antd-v5/esm/__builtins__/sort.js
var import_react10 = __toESM(require_react());
var __assign2 = function() {
  __assign2 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign2.apply(this, arguments);
};
var __rest = function(s, e) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
function SortableContainer(Component) {
  return function(_a) {
    var list = _a.list, _b = _a.start, start = _b === void 0 ? 0 : _b, accessibility = _a.accessibility, onSortStart = _a.onSortStart, onSortEnd = _a.onSortEnd, props = __rest(_a, ["list", "start", "accessibility", "onSortStart", "onSortEnd"]);
    var _onSortEnd = function(event) {
      var active = event.active, over = event.over;
      if (!over)
        return;
      var oldIndex = +active.id - 1;
      var newIndex = +over.id - 1;
      onSortEnd === null || onSortEnd === void 0 ? void 0 : onSortEnd({
        oldIndex,
        newIndex
      });
    };
    return import_react10.default.createElement(
      DndContext,
      { accessibility, onDragStart: onSortStart, onDragEnd: _onSortEnd },
      import_react10.default.createElement(
        SortableContext,
        { items: list.map(function(_, index) {
          return index + start + 1;
        }), strategy: verticalListSortingStrategy },
        import_react10.default.createElement(Component, __assign2({}, props), props.children)
      )
    );
  };
}
var useSortableItem = function() {
  return (0, import_react10.useContext)(SortableItemContext);
};
var SortableItemContext = (0, import_react10.createContext)({});
function SortableElement(Component) {
  return function(_a) {
    var _b = _a.index, index = _b === void 0 ? 0 : _b, lockAxis = _a.lockAxis, props = __rest(_a, ["index", "lockAxis"]);
    var sortable = useSortable({
      id: index + 1
    });
    var setNodeRef = sortable.setNodeRef, transform = sortable.transform, transition = sortable.transition, isDragging = sortable.isDragging;
    if (transform) {
      switch (lockAxis) {
        case "x":
          transform.y = 0;
          break;
        case "y":
          transform.x = 0;
          break;
        default:
          break;
      }
    }
    var style = (0, import_react10.useMemo)(function() {
      var zIndex = transform ? 1 : "none";
      var position = transform ? "relative" : "unset";
      var itemStyle = {
        position,
        touchAction: "none",
        zIndex,
        transform: transform ? "translate3d(".concat(transform.x, "px, ").concat(transform.y, "px, 0)") : "none",
        transition: "".concat(transform ? "all 200ms ease" : "")
      };
      var dragStyle = {
        transition,
        opacity: "0.8",
        transform: "translate3d(".concat((transform === null || transform === void 0 ? void 0 : transform.x) || 0, "px, ").concat((transform === null || transform === void 0 ? void 0 : transform.y) || 0, "px, 0)")
      };
      var computedStyle = isDragging ? __assign2(__assign2(__assign2({}, itemStyle), dragStyle), props.style) : __assign2(__assign2({}, itemStyle), props.style);
      return computedStyle;
    }, [isDragging, transform, transition, props.style]);
    return import_react10.default.createElement(SortableItemContext.Provider, { value: sortable }, Component(__assign2(__assign2({}, props), { style, ref: setNodeRef })));
  };
}
function SortableHandle(Component) {
  return function(props) {
    var _a = useSortableItem(), attributes = _a.attributes, listeners = _a.listeners;
    return import_react10.default.createElement(Component, __assign2({}, props, attributes, listeners));
  };
}

// node_modules/@formily/antd-v5/esm/__builtins__/style.js
var __assign3 = function() {
  __assign3 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign3.apply(this, arguments);
};
var genCommonStyle = function(token, componentPrefixCls) {
  var _a, _b;
  var fontFamily = token.fontFamily, fontSize = token.fontSize;
  var rootPrefixSelector = '[class^="'.concat(componentPrefixCls, '"], [class*=" ').concat(componentPrefixCls, '"]');
  return _a = {}, _a[rootPrefixSelector] = (_b = {
    fontFamily,
    fontSize,
    boxSizing: "border-box",
    "&::before, &::after": {
      boxSizing: "border-box"
    }
  }, _b[rootPrefixSelector] = {
    boxSizing: "border-box",
    "&::before, &::after": {
      boxSizing: "border-box"
    }
  }, _b), _a;
};
var genStyleHook = function(component, styleFn) {
  return function(prefixCls) {
    var _a = useToken(), theme = _a.theme, token = _a.token, hashId = _a.hashId;
    var _b = useConfig(), getPrefixCls = _b.getPrefixCls, iconPrefixCls = _b.iconPrefixCls, csp = _b.csp;
    var rootPrefixCls = getPrefixCls();
    return [
      useStyleRegister({
        nonce: csp === null || csp === void 0 ? void 0 : csp.nonce,
        theme,
        token,
        hashId,
        path: ["formily-antd", component, prefixCls, iconPrefixCls]
      }, function() {
        var componentCls = ".".concat(prefixCls);
        var mergedToken = merge(token, __assign3(__assign3({}, token["Form"]), {
          // Merge the antd form token
          componentCls,
          prefixCls,
          iconCls: ".".concat(iconPrefixCls),
          antCls: ".".concat(rootPrefixCls)
        }));
        var styleInterpolation = styleFn(mergedToken, {
          hashId,
          prefixCls,
          rootPrefixCls,
          iconPrefixCls
        });
        return [genCommonStyle(token, prefixCls), styleInterpolation];
      }),
      hashId
    ];
  };
};

// node_modules/@formily/antd-v5/esm/array-base/style.js
var style_default = genStyleHook("array-base", function(token) {
  var _a;
  var componentCls = token.componentCls, colorText = token.colorText, fontSizeLG = token.fontSizeLG, colorPrimaryText = token.colorPrimaryText, colorTextDisabled = token.colorTextDisabled;
  return _a = {}, _a[componentCls] = {
    "&-remove, &-copy": {
      transition: "all 0.25s ease-in-out",
      color: colorText,
      fontSize: fontSizeLG,
      ":hover": {
        color: colorPrimaryText
      },
      "&-disabled": {
        color: colorTextDisabled,
        cursor: "not-allowed !important",
        "&:hover": {
          color: colorTextDisabled
        }
      }
    },
    "&-sort-handle": {
      cursor: "move",
      color: "#888 !important"
    },
    "&-addition": {
      transition: "all 0.25s ease-in-out"
    },
    "&-move-down, &-move-up": {
      transition: "all 0.25s ease-in-out",
      color: colorText,
      fontSize: fontSizeLG,
      marginInlineStart: 6,
      ":hover": {
        color: colorPrimaryText
      },
      "&-disabled": {
        color: colorTextDisabled,
        cursor: "not-allowed !important",
        "&:hover": {
          color: colorTextDisabled
        }
      }
    }
  }, _a;
});

// node_modules/@formily/antd-v5/esm/array-base/index.js
var __assign4 = function() {
  __assign4 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign4.apply(this, arguments);
};
var __rest2 = function(s, e) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
var __read = function(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
};
var ArrayBaseContext = (0, import_react12.createContext)(null);
var ItemContext = (0, import_react12.createContext)(null);
var takeRecord = function(val, index) {
  return typeof val === "function" ? val(index) : val;
};
var useArray = function() {
  return (0, import_react12.useContext)(ArrayBaseContext);
};
var useIndex = function(index) {
  var ctx = (0, import_react12.useContext)(ItemContext);
  return (ctx ? ctx.index : index) || 0;
};
var useRecord = function(record) {
  var ctx = (0, import_react12.useContext)(ItemContext);
  return takeRecord(ctx ? ctx.record : record, ctx === null || ctx === void 0 ? void 0 : ctx.index);
};
var getSchemaDefaultValue = function(schema) {
  if ((schema === null || schema === void 0 ? void 0 : schema.type) === "array")
    return [];
  if ((schema === null || schema === void 0 ? void 0 : schema.type) === "object")
    return {};
  if ((schema === null || schema === void 0 ? void 0 : schema.type) === "void") {
    for (var key2 in schema.properties) {
      var value = getSchemaDefaultValue(schema.properties[key2]);
      if (isValid(value))
        return value;
    }
  }
};
var getDefaultValue = function(defaultValue2, schema) {
  if (isValid(defaultValue2))
    return clone(defaultValue2);
  if (Array.isArray(schema === null || schema === void 0 ? void 0 : schema.items))
    return getSchemaDefaultValue(schema === null || schema === void 0 ? void 0 : schema.items[0]);
  return getSchemaDefaultValue(schema === null || schema === void 0 ? void 0 : schema.items);
};
var InternalArrayBase = function(props) {
  var field = useField();
  var schema = useFieldSchema();
  return import_react12.default.createElement(
    RecordsScope,
    { getRecords: function() {
      return field.value;
    } },
    import_react12.default.createElement(ArrayBaseContext.Provider, { value: { field, schema, props } }, props.children)
  );
};
var Item = function(_a) {
  var children = _a.children, props = __rest2(_a, ["children"]);
  return import_react12.default.createElement(
    ItemContext.Provider,
    { value: props },
    import_react12.default.createElement(RecordScope, { getIndex: function() {
      return props.index;
    }, getRecord: function() {
      return takeRecord(props.record, props.index);
    } }, children)
  );
};
var InternalSortHandle = SortableHandle(function(props) {
  var prefixCls = usePrefixCls("formily-array-base");
  var _a = __read(style_default(prefixCls), 2), wrapSSR = _a[0], hashId = _a[1];
  return wrapSSR(import_react12.default.createElement(MenuOutlined_default, __assign4({}, props, { className: (0, import_classnames.default)("".concat(prefixCls, "-sort-handle"), hashId, props.className), style: __assign4({}, props.style) })));
});
var SortHandle = function(props) {
  var _a;
  var array = useArray();
  if (!array)
    return null;
  if (((_a = array.field) === null || _a === void 0 ? void 0 : _a.pattern) !== "editable")
    return null;
  return import_react12.default.createElement(InternalSortHandle, __assign4({}, props));
};
var Index = function(props) {
  var index = useIndex();
  var prefixCls = usePrefixCls("formily-array-base");
  var _a = __read(style_default(prefixCls), 2), wrapSSR = _a[0], hashId = _a[1];
  return wrapSSR(import_react12.default.createElement(
    "span",
    __assign4({}, props, { className: (0, import_classnames.default)("".concat(prefixCls, "-index"), hashId) }),
    "#",
    (index || 0) + 1,
    "."
  ));
};
var Addition = function(props) {
  var _a, _b;
  var self = useField();
  var array = useArray();
  var prefixCls = usePrefixCls("formily-array-base");
  var _c = __read(style_default(prefixCls), 2), wrapSSR = _c[0], hashId = _c[1];
  if (!array)
    return null;
  if (((_a = array.field) === null || _a === void 0 ? void 0 : _a.pattern) !== "editable" && ((_b = array.field) === null || _b === void 0 ? void 0 : _b.pattern) !== "disabled")
    return null;
  return wrapSSR(import_react12.default.createElement(button_default, __assign4({ type: "dashed", block: true }, props, { disabled: self === null || self === void 0 ? void 0 : self.disabled, className: (0, import_classnames.default)("".concat(prefixCls, "-addition"), hashId, props.className), onClick: function(e) {
    var _a2, _b2, _c2, _d, _e, _f, _g, _h, _j, _k, _l;
    if ((_a2 = array.props) === null || _a2 === void 0 ? void 0 : _a2.disabled)
      return;
    var defaultValue2 = getDefaultValue(props.defaultValue, array.schema);
    if (props.method === "unshift") {
      (_c2 = (_b2 = array.field) === null || _b2 === void 0 ? void 0 : _b2.unshift) === null || _c2 === void 0 ? void 0 : _c2.call(_b2, defaultValue2);
      (_e = (_d = array.props) === null || _d === void 0 ? void 0 : _d.onAdd) === null || _e === void 0 ? void 0 : _e.call(_d, 0);
    } else {
      (_g = (_f = array.field) === null || _f === void 0 ? void 0 : _f.push) === null || _g === void 0 ? void 0 : _g.call(_f, defaultValue2);
      (_j = (_h = array.props) === null || _h === void 0 ? void 0 : _h.onAdd) === null || _j === void 0 ? void 0 : _j.call(_h, ((_l = (_k = array === null || array === void 0 ? void 0 : array.field) === null || _k === void 0 ? void 0 : _k.value) === null || _l === void 0 ? void 0 : _l.length) - 1);
    }
    if (props.onClick) {
      props.onClick(e);
    }
  }, icon: isUndef(props.icon) ? import_react12.default.createElement(PlusOutlined_default, null) : props.icon }), props.title || self.title));
};
var Copy = (0, import_react12.forwardRef)(function(props, ref) {
  var _a;
  var self = useField();
  var array = useArray();
  var index = useIndex(props.index) || 0;
  var prefixCls = usePrefixCls("formily-array-base");
  var _b = __read(style_default(prefixCls), 2), wrapSSR = _b[0], hashId = _b[1];
  if (!array)
    return null;
  if (((_a = array.field) === null || _a === void 0 ? void 0 : _a.pattern) !== "editable")
    return null;
  return wrapSSR(import_react12.default.createElement(button_default, __assign4({ type: "ghost" }, props, { style: __assign4({ padding: "0 0 0 6px", width: "auto", height: "auto" }, props.style), disabled: self === null || self === void 0 ? void 0 : self.disabled, className: (0, import_classnames.default)("".concat(prefixCls, "-copy"), hashId, (self === null || self === void 0 ? void 0 : self.disabled) ? "".concat(prefixCls, "-copy-disabled") : "", props.className), ref, onClick: function(e) {
    var _a2, _b2, _c, _d, _e, _f;
    if (self === null || self === void 0 ? void 0 : self.disabled)
      return;
    e.stopPropagation();
    if ((_a2 = array.props) === null || _a2 === void 0 ? void 0 : _a2.disabled)
      return;
    if (props.onClick) {
      props.onClick(e);
      if (e.defaultPrevented)
        return;
    }
    var value = clone((_b2 = array === null || array === void 0 ? void 0 : array.field) === null || _b2 === void 0 ? void 0 : _b2.value[index]);
    var distIndex = index + 1;
    (_d = (_c = array.field) === null || _c === void 0 ? void 0 : _c.insert) === null || _d === void 0 ? void 0 : _d.call(_c, distIndex, value);
    (_f = (_e = array.props) === null || _e === void 0 ? void 0 : _e.onCopy) === null || _f === void 0 ? void 0 : _f.call(_e, distIndex);
  }, icon: isUndef(props.icon) ? import_react12.default.createElement(CopyOutlined_default, null) : props.icon }), props.title || self.title));
});
var Remove = (0, import_react12.forwardRef)(function(props, ref) {
  var _a;
  var index = useIndex(props.index);
  var self = useField();
  var array = useArray();
  var prefixCls = usePrefixCls("formily-array-base");
  var _b = __read(style_default(prefixCls), 2), wrapSSR = _b[0], hashId = _b[1];
  if (!array)
    return null;
  if (((_a = array.field) === null || _a === void 0 ? void 0 : _a.pattern) !== "editable")
    return null;
  return wrapSSR(import_react12.default.createElement(button_default, __assign4({ type: "ghost" }, props, { style: __assign4({ padding: "0 0 0 6px", width: "auto", height: "auto" }, props.style), disabled: self === null || self === void 0 ? void 0 : self.disabled, className: (0, import_classnames.default)("".concat(prefixCls, "-remove"), hashId, (self === null || self === void 0 ? void 0 : self.disabled) ? "".concat(prefixCls, "-remove-disabled") : "", props.className), ref, onClick: function(e) {
    var _a2, _b2, _c, _d;
    if (self === null || self === void 0 ? void 0 : self.disabled)
      return;
    e.stopPropagation();
    if (props.onClick) {
      props.onClick(e);
      if (e.defaultPrevented)
        return;
    }
    (_b2 = (_a2 = array.field) === null || _a2 === void 0 ? void 0 : _a2.remove) === null || _b2 === void 0 ? void 0 : _b2.call(_a2, index);
    (_d = (_c = array.props) === null || _c === void 0 ? void 0 : _c.onRemove) === null || _d === void 0 ? void 0 : _d.call(_c, index);
  }, icon: isUndef(props.icon) ? import_react12.default.createElement(DeleteOutlined_default, null) : props.icon }), props.title || self.title));
});
var MoveDown = (0, import_react12.forwardRef)(function(props, ref) {
  var _a;
  var index = useIndex(props.index);
  var self = useField();
  var array = useArray();
  var prefixCls = usePrefixCls("formily-array-base");
  if (!array)
    return null;
  if (((_a = array.field) === null || _a === void 0 ? void 0 : _a.pattern) !== "editable")
    return null;
  return import_react12.default.createElement(button_default, __assign4({ type: "ghost" }, props, { style: __assign4({ padding: "0 0 0 6px", width: "auto", height: "auto" }, props.style), disabled: self === null || self === void 0 ? void 0 : self.disabled, className: (0, import_classnames.default)("".concat(prefixCls, "-move-down"), (self === null || self === void 0 ? void 0 : self.disabled) ? "".concat(prefixCls, "-move-down-disabled") : "", props.className), ref, onClick: function(e) {
    var _a2, _b, _c, _d;
    if (self === null || self === void 0 ? void 0 : self.disabled)
      return;
    e.stopPropagation();
    if (props.onClick) {
      props.onClick(e);
      if (e.defaultPrevented)
        return;
    }
    (_b = (_a2 = array.field) === null || _a2 === void 0 ? void 0 : _a2.moveDown) === null || _b === void 0 ? void 0 : _b.call(_a2, index);
    (_d = (_c = array.props) === null || _c === void 0 ? void 0 : _c.onMoveDown) === null || _d === void 0 ? void 0 : _d.call(_c, index);
  }, icon: isUndef(props.icon) ? import_react12.default.createElement(DownOutlined_default, null) : props.icon }), props.title || self.title);
});
var MoveUp = (0, import_react12.forwardRef)(function(props, ref) {
  var _a;
  var index = useIndex(props.index);
  var self = useField();
  var array = useArray();
  var prefixCls = usePrefixCls("formily-array-base");
  if (!array)
    return null;
  if (((_a = array.field) === null || _a === void 0 ? void 0 : _a.pattern) !== "editable")
    return null;
  return import_react12.default.createElement(button_default, __assign4({ type: "ghost" }, props, { style: __assign4({ padding: "0 0 0 6px", width: "auto", height: "auto" }, props.style), disabled: self === null || self === void 0 ? void 0 : self.disabled, className: (0, import_classnames.default)("".concat(prefixCls, "-move-up"), (self === null || self === void 0 ? void 0 : self.disabled) ? "".concat(prefixCls, "-move-up-disabled") : "", props.className), ref, onClick: function(e) {
    var _a2, _b, _c;
    if (self === null || self === void 0 ? void 0 : self.disabled)
      return;
    e.stopPropagation();
    if (props.onClick) {
      props.onClick(e);
      if (e.defaultPrevented)
        return;
    }
    (_a2 = array === null || array === void 0 ? void 0 : array.field) === null || _a2 === void 0 ? void 0 : _a2.moveUp(index);
    (_c = (_b = array === null || array === void 0 ? void 0 : array.props) === null || _b === void 0 ? void 0 : _b.onMoveUp) === null || _c === void 0 ? void 0 : _c.call(_b, index);
  }, icon: isUndef(props.icon) ? import_react12.default.createElement(UpOutlined_default, null) : props.icon }), props.title || self.title);
});
function mixin(target) {
  return Object.assign(target, {
    Index,
    SortHandle,
    Addition,
    Copy,
    Remove,
    MoveDown,
    MoveUp,
    useArray,
    useIndex,
    useRecord
  });
}
var ArrayBase = Object.assign(InternalArrayBase, {
  Item,
  Index,
  SortHandle,
  Addition,
  Copy,
  Remove,
  MoveDown,
  MoveUp,
  useArray,
  useIndex,
  useRecord,
  mixin
});
var array_base_default = ArrayBase;

// node_modules/@formily/antd-v5/esm/array-cards/index.js
var import_classnames2 = __toESM(require_classnames());
var import_react14 = __toESM(require_react());

// node_modules/@formily/antd-v5/esm/array-cards/style.js
var style_default2 = genStyleHook("array-cards", function(token) {
  var _a, _b;
  var componentCls = token.componentCls, antCls = token.antCls;
  var arrayBase = "".concat(antCls, "-formily-array-base");
  return _a = {}, _a[componentCls] = (_b = {
    "&-item": {
      marginBottom: "10px !important"
    }
  }, _b["".concat(arrayBase, "-copy")] = {
    marginInlineStart: 6
  }, _b), _a;
});

// node_modules/@formily/antd-v5/esm/array-cards/index.js
var __assign5 = function() {
  __assign5 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign5.apply(this, arguments);
};
var __read2 = function(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
};
var isAdditionComponent = function(schema) {
  var _a;
  return ((_a = schema["x-component"]) === null || _a === void 0 ? void 0 : _a.indexOf("Addition")) > -1;
};
var isIndexComponent = function(schema) {
  var _a, _b;
  return ((_b = (_a = schema["x-component"]) === null || _a === void 0 ? void 0 : _a.indexOf) === null || _b === void 0 ? void 0 : _b.call(_a, "Index")) > -1;
};
var isRemoveComponent = function(schema) {
  var _a, _b;
  return ((_b = (_a = schema["x-component"]) === null || _a === void 0 ? void 0 : _a.indexOf) === null || _b === void 0 ? void 0 : _b.call(_a, "Remove")) > -1;
};
var isCopyComponent = function(schema) {
  var _a, _b;
  return ((_b = (_a = schema["x-component"]) === null || _a === void 0 ? void 0 : _a.indexOf) === null || _b === void 0 ? void 0 : _b.call(_a, "Copy")) > -1;
};
var isMoveUpComponent = function(schema) {
  var _a, _b;
  return ((_b = (_a = schema["x-component"]) === null || _a === void 0 ? void 0 : _a.indexOf) === null || _b === void 0 ? void 0 : _b.call(_a, "MoveUp")) > -1;
};
var isMoveDownComponent = function(schema) {
  var _a, _b;
  return ((_b = (_a = schema["x-component"]) === null || _a === void 0 ? void 0 : _a.indexOf) === null || _b === void 0 ? void 0 : _b.call(_a, "MoveDown")) > -1;
};
var isOperationComponent = function(schema) {
  return isAdditionComponent(schema) || isRemoveComponent(schema) || isCopyComponent(schema) || isMoveDownComponent(schema) || isMoveUpComponent(schema);
};
var InternalArrayCards = observer(function(props) {
  var field = useField();
  var schema = useFieldSchema();
  var dataSource = Array.isArray(field.value) ? field.value : [];
  var prefixCls = usePrefixCls("formily-array-cards", props);
  var _a = __read2(style_default2(prefixCls), 2), wrapSSR = _a[0], hashId = _a[1];
  if (!schema)
    throw new Error("can not found schema object");
  var renderItems = function() {
    return dataSource === null || dataSource === void 0 ? void 0 : dataSource.map(function(item, index) {
      var items = Array.isArray(schema.items) ? schema.items[index] || schema.items[0] : schema.items;
      var title = import_react14.default.createElement(
        "span",
        null,
        items ? import_react14.default.createElement(RecursionField, { schema: items, name: index, filterProperties: function(schema2) {
          if (!isIndexComponent(schema2))
            return false;
          return true;
        }, onlyRenderProperties: true }) : null,
        props.title || field.title
      );
      var extra = import_react14.default.createElement(
        "span",
        null,
        items ? import_react14.default.createElement(RecursionField, { schema: items, name: index, filterProperties: function(schema2) {
          if (!isOperationComponent(schema2))
            return false;
          return true;
        }, onlyRenderProperties: true }) : null,
        props.extra
      );
      var content = items ? import_react14.default.createElement(RecursionField, { schema: items, name: index, filterProperties: function(schema2) {
        if (isIndexComponent(schema2))
          return false;
        if (isOperationComponent(schema2))
          return false;
        return true;
      } }) : null;
      return import_react14.default.createElement(
        ArrayBase.Item,
        { key: index, index, record: function() {
          var _a2;
          return (_a2 = field.value) === null || _a2 === void 0 ? void 0 : _a2[index];
        } },
        import_react14.default.createElement(card_default, __assign5({}, props, { onChange: function() {
        }, className: (0, import_classnames2.default)("".concat(prefixCls, "-item"), hashId, props.className), title, extra }), content)
      );
    });
  };
  var renderAddition = function() {
    return schema.reduceProperties(function(addition, schema2, key2) {
      if (isAdditionComponent(schema2)) {
        return import_react14.default.createElement(RecursionField, { schema: schema2, name: key2 });
      }
      return addition;
    }, null);
  };
  var renderEmpty = function() {
    if (dataSource === null || dataSource === void 0 ? void 0 : dataSource.length)
      return;
    return import_react14.default.createElement(
      card_default,
      __assign5({}, props, { onChange: function() {
      }, className: (0, import_classnames2.default)("".concat(prefixCls, "-item"), hashId, props.className), title: props.title || field.title }),
      import_react14.default.createElement(empty_default, null)
    );
  };
  return wrapSSR(import_react14.default.createElement(
    ArrayBase,
    null,
    renderEmpty(),
    renderItems(),
    renderAddition()
  ));
});
var ArrayCards = ArrayBase.mixin(InternalArrayCards);
ArrayCards.displayName = "ArrayCards";

// node_modules/@formily/antd-v5/esm/array-collapse/index.js
var import_classnames3 = __toESM(require_classnames());
var import_react16 = __toESM(require_react());

// node_modules/@formily/antd-v5/esm/array-collapse/style.js
var style_default3 = genStyleHook("", function(token) {
  var _a;
  var componentCls = token.componentCls;
  return _a = {}, _a[componentCls] = {
    "&-item": {
      marginBottom: "10px !important"
    }
  }, _a;
});

// node_modules/@formily/antd-v5/esm/array-collapse/index.js
var __assign6 = function() {
  __assign6 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign6.apply(this, arguments);
};
var __read3 = function(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
};
var isAdditionComponent2 = function(schema) {
  var _a, _b;
  return ((_b = (_a = schema["x-component"]) === null || _a === void 0 ? void 0 : _a.indexOf) === null || _b === void 0 ? void 0 : _b.call(_a, "Addition")) > -1;
};
var isIndexComponent2 = function(schema) {
  var _a, _b;
  return ((_b = (_a = schema["x-component"]) === null || _a === void 0 ? void 0 : _a.indexOf) === null || _b === void 0 ? void 0 : _b.call(_a, "Index")) > -1;
};
var isRemoveComponent2 = function(schema) {
  var _a, _b;
  return ((_b = (_a = schema["x-component"]) === null || _a === void 0 ? void 0 : _a.indexOf) === null || _b === void 0 ? void 0 : _b.call(_a, "Remove")) > -1;
};
var isMoveUpComponent2 = function(schema) {
  var _a, _b;
  return ((_b = (_a = schema["x-component"]) === null || _a === void 0 ? void 0 : _a.indexOf) === null || _b === void 0 ? void 0 : _b.call(_a, "MoveUp")) > -1;
};
var isMoveDownComponent2 = function(schema) {
  var _a, _b;
  return ((_b = (_a = schema["x-component"]) === null || _a === void 0 ? void 0 : _a.indexOf) === null || _b === void 0 ? void 0 : _b.call(_a, "MoveDown")) > -1;
};
var isOperationComponent2 = function(schema) {
  return isAdditionComponent2(schema) || isRemoveComponent2(schema) || isMoveDownComponent2(schema) || isMoveUpComponent2(schema);
};
var range = function(count) {
  return Array.from({ length: count }).map(function(_, i) {
    return i;
  });
};
var takeDefaultActiveKeys = function(dataSourceLength, defaultOpenPanelCount) {
  if (dataSourceLength < defaultOpenPanelCount)
    return range(dataSourceLength);
  return range(defaultOpenPanelCount);
};
var insertActiveKeys = function(activeKeys, index) {
  if (activeKeys.length <= index)
    return activeKeys.concat(index);
  return activeKeys.reduce(function(buf, key2) {
    if (key2 < index)
      return buf.concat(key2);
    if (key2 === index)
      return buf.concat([key2, key2 + 1]);
    return buf.concat(key2 + 1);
  }, []);
};
var InternalArrayCollapse = observer(function(props) {
  var _a;
  var field = useField();
  var dataSource = Array.isArray(field.value) ? field.value : [];
  var _b = __read3((0, import_react16.useState)(takeDefaultActiveKeys(dataSource.length, (_a = props.defaultOpenPanelCount) !== null && _a !== void 0 ? _a : 5)), 2), activeKeys = _b[0], setActiveKeys = _b[1];
  var schema = useFieldSchema();
  var prefixCls = usePrefixCls("formily-array-collapse", props);
  var _c = __read3(style_default3(prefixCls), 2), wrapSSR = _c[0], hashId = _c[1];
  (0, import_react16.useEffect)(function() {
    var _a2;
    if (!field.modified && dataSource.length) {
      setActiveKeys(takeDefaultActiveKeys(dataSource.length, (_a2 = props.defaultOpenPanelCount) !== null && _a2 !== void 0 ? _a2 : 5));
    }
  }, [dataSource.length, field]);
  if (!schema)
    throw new Error("can not found schema object");
  var renderAddition = function() {
    return schema.reduceProperties(function(addition, schema2, key2) {
      if (isAdditionComponent2(schema2)) {
        return import_react16.default.createElement(RecursionField, { schema: schema2, name: key2 });
      }
      return addition;
    }, null);
  };
  var renderEmpty = function() {
    if (dataSource.length)
      return;
    return import_react16.default.createElement(
      card_default,
      { className: (0, import_classnames3.default)("".concat(prefixCls, "-item"), hashId, props.className) },
      import_react16.default.createElement(empty_default, null)
    );
  };
  var renderItems = function() {
    return import_react16.default.createElement(collapse_default, __assign6({}, props, { activeKey: activeKeys, onChange: function(keys) {
      setActiveKeys(toArr(keys).map(Number));
    }, className: (0, import_classnames3.default)("".concat(prefixCls, "-item"), hashId, props.className) }), dataSource.map(function(item, index) {
      var items = Array.isArray(schema.items) ? schema.items[index] || schema.items[0] : schema.items;
      if (!items)
        return null;
      var panelProps = field.query("".concat(field.address, ".").concat(index)).get("componentProps");
      var props2 = items["x-component-props"];
      var header = function() {
        var header2 = (panelProps === null || panelProps === void 0 ? void 0 : panelProps.header) || props2.header || field.title;
        var path = field.address.concat(index);
        var errors = field.form.queryFeedbacks({
          type: "error",
          address: "".concat(path, ".**")
        });
        return import_react16.default.createElement(
          array_base_default.Item,
          { index, record: function() {
            var _a2;
            return (_a2 = field.value) === null || _a2 === void 0 ? void 0 : _a2[index];
          } },
          import_react16.default.createElement(RecursionField, { schema: items, name: index, filterProperties: function(schema2) {
            if (!isIndexComponent2(schema2))
              return false;
            return true;
          }, onlyRenderProperties: true }),
          errors.length ? import_react16.default.createElement(badge_default, { size: "small", className: "errors-badge", count: errors.length }, header2) : header2
        );
      };
      var extra = import_react16.default.createElement(
        array_base_default.Item,
        { index, record: item },
        panelProps === null || panelProps === void 0 ? void 0 : panelProps.extra,
        import_react16.default.createElement(RecursionField, { schema: items, name: index, filterProperties: function(schema2) {
          if (!isOperationComponent2(schema2))
            return false;
          return true;
        }, onlyRenderProperties: true })
      );
      var content = import_react16.default.createElement(RecursionField, { schema: items, name: index, filterProperties: function(schema2) {
        if (isIndexComponent2(schema2))
          return false;
        if (isOperationComponent2(schema2))
          return false;
        return true;
      } });
      return import_react16.default.createElement(
        collapse_default.Panel,
        __assign6({}, props2, panelProps, { forceRender: true, key: index, header: header(), extra }),
        import_react16.default.createElement(array_base_default.Item, { index, key: index, record: item }, content)
      );
    }));
  };
  return wrapSSR(import_react16.default.createElement(
    array_base_default,
    { onAdd: function(index) {
      setActiveKeys(insertActiveKeys(activeKeys, index));
    } },
    renderEmpty(),
    renderItems(),
    renderAddition()
  ));
});
var CollapsePanel = function(_a) {
  var children = _a.children;
  return import_react16.default.createElement(import_react16.Fragment, null, children);
};
CollapsePanel.displayName = "CollapsePanel";
var ArrayCollapse = Object.assign(array_base_default.mixin(InternalArrayCollapse), {
  CollapsePanel
});
ArrayCollapse.displayName = "ArrayCollapse";

// node_modules/@formily/antd-v5/esm/array-items/index.js
var import_classnames4 = __toESM(require_classnames());
var import_react18 = __toESM(require_react());

// node_modules/@formily/antd-v5/esm/array-items/style.js
var style_default4 = genStyleHook("array-items", function(token) {
  var _a, _b, _c, _d, _e, _f;
  var componentCls = token.componentCls, antCls = token.antCls, colorBgBase = token.colorBgBase, fontSizeSM = token.fontSizeSM, colorBorder = token.colorBorder, colorText = token.colorText;
  var itemCls = "".concat(antCls, "-formily-item");
  return _a = {}, _a[componentCls] = (_b = {}, _b["".concat(componentCls, "-item-inner")] = {
    visibility: "visible"
  }, _b["&-item"] = {
    zIndex: 1e5
  }, _b["&-card"] = (_c = {
    display: "flex",
    border: "1px solid ".concat(colorBorder),
    marginBottom: 10,
    padding: "3px 6px",
    background: colorBgBase,
    justifyContent: "space-between",
    color: colorText
  }, _c["".concat(itemCls, ":not(").concat(itemCls, "-feedback-layout-popover)")] = (_d = {
    marginBottom: "0 !important"
  }, _d["".concat(itemCls, "-help")] = {
    position: "absolute",
    fontSize: fontSizeSM,
    top: "100%",
    background: colorBgBase,
    width: "100%",
    marginTop: 3,
    padding: 3,
    zIndex: 1,
    borderRadius: 3,
    boxShadow: "0 0 10px ".concat(colorBorder)
  }, _d), _c), _b["&-divide"] = (_e = {
    display: "flex",
    borderBottom: "1px solid ".concat(colorBorder),
    padding: "10px 0",
    justifyContent: "space-between"
  }, _e["".concat(itemCls, ":not(").concat(itemCls, "-feedback-layout-popover)")] = (_f = {
    marginBottom: "0 !important"
  }, _f["".concat(itemCls, "-help")] = {
    position: "absolute",
    fontSize: fontSizeSM,
    top: "100%",
    background: colorBgBase,
    width: "100%",
    marginTop: 3,
    padding: 3,
    zIndex: 1,
    borderRadius: 3,
    boxShadow: "0 0 10px ".concat(colorBorder)
  }, _f), _e), _b), _a;
});

// node_modules/@formily/antd-v5/esm/array-items/index.js
var __assign7 = function() {
  __assign7 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign7.apply(this, arguments);
};
var __read4 = function(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
};
var SortableItem = SortableElement(function(props) {
  var prefixCls = usePrefixCls("formily-array-items");
  var _a = __read4(style_default4(prefixCls), 2), wrapSSR = _a[0], hashId = _a[1];
  return wrapSSR(import_react18.default.createElement("div", __assign7({}, props, { className: (0, import_classnames4.default)("".concat(prefixCls, "-item"), hashId, props.className) }), props.children));
});
var SortableList = SortableContainer(function(props) {
  var prefixCls = usePrefixCls("formily-array-items");
  var _a = __read4(style_default4(prefixCls), 2), wrapSSR = _a[0], hashId = _a[1];
  return wrapSSR(import_react18.default.createElement("div", __assign7({}, props, { className: (0, import_classnames4.default)("".concat(prefixCls, "-list"), hashId, props.className) }), props.children));
});
var isAdditionComponent3 = function(schema) {
  var _a;
  return ((_a = schema["x-component"]) === null || _a === void 0 ? void 0 : _a.indexOf("Addition")) > -1;
};
var useAddition = function() {
  var schema = useFieldSchema();
  return schema.reduceProperties(function(addition, schema2, key2) {
    if (isAdditionComponent3(schema2)) {
      return import_react18.default.createElement(RecursionField, { schema: schema2, name: key2 });
    }
    return addition;
  }, null);
};
var InternalArrayItems = observer(function(props) {
  var field = useField();
  var prefixCls = usePrefixCls("formily-array-items");
  var _a = __read4(style_default4(prefixCls), 2), wrapSSR = _a[0], hashId = _a[1];
  var schema = useFieldSchema();
  var addition = useAddition();
  var dataSource = Array.isArray(field.value) ? field.value : [];
  if (!schema)
    throw new Error("can not found schema object");
  return wrapSSR(import_react18.default.createElement(
    ArrayBase,
    null,
    import_react18.default.createElement(
      "div",
      __assign7({}, props, { onChange: function() {
      }, className: (0, import_classnames4.default)(prefixCls, hashId, props.className) }),
      import_react18.default.createElement(SortableList, { list: dataSource.slice(), className: "".concat(prefixCls, "-sort-helper"), onSortEnd: function(event) {
        var oldIndex = event.oldIndex, newIndex = event.newIndex;
        field.move(oldIndex, newIndex);
      } }, dataSource === null || dataSource === void 0 ? void 0 : dataSource.map(function(item, index) {
        var items = Array.isArray(schema.items) ? schema.items[index] || schema.items[0] : schema.items;
        return import_react18.default.createElement(
          ArrayBase.Item,
          { key: index, index, record: function() {
            var _a2;
            return (_a2 = field.value) === null || _a2 === void 0 ? void 0 : _a2[index];
          } },
          import_react18.default.createElement(
            SortableItem,
            { key: "item-".concat(index), lockAxis: "y", index },
            import_react18.default.createElement("div", { className: "".concat(prefixCls, "-item-inner") }, items ? import_react18.default.createElement(RecursionField, { schema: items, name: index }) : null)
          )
        );
      })),
      addition
    )
  ));
});
var Item2 = function(props) {
  var prefixCls = usePrefixCls("formily-array-items");
  var _a = __read4(style_default4(prefixCls), 2), wrapSSR = _a[0], hashId = _a[1];
  return wrapSSR(import_react18.default.createElement("div", __assign7({}, props, { onChange: function() {
  }, className: (0, import_classnames4.default)("".concat(prefixCls, "-").concat(props.type || "card"), hashId, props.className) }), props.children));
};
var ArrayItems = Object.assign(ArrayBase.mixin(InternalArrayItems), {
  Item: Item2
});
ArrayItems.displayName = "ArrayItems";

// node_modules/@formily/antd-v5/esm/array-table/index.js
var import_classnames5 = __toESM(require_classnames());
var import_react20 = __toESM(require_react());

// node_modules/@formily/antd-v5/esm/array-table/style.js
var style_default5 = genStyleHook("array-table", function(token) {
  var _a, _b, _c, _d, _e, _f;
  var componentCls = token.componentCls, antCls = token.antCls, colorErrorBorder = token.colorErrorBorder, fontSizeSM = token.fontSizeSM, colorBgBase = token.colorBgBase, colorBorder = token.colorBorder, colorBgContainer = token.colorBgContainer;
  var itemCls = "".concat(antCls, "-formily-item");
  return _a = {}, _a[componentCls] = (_b = {}, _b["".concat(componentCls, "-pagination")] = (_c = {
    display: "flex",
    justifyContent: "center"
  }, _c["".concat(componentCls, "-status-select.has-error")] = (_d = {}, _d["".concat(antCls, "-select-selector")] = {
    borderColor: "".concat(colorErrorBorder, " !important")
  }, _d), _c), _b["".concat(antCls, "-table")] = {
    td: (_e = {
      visibility: "visible"
    }, _e["".concat(itemCls, ":not(").concat(itemCls, "-feedback-layout-popover)")] = (_f = {
      marginBottom: "0 !important"
    }, _f["".concat(itemCls, "-help")] = {
      position: "absolute",
      fontSize: fontSizeSM,
      top: "100%",
      background: colorBgBase,
      width: "100%",
      marginTop: 3,
      padding: 3,
      zIndex: 1,
      borderRadius: 3,
      boxShadow: "0 0 10px ".concat(colorBorder),
      animation: "none",
      transform: "translateY(0)",
      opacity: 1
    }, _f), _e),
    table: {
      overflow: "hidden"
    }
  }, _b["".concat(componentCls, "-sort-helper")] = {
    background: colorBgContainer,
    border: "1px solid ".concat(colorBorder),
    zIndex: 10
  }, _b), _a;
});

// node_modules/@formily/antd-v5/esm/array-table/index.js
var __assign8 = function() {
  __assign8 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign8.apply(this, arguments);
};
var __rest3 = function(s, e) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
var __read5 = function(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
};
var SortableRow = SortableElement(function(props) {
  return import_react20.default.createElement("tr", __assign8({}, props));
});
var SortableBodyRaw = SortableContainer(function(_a) {
  var tbodyRef = _a.tbodyRef, props = __rest3(_a, ["tbodyRef"]);
  return import_react20.default.createElement("tbody", __assign8({}, props, { ref: tbodyRef }));
});
var SortableBody = (0, import_react20.forwardRef)(function(props, ref) {
  return import_react20.default.createElement(SortableBodyRaw, __assign8({}, props, { tbodyRef: ref }));
});
var isColumnComponent = function(schema) {
  var _a;
  return ((_a = schema["x-component"]) === null || _a === void 0 ? void 0 : _a.indexOf("Column")) > -1;
};
var isOperationsComponent = function(schema) {
  var _a;
  return ((_a = schema["x-component"]) === null || _a === void 0 ? void 0 : _a.indexOf("Operations")) > -1;
};
var isAdditionComponent4 = function(schema) {
  var _a;
  return ((_a = schema["x-component"]) === null || _a === void 0 ? void 0 : _a.indexOf("Addition")) > -1;
};
var useArrayTableSources = function() {
  var arrayField = useField();
  var schema = useFieldSchema();
  var parseSources = function(schema2) {
    var _a, _b, _c;
    if (isColumnComponent(schema2) || isOperationsComponent(schema2) || isAdditionComponent4(schema2)) {
      if (!((_a = schema2["x-component-props"]) === null || _a === void 0 ? void 0 : _a["dataIndex"]) && !schema2["name"])
        return [];
      var name_1 = ((_b = schema2["x-component-props"]) === null || _b === void 0 ? void 0 : _b["dataIndex"]) || schema2["name"];
      var field = arrayField.query(arrayField.address.concat(name_1)).take();
      var columnProps = ((_c = field === null || field === void 0 ? void 0 : field.component) === null || _c === void 0 ? void 0 : _c[1]) || schema2["x-component-props"] || {};
      var display = (field === null || field === void 0 ? void 0 : field.display) || schema2["x-display"];
      return [
        {
          name: name_1,
          display,
          field,
          schema: schema2,
          columnProps
        }
      ];
    } else if (schema2.properties) {
      return schema2.reduceProperties(function(buf, schema3) {
        return buf.concat(parseSources(schema3));
      }, []);
    }
    return [];
  };
  var parseArrayItems = function(schema2) {
    if (!schema2)
      return [];
    var sources = [];
    var items = isArr(schema2) ? schema2 : [schema2];
    return items.reduce(function(columns, schema3) {
      var item = parseSources(schema3);
      if (item) {
        return columns.concat(item);
      }
      return columns;
    }, sources);
  };
  if (!schema)
    throw new Error("can not found schema object");
  return parseArrayItems(schema.items);
};
var indexKey = "__DO_NOT_USE_THIS_PROPERTY_index__";
var useArrayTableColumns = function(dataSource, field, sources) {
  return sources.reduce(function(buf, _a, key2) {
    var name = _a.name, columnProps = _a.columnProps, schema = _a.schema, display = _a.display;
    if (display !== "visible")
      return buf;
    if (!isColumnComponent(schema))
      return buf;
    return buf.concat(__assign8(__assign8({}, columnProps), { key: key2, dataIndex: name, render: function(value, record) {
      var index = record[indexKey];
      var children = import_react20.default.createElement(
        ArrayBase.Item,
        { index, record: function() {
          var _a2;
          return (_a2 = field === null || field === void 0 ? void 0 : field.value) === null || _a2 === void 0 ? void 0 : _a2[index];
        } },
        import_react20.default.createElement(RecursionField, { schema, name: index, onlyRenderProperties: true })
      );
      return index > -1 ? children : null;
    } }));
  }, []);
};
var useAddition2 = function() {
  var schema = useFieldSchema();
  return schema.reduceProperties(function(addition, schema2, key2) {
    if (isAdditionComponent4(schema2)) {
      return import_react20.default.createElement(RecursionField, { schema: schema2, name: key2 });
    }
    return addition;
  }, null);
};
var schedulerRequest = {
  request: null
};
var StatusSelect = observer(function(props) {
  var _a;
  var field = useField();
  var prefixCls = usePrefixCls("formily-array-table");
  var _b = __read5(style_default5(prefixCls), 2), wrapSSR = _b[0], hashId = _b[1];
  var errors = field.errors;
  var parseIndex = function(address) {
    var _a2;
    return Number((_a2 = address === null || address === void 0 ? void 0 : address.slice(address.indexOf(field.address.toString())).match(/(\d+)/)) === null || _a2 === void 0 ? void 0 : _a2[1]);
  };
  var options = (_a = props.options) === null || _a === void 0 ? void 0 : _a.map(function(_a2) {
    var label = _a2.label, value = _a2.value;
    var val = Number(value);
    var hasError = errors.some(function(_a3) {
      var _b2;
      var address = _a3.address;
      var currentIndex = parseIndex(address);
      var startIndex = props.pageSize ? (val - 1) * props.pageSize : 0;
      var endIndex = props.pageSize ? val * props.pageSize : ((_b2 = props.options) === null || _b2 === void 0 ? void 0 : _b2.length) || 0;
      return currentIndex >= startIndex && currentIndex <= endIndex;
    });
    return {
      label: hasError ? import_react20.default.createElement(badge_default, { dot: true }, label) : label,
      value
    };
  });
  var width = String(options === null || options === void 0 ? void 0 : options.length).length * 15;
  return wrapSSR(import_react20.default.createElement(select_default, { value: props.value, onChange: props.onChange, options, virtual: true, style: {
    width: width < 60 ? 60 : width
  }, className: (0, import_classnames5.default)("".concat(prefixCls, "-status-select"), hashId, {
    "has-error": errors === null || errors === void 0 ? void 0 : errors.length
  }) }));
}, {
  scheduler: function(update) {
    clearTimeout(schedulerRequest.request);
    schedulerRequest.request = setTimeout(function() {
      update();
    }, 100);
  }
});
var PaginationContext = (0, import_react20.createContext)({});
var usePagination = function() {
  return (0, import_react20.useContext)(PaginationContext);
};
var ArrayTablePagination = function(props) {
  var _a;
  var _b = __read5((0, import_react20.useState)(1), 2), current = _b[0], setCurrent = _b[1];
  var prefixCls = usePrefixCls("formily-array-table");
  var _c = __read5(style_default5(prefixCls), 2), wrapSSR = _c[0], hashId = _c[1];
  var _d = __read5((0, import_react20.useState)(props.pageSize || 10), 2), pageSize = _d[0], setPageSize = _d[1];
  var size2 = props.size || "default";
  var dataSource = props.dataSource || [];
  var showPagination = props.showPagination;
  var startIndex = (current - 1) * pageSize;
  var endIndex = startIndex + pageSize - 1;
  var total = (dataSource === null || dataSource === void 0 ? void 0 : dataSource.length) || 0;
  var totalPage = Math.ceil(total / pageSize);
  var pages = Array.from(new Array(totalPage)).map(function(_, index) {
    var page = index + 1;
    return {
      label: page,
      value: page
    };
  });
  var handleChange = function(current2) {
    setCurrent(current2);
  };
  var handleSizeChange = function(_, size3) {
    setPageSize(size3);
  };
  (0, import_react20.useEffect)(function() {
    setPageSize(props.pageSize || 10);
  }, [props.pageSize]);
  (0, import_react20.useEffect)(function() {
    if (totalPage > 0 && totalPage < current) {
      handleChange(totalPage);
    }
  }, [totalPage, current]);
  var renderPagination = function() {
    if (!showPagination || totalPage < 1)
      return;
    return import_react20.default.createElement(
      "div",
      { className: (0, import_classnames5.default)("".concat(prefixCls, "-pagination"), hashId) },
      import_react20.default.createElement(
        space_default,
        null,
        import_react20.default.createElement(StatusSelect, { value: current, pageSize, onChange: handleChange, options: pages, notFoundContent: false }),
        import_react20.default.createElement(pagination_default, __assign8({}, props, { pageSize, current, total: dataSource.length, size: size2, showSizeChanger: true, onShowSizeChange: handleSizeChange, onChange: handleChange }))
      )
    );
  };
  return wrapSSR(import_react20.default.createElement(
    import_react20.Fragment,
    null,
    import_react20.default.createElement(PaginationContext.Provider, { value: { totalPage, pageSize, startIndex, changePage: handleChange } }, (_a = props.children) === null || _a === void 0 ? void 0 : _a.call(props, showPagination ? dataSource === null || dataSource === void 0 ? void 0 : dataSource.slice(startIndex, endIndex + 1) : dataSource, renderPagination(), {
      startIndex
    }))
  ));
};
var WrapperComp = function(props) {
  var ref = (0, import_react20.useRef)(null);
  var field = useField();
  var prefixCls = usePrefixCls("formily-array-table");
  var startIndex = (0, import_react20.useContext)(PaginationContext).startIndex;
  var dataSource = Array.isArray(field.value) ? field.value.slice() : [];
  var addTdStyles = function(id) {
    var _a;
    var node = (_a = ref.current) === null || _a === void 0 ? void 0 : _a.querySelector(".".concat(prefixCls, "-row-").concat(id));
    var helper = document.body.querySelector(".".concat(prefixCls, "-sort-helper"));
    if (!helper)
      return;
    var tds = node === null || node === void 0 ? void 0 : node.querySelectorAll("td");
    if (!tds)
      return;
    requestAnimationFrame(function() {
      helper.querySelectorAll("td").forEach(function(td, index) {
        if (tds[index]) {
          td.style.width = getComputedStyle(tds[index]).width;
        }
      });
    });
  };
  return import_react20.default.createElement(SortableBody, __assign8({ ref }, props, { start: startIndex, list: dataSource.slice(), accessibility: {
    container: ref.current || void 0
  }, onSortStart: function(event) {
    addTdStyles(event.active.id);
  }, onSortEnd: function(_a) {
    var oldIndex = _a.oldIndex, newIndex = _a.newIndex;
    field.move(oldIndex, newIndex);
  }, className: (0, import_classnames5.default)("".concat(prefixCls, "-sort-helper"), props.className) }));
};
var RowComp = function(props) {
  var prefixCls = usePrefixCls("formily-array-table");
  var index = props["data-row-key"] || 0;
  return import_react20.default.createElement(SortableRow, __assign8({ lockAxis: "y" }, props, { index, className: (0, import_classnames5.default)(props.className, "".concat(prefixCls, "-row-").concat(index + 1)) }));
};
var InternalArrayTable = observer(function(props) {
  var ref = (0, import_react20.useRef)(null);
  var field = useField();
  var prefixCls = usePrefixCls("formily-array-table");
  var _a = __read5(style_default5(prefixCls), 2), wrapSSR = _a[0], hashId = _a[1];
  var dataSource = Array.isArray(field.value) ? field.value.slice() : [];
  dataSource.forEach(function(item, index) {
    item[indexKey] = index;
  });
  var sources = useArrayTableSources();
  var columns = useArrayTableColumns(dataSource, field, sources);
  var pagination = isBool(props.pagination) ? {} : props.pagination;
  var showPagination = isBool(props.pagination) ? props.pagination : true;
  var addition = useAddition2();
  var defaultRowKey = function(record) {
    return record[indexKey];
  };
  return wrapSSR(import_react20.default.createElement(ArrayTablePagination, __assign8({}, pagination, { dataSource, showPagination }), function(dataSource2, pager, _a2) {
    var startIndex = _a2.startIndex;
    return import_react20.default.createElement(
      "div",
      { ref, className: (0, import_classnames5.default)(prefixCls, hashId) },
      import_react20.default.createElement(
        ArrayBase,
        null,
        import_react20.default.createElement(table_default, __assign8({ size: "small", bordered: true, rowKey: defaultRowKey }, props, { onChange: function() {
        }, pagination: false, columns, dataSource: dataSource2, components: {
          body: {
            wrapper: WrapperComp,
            row: RowComp
          }
        } })),
        import_react20.default.createElement("div", { style: { marginTop: 5, marginBottom: 5 } }, pager),
        sources.map(function(column, key2) {
          if (!isColumnComponent(column.schema))
            return;
          return import_react20.default.createElement(RecursionField, {
            name: column.name,
            schema: column.schema,
            onlyRenderSelf: true,
            key: key2
          });
        }),
        addition
      )
    );
  }));
});
var Column = function() {
  return import_react20.default.createElement(import_react20.Fragment, null);
};
var Addition2 = function(props) {
  var array = ArrayBase.useArray();
  var _a = usePagination(), _b = _a.totalPage, totalPage = _b === void 0 ? 0 : _b, _c = _a.pageSize, pageSize = _c === void 0 ? 10 : _c, changePage = _a.changePage;
  return import_react20.default.createElement(ArrayBase.Addition, __assign8({}, props, { onClick: function(e) {
    var _a2, _b2;
    var total = ((_a2 = array === null || array === void 0 ? void 0 : array.field) === null || _a2 === void 0 ? void 0 : _a2.value.length) || 0;
    if (total === totalPage * pageSize + 1 && isFn(changePage)) {
      changePage(totalPage + 1);
    }
    (_b2 = props.onClick) === null || _b2 === void 0 ? void 0 : _b2.call(props, e);
  } }));
};
var ArrayTable = Object.assign(ArrayBase.mixin(InternalArrayTable), {
  Column,
  Addition: Addition2
});
ArrayTable.displayName = "ArrayTable";

// node_modules/@formily/antd-v5/esm/array-tabs/index.js
var import_react22 = __toESM(require_react());
var __assign9 = function() {
  __assign9 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign9.apply(this, arguments);
};
var __read6 = function(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
};
var FeedbackBadge = observer(function(props) {
  var field = useField();
  var tab = "".concat(field.title || "Untitled", " ").concat(props.index + 1);
  var errors = field.errors.filter(function(error) {
    var _a;
    return (_a = error.address) === null || _a === void 0 ? void 0 : _a.includes("".concat(field.address, ".").concat(props.index));
  });
  if (errors.length) {
    return import_react22.default.createElement(badge_default, { size: "small", className: "errors-badge", count: errors.length }, tab);
  }
  return import_react22.default.createElement(import_react22.Fragment, null, tab);
});
var ArrayTabs = observer(function(props) {
  var field = useField();
  var schema = useFieldSchema();
  var _a = __read6((0, import_react22.useState)("tab-0"), 2), activeKey = _a[0], setActiveKey = _a[1];
  var value = Array.isArray(field.value) ? field.value : [];
  var dataSource = (value === null || value === void 0 ? void 0 : value.length) ? value : [{}];
  var onEdit = function(targetKey, type) {
    var _a2, _b;
    if (type == "add") {
      var id = dataSource.length;
      if ((_a2 = field === null || field === void 0 ? void 0 : field.value) === null || _a2 === void 0 ? void 0 : _a2.length) {
        field.push(null);
      } else {
        field.push(null, null);
      }
      setActiveKey("tab-".concat(id));
    } else if (type == "remove") {
      var index = Number((_b = targetKey.match(/-(\d+)/)) === null || _b === void 0 ? void 0 : _b[1]);
      if (index - 1 > -1) {
        setActiveKey("tab-".concat(index - 1));
      }
      field.remove(index);
    }
  };
  return import_react22.default.createElement(tabs_default, __assign9({}, props, { activeKey, onChange: function(key2) {
    setActiveKey(key2);
  }, type: "editable-card", onEdit, items: dataSource === null || dataSource === void 0 ? void 0 : dataSource.map(function(item, index) {
    var items = Array.isArray(schema.items) ? schema.items[index] : schema.items;
    var key2 = "tab-".concat(index);
    return {
      key: key2,
      label: import_react22.default.createElement(FeedbackBadge, { index }),
      forceRender: true,
      closable: index !== 0,
      children: items ? import_react22.default.createElement(RecursionField, { schema: items, name: index }) : null
    };
  }) }));
});

// node_modules/@formily/antd-v5/esm/cascader/index.js
var import_react26 = __toESM(require_react());

// node_modules/@formily/antd-v5/esm/preview-text/index.js
var import_classnames6 = __toESM(require_classnames());
var import_react24 = __toESM(require_react());

// node_modules/@formily/antd-v5/esm/preview-text/style.js
var style_default6 = genStyleHook("preview-text", function(token) {
  var _a, _b;
  var componentCls = token.componentCls, antCls = token.antCls, fontSize = token.fontSize, fontWeightStrong = token.fontWeightStrong;
  return [
    (_a = {}, _a[componentCls] = (_b = {
      fontSize,
      fontWeight: fontWeightStrong
    }, _b["".concat(antCls, "-tag:last-child")] = {
      marginInlineEnd: 0
    }, _b), _a)
  ];
});

// node_modules/@formily/antd-v5/esm/preview-text/index.js
var __read7 = function(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
};
var PlaceholderContext = (0, import_react24.createContext)("N/A");
var Placeholder = PlaceholderContext.Provider;
var usePlaceholder = function(value) {
  var placeholder = (0, import_react24.useContext)(PlaceholderContext) || "N/A";
  return isValid(value) && value !== "" ? value : placeholder;
};
var Input = observer(function(props) {
  var prefixCls = usePrefixCls("form-text", props);
  var _a = __read7(style_default6(prefixCls), 2), wrapSSR = _a[0], hashId = _a[1];
  return wrapSSR(import_react24.default.createElement(
    space_default,
    { className: (0, import_classnames6.default)(prefixCls, hashId, props.className), style: props.style },
    props.addonBefore,
    props.prefix,
    usePlaceholder(props.value),
    props.suffix,
    props.addonAfter
  ));
});
var NumberPicker = observer(function(props) {
  var prefixCls = usePrefixCls("form-text", props);
  var _a = __read7(style_default6(prefixCls), 2), wrapSSR = _a[0], hashId = _a[1];
  return wrapSSR(import_react24.default.createElement(
    space_default,
    { className: (0, import_classnames6.default)(prefixCls, hashId, props.className), style: props.style },
    props.addonBefore,
    props.prefix,
    usePlaceholder(props.formatter ? props.formatter(String(props.value), {
      userTyping: false,
      input: ""
    }) : props.value),
    props["suffix"],
    props.addonAfter
  ));
});
var Select = observer(function(props) {
  var _a, _b;
  var field = useField();
  var prefixCls = usePrefixCls("form-text", props);
  var _c = __read7(style_default6(prefixCls), 2), wrapSSR = _c[0], hashId = _c[1];
  var dataSource = ((_a = field === null || field === void 0 ? void 0 : field.dataSource) === null || _a === void 0 ? void 0 : _a.length) ? field.dataSource : ((_b = props === null || props === void 0 ? void 0 : props.options) === null || _b === void 0 ? void 0 : _b.length) ? props.options : [];
  var placeholder = usePlaceholder();
  var getSelected = function() {
    var value = props.value;
    if (props.mode === "multiple" || props.mode === "tags") {
      if (props.labelInValue) {
        return isArr(value) ? value : [];
      } else {
        return isArr(value) ? value.map(function(val) {
          return { label: val, value: val };
        }) : [];
      }
    } else {
      if (props.labelInValue) {
        return isValid(value) ? [value] : [];
      } else {
        return isValid(value) ? [{ label: value, value }] : [];
      }
    }
  };
  var getLabel = function(target) {
    var _a2, _b2;
    var labelKey = ((_a2 = props.fieldNames) === null || _a2 === void 0 ? void 0 : _a2.label) || "label";
    return ((_b2 = dataSource === null || dataSource === void 0 ? void 0 : dataSource.find(function(item) {
      var _a3;
      var valueKey = ((_a3 = props.fieldNames) === null || _a3 === void 0 ? void 0 : _a3.value) || "value";
      return item[valueKey] == (target === null || target === void 0 ? void 0 : target.value);
    })) === null || _b2 === void 0 ? void 0 : _b2[labelKey]) || target.label || placeholder;
  };
  var getLabels = function() {
    var selected = getSelected();
    if (!selected.length)
      return placeholder;
    if (selected.length === 1)
      return getLabel(selected[0]);
    return selected.map(function(item, key2) {
      return import_react24.default.createElement(tag_default, { key: key2 }, getLabel(item));
    });
  };
  return wrapSSR(import_react24.default.createElement("div", { className: (0, import_classnames6.default)(prefixCls, hashId, props.className), style: props.style }, getLabels()));
});
var TreeSelect = observer(function(props) {
  var _a, _b;
  var field = useField();
  var placeholder = usePlaceholder();
  var prefixCls = usePrefixCls("form-text", props);
  var _c = __read7(style_default6(prefixCls), 2), wrapSSR = _c[0], hashId = _c[1];
  var dataSource = ((_a = field === null || field === void 0 ? void 0 : field.dataSource) === null || _a === void 0 ? void 0 : _a.length) ? field.dataSource : ((_b = props === null || props === void 0 ? void 0 : props.treeData) === null || _b === void 0 ? void 0 : _b.length) ? props.treeData : [];
  var getSelected = function() {
    var value = props.value;
    if (props.multiple) {
      if (props.labelInValue) {
        return isArr(value) ? value : [];
      } else {
        return isArr(value) ? value.map(function(val) {
          return { label: val, value: val };
        }) : [];
      }
    } else {
      if (props.labelInValue) {
        return value ? [value] : [];
      } else {
        return value ? [{ label: value, value }] : [];
      }
    }
  };
  var findLabel = function(value, dataSource2, treeNodeLabelProp) {
    var _a2, _b2;
    var valueKey = ((_a2 = props.fieldNames) === null || _a2 === void 0 ? void 0 : _a2.value) || "value";
    for (var i = 0; i < (dataSource2 === null || dataSource2 === void 0 ? void 0 : dataSource2.length); i++) {
      var item = dataSource2[i];
      if ((item === null || item === void 0 ? void 0 : item[valueKey]) === value) {
        return (_b2 = item === null || item === void 0 ? void 0 : item.label) !== null && _b2 !== void 0 ? _b2 : item[treeNodeLabelProp];
      } else {
        var childLabel = findLabel(value, item === null || item === void 0 ? void 0 : item.children, treeNodeLabelProp);
        if (childLabel)
          return childLabel;
      }
    }
  };
  var getLabels = function() {
    var selected = getSelected();
    if (!(selected === null || selected === void 0 ? void 0 : selected.length))
      return import_react24.default.createElement(tag_default, null, placeholder);
    return selected.map(function(_a2, key2) {
      var value = _a2.value, label = _a2.label;
      return import_react24.default.createElement(tag_default, { key: key2 }, findLabel(value, dataSource, props.treeNodeLabelProp) || label || placeholder);
    });
  };
  return wrapSSR(import_react24.default.createElement("div", { className: (0, import_classnames6.default)(prefixCls, hashId, props.className), style: props.style }, getLabels()));
});
var Cascader = observer(function(props) {
  var _a, _b;
  var field = useField();
  var placeholder = usePlaceholder();
  var prefixCls = usePrefixCls("form-text", props);
  var _c = __read7(style_default6(prefixCls), 2), wrapSSR = _c[0], hashId = _c[1];
  var dataSource = ((_a = field === null || field === void 0 ? void 0 : field.dataSource) === null || _a === void 0 ? void 0 : _a.length) ? field.dataSource : ((_b = props === null || props === void 0 ? void 0 : props.options) === null || _b === void 0 ? void 0 : _b.length) ? props.options : [];
  var findSelectedItem = function(items, val) {
    return items.find(function(item) {
      return item.value == val;
    });
  };
  var findSelectedItems = function(sources, selectedValues) {
    return selectedValues.map(function(value) {
      var result = [];
      var items = sources;
      value.forEach(function(val) {
        var _a2, _b2;
        var selectedItem = findSelectedItem(items, val);
        result.push({
          label: (_a2 = selectedItem === null || selectedItem === void 0 ? void 0 : selectedItem.label) !== null && _a2 !== void 0 ? _a2 : "",
          value: selectedItem === null || selectedItem === void 0 ? void 0 : selectedItem.value
        });
        items = (_b2 = selectedItem === null || selectedItem === void 0 ? void 0 : selectedItem.children) !== null && _b2 !== void 0 ? _b2 : [];
      });
      return result;
    });
  };
  var getSelected = function() {
    var val = toArr(props.value);
    return props.multiple ? val : [val];
  };
  var getLabels = function() {
    var selected = getSelected();
    var values = findSelectedItems(dataSource, selected);
    var labels = values.map(function(val) {
      return val.map(function(item) {
        return item.label;
      }).join("/");
    }).join(" ");
    return labels || placeholder;
  };
  return wrapSSR(import_react24.default.createElement("div", { className: (0, import_classnames6.default)(prefixCls, hashId, props.className), style: props.style }, getLabels()));
});
var DatePicker = function(props) {
  var placeholder = usePlaceholder();
  var prefixCls = usePrefixCls("form-text", props);
  var getLabels = function() {
    var labels = formatDayjsValue(props.value, props.format, placeholder);
    return isArr(labels) ? labels.join("~") : labels;
  };
  return import_react24.default.createElement("div", { className: (0, import_classnames6.default)(prefixCls, props.className) }, getLabels());
};
var DateRangePicker = function(props) {
  var placeholder = usePlaceholder();
  var prefixCls = usePrefixCls("form-text", props);
  var getLabels = function() {
    var labels = formatDayjsValue(props.value, props.format, placeholder);
    return isArr(labels) ? labels.join("~") : labels;
  };
  return import_react24.default.createElement("div", { className: (0, import_classnames6.default)(prefixCls, props.className), style: props.style }, getLabels());
};
var TimePicker = function(props) {
  var placeholder = usePlaceholder();
  var prefixCls = usePrefixCls("form-text", props);
  var getLabels = function() {
    var labels = formatDayjsValue(props.value, props.format, placeholder);
    return isArr(labels) ? labels.join("~") : labels;
  };
  return import_react24.default.createElement("div", { className: (0, import_classnames6.default)(prefixCls, props.className), style: props.style }, getLabels());
};
var TimeRangePicker = function(props) {
  var placeholder = usePlaceholder();
  var prefixCls = usePrefixCls("form-text", props);
  var getLabels = function() {
    var labels = formatDayjsValue(props.value, props.format, placeholder);
    return isArr(labels) ? labels.join("~") : labels;
  };
  return import_react24.default.createElement("div", { className: (0, import_classnames6.default)(prefixCls, props.className), style: props.style }, getLabels());
};
var InternalPreviewText = observer(function(props) {
  var prefixCls = usePrefixCls("form-text", props);
  return import_react24.default.createElement("div", { className: (0, import_classnames6.default)(prefixCls, props.className), style: props.style }, usePlaceholder(props.value));
});
var PreviewText = Object.assign(InternalPreviewText, {
  Input,
  Select,
  TreeSelect,
  Cascader,
  DatePicker,
  DateRangePicker,
  TimePicker,
  TimeRangePicker,
  Placeholder,
  usePlaceholder,
  NumberPicker
});

// node_modules/@formily/antd-v5/esm/cascader/index.js
var __assign10 = function() {
  __assign10 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign10.apply(this, arguments);
};
var Cascader2 = connect(cascader_default, mapProps({
  dataSource: "options"
}, function(props, field) {
  return __assign10(__assign10({}, props), { suffixIcon: (field === null || field === void 0 ? void 0 : field["loading"]) || (field === null || field === void 0 ? void 0 : field["validating"]) ? import_react26.default.createElement(LoadingOutlined_default, null) : props.suffixIcon });
}), mapReadPretty(PreviewText.Cascader));

// node_modules/@formily/antd-v5/esm/checkbox/index.js
var InternalCheckbox = connect(checkbox_default, mapProps({
  value: "checked",
  onInput: "onChange"
}));
var Group = connect(checkbox_default.Group, mapProps({
  dataSource: "options"
}), mapReadPretty(PreviewText.Select, {
  mode: "tags"
}));
var Checkbox = Object.assign(InternalCheckbox, {
  __ANT_CHECKBOX: true,
  Group
});

// node_modules/@formily/antd-v5/esm/date-picker/index.js
var __assign11 = function() {
  __assign11 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign11.apply(this, arguments);
};
var mapDateFormat = function() {
  var getDefaultFormat = function(props) {
    if (props["picker"] === "month") {
      return "YYYY-MM";
    } else if (props["picker"] === "quarter") {
      return "YYYY-\\QQ";
    } else if (props["picker"] === "year") {
      return "YYYY";
    } else if (props["picker"] === "week") {
      return "gggg-wo";
    }
    return props["showTime"] ? "YYYY-MM-DD HH:mm:ss" : "YYYY-MM-DD";
  };
  return function(props) {
    var format = props["format"] || getDefaultFormat(props);
    var onChange = props.onChange;
    return __assign11(__assign11({}, props), { format, value: dayjsable(props.value, format === "gggg-wo" ? "gggg-ww" : format), onChange: function(value) {
      if (onChange) {
        onChange(formatDayjsValue(value, format));
      }
    } });
  };
};
var InternalDatePicker = connect(date_picker_default, mapProps(mapDateFormat()), mapReadPretty(PreviewText.DatePicker));
var RangePicker = connect(date_picker_default.RangePicker, mapProps(mapDateFormat()), mapReadPretty(PreviewText.DateRangePicker));
var DatePicker2 = Object.assign(InternalDatePicker, {
  RangePicker
});

// node_modules/@formily/antd-v5/esm/editable/index.js
var import_classnames9 = __toESM(require_classnames());
var import_react36 = __toESM(require_react());

// node_modules/@formily/antd-v5/esm/form-item/index.js
var import_classnames8 = __toESM(require_classnames());
var import_react34 = __toESM(require_react());

// node_modules/@formily/antd-v5/esm/form-layout/index.js
var import_react30 = __toESM(require_react());

// node_modules/@formily/antd-v5/esm/form-layout/useResponsiveFormLayout.js
var import_react29 = __toESM(require_react());
var __assign12 = function() {
  __assign12 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign12.apply(this, arguments);
};
var __rest4 = function(s, e) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
var __read8 = function(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
};
var calcBreakpointIndex = function(breakpoints, width) {
  if (!breakpoints)
    return;
  for (var i = 0; i < breakpoints.length; i++) {
    if (width <= breakpoints[i]) {
      return i;
    }
  }
};
var calcFactor = function(value, breakpointIndex) {
  var _a;
  if (Array.isArray(value)) {
    if (breakpointIndex === -1)
      return value[0];
    return (_a = value[breakpointIndex || value.length - 1]) !== null && _a !== void 0 ? _a : value[value.length - 1];
  } else {
    return value;
  }
};
var factor = function(value, breakpointIndex) {
  return isValid(value) ? calcFactor(value, breakpointIndex) : value;
};
var calculateProps = function(target, props) {
  var clientWidth = target.clientWidth;
  var breakpoints = props.breakpoints, layout = props.layout, labelAlign = props.labelAlign, wrapperAlign = props.wrapperAlign, labelCol = props.labelCol, wrapperCol = props.wrapperCol, otherProps = __rest4(props, ["breakpoints", "layout", "labelAlign", "wrapperAlign", "labelCol", "wrapperCol"]);
  var breakpointIndex = calcBreakpointIndex(breakpoints, clientWidth);
  return __assign12({ layout: factor(layout, breakpointIndex), labelAlign: factor(labelAlign, breakpointIndex), wrapperAlign: factor(wrapperAlign, breakpointIndex), labelCol: factor(labelCol, breakpointIndex), wrapperCol: factor(wrapperCol, breakpointIndex) }, otherProps);
};
var useResponsiveFormLayout = function(props) {
  var ref = (0, import_react29.useRef)(null);
  var breakpoints = props.breakpoints;
  if (!isArr(breakpoints)) {
    return { ref, props };
  }
  var _a = __read8((0, import_react29.useState)(props), 2), layoutProps = _a[0], setLayout = _a[1];
  var updateUI = function() {
    if (ref.current) {
      setLayout(calculateProps(ref.current, props));
    }
  };
  (0, import_react29.useEffect)(function() {
    var observer2 = function() {
      updateUI();
    };
    var resizeObserver = new ResizeObserver(observer2);
    if (ref.current) {
      resizeObserver.observe(ref.current);
    }
    updateUI();
    return function() {
      resizeObserver.disconnect();
    };
  }, []);
  return {
    ref,
    props: layoutProps
  };
};

// node_modules/@formily/antd-v5/esm/form-layout/index.js
var import_classnames7 = __toESM(require_classnames());
var __assign13 = function() {
  __assign13 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign13.apply(this, arguments);
};
var __rest5 = function(s, e) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
var FormLayoutDeepContext = (0, import_react30.createContext)({});
var FormLayoutShallowContext = (0, import_react30.createContext)({});
var useFormDeepLayout = function() {
  return (0, import_react30.useContext)(FormLayoutDeepContext);
};
var useFormShallowLayout = function() {
  return (0, import_react30.useContext)(FormLayoutShallowContext);
};
var useFormLayout = function() {
  return __assign13(__assign13({}, useFormDeepLayout()), useFormShallowLayout());
};
var FormLayout = function(_a) {
  var _b;
  var _c = _a.shallow, shallow = _c === void 0 ? true : _c, children = _a.children, prefixCls = _a.prefixCls, className = _a.className, style = _a.style, otherProps = __rest5(_a, ["shallow", "children", "prefixCls", "className", "style"]);
  var _d = useResponsiveFormLayout(otherProps), ref = _d.ref, props = _d.props;
  var deepLayout = useFormDeepLayout();
  var formPrefixCls = usePrefixCls("form", { prefixCls });
  var layoutPrefixCls = usePrefixCls("formily-layout", { prefixCls });
  var layoutClassName = (0, import_classnames7.default)(layoutPrefixCls, (_b = {}, _b["".concat(formPrefixCls, "-").concat(props.layout)] = true, _b["".concat(formPrefixCls, "-rtl")] = props.direction === "rtl", _b["".concat(formPrefixCls, "-").concat(props.size)] = props.size, _b), className);
  var renderChildren = function() {
    var newDeepLayout = __assign13({}, deepLayout);
    if (!shallow) {
      Object.assign(newDeepLayout, props);
    } else {
      if (props.size) {
        newDeepLayout.size = props.size;
      }
      if (props.colon) {
        newDeepLayout.colon = props.colon;
      }
    }
    return import_react30.default.createElement(
      FormLayoutDeepContext.Provider,
      { value: newDeepLayout },
      import_react30.default.createElement(FormLayoutShallowContext.Provider, { value: shallow ? props : void 0 }, children)
    );
  };
  return import_react30.default.createElement("div", { ref, className: layoutClassName, style }, renderChildren());
};
FormLayout.useFormDeepLayout = useFormDeepLayout;
FormLayout.useFormShallowLayout = useFormShallowLayout;
FormLayout.useFormLayout = useFormLayout;

// node_modules/@formily/antd-v5/esm/form-item/hooks/useOverflow.js
var import_react31 = __toESM(require_react());
var __read9 = function(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
};
function useOverflow() {
  var _a = __read9((0, import_react31.useState)(false), 2), overflow = _a[0], setOverflow = _a[1];
  var containerRef = (0, import_react31.useRef)(null);
  var contentRef = (0, import_react31.useRef)(null);
  var layout = useFormLayout();
  var labelCol = JSON.stringify(layout.labelCol);
  (0, import_react31.useEffect)(function() {
    requestAnimationFrame(function() {
      if (containerRef.current && contentRef.current) {
        var contentWidth = contentRef.current.getBoundingClientRect().width;
        var containerWidth = containerRef.current.getBoundingClientRect().width;
        if (contentWidth && containerWidth && containerWidth < contentWidth) {
          if (!overflow)
            setOverflow(true);
        } else {
          if (overflow)
            setOverflow(false);
        }
      }
    });
  }, [labelCol]);
  return {
    overflow,
    containerRef,
    contentRef
  };
}

// node_modules/@formily/antd-v5/esm/form-item/hooks/useFormItemLayout.js
var import_react32 = __toESM(require_react());
var __assign14 = function() {
  __assign14 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign14.apply(this, arguments);
};
var useFormItemLayout = function(props) {
  var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m, _o, _p, _q, _r, _s, _t, _u, _v, _w, _x, _y;
  var layout = useFormLayout();
  var layoutType = (_b = (_a = props.layout) !== null && _a !== void 0 ? _a : layout.layout) !== null && _b !== void 0 ? _b : "horizontal";
  return __assign14(__assign14({}, props), { layout: layoutType, colon: (_c = props.colon) !== null && _c !== void 0 ? _c : layout.colon, labelAlign: layoutType === "vertical" ? (_d = props.labelAlign) !== null && _d !== void 0 ? _d : "left" : (_f = (_e = props.labelAlign) !== null && _e !== void 0 ? _e : layout.labelAlign) !== null && _f !== void 0 ? _f : "right", labelWrap: (_g = props.labelWrap) !== null && _g !== void 0 ? _g : layout.labelWrap, labelWidth: (_h = props.labelWidth) !== null && _h !== void 0 ? _h : layout.labelWidth, wrapperWidth: (_j = props.wrapperWidth) !== null && _j !== void 0 ? _j : layout.wrapperWidth, labelCol: (_k = props.labelCol) !== null && _k !== void 0 ? _k : layout.labelCol, wrapperCol: (_l = props.wrapperCol) !== null && _l !== void 0 ? _l : layout.wrapperCol, wrapperAlign: (_m = props.wrapperAlign) !== null && _m !== void 0 ? _m : layout.wrapperAlign, wrapperWrap: (_o = props.wrapperWrap) !== null && _o !== void 0 ? _o : layout.wrapperWrap, fullness: (_p = props.fullness) !== null && _p !== void 0 ? _p : layout.fullness, size: (_q = props.size) !== null && _q !== void 0 ? _q : layout.size, inset: (_r = props.inset) !== null && _r !== void 0 ? _r : layout.inset, asterisk: props.asterisk, bordered: (_s = props.bordered) !== null && _s !== void 0 ? _s : layout.bordered, feedbackIcon: props.feedbackIcon, feedbackLayout: (_u = (_t = props.feedbackLayout) !== null && _t !== void 0 ? _t : layout.feedbackLayout) !== null && _u !== void 0 ? _u : "loose", tooltipLayout: (_w = (_v = props.tooltipLayout) !== null && _v !== void 0 ? _v : layout.tooltipLayout) !== null && _w !== void 0 ? _w : "icon", tooltipIcon: (_y = (_x = props.tooltipIcon) !== null && _x !== void 0 ? _x : layout.tooltipIcon) !== null && _y !== void 0 ? _y : import_react32.default.createElement(QuestionCircleOutlined_default, null) });
};

// node_modules/@formily/antd-v5/esm/form-item/style/animation.js
var antShowHelpIn = new Keyframes_default("antShowHelpIn", {
  "0%": {
    transform: "translateY(-5px)",
    opacity: 0
  },
  to: {
    transform: " translateY(0)",
    opacity: 1
  }
});
var getAnimationStyle = function(token) {
  var _a;
  var componentCls = token.componentCls;
  var helpCls = "".concat(componentCls, "-help");
  return _a = {}, _a[helpCls] = {
    "&-appear, &-enter": {
      animationDuration: "0.3s",
      animationFillMode: "both",
      animationPlayState: "paused",
      opacity: 0,
      animationTimingFunction: "cubic-bezier(0.645, 0.045, 0.355, 1)",
      "&-active": {
        animationPlayState: "running",
        animationName: antShowHelpIn
      }
    }
  }, _a;
};

// node_modules/@formily/antd-v5/esm/form-item/style/grid.js
var __assign15 = function() {
  __assign15 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign15.apply(this, arguments);
};
var getGridStyle = function(token) {
  var _a;
  var componentCls = token.componentCls;
  var colCls = "".concat(componentCls, "-item-col");
  var genGrid = function(grid) {
    return {
      flex: "0 0 ".concat(grid / 24 * 100, "%"),
      maxWidth: "".concat(grid / 24 * 100, "%")
    };
  };
  var genGrids = function() {
    return Array.from({ length: 24 }, function(_, i) {
      var _a2;
      var gridCls = "".concat(colCls, "-").concat(i + 1);
      return _a2 = {}, _a2[gridCls] = genGrid(i + 1), _a2;
    }).reduce(function(acc, cur) {
      return __assign15(__assign15({}, acc), cur);
    }, {});
  };
  return _a = {}, _a[componentCls] = __assign15({}, genGrids()), _a;
};

// node_modules/@formily/antd-v5/esm/form-item/style/other.js
var __assign16 = function() {
  __assign16 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign16.apply(this, arguments);
};
var genOtherStyle = function(token) {
  var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m, _o, _p, _q, _r, _s, _t, _u, _v, _w, _x, _y, _z, _0, _1, _2, _3, _4, _5, _6;
  var componentCls = token.componentCls, colorError = token.colorError, colorWarningBg = token.colorWarningBg, colorWarning = token.colorWarning, colorBorder = token.colorBorder, colorSuccess = token.colorSuccess, lineWidth = token.lineWidth, colorPrimaryBorderHover = token.colorPrimaryBorderHover, colorPrimary = token.colorPrimary, antCls = token.antCls, fontSize = token.fontSize, marginSM = token.marginSM, marginLG = token.marginLG, controlHeight = token.controlHeight, controlHeightSM = token.controlHeightSM, lineHeight = token.lineHeight, controlOutlineWidth = token.controlOutlineWidth, controlOutline = token.controlOutline, colorErrorOutline = token.colorErrorOutline, colorWarningOutline = token.colorWarningOutline, colorTextSecondary = token.colorTextSecondary, paddingXS = token.paddingXS;
  var hover = function(color) {
    if (color === void 0) {
      color = colorPrimaryBorderHover;
    }
    return {
      borderColor: color,
      borderInlineEndWidth: lineWidth
    };
  };
  var active = function(color, outlineColor) {
    if (color === void 0) {
      color = colorPrimary;
    }
    if (outlineColor === void 0) {
      outlineColor = controlOutline;
    }
    return {
      borderColor: color,
      borderInlineEndWidth: lineWidth,
      outline: 0,
      boxShadow: "0 0 0 ".concat(controlOutlineWidth, "px ").concat(outlineColor)
    };
  };
  var extraLabelHeight = controlHeightSM - 2;
  return _a = {}, _a[componentCls] = (_b = {
    "&-layout-vertical": (_c = {
      display: "block"
    }, // Vertical Label: https://github.com/ant-design/ant-design/blob/master/components/form/style/index.ts#L391C1-L404C4
    _c["".concat(componentCls, "-label")] = {
      minHeight: lineHeight * fontSize + paddingXS,
      lineHeight,
      padding: "0 0 ".concat(paddingXS, "px"),
      "&-content": {
        whiteSpace: "initial",
        textAlign: "start"
      }
    }, _c["".concat(antCls, "-label")] = {
      minHeight: controlHeight - 10,
      lineHeight
    }, _c),
    "&-success": (_d = {}, _d["".concat(antCls, "-select-selector,\n          ").concat(antCls, "-cascader-picker,\n          ").concat(antCls, "-picker,\n          ").concat(antCls, "-input,\n          ").concat(antCls, "-input-number,\n          ").concat(antCls, "-input-affix-wrapper,\n          ").concat(antCls, "-input-affix-wrapper,\n          ").concat(antCls, "-input")] = {
      borderColor: "".concat(colorSuccess, " !important")
    }, _d["".concat(antCls, "-select-selector,\n        ").concat(antCls, "-cascader-picker,\n        ").concat(antCls, "-picker,\n        ").concat(antCls, "-input,\n        ").concat(antCls, "-input-number,\n        ").concat(antCls, "-input-affix-wrapper,\n        ").concat(antCls, "-input-affix-wrapper:hover,\n        ").concat(antCls, "-input:hover")] = {
      borderColor: "".concat(colorSuccess, " !important")
    }, _d["".concat(antCls, "-input-affix-wrapper-focused,\n        ").concat(antCls, "-input-affix-wrapper:focus,\n        ").concat(antCls, "-input-focused,\n        ").concat(antCls, "-input:focus")] = {
      borderColor: "".concat(colorSuccess, " !important"),
      borderInlineEndWidth: "1px !important",
      outline: 0
    }, _d),
    "&-warning": (_e = {}, _e["".concat(antCls, "-select-selector,\n          ").concat(antCls, "-cascader-picker,\n          ").concat(antCls, "-picker,\n          ").concat(antCls, "-input,\n          ").concat(antCls, "-input-number,\n          ").concat(antCls, "-input-affix-wrapper,\n          ").concat(antCls, "-input-affix-wrapper,\n          ").concat(antCls, "-input")] = {
      borderColor: colorWarning
    }, _e["".concat(antCls, "-select-selector,\n          ").concat(antCls, "-cascader-picker,\n          ").concat(antCls, "-picker,\n          ").concat(antCls, "-input,\n          ").concat(antCls, "-input-number,\n          ").concat(antCls, "-input-affix-wrapper,\n          ").concat(antCls, "-input-affix-wrapper:hover,\n          ").concat(antCls, "-input:hover")] = {
      borderColor: colorWarning
    }, _e["".concat(antCls, "-select:not(").concat(antCls, "-select-disabled):not(").concat(antCls, "-select-customize-input)")] = (_f = {}, _f["".concat(antCls, "-select-selector")] = {
      backgroundColor: colorWarningBg,
      borderColor: colorWarning
    }, _f["&".concat(antCls, "-select-open ").concat(antCls, "-select-selector,\n              &").concat(antCls, "-select-focused ").concat(antCls, "-select-selector")] = __assign16({}, active(colorWarning, colorWarningOutline)), _f), _e["".concat(antCls, "-input-number,\n          ").concat(antCls, "-picker")] = (_g = {
      backgroundColor: colorWarningBg,
      borderColor: colorWarning
    }, _g["&-focused,\n            &:focus"] = __assign16({}, active(colorWarning, colorWarningOutline)), _g["&:not([disabled]):hover"] = {
      backgroundColor: colorWarningBg,
      borderColor: colorWarning
    }, _g), _e["".concat(antCls, "-cascader-picker:focus ").concat(antCls, "-cascader-input")] = __assign16({ backgroundColor: colorWarningBg }, active(colorWarning, colorWarningOutline)), _e["".concat(antCls, "-input-affix-wrapper-focused,\n          ").concat(antCls, "-input-affix-wrapper:focus,\n          ").concat(antCls, "-input-focused,\n          ").concat(antCls, "-input:focus")] = __assign16({}, active(colorWarning, colorWarningOutline)), _e),
    "&-error": (_h = {}, _h["".concat(antCls, "-select-selector,\n            ").concat(antCls, "-cascader-picker,\n            ").concat(antCls, "-picker,\n            ").concat(antCls, "-input,\n            ").concat(antCls, "-input-number,\n            ").concat(antCls, "-input-affix-wrapper,\n            ").concat(antCls, "-input")] = {
      borderColor: "".concat(colorError, " !important")
    }, _h["".concat(antCls, "-select-selector,\n            ").concat(antCls, "-cascader-picker,\n            ").concat(antCls, "-picker,\n            ").concat(antCls, "-input,\n            ").concat(antCls, "-input-number,\n            ").concat(antCls, "-input-affix-wrapper,\n            ").concat(antCls, "-input-affix-wrapper:hover,\n            ").concat(antCls, "-input:hover")] = {
      borderColor: "".concat(colorError, "  !important")
    }, _h["".concat(antCls, "-select:not(").concat(antCls, "-select-disabled):not(").concat(antCls, "-select-customize-input)")] = (_j = {}, _j["".concat(antCls, "-select-selector")] = {
      borderColor: "".concat(colorError, "  !important")
    }, _j["&".concat(antCls, "-select-open ").concat(antCls, "-select-selector,\n                &").concat(antCls, "-select-focused ").concat(antCls, "-select-selector")] = __assign16({}, active(colorError, colorErrorOutline)), _j), _h["".concat(antCls, "-input-number,\n            ").concat(antCls, "-picker")] = (_k = {
      borderColor: colorError
    }, _k["&-focused,\n            &:focus"] = __assign16({}, active(colorError, colorErrorOutline)), _k["&:not([disabled]):hover"] = {
      borderColor: colorError
    }, _k), _h["".concat(antCls, "-cascader-picker:focus ").concat(antCls, "-cascader-input")] = __assign16({}, active(colorError, colorErrorOutline)), _h["".concat(antCls, "-input-affix-wrapper-focused,\n            ").concat(antCls, "-input-affix-wrapper:focus,\n            ").concat(antCls, "-input-focused,\n            ").concat(antCls, "-input:focus")] = __assign16({}, active(colorError, colorErrorOutline)), _h),
    "&-feedback-layout": {
      "&-terse": (_l = {
        marginBottom: marginSM
      }, _l["&".concat(componentCls, "-feedback-has-text:not(").concat(componentCls, "-inset)")] = {
        marginBottom: 0
      }, _l),
      "&-loose": (_m = {
        marginBottom: marginLG
      }, _m["&".concat(componentCls, "-feedback-has-text:not(").concat(componentCls, "-inset)")] = {
        marginBottom: Math.max(marginLG - extraLabelHeight, 0)
      }, _m),
      "&-none": (_o = {
        marginBottom: 0
      }, _o["&".concat(componentCls, "-feedback-has-text:not(").concat(componentCls, "-inset)")] = {
        marginBottom: 0
      }, _o)
    },
    "&-control-wrap": (_p = {}, _p["".concat(componentCls, "-control")] = {
      whiteSpace: "pre-line",
      wordBreak: "break-all"
    }, _p),
    "&-control-align": {
      "&-left": (_q = {}, _q["".concat(componentCls, "-control-content")] = {
        justifyContent: "flex-start"
      }, _q),
      "&-right": (_r = {}, _r["".concat(componentCls, "-control-content")] = {
        justifyContent: "flex-end"
      }, _r)
    },
    "input[type='radio'], input[type='checkbox']": {
      width: fontSize,
      height: fontSize
    },
    "&-feedback-layout-popover": {
      marginBottom: 8
    },
    "&-fullness": (_s = {}, _s["> ".concat(componentCls, "-control")] = (_t = {}, _t["> ".concat(componentCls, "-control-content")] = (_u = {}, _u["> ".concat(componentCls, "-control-content-component")] = {
      "> *:first-child": {
        width: "100%"
      }
    }, _u), _t), _s),
    "&-inset": (_v = {
      borderRadius: 2,
      border: "1px solid ".concat(colorBorder),
      paddingInlineStart: 12,
      transition: "0.3s all"
    }, _v["".concat(antCls, "-input-number,\n          ").concat(antCls, "-picker,\n          ").concat(antCls, "-cascader-picker:focus ").concat(antCls, "-cascader-input,\n          ").concat(antCls, "-select:not(").concat(antCls, "-select-customize-input)\n          ").concat(antCls, "-select-selector,\n          ").concat(antCls, "-input-affix-wrapper,\n          ").concat(antCls, "-input")] = {
      border: "none !important",
      boxShadow: "none !important"
    }, _v["".concat(antCls, "-input-number-handler-wrap")] = (_w = {
      border: "none !important"
    }, _w["".concat(antCls, "-input-number-handler")] = {
      border: "none !important"
    }, _w), _v["&:hover"] = __assign16({}, hover()), _v),
    "&-active": (_x = {}, _x["".concat(componentCls, "-control-content-component-has-feedback-icon")] = __assign16({}, active()), _x["".concat(antCls, "-input-number,\n          ").concat(antCls, "-picker,\n          ").concat(antCls, "-cascader-picker:focus ").concat(antCls, "-cascader-input,\n          ").concat(antCls, "-select:not(").concat(antCls, "-select-customize-input)\n          ").concat(antCls, "-select-selector,\n          ").concat(antCls, "-input")] = __assign16({}, active()), _x),
    "&-inset-active": __assign16({}, active()),
    "&-bordered-none": (_y = {}, _y["".concat(antCls, "-input-number,\n          ").concat(antCls, "-input-affix-wrapper,\n          ").concat(antCls, "-picker,\n          ").concat(antCls, "-cascader-picker:focus ").concat(antCls, "-cascader-input,\n          ").concat(antCls, "-select:not(").concat(antCls, "-select-customize-input)\n          ").concat(antCls, "-select-selector,\n          ").concat(antCls, "-input")] = {
      border: "none !important",
      boxShadow: "none !important"
    }, _y["".concat(antCls, "-input-number-handler-wrap")] = (_z = {
      border: "none !important"
    }, _z["".concat(antCls, "-input-number-handler")] = {
      border: "none !important"
    }, _z), _y),
    "&:hover": (_0 = {}, _0["".concat(componentCls, "-control-content-component-has-feedback-icon")] = __assign16({}, hover()), _0)
  }, _b["textarea".concat(antCls, "-input")] = {
    height: "auto"
  }, _b["".concat(componentCls, "-label-tooltip-icon")] = {
    marginInlineStart: 4,
    color: token.colorTextSecondary,
    display: "flex",
    alignItems: "center",
    maxHeight: controlHeight,
    span: {
      display: "inline-block"
    }
  }, _b["".concat(componentCls, "-asterisk")] = {
    color: colorError,
    marginInlineEnd: "4px",
    display: "inline-block",
    fontFamily: "SimSun, sans-serif"
  }, _b["".concat(componentCls, "-colon")] = {
    marginInlineStart: 2,
    marginInlineEnd: 8
  }, _b["".concat(componentCls, "-control")] = (_1 = {
    flex: 1,
    maxWidth: "100%"
  }, _1["".concat(componentCls, "-control-content")] = (_2 = {
    display: "flex"
  }, _2["".concat(componentCls, "-control-content-component")] = (_3 = {
    width: "100%",
    minHeight: controlHeight - 2,
    lineHeight: "".concat(controlHeight - 2, "px")
  }, _3["&-has-feedback-icon"] = {
    flex: 1,
    position: "relative",
    display: "flex",
    alignItems: "center"
  }, _3), _2["".concat(componentCls, "-addon-before ")] = {
    marginInlineEnd: 8,
    display: "inline-flex",
    alignItems: "center",
    minHeight: controlHeight,
    flexShrink: 0
  }, _2["".concat(componentCls, "-addon-after")] = {
    marginInlineStart: 8,
    display: "inline-flex",
    alignItems: "center",
    minHeight: controlHeight,
    flexShrink: 0
  }, _2), _1["".concat(componentCls, "-help,\n        ").concat(componentCls, "-extra ")] = {
    minHeight: extraLabelHeight,
    lineHeight: "".concat(extraLabelHeight, "px"),
    color: colorTextSecondary
  }, _1), _b["".concat(componentCls, "-help,\n          ").concat(componentCls, "-extra")] = {
    clear: "both",
    minHeight: extraLabelHeight,
    color: "rgba(0, 0, 0, 0.45)",
    transition: "color 0.3s cubic-bezier(0.215, 0.61, 0.355, 1)",
    paddingTop: 0
  }, _b["".concat(componentCls, "-control-content-component-has-feedback-icon")] = (_4 = {
    borderRadius: 2,
    border: "1px solid ".concat(colorBorder),
    paddingInlineEnd: 8,
    transition: "all 0.3s",
    touchAction: "manipulation",
    outline: "none"
  }, _4["".concat(antCls, "-input-number,\n            ").concat(antCls, "-picker,\n            ").concat(antCls, "-cascader-picker:focus ").concat(antCls, "-cascader-input,\n            ").concat(antCls, "-select:not(").concat(antCls, "-select-customize-input)\n            ").concat(antCls, "-select-selector,\n            ").concat(antCls, "-input-affix-wrapper,\n            ").concat(antCls, "-input")] = {
    border: "none !important",
    boxShadow: "none !important"
  }, _4), _b["".concat(componentCls, "-error-help")] = {
    color: "".concat(colorError, "  !important")
  }, _b["".concat(componentCls, "-warning-help")] = {
    color: "".concat(colorWarning, " !important")
  }, _b["".concat(componentCls, "-success-help")] = {
    color: "".concat(colorSuccess, " !important")
  }, _b["".concat(antCls, "-upload")] = {
    background: "transparent"
  }, _b["".concat(antCls, "-upload").concat(antCls, "-upload-drag")] = {
    background: token.colorBgBase
  }, _b["".concat(antCls, "-radio-inline, ").concat(antCls, "-checkbox-inline")] = {
    display: "inline-block",
    marginInlineStart: marginSM,
    fontWeight: "normal",
    verticalAlign: "middle",
    cursor: "pointer",
    "&:first-child": {
      marginInlineStart: 0
    }
  }, _b["".concat(antCls, "-radio-vertical,\n          ").concat(antCls, "-checkbox-vertical")] = {
    display: "block"
  }, _b["".concat(antCls, "-checkbox-vertical + ").concat(antCls, "-checkbox-vertical,\n          ").concat(antCls, "-radio-vertical + ").concat(antCls, "-radio-vertical")] = {
    marginInlineStart: 0
  }, _b["".concat(antCls, "-input-number")] = (_5 = {
    width: "100%",
    verticalAlign: "top"
  }, _5["+ ".concat(antCls, "-form-text")] = {
    marginInlineStart: marginSM
  }, _5["&-handler-wrap"] = {
    zIndex: 2
  }, _5), _b["".concat(antCls, "-select,\n          ").concat(antCls, "-cascader-picker,\n          ").concat(antCls, "-picker")] = {
    width: "100%"
  }, _b["".concat(antCls, "-input-group ").concat(antCls, "-select,\n          ").concat(antCls, "-input-group ").concat(antCls, "-cascader-picker")] = {
    width: "auto"
  }, _b["&-popover"] = (_6 = {}, _6["".concat(componentCls, "-error-help")] = {
    color: "".concat(colorError, "  !important")
  }, _6["".concat(componentCls, "-warning-help")] = {
    color: colorWarning
  }, _6["".concat(componentCls, "-success-help")] = {
    color: "".concat(colorSuccess, " !important")
  }, _6), _b), _a;
};

// node_modules/@formily/antd-v5/esm/form-item/style/index.js
var genSmallStyle = function(token) {
  var _a, _b, _c, _d, _e, _f;
  var componentCls = token.componentCls, antCls = token.antCls, controlHeightSM = token.controlHeightSM, marginLG = token.marginLG, fontSizeSM = token.fontSizeSM, lineHeightSM = token.lineHeightSM;
  return _a = {
    fontSize: fontSizeSM,
    lineHeight: controlHeightSM
  }, _a["".concat(componentCls, "-label")] = {
    lineHeight: "".concat(controlHeightSM, "px"),
    minHeight: controlHeightSM - 4,
    "> label": {
      height: controlHeightSM - 2
    }
  }, _a["".concat(componentCls, "-control-content")] = {
    " &-component": {
      minHeight: controlHeightSM - 4,
      lineHeight: lineHeightSM
    }
  }, _a["".concat(componentCls, "-help,\n      ").concat(componentCls, "-extra")] = {
    minHeight: controlHeightSM - 4,
    lineHeight: "".concat(controlHeightSM - 4, "px")
  }, _a["".concat(componentCls, "-control-content")] = {
    minHeight: controlHeightSM - 4
  }, _a["".concat(antCls, "-input-affix-wrapper,\n      ").concat(antCls, "-input-number,\n      ").concat(antCls, "-picker")] = {
    padding: "0px 11px",
    input: {
      height: controlHeightSM - 4,
      fontSize: fontSizeSM
    }
  }, _a["".concat(antCls, "-cascader-picker")] = {
    height: controlHeightSM - 4,
    input: {
      padding: "0 7px",
      height: controlHeightSM - 4,
      fontSize: fontSizeSM
    }
  }, _a["".concat(antCls, "-select-single:not(").concat(antCls, "-select-customize-input) ").concat(antCls, "-select-selector")] = (_b = {
    padding: "0px 11px",
    height: controlHeightSM - 4,
    fontSize: fontSizeSM,
    lineHeight: "".concat(controlHeightSM, "px")
  }, _b["".concat(antCls, "-select-selection-search")] = {
    height: controlHeightSM,
    lineHeight: "".concat(controlHeightSM - 4, "px"),
    "&-input": {
      height: controlHeightSM - 4,
      lineHeight: "".concat(controlHeightSM - 4, "px")
    }
  }, _b["".concat(antCls, "-select-selection-placeholder")] = {
    height: controlHeightSM,
    lineHeight: "".concat(controlHeightSM - 4, "px")
  }, _b["".concat(antCls, "-select-selection-item")] = {
    height: controlHeightSM,
    lineHeight: "".concat(controlHeightSM - 4, "px")
  }, _b["".concat(antCls, "-select-multiple:not(").concat(antCls, "-select-customize-input)\n          ").concat(antCls, "-select-selector")] = (_c = {
    padding: "0px 2px",
    height: controlHeightSM - 4,
    fontSize: fontSizeSM,
    lineHeight: "".concat(controlHeightSM, "px"),
    "&::after": {
      height: controlHeightSM - 8,
      lineHeight: "".concat(controlHeightSM - 8, "px")
    }
  }, _c["".concat(antCls, "-select-selection-search")] = {
    height: controlHeightSM - 8,
    lineHeight: "".concat(controlHeightSM - 8, "px"),
    marginInlineStart: 0,
    "&-input": {
      height: controlHeightSM,
      lineHeight: "".concat(controlHeightSM - 4, "px")
    }
  }, _c["".concat(antCls, "-select-selection-placeholder")] = {
    height: controlHeightSM - 4,
    lineHeight: "".concat(controlHeightSM, "px"),
    marginInlineStart: 4
  }, _c["".concat(antCls, "-select-selection-overflow-item")] = (_d = {
    alignSelf: "flex-start"
  }, _d["".concat(antCls, "-select-selection-item")] = {
    lineHeight: "".concat(controlHeightSM - 10, "px"),
    height: controlHeightSM - 8
  }, _d), _c), _b["&".concat(componentCls, "-feedback-layout-terse")] = (_e = {
    marginBottom: 8
  }, _e["&".concat(componentCls, "-feedback-has-text:not(").concat(componentCls, "-inset)")] = {
    marginBottom: 0
  }, _e), _b["&".concat(componentCls, "-feedback-layout-loose")] = (_f = {
    marginBottom: marginLG
  }, _f["&".concat(componentCls, "-feedback-has-text:not(").concat(componentCls, "-inset)")] = {
    marginBottom: 0
  }, _f), _b), _a;
};
var genLargeStyle = function(token) {
  var _a, _b, _c, _d, _e, _f;
  var componentCls = token.componentCls, antCls = token.antCls, fontSizeLG = token.fontSizeLG, controlHeightLG = token.controlHeightLG, controlHeightSM = token.controlHeightSM, marginLG = token.marginLG, lineHeightLG = token.lineHeightLG;
  return _a = {
    fontSize: fontSizeLG,
    lineHeight: "".concat(controlHeightLG, "px")
  }, _a["".concat(componentCls, "-label")] = {
    lineHeight: "".concat(controlHeightLG, "px"),
    minHeight: controlHeightLG - 2,
    "> label": {
      height: controlHeightLG
    }
  }, _a["".concat(componentCls, "-control-content")] = {
    " &-component": {
      minHeight: controlHeightLG - 2,
      lineHeight: lineHeightLG
    }
  }, _a["".concat(componentCls, "-help,\n      ").concat(componentCls, "-extra")] = {
    minHeight: controlHeightSM,
    lineHeight: "".concat(controlHeightSM, "px")
  }, _a["".concat(componentCls, "-control-content")] = {
    minHeight: controlHeightLG - 2
  }, _a["".concat(antCls, "-input")] = {
    fontSize: fontSizeLG
  }, _a["".concat(antCls, "-input-number")] = {
    fontSize: fontSizeLG,
    input: {
      height: controlHeightLG - 2
    }
  }, _a["".concat(antCls, "-input-affix-wrapper,\n      ").concat(antCls, "-picker")] = {
    padding: "0px 11px",
    lineHeight: "".concat(controlHeightLG - 2, "px"),
    input: {
      height: controlHeightLG - 2,
      fontSize: fontSizeLG
    }
  }, _a["".concat(antCls, "-btn")] = {
    height: controlHeightLG,
    padding: "0 8px"
  }, _a["".concat(antCls, "-radio-button-wrapper")] = {
    height: controlHeightLG,
    lineHeight: "".concat(controlHeightLG, "px")
  }, _a["".concat(antCls, "-cascader-picker")] = {
    height: controlHeightLG - 2,
    input: {
      padding: "0 11px",
      height: controlHeightLG - 2,
      fontSize: fontSizeLG
    }
  }, _a["".concat(antCls, "-select-single:not(").concat(antCls, "-select-customize-input) ").concat(antCls, "-select-selector")] = (_b = {
    padding: "0px 11px",
    height: controlHeightLG,
    fontSize: fontSizeLG,
    lineHeight: "".concat(controlHeightLG, "px")
  }, _b["".concat(antCls, "-select-selection-search")] = {
    height: controlHeightLG,
    lineHeight: "".concat(controlHeightLG - 2, "px"),
    "&-input": {
      height: controlHeightLG,
      lineHeight: "".concat(controlHeightLG - 2, "px")
    }
  }, _b["".concat(antCls, "-select-selection-placeholder")] = {
    height: controlHeightLG,
    lineHeight: "".concat(controlHeightLG, "px")
  }, _b["".concat(antCls, "-select-selection-item")] = {
    height: controlHeightLG,
    lineHeight: "".concat(controlHeightLG, "px")
  }, _b["".concat(antCls, "-select-multiple:not(").concat(antCls, "-select-customize-input)\n          ").concat(antCls, "-select-selector")] = (_c = {
    padding: "0px 2px",
    height: controlHeightLG - 2,
    fontSize: fontSizeLG,
    lineHeight: "".concat(controlHeightLG, "px"),
    "&::after": {
      height: controlHeightLG - 8,
      lineHeight: "".concat(controlHeightLG - 8, "px")
    }
  }, _c["".concat(antCls, "-select-selection-search")] = {
    height: controlHeightLG - 8,
    lineHeight: "".concat(controlHeightLG - 8, "px"),
    "&-input": {
      height: controlHeightLG - 12,
      lineHeight: "".concat(controlHeightLG - 12, "px")
    }
  }, _c["".concat(antCls, "-select-selection-placeholder")] = {
    height: controlHeightLG - 8,
    lineHeight: "".concat(controlHeightLG - 8, "px")
  }, _c["".concat(antCls, "-select-selection-overflow-item")] = (_d = {
    alignSelf: "flex-start"
  }, _d["".concat(antCls, "-select-selection-item")] = {
    lineHeight: "".concat(controlHeightLG - 10, "px"),
    height: controlHeightLG - 8
  }, _d), _c), _b["&".concat(componentCls, "-feedback-layout-terse")] = (_e = {
    marginBottom: 8
  }, _e["&".concat(componentCls, "-feedback-has-text:not(").concat(componentCls, "-inset)")] = {
    marginBottom: 0
  }, _e), _b["&".concat(componentCls, "-feedback-layout-loose")] = (_f = {
    marginBottom: marginLG
  }, _f["&".concat(componentCls, "-feedback-has-text:not(").concat(componentCls, "-inset)")] = {
    marginBottom: 0
  }, _f), _b), _a;
};
var genLabelStyle = function(token) {
  var _a, _b, _c;
  var componentCls = token.componentCls, controlHeight = token.controlHeight, controlHeightSM = token.controlHeightSM, marginLG = token.marginLG, marginSM = token.marginSM, lineHeight = token.lineHeight;
  return {
    lineHeight: "".concat(controlHeight, "px"),
    minHeight: controlHeight - 2,
    position: "relative",
    display: "flex",
    color: token.colorTextHeading,
    label: {
      cursor: "text"
    },
    "&-content": {
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap",
      label: {
        whiteSpace: "nowrap"
      }
    },
    "&-tooltip": {
      cursor: "help",
      "*": {
        cursor: "help"
      },
      label: {
        borderBottom: "1px dashed currentColor"
      }
    },
    "&-feedback-layout": {
      "&-terse": {
        marginBottom: marginSM
      },
      "&-loose": {
        marginBottom: marginLG
      },
      "&-none": {
        marginBottom: 0
      },
      "&-terse, &-loosee, &-none": (_a = {}, _a["&.".concat(componentCls, "-feedback-has-text:not(").concat(componentCls, "-inset)")] = {
        marginBottom: 0
      }, _a)
    },
    "&-control": (_b = {
      flex: 1,
      maxWidth: "100%"
    }, _b["&.".concat(componentCls, "-control-content")] = (_c = {
      display: "flex",
      "&-component": {
        width: "100%",
        minHeight: controlHeight - 2,
        lineHeight,
        "&-has-feedback-icon": {
          flex: 1,
          position: "relative",
          display: "flex",
          alignItems: "center"
        }
      }
    }, _c["".concat(componentCls, "-addon")] = {
      "&-before": {
        marginInlineEnd: marginSM
      },
      "&-after": {
        marginInlineEnd: marginSM
      },
      "&-before, &-after": {
        display: "inline-flex",
        alignItems: "center",
        minHeight: controlHeight,
        flexShrink: 0
      }
    }, _c), _b["".concat(componentCls, "-help,\n        ").concat(componentCls, "-extra")] = {
      minHeight: controlHeightSM,
      lineHeight: "".concat(controlHeightSM, "px"),
      color: token.colorTextSecondary
    }, _b)
  };
};
var genFormItemStyle = function(token) {
  var _a, _b, _c, _d, _e, _f, _g;
  var componentCls = token.componentCls, fontSize = token.fontSize, marginLG = token.marginLG;
  return _a = {}, _a[componentCls] = (_b = {
    display: "flex",
    position: "relative",
    marginBottom: marginLG,
    fontSize
  }, _b["".concat(componentCls, "-label")] = genLabelStyle(token), _b["&".concat(componentCls, "-size-small")] = genSmallStyle(token), _b["&".concat(componentCls, "-size-large")] = genLargeStyle(token), _b["&-label"] = (_c = {
    "&-align": (_d = {}, _d["&-left"] = (_e = {}, _e["> ".concat(componentCls, "-label")] = {
      justifyContent: "flex-start"
    }, _e), _d["&-right"] = (_f = {}, _f["> ".concat(componentCls, "-label")] = {
      justifyContent: "flex-end"
    }, _f), _d)
  }, _c["&-wrap"] = (_g = {}, _g["".concat(componentCls, "-label")] = {
    label: {
      whiteSpace: "pre-line",
      wordBreak: "break-all"
    }
  }, _g), _c), _b), _a;
};
var style_default7 = genStyleHook("Form", function(token) {
  return [
    genFormItemStyle(token),
    getAnimationStyle(token),
    genOtherStyle(token),
    getGridStyle(token)
  ];
});

// node_modules/@formily/antd-v5/esm/form-item/index.js
var __assign17 = function() {
  __assign17 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign17.apply(this, arguments);
};
var __rest6 = function(s, e) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
var __read10 = function(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
};
var ICON_MAP = {
  error: import_react34.default.createElement(CloseCircleOutlined_default, null),
  success: import_react34.default.createElement(CheckCircleOutlined_default, null),
  warning: import_react34.default.createElement(ExclamationCircleOutlined_default, null)
};
var BaseItem = function(_a) {
  var _b, _c, _d, _e, _f;
  var children = _a.children, props = __rest6(_a, ["children"]);
  var _g = __read10((0, import_react34.useState)(false), 2), active = _g[0], setActive = _g[1];
  var formLayout = useFormItemLayout(props);
  var _h = useOverflow(), containerRef = _h.containerRef, contentRef = _h.contentRef, overflow = _h.overflow;
  var label = formLayout.label, style = formLayout.style, layout = formLayout.layout, _j = formLayout.colon, colon = _j === void 0 ? true : _j, addonBefore = formLayout.addonBefore, addonAfter = formLayout.addonAfter, asterisk = formLayout.asterisk, feedbackStatus = formLayout.feedbackStatus, extra = formLayout.extra, feedbackText = formLayout.feedbackText, fullness = formLayout.fullness, feedbackLayout = formLayout.feedbackLayout, feedbackIcon = formLayout.feedbackIcon, getPopupContainer = formLayout.getPopupContainer, inset = formLayout.inset, _k = formLayout.bordered, bordered = _k === void 0 ? true : _k, labelWidth = formLayout.labelWidth, wrapperWidth = formLayout.wrapperWidth, labelCol = formLayout.labelCol, wrapperCol = formLayout.wrapperCol, labelAlign = formLayout.labelAlign, _l = formLayout.wrapperAlign, wrapperAlign = _l === void 0 ? "left" : _l, size2 = formLayout.size, labelWrap = formLayout.labelWrap, wrapperWrap = formLayout.wrapperWrap, tooltipLayout = formLayout.tooltipLayout, tooltip = formLayout.tooltip, tooltipIcon = formLayout.tooltipIcon;
  var labelStyle = __assign17({}, formLayout.labelStyle);
  var wrapperStyle = __assign17({}, formLayout.wrapperStyle);
  var prefixCls = usePrefixCls("formily-item", props);
  var _m = __read10(style_default7(prefixCls), 2), wrapSSR = _m[0], hashId = _m[1];
  var enableCol = false;
  if (labelWidth || wrapperWidth) {
    if (labelWidth) {
      labelStyle.width = labelWidth === "auto" ? void 0 : labelWidth;
      labelStyle.maxWidth = labelWidth === "auto" ? void 0 : labelWidth;
    }
    if (wrapperWidth) {
      wrapperStyle.width = wrapperWidth === "auto" ? void 0 : wrapperWidth;
      wrapperStyle.maxWidth = wrapperWidth === "auto" ? void 0 : wrapperWidth;
    }
  }
  if (labelCol || wrapperCol) {
    if (!labelStyle.width && !wrapperStyle.width && layout !== "vertical") {
      enableCol = true;
    }
  }
  var formatChildren = feedbackLayout === "popover" ? import_react34.default.createElement(
    popover_default,
    { autoAdjustOverflow: true, overlayClassName: "".concat(prefixCls, "-popover"), placement: "top", content: import_react34.default.createElement(
      "div",
      { className: (0, import_classnames8.default)((_b = {}, _b["".concat(prefixCls, "-").concat(feedbackStatus, "-help")] = !!feedbackStatus, _b["".concat(prefixCls, "-help")] = true, _b)) },
      feedbackStatus ? ICON_MAP[feedbackStatus] : null,
      " ",
      feedbackText
    ), open: !!feedbackText, getPopupContainer },
    import_react34.default.createElement(import_react34.default.Fragment, null, children)
  ) : children;
  var gridStyles = {};
  var getOverflowTooltip = function() {
    if (overflow) {
      return import_react34.default.createElement(
        "div",
        null,
        import_react34.default.createElement("div", null, label),
        import_react34.default.createElement("div", null, tooltip)
      );
    }
    return tooltip;
  };
  var renderLabelText = function() {
    var labelChildren = import_react34.default.createElement(
      "div",
      { className: "".concat(prefixCls, "-label-content"), ref: containerRef },
      import_react34.default.createElement(
        "span",
        { ref: contentRef },
        asterisk && import_react34.default.createElement("span", { className: "".concat(prefixCls, "-asterisk") }, "*"),
        import_react34.default.createElement("label", null, label)
      )
    );
    if (tooltipLayout === "text" && tooltip || overflow) {
      return import_react34.default.createElement(tooltip_default, { placement: "top", title: getOverflowTooltip() }, labelChildren);
    }
    return labelChildren;
  };
  var renderTooltipIcon = function() {
    if (tooltip && tooltipLayout === "icon" && !overflow) {
      return import_react34.default.createElement(
        "span",
        { className: "".concat(prefixCls, "-label-tooltip-icon") },
        import_react34.default.createElement(tooltip_default, { placement: "top", title: tooltip }, tooltipIcon)
      );
    }
  };
  var renderLabel = function() {
    var _a2;
    if (!label)
      return null;
    return import_react34.default.createElement(
      "div",
      { className: (0, import_classnames8.default)((_a2 = {}, _a2["".concat(prefixCls, "-label")] = true, _a2["".concat(prefixCls, "-label-tooltip")] = tooltip && tooltipLayout === "text" || overflow, _a2["".concat(prefixCls, "-item-col-").concat(labelCol)] = enableCol && !!labelCol, _a2)), style: labelStyle },
      renderLabelText(),
      renderTooltipIcon(),
      label !== " " && import_react34.default.createElement("span", { className: "".concat(prefixCls, "-colon") }, colon ? ":" : "")
    );
  };
  return wrapSSR(import_react34.default.createElement(
    "div",
    __assign17({}, pickDataProps(props), { style: __assign17(__assign17({}, style), gridStyles), "data-grid-span": props.gridSpan, className: (0, import_classnames8.default)(props.className, (_c = {}, _c["".concat(prefixCls)] = true, _c["".concat(prefixCls, "-layout-").concat(layout)] = true, _c["".concat(prefixCls, "-").concat(feedbackStatus)] = !!feedbackStatus, _c["".concat(prefixCls, "-feedback-has-text")] = !!feedbackText, _c["".concat(prefixCls, "-size-").concat(size2)] = !!size2, _c["".concat(prefixCls, "-feedback-layout-").concat(feedbackLayout)] = !!feedbackLayout, _c["".concat(prefixCls, "-fullness")] = !!fullness || !!inset || !!feedbackIcon, _c["".concat(prefixCls, "-inset")] = !!inset, _c["".concat(prefixCls, "-active")] = active, _c["".concat(prefixCls, "-inset-active")] = !!inset && active, _c["".concat(prefixCls, "-label-align-").concat(labelAlign)] = true, _c["".concat(prefixCls, "-control-align-").concat(wrapperAlign)] = true, _c["".concat(prefixCls, "-label-wrap")] = !!labelWrap, _c["".concat(prefixCls, "-control-wrap")] = !!wrapperWrap, _c["".concat(prefixCls, "-bordered-none")] = bordered === false || !!inset || !!feedbackIcon, _c), hashId), onFocus: function() {
      if (feedbackIcon || inset) {
        setActive(true);
      }
    }, onBlur: function() {
      if (feedbackIcon || inset) {
        setActive(false);
      }
    } }),
    renderLabel(),
    import_react34.default.createElement(
      "div",
      { className: (0, import_classnames8.default)((_d = {}, _d["".concat(prefixCls, "-control")] = true, _d["".concat(prefixCls, "-item-col-").concat(wrapperCol)] = enableCol && !!wrapperCol && label, _d)) },
      import_react34.default.createElement(
        "div",
        { className: (0, import_classnames8.default)("".concat(prefixCls, "-control-content")) },
        addonBefore && import_react34.default.createElement("div", { className: (0, import_classnames8.default)("".concat(prefixCls, "-addon-before")) }, addonBefore),
        import_react34.default.createElement(
          "div",
          { style: wrapperStyle, className: (0, import_classnames8.default)((_e = {}, _e["".concat(prefixCls, "-control-content-component")] = true, _e["".concat(prefixCls, "-control-content-component-has-feedback-icon")] = !!feedbackIcon, _e)) },
          import_react34.default.createElement(FormLayoutShallowContext.Provider, { value: {} }, formatChildren),
          feedbackIcon && import_react34.default.createElement("div", { className: (0, import_classnames8.default)("".concat(prefixCls, "-feedback-icon")) }, feedbackIcon)
        ),
        addonAfter && import_react34.default.createElement("div", { className: (0, import_classnames8.default)("".concat(prefixCls, "-addon-after")) }, addonAfter)
      ),
      !!feedbackText && feedbackLayout !== "popover" && feedbackLayout !== "none" && import_react34.default.createElement("div", { className: (0, import_classnames8.default)((_f = {}, _f["".concat(prefixCls, "-").concat(feedbackStatus, "-help")] = !!feedbackStatus, _f["".concat(prefixCls, "-help")] = true, _f["".concat(prefixCls, "-help-enter")] = true, _f["".concat(prefixCls, "-help-enter-active")] = true, _f)) }, feedbackText),
      extra && import_react34.default.createElement("div", { className: (0, import_classnames8.default)("".concat(prefixCls, "-extra")) }, extra)
    )
  ));
};
var FormItem = Object.assign(connect(BaseItem, mapProps(function(props, field) {
  if (isVoidField(field))
    return {
      label: field.title || props.label,
      asterisk: props.asterisk,
      extra: props.extra || field.description
    };
  if (!field)
    return props;
  var takeFeedbackStatus = function() {
    if (field.validating)
      return "pending";
    return field.decoratorProps.feedbackStatus || field.validateStatus;
  };
  var takeMessage = function() {
    var split = function(messages) {
      return messages.reduce(function(buf, text, index) {
        if (!text)
          return buf;
        return index < messages.length - 1 ? buf.concat([text, ", "]) : buf.concat([text]);
      }, []);
    };
    if (field.validating)
      return;
    if (props.feedbackText)
      return props.feedbackText;
    if (field.selfErrors.length)
      return split(field.selfErrors);
    if (field.selfWarnings.length)
      return split(field.selfWarnings);
    if (field.selfSuccesses.length)
      return split(field.selfSuccesses);
  };
  var takeAsterisk = function() {
    if ("asterisk" in props) {
      return props.asterisk;
    }
    if (field.required && field.pattern !== "readPretty") {
      return true;
    }
    return false;
  };
  return {
    label: props.label || field.title,
    feedbackStatus: takeFeedbackStatus(),
    feedbackText: takeMessage(),
    asterisk: takeAsterisk(),
    extra: props.extra || field.description
  };
})), {
  BaseItem
});

// node_modules/@formily/antd-v5/esm/editable/style.js
var style_default8 = genStyleHook("editable", function(token) {
  var _a, _b, _c;
  var antCls = token.antCls, componentCls = token.componentCls, fontSizeSM = token.fontSizeSM, colorPrimary = token.colorPrimary;
  return _a = {}, _a[componentCls] = (_b = {
    cursor: "pointer",
    display: "inline-block !important"
  }, _b["".concat(antCls, "-form-text")] = (_c = {
    display: "flex",
    alignItems: "center"
  }, _c["".concat(antCls, "-tag")] = {
    transition: "none !important"
  }, _c["".concat(antCls, "-tag:last-child")] = {
    paddingInlineEnd: "0 !important"
  }, _c), _b["".concat(componentCls, "-content")] = {
    display: "flex",
    alignItems: "center",
    "> *": {
      marginInlineEnd: 3,
      "&:last-child": {
        marginInlineEnd: 0
      }
    }
  }, _b["".concat(componentCls, "-edit-btn,\n        ").concat(componentCls, "-close-btn")] = {
    transition: "all .25s ease-in-out",
    color: "#aaa",
    fontSize: fontSizeSM,
    "&:hover": {
      color: colorPrimary
    }
  }, _b["".concat(componentCls, "-preview")] = {
    whiteSpace: "nowrap",
    textOverflow: "ellipsis",
    overflow: "hidden",
    wordBreak: "break-all",
    maxWidth: "100%",
    display: "block"
  }, _b), _a;
});

// node_modules/@formily/antd-v5/esm/editable/index.js
var __assign18 = function() {
  __assign18 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign18.apply(this, arguments);
};
var __awaiter3 = function(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var __generator3 = function(thisArg, body) {
  var _ = { label: 0, sent: function() {
    if (t[0] & 1) throw t[1];
    return t[1];
  }, trys: [], ops: [] }, f, y, t, g;
  return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() {
    return this;
  }), g;
  function verb(n) {
    return function(v) {
      return step([n, v]);
    };
  }
  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");
    while (g && (g = 0, op[0] && (_ = 0)), _) try {
      if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
      if (y = 0, t) op = [op[0] & 2, t.value];
      switch (op[0]) {
        case 0:
        case 1:
          t = op;
          break;
        case 4:
          _.label++;
          return { value: op[1], done: false };
        case 5:
          _.label++;
          y = op[1];
          op = [0];
          continue;
        case 7:
          op = _.ops.pop();
          _.trys.pop();
          continue;
        default:
          if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
            _ = 0;
            continue;
          }
          if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
            _.label = op[1];
            break;
          }
          if (op[0] === 6 && _.label < t[1]) {
            _.label = t[1];
            t = op;
            break;
          }
          if (t && _.label < t[2]) {
            _.label = t[2];
            _.ops.push(op);
            break;
          }
          if (t[2]) _.ops.pop();
          _.trys.pop();
          continue;
      }
      op = body.call(thisArg, _);
    } catch (e) {
      op = [6, e];
      y = 0;
    } finally {
      f = t = 0;
    }
    if (op[0] & 5) throw op[1];
    return { value: op[0] ? op[1] : void 0, done: true };
  }
};
var __read11 = function(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
};
var useParentPattern = function() {
  var _a, _b;
  var field = useField();
  return ((_a = field === null || field === void 0 ? void 0 : field.parent) === null || _a === void 0 ? void 0 : _a.pattern) || ((_b = field === null || field === void 0 ? void 0 : field.form) === null || _b === void 0 ? void 0 : _b.pattern);
};
var useEditable = function() {
  var pattern = useParentPattern();
  var field = useField();
  (0, import_react36.useLayoutEffect)(function() {
    if (pattern === "editable") {
      return field.setPattern("readPretty");
    }
  }, [pattern]);
  return [
    field.pattern === "editable",
    function(payload) {
      if (pattern !== "editable")
        return;
      field.setPattern(payload ? "editable" : "readPretty");
    }
  ];
};
var useFormItemProps = function() {
  var field = useField();
  if (isVoidField(field))
    return {};
  if (!field)
    return {};
  var takeMessage = function() {
    if (field.selfErrors.length)
      return field.selfErrors;
    if (field.selfWarnings.length)
      return field.selfWarnings;
    if (field.selfSuccesses.length)
      return field.selfSuccesses;
  };
  return {
    feedbackStatus: field.validateStatus === "validating" ? "pending" : field.validateStatus,
    feedbackText: takeMessage(),
    extra: field.description
  };
};
var InternalEditable = observer(function(props) {
  var _a = __read11(useEditable(), 2), editable = _a[0], setEditable = _a[1];
  var pattern = useParentPattern();
  var itemProps = useFormItemProps();
  var field = useField();
  var basePrefixCls = usePrefixCls();
  var prefixCls = usePrefixCls("formily-editable");
  var _b = __read11(style_default8(prefixCls), 2), wrapSSR = _b[0], hashId = _b[1];
  var ref = (0, import_react36.useRef)();
  var innerRef = (0, import_react36.useRef)(null);
  var recover = function() {
    var _a2;
    if (ref.current && !((_a2 = field === null || field === void 0 ? void 0 : field.errors) === null || _a2 === void 0 ? void 0 : _a2.length)) {
      setEditable(false);
    }
  };
  var renderEditHelper = function() {
    if (editable)
      return;
    return import_react36.default.createElement(
      BaseItem,
      __assign18({}, props, itemProps),
      pattern === "editable" && import_react36.default.createElement(EditOutlined_default, { className: "".concat(prefixCls, "-edit-btn") }),
      pattern !== "editable" && import_react36.default.createElement(MessageOutlined_default, { className: "".concat(prefixCls, "-edit-btn") })
    );
  };
  var renderCloseHelper = function() {
    if (!editable)
      return;
    return import_react36.default.createElement(
      BaseItem,
      __assign18({}, props),
      import_react36.default.createElement(CloseOutlined_default, { className: "".concat(prefixCls, "-close-btn") })
    );
  };
  useClickAway(function(e) {
    var target = e.target;
    if (target === null || target === void 0 ? void 0 : target.closest(".".concat(basePrefixCls, "-select-dropdown")))
      return;
    if (target === null || target === void 0 ? void 0 : target.closest(".".concat(basePrefixCls, "-picker-dropdown")))
      return;
    if (target === null || target === void 0 ? void 0 : target.closest(".".concat(basePrefixCls, "-cascader-menus")))
      return;
    recover();
  }, innerRef);
  var onClick = function(e) {
    var _a2;
    var target = e.target;
    var close = ((_a2 = innerRef.current) === null || _a2 === void 0 ? void 0 : _a2.querySelector(".".concat(prefixCls, "-close-btn"))) || null;
    if ((target === null || target === void 0 ? void 0 : target.contains(close)) || (close === null || close === void 0 ? void 0 : close.contains(target))) {
      recover();
    } else if (!ref.current) {
      setTimeout(function() {
        setEditable(true);
        setTimeout(function() {
          var _a3, _b2;
          (_b2 = (_a3 = innerRef.current) === null || _a3 === void 0 ? void 0 : _a3.querySelector("input")) === null || _b2 === void 0 ? void 0 : _b2.focus();
        });
      });
    }
  };
  ref.current = editable;
  return wrapSSR(import_react36.default.createElement(
    "div",
    { className: (0, import_classnames9.default)(prefixCls, hashId), ref: innerRef, onClick },
    import_react36.default.createElement(
      "div",
      { className: "".concat(prefixCls, "-content") },
      import_react36.default.createElement(BaseItem, __assign18({}, props, itemProps), props.children),
      renderEditHelper(),
      renderCloseHelper()
    )
  ));
});
var Popover = observer(function(props) {
  var field = useField();
  var pattern = useParentPattern();
  var _a = __read11((0, import_react36.useState)(false), 2), open = _a[0], setOpen = _a[1];
  var prefixCls = usePrefixCls("formily-editable");
  var _b = __read11(style_default8(prefixCls), 2), wrapSSR = _b[0], hashId = _b[1];
  var closePopover = function() {
    return __awaiter3(void 0, void 0, void 0, function() {
      var errors;
      return __generator3(this, function(_a2) {
        switch (_a2.label) {
          case 0:
            _a2.trys.push([0, , 2, 3]);
            return [4, field.form.validate("".concat(field.address, ".*"))];
          case 1:
            _a2.sent();
            return [3, 3];
          case 2:
            errors = field.form.queryFeedbacks({
              type: "error",
              address: "".concat(field.address, ".*")
            });
            if (errors === null || errors === void 0 ? void 0 : errors.length)
              return [
                2
                /*return*/
              ];
            setOpen(false);
            return [
              7
              /*endfinally*/
            ];
          case 3:
            return [
              2
              /*return*/
            ];
        }
      });
    });
  };
  var openPopover = function() {
    setOpen(true);
  };
  return wrapSSR(import_react36.default.createElement(
    popover_default,
    __assign18({}, props, { title: props.title || field.title, open, className: (0, import_classnames9.default)(prefixCls, hashId, props.className), content: props.children, trigger: "click", destroyTooltipOnHide: true, onOpenChange: function(open2) {
      if (open2) {
        openPopover();
      } else {
        closePopover();
      }
    } }),
    import_react36.default.createElement(
      "div",
      null,
      import_react36.default.createElement(
        BaseItem,
        { className: "".concat(prefixCls, "-trigger") },
        import_react36.default.createElement(
          "div",
          { className: "".concat(prefixCls, "-content") },
          import_react36.default.createElement("span", { className: "".concat(prefixCls, "-preview") }, props.title || field.title),
          pattern === "editable" && import_react36.default.createElement(EditOutlined_default, { className: "".concat(prefixCls, "-edit-btn") }),
          pattern !== "editable" && import_react36.default.createElement(MessageOutlined_default, { className: "".concat(prefixCls, "-edit-btn") })
        )
      )
    )
  ));
});
var Editable = Object.assign(InternalEditable, { Popover });

// node_modules/@formily/antd-v5/esm/form/index.js
var import_react38 = __toESM(require_react());
var __assign19 = function() {
  __assign19 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign19.apply(this, arguments);
};
var __rest7 = function(s, e) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
var Form = function(_a) {
  var form = _a.form, _b = _a.component, component = _b === void 0 ? "form" : _b, onAutoSubmit = _a.onAutoSubmit, onAutoSubmitFailed = _a.onAutoSubmitFailed, previewTextPlaceholder = _a.previewTextPlaceholder, props = __rest7(_a, ["form", "component", "onAutoSubmit", "onAutoSubmitFailed", "previewTextPlaceholder"]);
  var top = useParentForm();
  var renderContent = function(form2) {
    return import_react38.default.createElement(
      RecordScope,
      { getRecord: function() {
        return isForm(form2) ? form2.values : form2.value;
      } },
      import_react38.default.createElement(
        PreviewText.Placeholder,
        { value: previewTextPlaceholder },
        import_react38.default.createElement(FormLayout, __assign19({}, props), import_react38.default.createElement(component, {
          onSubmit: function(e) {
            var _a2, _b2;
            (_a2 = e === null || e === void 0 ? void 0 : e.stopPropagation) === null || _a2 === void 0 ? void 0 : _a2.call(e);
            (_b2 = e === null || e === void 0 ? void 0 : e.preventDefault) === null || _b2 === void 0 ? void 0 : _b2.call(e);
            form2.submit(onAutoSubmit).catch(onAutoSubmitFailed);
          }
        }, props.children))
      )
    );
  };
  if (form)
    return import_react38.default.createElement(FormProvider, { form }, renderContent(form));
  if (!top)
    throw new Error("must pass form instance by createForm");
  return renderContent(top);
};

// node_modules/@formily/antd-v5/esm/form-button-group/index.js
var import_classnames10 = __toESM(require_classnames());
var import_react40 = __toESM(require_react());

// node_modules/react-sticky-box/dist/index.js
var import_react39 = __toESM(require_react());
var getScrollParent = (node) => {
  let parent = node;
  while (parent = parent.parentElement) {
    const overflowYVal = getComputedStyle(parent, null).getPropertyValue("overflow-y");
    if (parent === document.body)
      return window;
    if (overflowYVal === "auto" || overflowYVal === "scroll")
      return parent;
  }
  return window;
};
var offsetTill = (node, target) => {
  let current = node;
  let offset = 0;
  if (target.firstChild && target.firstChild.offsetParent !== target) {
    offset += node.offsetTop - target.offsetTop;
    target = node.offsetParent;
    offset += -node.offsetTop;
  }
  do {
    offset += current.offsetTop;
    current = current.offsetParent;
  } while (current && current !== target);
  return offset;
};
var getParentNode = (node) => {
  let currentParent = node.parentNode;
  while (currentParent) {
    const style = getComputedStyle(currentParent, null);
    if (style.getPropertyValue("display") !== "contents")
      break;
    currentParent = currentParent.parentNode;
  }
  return currentParent || window;
};
var stickyProp = null;
if (typeof CSS !== "undefined" && CSS.supports) {
  if (CSS.supports("position", "sticky"))
    stickyProp = "sticky";
  else if (CSS.supports("position", "-webkit-sticky"))
    stickyProp = "-webkit-sticky";
}
var passiveArg = false;
try {
  opts = Object.defineProperty({}, "passive", {
    get() {
      passiveArg = { passive: true };
    }
  });
  window.addEventListener("testPassive", null, opts);
  window.removeEventListener("testPassive", null, opts);
} catch (e) {
}
var opts;
var registerNode = (node, { offsetTop, offsetBottom, bottom }) => {
  const scrollPane = getScrollParent(node);
  let latestScrollY = scrollPane === window ? window.scrollY : scrollPane.scrollTop;
  const unsubs = [];
  let mode, offset, nodeHeight, naturalTop, parentHeight, scrollPaneOffset, viewPortHeight;
  const getCurrentOffset = () => {
    if (mode === "relative")
      return offset;
    if (mode === "stickyTop") {
      return Math.max(0, scrollPaneOffset + latestScrollY - naturalTop + offsetTop);
    }
    if (mode === "stickyBottom") {
      return Math.max(0, scrollPaneOffset + latestScrollY + viewPortHeight - (naturalTop + nodeHeight + offsetBottom));
    }
  };
  const changeToStickyBottomIfBoxTooLow = (scrollY) => {
    if (scrollY + scrollPaneOffset + viewPortHeight >= naturalTop + nodeHeight + offset + offsetBottom) {
      changeMode("stickyBottom");
    }
  };
  const changeMode = (newMode) => {
    mode = newMode;
    if (newMode === "relative") {
      node.style.position = "relative";
      if (bottom) {
        const nextBottom = Math.max(0, parentHeight - nodeHeight - offset);
        node.style.bottom = `${nextBottom}px`;
      } else {
        node.style.top = `${offset}px`;
      }
    } else {
      node.style.position = stickyProp;
      if (newMode === "stickyBottom") {
        if (bottom) {
          node.style.bottom = `${offsetBottom}px`;
        } else {
          node.style.top = `${viewPortHeight - nodeHeight - offsetBottom}px`;
        }
      } else {
        if (bottom) {
          node.style.bottom = `${viewPortHeight - nodeHeight - offsetBottom}px`;
        } else {
          node.style.top = `${offsetTop}px`;
        }
      }
    }
    offset = getCurrentOffset();
  };
  const initial = () => {
    if (bottom) {
      if (mode !== "stickyBottom")
        changeMode("stickyBottom");
    } else {
      if (mode !== "stickyTop")
        changeMode("stickyTop");
    }
  };
  const addListener = (element, event, handler, passive) => {
    element.addEventListener(event, handler, passive);
    unsubs.push(() => element.removeEventListener(event, handler));
  };
  const handleScroll = () => {
    const scrollY = scrollPane === window ? window.scrollY : scrollPane.scrollTop;
    if (scrollY === latestScrollY)
      return;
    if (nodeHeight + offsetTop + offsetBottom <= viewPortHeight) {
      initial();
      latestScrollY = scrollY;
      return;
    }
    const scrollDelta = scrollY - latestScrollY;
    offset = getCurrentOffset();
    if (scrollDelta > 0) {
      if (mode === "stickyTop") {
        if (scrollY + scrollPaneOffset + offsetTop > naturalTop) {
          if (scrollY + scrollPaneOffset + viewPortHeight <= naturalTop + nodeHeight + offset + offsetBottom) {
            changeMode("relative");
          } else {
            changeMode("stickyBottom");
          }
        }
      } else if (mode === "relative") {
        changeToStickyBottomIfBoxTooLow(scrollY);
      }
    } else {
      if (mode === "stickyBottom") {
        if (scrollPaneOffset + scrollY + viewPortHeight < naturalTop + parentHeight + offsetBottom) {
          if (scrollPaneOffset + scrollY + offsetTop >= naturalTop + offset) {
            changeMode("relative");
          } else {
            changeMode("stickyTop");
          }
        }
      } else if (mode === "relative") {
        if (scrollPaneOffset + scrollY + offsetTop < naturalTop + offset) {
          changeMode("stickyTop");
        }
      }
    }
    latestScrollY = scrollY;
  };
  const handleWindowResize = () => {
    viewPortHeight = window.innerHeight;
    scrollPaneOffset = 0;
    handleScroll();
  };
  const handleScrollPaneResize = () => {
    viewPortHeight = scrollPane.offsetHeight;
    if (viewPortHeight === 0) {
      console.warn(`react-sticky-box's scroll pane has a height of 0. This seems odd. Please check this node:`, scrollPane);
    }
    if (scrollPane.firstChild.offsetParent === scrollPane) {
      scrollPaneOffset = scrollPane.getBoundingClientRect().top;
    } else {
      scrollPaneOffset = 0;
    }
    handleScroll();
  };
  const handleParentNodeResize = () => {
    const parentNode = getParentNode(node);
    const computedParentStyle = getComputedStyle(parentNode, null);
    const parentPaddingTop = parseInt(computedParentStyle.getPropertyValue("padding-top"), 10);
    const parentPaddingBottom = parseInt(computedParentStyle.getPropertyValue("padding-bottom"), 10);
    const verticalParentPadding = parentPaddingTop + parentPaddingBottom;
    naturalTop = offsetTill(parentNode, scrollPane) + parentPaddingTop + scrollPaneOffset;
    const oldParentHeight = parentHeight;
    parentHeight = parentNode.getBoundingClientRect().height - verticalParentPadding;
    if (mode === "relative") {
      if (bottom) {
        changeMode("relative");
      } else {
        if (oldParentHeight > parentHeight) {
          changeToStickyBottomIfBoxTooLow(latestScrollY);
        }
      }
    }
    if (oldParentHeight !== parentHeight && mode === "relative") {
      latestScrollY = Number.POSITIVE_INFINITY;
      handleScroll();
    }
  };
  const handleNodeResize = ({ initial: initialArg } = {}) => {
    const prevHeight = nodeHeight;
    nodeHeight = node.getBoundingClientRect().height;
    if (!initialArg && prevHeight !== nodeHeight) {
      if (nodeHeight + offsetTop + offsetBottom <= viewPortHeight) {
        mode = void 0;
        initial();
        return;
      } else {
        const diff = prevHeight - nodeHeight;
        const lowestPossible = parentHeight - nodeHeight;
        const nextOffset = Math.min(lowestPossible, getCurrentOffset() + (bottom ? diff : 0));
        offset = Math.max(0, nextOffset);
        if (!bottom || mode !== "stickyBottom")
          changeMode("relative");
      }
    }
  };
  const addResizeObserver = (n, handler) => {
    const ro = new ResizeObserver_es_default(handler);
    ro.observe(n);
    unsubs.push(() => ro.disconnect());
  };
  addListener(scrollPane, "scroll", handleScroll, passiveArg);
  addListener(scrollPane, "mousewheel", handleScroll, passiveArg);
  if (scrollPane === window) {
    addListener(window, "resize", handleWindowResize);
    handleWindowResize();
  } else {
    addResizeObserver(scrollPane, handleScrollPaneResize);
    handleScrollPaneResize();
  }
  addResizeObserver(getParentNode(node), handleParentNodeResize);
  handleParentNodeResize();
  addResizeObserver(node, handleNodeResize);
  handleNodeResize({ initial: true });
  initial();
  return () => unsubs.forEach((fn) => fn());
};
var useStickyBox = ({ offsetTop = 0, offsetBottom = 0, bottom = false } = {}) => {
  const [node, setNode] = (0, import_react39.useState)(null);
  const argRef = (0, import_react39.useRef)({ offsetTop, offsetBottom, bottom });
  (0, import_react39.useEffect)(() => {
    argRef.current = { offsetTop, offsetBottom, bottom };
  });
  (0, import_react39.useEffect)(() => {
    if (!node)
      return;
    return registerNode(node, argRef.current);
  }, [node]);
  return setNode;
};
var StickyBox = ({ offsetTop, offsetBottom, bottom, children, className, style }) => {
  const ref = useStickyBox({ offsetTop, offsetBottom, bottom });
  return import_react39.default.createElement("div", {
    className,
    style,
    ref
  }, children);
};
var src_default = StickyBox;

// node_modules/@formily/antd-v5/esm/form-button-group/style.js
var style_default9 = genStyleHook("form-button-group", function(token) {
  var _a, _b, _c;
  var componentCls = token.componentCls, antCls = token.antCls, colorBorder = token.colorBorder;
  return _a = {}, _a[componentCls] = {
    "&-sticky": (_b = {
      padding: "10px 0",
      borderTop: "1px solid ".concat(colorBorder),
      zIndex: 999
    }, _b["".concat(componentCls, "-sticky-inner")] = (_c = {
      display: "flex"
    }, _c["".concat(antCls, "-formily-item")] = {
      flex: 2
    }, _c), _b)
  }, _a;
});

// node_modules/@formily/antd-v5/esm/form-button-group/index.js
var __assign20 = function() {
  __assign20 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign20.apply(this, arguments);
};
var __rest8 = function(s, e) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
var __read12 = function(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
};
function getInheritedBackgroundColor(el) {
  var defaultStyle = getDefaultBackground();
  var backgroundColor = window.getComputedStyle(el).backgroundColor;
  if (backgroundColor != defaultStyle)
    return backgroundColor;
  if (!el.parentElement)
    return defaultStyle;
  return getInheritedBackgroundColor(el.parentElement);
}
function getDefaultBackground() {
  var div = document.createElement("div");
  document.head.appendChild(div);
  var bg = window.getComputedStyle(div).backgroundColor;
  document.head.removeChild(div);
  return bg;
}
var FormButtonGroup = function(_a) {
  var _b = _a.align, align = _b === void 0 ? "left" : _b, gutter = _a.gutter, props = __rest8(_a, ["align", "gutter"]);
  var prefixCls = usePrefixCls("formily-button-group");
  return import_react40.default.createElement(space_default, __assign20({}, props, { size: gutter, className: (0, import_classnames10.default)(prefixCls, props.className), style: __assign20(__assign20({}, props.style), { justifyContent: align === "left" ? "flex-start" : align === "right" ? "flex-end" : "center", display: "flex" }) }), props.children);
};
FormButtonGroup.FormItem = function(_a) {
  var _b;
  var gutter = _a.gutter, props = __rest8(_a, ["gutter"]);
  return import_react40.default.createElement(BaseItem, __assign20({}, props, { label: " ", style: __assign20(__assign20({ margin: 0, padding: 0 }, props.style), { width: "100%" }), colon: false }), ((_b = props.children) === null || _b === void 0 ? void 0 : _b["length"]) ? import_react40.default.createElement(space_default, { size: gutter }, props.children) : props.children);
};
FormButtonGroup.Sticky = function(_a) {
  var _b = _a.align, align = _b === void 0 ? "left" : _b, props = __rest8(_a, ["align"]);
  var ref = (0, import_react40.useRef)(null);
  var _c = __read12((0, import_react40.useState)("transparent"), 2), color = _c[0], setColor = _c[1];
  var prefixCls = usePrefixCls("formily-button-group");
  var _d = __read12(style_default9(prefixCls), 2), wrapSSR = _d[0], hashId = _d[1];
  (0, import_react40.useLayoutEffect)(function() {
    if (ref.current) {
      var computed = getInheritedBackgroundColor(ref.current);
      if (computed !== color) {
        setColor(computed);
      }
    }
  });
  return wrapSSR(import_react40.default.createElement(
    src_default,
    __assign20({}, props, { className: (0, import_classnames10.default)("".concat(prefixCls, "-sticky"), hashId, props.className), style: __assign20({ backgroundColor: color }, props.style), bottom: true }),
    import_react40.default.createElement("div", { ref, className: "".concat(prefixCls, "-sticky-inner"), style: __assign20(__assign20({}, props.style), { justifyContent: align === "left" ? "flex-start" : align === "right" ? "flex-end" : "center" }) }, props.children)
  ));
};

// node_modules/@formily/antd-v5/esm/form-collapse/index.js
var import_classnames11 = __toESM(require_classnames());
var import_react42 = __toESM(require_react());
var __assign21 = function() {
  __assign21 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign21.apply(this, arguments);
};
var __rest9 = function(s, e) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
var usePanels = function() {
  var collapseField = useField();
  var schema = useFieldSchema();
  var panels = [];
  schema.mapProperties(function(schema2, name) {
    var _a, _b, _c;
    var field = collapseField.query(collapseField.address.concat(name)).take();
    if ((field === null || field === void 0 ? void 0 : field.display) === "none" || (field === null || field === void 0 ? void 0 : field.display) === "hidden")
      return;
    if (((_a = schema2["x-component"]) === null || _a === void 0 ? void 0 : _a.indexOf("CollapsePanel")) > -1) {
      var key2 = ((_b = field === null || field === void 0 ? void 0 : field.componentProps) === null || _b === void 0 ? void 0 : _b.key) || ((_c = schema2 === null || schema2 === void 0 ? void 0 : schema2["x-component-props"]) === null || _c === void 0 ? void 0 : _c.key) || name;
      panels.push({
        name,
        props: __assign21(__assign21(__assign21({}, schema2 === null || schema2 === void 0 ? void 0 : schema2["x-component-props"]), field === null || field === void 0 ? void 0 : field.componentProps), { key: key2 }),
        schema: schema2
      });
    }
  });
  return panels;
};
var createFormCollapse = function(defaultActiveKeys) {
  var formCollapse = model({
    activeKeys: defaultActiveKeys,
    setActiveKeys: function(keys) {
      formCollapse.activeKeys = keys;
    },
    hasActiveKey: function(key2) {
      if (Array.isArray(formCollapse.activeKeys)) {
        if (formCollapse.activeKeys.includes(key2)) {
          return true;
        }
      } else if (formCollapse.activeKeys == key2) {
        return true;
      }
      return false;
    },
    addActiveKey: function(key2) {
      if (formCollapse.hasActiveKey(key2))
        return;
      formCollapse.activeKeys = toArr(formCollapse.activeKeys).concat(key2);
    },
    removeActiveKey: function(key2) {
      if (Array.isArray(formCollapse.activeKeys)) {
        formCollapse.activeKeys = formCollapse.activeKeys.filter(function(item) {
          return item != key2;
        });
      } else {
        formCollapse.activeKeys = "";
      }
    },
    toggleActiveKey: function(key2) {
      if (formCollapse.hasActiveKey(key2)) {
        formCollapse.removeActiveKey(key2);
      } else {
        formCollapse.addActiveKey(key2);
      }
    }
  });
  return markRaw(formCollapse);
};
var InternalFormCollapse = observer(function(_a) {
  var formCollapse = _a.formCollapse, props = __rest9(_a, ["formCollapse"]);
  var field = useField();
  var panels = usePanels();
  var prefixCls = usePrefixCls("formily-collapse", props);
  var _formCollapse = (0, import_react42.useMemo)(function() {
    return formCollapse ? formCollapse : createFormCollapse(props.defaultActiveKey);
  }, []);
  var takeActiveKeys = function() {
    var _a2;
    if (props.activeKey)
      return props.activeKey;
    if (_formCollapse === null || _formCollapse === void 0 ? void 0 : _formCollapse.activeKeys)
      return _formCollapse === null || _formCollapse === void 0 ? void 0 : _formCollapse.activeKeys;
    if (props.accordion)
      return (_a2 = panels[0]) === null || _a2 === void 0 ? void 0 : _a2.name;
    return panels.map(function(item) {
      return item.name;
    });
  };
  var badgedHeader = function(key2, props2) {
    var errors = field.form.queryFeedbacks({
      type: "error",
      address: "".concat(field.address.concat(key2), ".*")
    });
    if (errors.length) {
      return import_react42.default.createElement(badge_default, { size: "small", className: "errors-badge", count: errors.length }, props2.header);
    }
    return props2.header;
  };
  return import_react42.default.createElement(collapse_default, __assign21({}, props, { className: (0, import_classnames11.default)(prefixCls, props.className), activeKey: takeActiveKeys(), onChange: function(key2) {
    var _a2, _b;
    (_a2 = props === null || props === void 0 ? void 0 : props.onChange) === null || _a2 === void 0 ? void 0 : _a2.call(props, key2);
    (_b = _formCollapse === null || _formCollapse === void 0 ? void 0 : _formCollapse.setActiveKeys) === null || _b === void 0 ? void 0 : _b.call(_formCollapse, key2);
  } }), panels.map(function(_a2, index) {
    var props2 = _a2.props, schema = _a2.schema, name = _a2.name;
    return import_react42.default.createElement(
      collapse_default.Panel,
      __assign21({ key: index }, props2, { header: badgedHeader(name, props2), forceRender: true }),
      import_react42.default.createElement(RecursionField, { schema, name })
    );
  }));
});
var CollapsePanel2 = function(_a) {
  var children = _a.children;
  return import_react42.default.createElement(import_react42.Fragment, null, children);
};
var FormCollapse = Object.assign(InternalFormCollapse, {
  CollapsePanel: CollapsePanel2,
  createFormCollapse
});

// node_modules/@formily/antd-v5/esm/form-dialog/index.js
var import_react44 = __toESM(require_react());
var import_react_dom3 = __toESM(require_react_dom());
var __assign22 = function() {
  __assign22 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign22.apply(this, arguments);
};
var __awaiter4 = function(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var __generator4 = function(thisArg, body) {
  var _ = { label: 0, sent: function() {
    if (t[0] & 1) throw t[1];
    return t[1];
  }, trys: [], ops: [] }, f, y, t, g;
  return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() {
    return this;
  }), g;
  function verb(n) {
    return function(v) {
      return step([n, v]);
    };
  }
  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");
    while (g && (g = 0, op[0] && (_ = 0)), _) try {
      if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
      if (y = 0, t) op = [op[0] & 2, t.value];
      switch (op[0]) {
        case 0:
        case 1:
          t = op;
          break;
        case 4:
          _.label++;
          return { value: op[1], done: false };
        case 5:
          _.label++;
          y = op[1];
          op = [0];
          continue;
        case 7:
          op = _.ops.pop();
          _.trys.pop();
          continue;
        default:
          if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
            _ = 0;
            continue;
          }
          if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
            _.label = op[1];
            break;
          }
          if (op[0] === 6 && _.label < t[1]) {
            _.label = t[1];
            t = op;
            break;
          }
          if (t && _.label < t[2]) {
            _.label = t[2];
            _.ops.push(op);
            break;
          }
          if (t[2]) _.ops.pop();
          _.trys.pop();
          continue;
      }
      op = body.call(thisArg, _);
    } catch (e) {
      op = [6, e];
      y = 0;
    } finally {
      f = t = 0;
    }
    if (op[0] & 5) throw op[1];
    return { value: op[0] ? op[1] : void 0, done: true };
  }
};
var __read13 = function(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
};
var isModalTitle = function(props) {
  return isNum(props) || isStr(props) || isBool(props) || import_react44.default.isValidElement(props);
};
var getModelProps = function(props) {
  if (isModalTitle(props)) {
    return {
      title: props
    };
  } else {
    return props;
  }
};
function FormDialog(title, id, renderer) {
  var _this = this;
  if (isFn(id) || import_react44.default.isValidElement(id)) {
    renderer = id;
    id = "form-dialog";
  }
  var env = {
    host: document.createElement("div"),
    form: null,
    promise: null,
    openMiddlewares: [],
    confirmMiddlewares: [],
    cancelMiddlewares: []
  };
  var root = createPortalRoot(env.host, id);
  var props = getModelProps(title);
  var modal = __assign22(__assign22({}, props), { afterClose: function() {
    var _a;
    (_a = props === null || props === void 0 ? void 0 : props.afterClose) === null || _a === void 0 ? void 0 : _a.call(props);
    root.unmount();
  } });
  var DialogContent = observer(function() {
    return import_react44.default.createElement(import_react44.Fragment, null, isFn(renderer) ? renderer(env.form) : renderer);
  });
  var renderDialog = function(open, resolve, reject) {
    if (open === void 0) {
      open = true;
    }
    var form = env.form;
    if (!form)
      return null;
    return import_react44.default.createElement(Observer, null, function() {
      return import_react44.default.createElement(
        modal_default,
        __assign22({}, modal, { open, confirmLoading: form.submitting, onCancel: function(e) {
          var _a;
          if (((_a = modal === null || modal === void 0 ? void 0 : modal.onCancel) === null || _a === void 0 ? void 0 : _a.call(modal, e)) !== false) {
            reject === null || reject === void 0 ? void 0 : reject();
          }
        }, onOk: function(e) {
          return __awaiter4(_this, void 0, void 0, function() {
            var _a;
            return __generator4(this, function(_b) {
              if (((_a = modal === null || modal === void 0 ? void 0 : modal.onOk) === null || _a === void 0 ? void 0 : _a.call(modal, e)) !== false) {
                resolve === null || resolve === void 0 ? void 0 : resolve();
              }
              return [
                2
                /*return*/
              ];
            });
          });
        } }),
        import_react44.default.createElement(
          FormProvider,
          { form },
          import_react44.default.createElement(DialogContent, null)
        )
      );
    });
  };
  document.body.appendChild(env.host);
  var formDialog = {
    forOpen: function(middleware) {
      if (isFn(middleware)) {
        env.openMiddlewares.push(middleware);
      }
      return formDialog;
    },
    forConfirm: function(middleware) {
      if (isFn(middleware)) {
        env.confirmMiddlewares.push(middleware);
      }
      return formDialog;
    },
    forCancel: function(middleware) {
      if (isFn(middleware)) {
        env.cancelMiddlewares.push(middleware);
      }
      return formDialog;
    },
    open: function(props2) {
      return __awaiter4(_this, void 0, void 0, function() {
        var _this2 = this;
        return __generator4(this, function(_a) {
          if (env.promise)
            return [2, env.promise];
          env.promise = new Promise(function(resolve, reject) {
            return __awaiter4(_this2, void 0, void 0, function() {
              var e_1;
              var _this3 = this;
              return __generator4(this, function(_a2) {
                switch (_a2.label) {
                  case 0:
                    _a2.trys.push([0, 2, , 3]);
                    return [4, loading(modal.loadingText, function() {
                      return applyMiddleware(props2, env.openMiddlewares);
                    })];
                  case 1:
                    props2 = _a2.sent();
                    env.form = env.form || createForm(props2);
                    return [3, 3];
                  case 2:
                    e_1 = _a2.sent();
                    reject(e_1);
                    return [3, 3];
                  case 3:
                    root.render(function() {
                      return renderDialog(true, function() {
                        var _a3;
                        (_a3 = env.form) === null || _a3 === void 0 ? void 0 : _a3.submit(function() {
                          return __awaiter4(_this3, void 0, void 0, function() {
                            var _a4;
                            return __generator4(this, function(_b) {
                              switch (_b.label) {
                                case 0:
                                  return [4, applyMiddleware(env.form, env.confirmMiddlewares)];
                                case 1:
                                  _b.sent();
                                  resolve(toJS((_a4 = env.form) === null || _a4 === void 0 ? void 0 : _a4.values));
                                  formDialog.close();
                                  return [
                                    2
                                    /*return*/
                                  ];
                              }
                            });
                          });
                        }).catch(function() {
                        });
                      }, function() {
                        return __awaiter4(_this3, void 0, void 0, function() {
                          return __generator4(this, function(_a3) {
                            switch (_a3.label) {
                              case 0:
                                return [4, loading(modal.loadingText, function() {
                                  return applyMiddleware(env.form, env.cancelMiddlewares);
                                })];
                              case 1:
                                _a3.sent();
                                formDialog.close();
                                return [
                                  2
                                  /*return*/
                                ];
                            }
                          });
                        });
                      });
                    });
                    return [
                      2
                      /*return*/
                    ];
                }
              });
            });
          });
          return [2, env.promise];
        });
      });
    },
    close: function() {
      if (!env.host)
        return;
      root.render(function() {
        return renderDialog(false);
      });
    }
  };
  return formDialog;
}
var DialogFooter = function(props) {
  var ref = (0, import_react44.useRef)(null);
  var _a = __read13((0, import_react44.useState)(), 2), footer = _a[0], setFooter = _a[1];
  var footerRef = (0, import_react44.useRef)();
  var prefixCls = usePrefixCls("modal");
  (0, import_react44.useLayoutEffect)(function() {
    var _a2;
    var content = (_a2 = ref.current) === null || _a2 === void 0 ? void 0 : _a2.closest(".".concat(prefixCls, "-content"));
    if (content) {
      if (!footerRef.current) {
        footerRef.current = content.querySelector(".".concat(prefixCls, "-footer"));
        if (!footerRef.current) {
          footerRef.current = document.createElement("div");
          footerRef.current.classList.add("".concat(prefixCls, "-footer"));
          content.appendChild(footerRef.current);
        }
      }
      setFooter(footerRef.current);
    }
  });
  footerRef.current = footer;
  return import_react44.default.createElement("div", { ref, style: { display: "none" } }, footer && (0, import_react_dom3.createPortal)(props.children, footer));
};
FormDialog.Footer = DialogFooter;
FormDialog.Portal = createPortalProvider("form-dialog");

// node_modules/@formily/antd-v5/esm/form-drawer/index.js
var import_react46 = __toESM(require_react());
var import_react_dom4 = __toESM(require_react_dom());
var __assign23 = function() {
  __assign23 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign23.apply(this, arguments);
};
var __awaiter5 = function(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var __generator5 = function(thisArg, body) {
  var _ = { label: 0, sent: function() {
    if (t[0] & 1) throw t[1];
    return t[1];
  }, trys: [], ops: [] }, f, y, t, g;
  return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() {
    return this;
  }), g;
  function verb(n) {
    return function(v) {
      return step([n, v]);
    };
  }
  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");
    while (g && (g = 0, op[0] && (_ = 0)), _) try {
      if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
      if (y = 0, t) op = [op[0] & 2, t.value];
      switch (op[0]) {
        case 0:
        case 1:
          t = op;
          break;
        case 4:
          _.label++;
          return { value: op[1], done: false };
        case 5:
          _.label++;
          y = op[1];
          op = [0];
          continue;
        case 7:
          op = _.ops.pop();
          _.trys.pop();
          continue;
        default:
          if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
            _ = 0;
            continue;
          }
          if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
            _.label = op[1];
            break;
          }
          if (op[0] === 6 && _.label < t[1]) {
            _.label = t[1];
            t = op;
            break;
          }
          if (t && _.label < t[2]) {
            _.label = t[2];
            _.ops.push(op);
            break;
          }
          if (t[2]) _.ops.pop();
          _.trys.pop();
          continue;
      }
      op = body.call(thisArg, _);
    } catch (e) {
      op = [6, e];
      y = 0;
    } finally {
      f = t = 0;
    }
    if (op[0] & 5) throw op[1];
    return { value: op[0] ? op[1] : void 0, done: true };
  }
};
var __read14 = function(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
};
var isDrawerTitle = function(props) {
  return isNum(props) || isStr(props) || isBool(props) || import_react46.default.isValidElement(props);
};
var getDrawerProps = function(props) {
  if (isDrawerTitle(props)) {
    return {
      title: props
    };
  } else {
    return props;
  }
};
function FormDrawer(title, id, renderer) {
  var _this = this;
  if (isFn(id) || import_react46.default.isValidElement(id)) {
    renderer = id;
    id = "form-drawer";
  }
  var env = {
    host: document.createElement("div"),
    openMiddlewares: [],
    form: null,
    promise: null
  };
  var root = createPortalRoot(env.host, id);
  var props = getDrawerProps(title);
  var drawer = __assign23(__assign23({ width: "40%" }, props), { onClose: function(e) {
    var _a;
    if (((_a = props === null || props === void 0 ? void 0 : props.onClose) === null || _a === void 0 ? void 0 : _a.call(props, e)) !== false) {
      formDrawer.close();
    }
  }, afterOpenChange: function(open) {
    var _a;
    (_a = props === null || props === void 0 ? void 0 : props.afterOpenChange) === null || _a === void 0 ? void 0 : _a.call(props, open);
    if (open)
      return;
    root.unmount();
  } });
  var DrawerContent = observer(function() {
    return import_react46.default.createElement(import_react46.Fragment, null, isFn(renderer) ? renderer(env.form) : renderer);
  });
  var renderDrawer = function(open) {
    if (open === void 0) {
      open = true;
    }
    var form = env.form;
    if (!form)
      return null;
    return import_react46.default.createElement(
      drawer_default,
      __assign23({}, drawer, { open }),
      import_react46.default.createElement(
        FormProvider,
        { form },
        import_react46.default.createElement(DrawerContent, null)
      )
    );
  };
  document.body.appendChild(env.host);
  var formDrawer = {
    forOpen: function(middleware) {
      if (isFn(middleware)) {
        env.openMiddlewares.push(middleware);
      }
      return formDrawer;
    },
    open: function(props2) {
      if (env.promise)
        return env.promise;
      env.promise = new Promise(function(resolve, reject) {
        return __awaiter5(_this, void 0, void 0, function() {
          var e_1;
          return __generator5(this, function(_a) {
            switch (_a.label) {
              case 0:
                _a.trys.push([0, 2, , 3]);
                return [4, loading(drawer.loadingText, function() {
                  return applyMiddleware(props2, env.openMiddlewares);
                })];
              case 1:
                props2 = _a.sent();
                env.form = env.form || createForm(__assign23(__assign23({}, props2), { effects: function(form) {
                  var _a2;
                  onFormSubmitSuccess(function() {
                    resolve(toJS(form.values));
                    formDrawer.close();
                  });
                  (_a2 = props2 === null || props2 === void 0 ? void 0 : props2.effects) === null || _a2 === void 0 ? void 0 : _a2.call(props2, form);
                } }));
                return [3, 3];
              case 2:
                e_1 = _a.sent();
                reject(e_1);
                return [3, 3];
              case 3:
                root.render(function() {
                  return renderDrawer(false);
                });
                setTimeout(function() {
                  root.render(function() {
                    return renderDrawer(true);
                  });
                }, 16);
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      });
      return env.promise;
    },
    close: function() {
      if (!env.host)
        return;
      root.render(function() {
        return renderDrawer(false);
      });
    }
  };
  return formDrawer;
}
var DrawerExtra = function(props) {
  var ref = (0, import_react46.useRef)(null);
  var _a = __read14((0, import_react46.useState)(), 2), extra = _a[0], setExtra = _a[1];
  var extraRef = (0, import_react46.useRef)();
  var prefixCls = usePrefixCls("drawer");
  (0, import_react46.useLayoutEffect)(function() {
    var _a2, _b;
    var content = (_b = (_a2 = ref.current) === null || _a2 === void 0 ? void 0 : _a2.closest(".".concat(prefixCls, "-content"))) === null || _b === void 0 ? void 0 : _b.querySelector(".".concat(prefixCls, "-header"));
    if (content) {
      if (!extraRef.current) {
        extraRef.current = content.querySelector(".".concat(prefixCls, "-extra"));
        if (!extraRef.current) {
          extraRef.current = document.createElement("div");
          extraRef.current.classList.add("".concat(prefixCls, "-extra"));
          content.appendChild(extraRef.current);
        }
      }
      setExtra(extraRef.current);
    }
  });
  extraRef.current = extra;
  return import_react46.default.createElement("div", { ref, style: { display: "none" } }, extra && (0, import_react_dom4.createPortal)(props.children, extra));
};
var DrawerFooter = function(props) {
  var ref = (0, import_react46.useRef)(null);
  var _a = __read14((0, import_react46.useState)(), 2), footer = _a[0], setFooter = _a[1];
  var footerRef = (0, import_react46.useRef)();
  var prefixCls = usePrefixCls("drawer");
  (0, import_react46.useLayoutEffect)(function() {
    var _a2;
    var content = (_a2 = ref.current) === null || _a2 === void 0 ? void 0 : _a2.closest(".".concat(prefixCls, "-content"));
    if (content) {
      if (!footerRef.current) {
        footerRef.current = content.querySelector(".".concat(prefixCls, "-footer"));
        if (!footerRef.current) {
          footerRef.current = document.createElement("div");
          footerRef.current.classList.add("".concat(prefixCls, "-footer"));
          content.appendChild(footerRef.current);
        }
      }
      setFooter(footerRef.current);
    }
  });
  footerRef.current = footer;
  return import_react46.default.createElement("div", { ref, style: { display: "none" } }, footer && (0, import_react_dom4.createPortal)(props.children, footer));
};
FormDrawer.Extra = DrawerExtra;
FormDrawer.Footer = DrawerFooter;
FormDrawer.Portal = createPortalProvider("form-drawer");

// node_modules/@formily/grid/esm/observer.js
var __assign24 = function() {
  __assign24 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign24.apply(this, arguments);
};
var isHTMLElement2 = function(node) {
  return node.nodeType === 1;
};
var ChildListMutationObserver = (
  /** @class */
  function() {
    function ChildListMutationObserver2(callback) {
      var _this = this;
      this.childList = [];
      this.handler = function(mutations) {
        mutations.forEach(function(mutation) {
          if (mutation.type === "childList") {
            mutation.addedNodes.forEach(function(node) {
              if (isHTMLElement2(node)) {
                _this.addObserver(node);
              }
            });
            mutation.removedNodes.forEach(function(node) {
              if (isHTMLElement2(node)) {
                _this.removeObserver(node);
              }
            });
          }
        });
        _this.callback(mutations, _this.observer);
      };
      this.observe = function(element, init) {
        _this.init = init;
        _this.observeChildList(element);
        _this.observer.observe(element, __assign24(__assign24({}, _this.init), { subtree: false, childList: true, characterData: false, characterDataOldValue: false, attributeOldValue: false }));
      };
      this.disconnect = function() {
        _this.observer.disconnect();
      };
      this.callback = callback;
      this.observer = new MutationObserver(this.handler);
    }
    ChildListMutationObserver2.prototype.observeChildList = function(element) {
      var _this = this;
      Array.from(element.children).forEach(function(node) {
        _this.addObserver(node);
      });
    };
    ChildListMutationObserver2.prototype.addObserver = function(element) {
      var _this = this;
      var child = this.childList.find(function(t) {
        return t.element === element;
      });
      if (!child) {
        var childIndex_1 = this.childList.length;
        var child_1 = {
          element,
          observer: new MutationObserver(this.callback),
          dispose: function() {
            if (child_1.observer) {
              child_1.observer.disconnect();
              delete child_1.observer;
              _this.childList.splice(childIndex_1, 1);
            }
          }
        };
        child_1.observer.observe(child_1.element, __assign24(__assign24({}, this.init), { subtree: false, childList: false, characterData: false, characterDataOldValue: false, attributeOldValue: false }));
        this.childList.push(child_1);
      }
    };
    ChildListMutationObserver2.prototype.removeObserver = function(element) {
      var _a;
      var child = this.childList.find(function(t) {
        return t.element === element;
      });
      if (child) {
        (_a = child.dispose) === null || _a === void 0 ? void 0 : _a.call(child);
      }
    };
    return ChildListMutationObserver2;
  }()
);

// node_modules/@juggle/resize-observer/lib/utils/resizeObservers.js
var resizeObservers = [];

// node_modules/@juggle/resize-observer/lib/algorithms/hasActiveObservations.js
var hasActiveObservations = function() {
  return resizeObservers.some(function(ro) {
    return ro.activeTargets.length > 0;
  });
};

// node_modules/@juggle/resize-observer/lib/algorithms/hasSkippedObservations.js
var hasSkippedObservations = function() {
  return resizeObservers.some(function(ro) {
    return ro.skippedTargets.length > 0;
  });
};

// node_modules/@juggle/resize-observer/lib/algorithms/deliverResizeLoopError.js
var msg = "ResizeObserver loop completed with undelivered notifications.";
var deliverResizeLoopError = function() {
  var event;
  if (typeof ErrorEvent === "function") {
    event = new ErrorEvent("error", {
      message: msg
    });
  } else {
    event = document.createEvent("Event");
    event.initEvent("error", false, false);
    event.message = msg;
  }
  window.dispatchEvent(event);
};

// node_modules/@juggle/resize-observer/lib/ResizeObserverBoxOptions.js
var ResizeObserverBoxOptions;
(function(ResizeObserverBoxOptions2) {
  ResizeObserverBoxOptions2["BORDER_BOX"] = "border-box";
  ResizeObserverBoxOptions2["CONTENT_BOX"] = "content-box";
  ResizeObserverBoxOptions2["DEVICE_PIXEL_CONTENT_BOX"] = "device-pixel-content-box";
})(ResizeObserverBoxOptions || (ResizeObserverBoxOptions = {}));

// node_modules/@juggle/resize-observer/lib/utils/freeze.js
var freeze = function(obj) {
  return Object.freeze(obj);
};

// node_modules/@juggle/resize-observer/lib/ResizeObserverSize.js
var ResizeObserverSize = /* @__PURE__ */ function() {
  function ResizeObserverSize2(inlineSize, blockSize) {
    this.inlineSize = inlineSize;
    this.blockSize = blockSize;
    freeze(this);
  }
  return ResizeObserverSize2;
}();

// node_modules/@juggle/resize-observer/lib/DOMRectReadOnly.js
var DOMRectReadOnly = function() {
  function DOMRectReadOnly2(x, y, width, height) {
    this.x = x;
    this.y = y;
    this.width = width;
    this.height = height;
    this.top = this.y;
    this.left = this.x;
    this.bottom = this.top + this.height;
    this.right = this.left + this.width;
    return freeze(this);
  }
  DOMRectReadOnly2.prototype.toJSON = function() {
    var _a = this, x = _a.x, y = _a.y, top = _a.top, right = _a.right, bottom = _a.bottom, left = _a.left, width = _a.width, height = _a.height;
    return { x, y, top, right, bottom, left, width, height };
  };
  DOMRectReadOnly2.fromRect = function(rectangle) {
    return new DOMRectReadOnly2(rectangle.x, rectangle.y, rectangle.width, rectangle.height);
  };
  return DOMRectReadOnly2;
}();

// node_modules/@juggle/resize-observer/lib/utils/element.js
var isSVG = function(target) {
  return target instanceof SVGElement && "getBBox" in target;
};
var isHidden = function(target) {
  if (isSVG(target)) {
    var _a = target.getBBox(), width = _a.width, height = _a.height;
    return !width && !height;
  }
  var _b = target, offsetWidth = _b.offsetWidth, offsetHeight = _b.offsetHeight;
  return !(offsetWidth || offsetHeight || target.getClientRects().length);
};
var isElement = function(obj) {
  var _a;
  if (obj instanceof Element) {
    return true;
  }
  var scope = (_a = obj === null || obj === void 0 ? void 0 : obj.ownerDocument) === null || _a === void 0 ? void 0 : _a.defaultView;
  return !!(scope && obj instanceof scope.Element);
};
var isReplacedElement = function(target) {
  switch (target.tagName) {
    case "INPUT":
      if (target.type !== "image") {
        break;
      }
    case "VIDEO":
    case "AUDIO":
    case "EMBED":
    case "OBJECT":
    case "CANVAS":
    case "IFRAME":
    case "IMG":
      return true;
  }
  return false;
};

// node_modules/@juggle/resize-observer/lib/utils/global.js
var global = typeof window !== "undefined" ? window : {};

// node_modules/@juggle/resize-observer/lib/algorithms/calculateBoxSize.js
var cache = /* @__PURE__ */ new WeakMap();
var scrollRegexp = /auto|scroll/;
var verticalRegexp = /^tb|vertical/;
var IE = /msie|trident/i.test(global.navigator && global.navigator.userAgent);
var parseDimension = function(pixel) {
  return parseFloat(pixel || "0");
};
var size = function(inlineSize, blockSize, switchSizes) {
  if (inlineSize === void 0) {
    inlineSize = 0;
  }
  if (blockSize === void 0) {
    blockSize = 0;
  }
  if (switchSizes === void 0) {
    switchSizes = false;
  }
  return new ResizeObserverSize((switchSizes ? blockSize : inlineSize) || 0, (switchSizes ? inlineSize : blockSize) || 0);
};
var zeroBoxes = freeze({
  devicePixelContentBoxSize: size(),
  borderBoxSize: size(),
  contentBoxSize: size(),
  contentRect: new DOMRectReadOnly(0, 0, 0, 0)
});
var calculateBoxSizes = function(target, forceRecalculation) {
  if (forceRecalculation === void 0) {
    forceRecalculation = false;
  }
  if (cache.has(target) && !forceRecalculation) {
    return cache.get(target);
  }
  if (isHidden(target)) {
    cache.set(target, zeroBoxes);
    return zeroBoxes;
  }
  var cs = getComputedStyle(target);
  var svg = isSVG(target) && target.ownerSVGElement && target.getBBox();
  var removePadding = !IE && cs.boxSizing === "border-box";
  var switchSizes = verticalRegexp.test(cs.writingMode || "");
  var canScrollVertically = !svg && scrollRegexp.test(cs.overflowY || "");
  var canScrollHorizontally = !svg && scrollRegexp.test(cs.overflowX || "");
  var paddingTop = svg ? 0 : parseDimension(cs.paddingTop);
  var paddingRight = svg ? 0 : parseDimension(cs.paddingRight);
  var paddingBottom = svg ? 0 : parseDimension(cs.paddingBottom);
  var paddingLeft = svg ? 0 : parseDimension(cs.paddingLeft);
  var borderTop = svg ? 0 : parseDimension(cs.borderTopWidth);
  var borderRight = svg ? 0 : parseDimension(cs.borderRightWidth);
  var borderBottom = svg ? 0 : parseDimension(cs.borderBottomWidth);
  var borderLeft = svg ? 0 : parseDimension(cs.borderLeftWidth);
  var horizontalPadding = paddingLeft + paddingRight;
  var verticalPadding = paddingTop + paddingBottom;
  var horizontalBorderArea = borderLeft + borderRight;
  var verticalBorderArea = borderTop + borderBottom;
  var horizontalScrollbarThickness = !canScrollHorizontally ? 0 : target.offsetHeight - verticalBorderArea - target.clientHeight;
  var verticalScrollbarThickness = !canScrollVertically ? 0 : target.offsetWidth - horizontalBorderArea - target.clientWidth;
  var widthReduction = removePadding ? horizontalPadding + horizontalBorderArea : 0;
  var heightReduction = removePadding ? verticalPadding + verticalBorderArea : 0;
  var contentWidth = svg ? svg.width : parseDimension(cs.width) - widthReduction - verticalScrollbarThickness;
  var contentHeight = svg ? svg.height : parseDimension(cs.height) - heightReduction - horizontalScrollbarThickness;
  var borderBoxWidth = contentWidth + horizontalPadding + verticalScrollbarThickness + horizontalBorderArea;
  var borderBoxHeight = contentHeight + verticalPadding + horizontalScrollbarThickness + verticalBorderArea;
  var boxes = freeze({
    devicePixelContentBoxSize: size(Math.round(contentWidth * devicePixelRatio), Math.round(contentHeight * devicePixelRatio), switchSizes),
    borderBoxSize: size(borderBoxWidth, borderBoxHeight, switchSizes),
    contentBoxSize: size(contentWidth, contentHeight, switchSizes),
    contentRect: new DOMRectReadOnly(paddingLeft, paddingTop, contentWidth, contentHeight)
  });
  cache.set(target, boxes);
  return boxes;
};
var calculateBoxSize = function(target, observedBox, forceRecalculation) {
  var _a = calculateBoxSizes(target, forceRecalculation), borderBoxSize = _a.borderBoxSize, contentBoxSize = _a.contentBoxSize, devicePixelContentBoxSize = _a.devicePixelContentBoxSize;
  switch (observedBox) {
    case ResizeObserverBoxOptions.DEVICE_PIXEL_CONTENT_BOX:
      return devicePixelContentBoxSize;
    case ResizeObserverBoxOptions.BORDER_BOX:
      return borderBoxSize;
    default:
      return contentBoxSize;
  }
};

// node_modules/@juggle/resize-observer/lib/ResizeObserverEntry.js
var ResizeObserverEntry = /* @__PURE__ */ function() {
  function ResizeObserverEntry2(target) {
    var boxes = calculateBoxSizes(target);
    this.target = target;
    this.contentRect = boxes.contentRect;
    this.borderBoxSize = freeze([boxes.borderBoxSize]);
    this.contentBoxSize = freeze([boxes.contentBoxSize]);
    this.devicePixelContentBoxSize = freeze([boxes.devicePixelContentBoxSize]);
  }
  return ResizeObserverEntry2;
}();

// node_modules/@juggle/resize-observer/lib/algorithms/calculateDepthForNode.js
var calculateDepthForNode = function(node) {
  if (isHidden(node)) {
    return Infinity;
  }
  var depth = 0;
  var parent = node.parentNode;
  while (parent) {
    depth += 1;
    parent = parent.parentNode;
  }
  return depth;
};

// node_modules/@juggle/resize-observer/lib/algorithms/broadcastActiveObservations.js
var broadcastActiveObservations = function() {
  var shallowestDepth = Infinity;
  var callbacks2 = [];
  resizeObservers.forEach(function processObserver(ro) {
    if (ro.activeTargets.length === 0) {
      return;
    }
    var entries = [];
    ro.activeTargets.forEach(function processTarget(ot) {
      var entry = new ResizeObserverEntry(ot.target);
      var targetDepth = calculateDepthForNode(ot.target);
      entries.push(entry);
      ot.lastReportedSize = calculateBoxSize(ot.target, ot.observedBox);
      if (targetDepth < shallowestDepth) {
        shallowestDepth = targetDepth;
      }
    });
    callbacks2.push(function resizeObserverCallback() {
      ro.callback.call(ro.observer, entries, ro.observer);
    });
    ro.activeTargets.splice(0, ro.activeTargets.length);
  });
  for (var _i = 0, callbacks_1 = callbacks2; _i < callbacks_1.length; _i++) {
    var callback = callbacks_1[_i];
    callback();
  }
  return shallowestDepth;
};

// node_modules/@juggle/resize-observer/lib/algorithms/gatherActiveObservationsAtDepth.js
var gatherActiveObservationsAtDepth = function(depth) {
  resizeObservers.forEach(function processObserver(ro) {
    ro.activeTargets.splice(0, ro.activeTargets.length);
    ro.skippedTargets.splice(0, ro.skippedTargets.length);
    ro.observationTargets.forEach(function processTarget(ot) {
      if (ot.isActive()) {
        if (calculateDepthForNode(ot.target) > depth) {
          ro.activeTargets.push(ot);
        } else {
          ro.skippedTargets.push(ot);
        }
      }
    });
  });
};

// node_modules/@juggle/resize-observer/lib/utils/process.js
var process = function() {
  var depth = 0;
  gatherActiveObservationsAtDepth(depth);
  while (hasActiveObservations()) {
    depth = broadcastActiveObservations();
    gatherActiveObservationsAtDepth(depth);
  }
  if (hasSkippedObservations()) {
    deliverResizeLoopError();
  }
  return depth > 0;
};

// node_modules/@juggle/resize-observer/lib/utils/queueMicroTask.js
var trigger;
var callbacks = [];
var notify = function() {
  return callbacks.splice(0).forEach(function(cb) {
    return cb();
  });
};
var queueMicroTask = function(callback) {
  if (!trigger) {
    var toggle_1 = 0;
    var el_1 = document.createTextNode("");
    var config = { characterData: true };
    new MutationObserver(function() {
      return notify();
    }).observe(el_1, config);
    trigger = function() {
      el_1.textContent = "".concat(toggle_1 ? toggle_1-- : toggle_1++);
    };
  }
  callbacks.push(callback);
  trigger();
};

// node_modules/@juggle/resize-observer/lib/utils/queueResizeObserver.js
var queueResizeObserver = function(cb) {
  queueMicroTask(function ResizeObserver3() {
    requestAnimationFrame(cb);
  });
};

// node_modules/@juggle/resize-observer/lib/utils/scheduler.js
var watching = 0;
var isWatching = function() {
  return !!watching;
};
var CATCH_PERIOD = 250;
var observerConfig = { attributes: true, characterData: true, childList: true, subtree: true };
var events2 = [
  "resize",
  "load",
  "transitionend",
  "animationend",
  "animationstart",
  "animationiteration",
  "keyup",
  "keydown",
  "mouseup",
  "mousedown",
  "mouseover",
  "mouseout",
  "blur",
  "focus"
];
var time = function(timeout) {
  if (timeout === void 0) {
    timeout = 0;
  }
  return Date.now() + timeout;
};
var scheduled = false;
var Scheduler = function() {
  function Scheduler2() {
    var _this = this;
    this.stopped = true;
    this.listener = function() {
      return _this.schedule();
    };
  }
  Scheduler2.prototype.run = function(timeout) {
    var _this = this;
    if (timeout === void 0) {
      timeout = CATCH_PERIOD;
    }
    if (scheduled) {
      return;
    }
    scheduled = true;
    var until = time(timeout);
    queueResizeObserver(function() {
      var elementsHaveResized = false;
      try {
        elementsHaveResized = process();
      } finally {
        scheduled = false;
        timeout = until - time();
        if (!isWatching()) {
          return;
        }
        if (elementsHaveResized) {
          _this.run(1e3);
        } else if (timeout > 0) {
          _this.run(timeout);
        } else {
          _this.start();
        }
      }
    });
  };
  Scheduler2.prototype.schedule = function() {
    this.stop();
    this.run();
  };
  Scheduler2.prototype.observe = function() {
    var _this = this;
    var cb = function() {
      return _this.observer && _this.observer.observe(document.body, observerConfig);
    };
    document.body ? cb() : global.addEventListener("DOMContentLoaded", cb);
  };
  Scheduler2.prototype.start = function() {
    var _this = this;
    if (this.stopped) {
      this.stopped = false;
      this.observer = new MutationObserver(this.listener);
      this.observe();
      events2.forEach(function(name) {
        return global.addEventListener(name, _this.listener, true);
      });
    }
  };
  Scheduler2.prototype.stop = function() {
    var _this = this;
    if (!this.stopped) {
      this.observer && this.observer.disconnect();
      events2.forEach(function(name) {
        return global.removeEventListener(name, _this.listener, true);
      });
      this.stopped = true;
    }
  };
  return Scheduler2;
}();
var scheduler = new Scheduler();
var updateCount = function(n) {
  !watching && n > 0 && scheduler.start();
  watching += n;
  !watching && scheduler.stop();
};

// node_modules/@juggle/resize-observer/lib/ResizeObservation.js
var skipNotifyOnElement = function(target) {
  return !isSVG(target) && !isReplacedElement(target) && getComputedStyle(target).display === "inline";
};
var ResizeObservation = function() {
  function ResizeObservation2(target, observedBox) {
    this.target = target;
    this.observedBox = observedBox || ResizeObserverBoxOptions.CONTENT_BOX;
    this.lastReportedSize = {
      inlineSize: 0,
      blockSize: 0
    };
  }
  ResizeObservation2.prototype.isActive = function() {
    var size2 = calculateBoxSize(this.target, this.observedBox, true);
    if (skipNotifyOnElement(this.target)) {
      this.lastReportedSize = size2;
    }
    if (this.lastReportedSize.inlineSize !== size2.inlineSize || this.lastReportedSize.blockSize !== size2.blockSize) {
      return true;
    }
    return false;
  };
  return ResizeObservation2;
}();

// node_modules/@juggle/resize-observer/lib/ResizeObserverDetail.js
var ResizeObserverDetail = /* @__PURE__ */ function() {
  function ResizeObserverDetail2(resizeObserver, callback) {
    this.activeTargets = [];
    this.skippedTargets = [];
    this.observationTargets = [];
    this.observer = resizeObserver;
    this.callback = callback;
  }
  return ResizeObserverDetail2;
}();

// node_modules/@juggle/resize-observer/lib/ResizeObserverController.js
var observerMap = /* @__PURE__ */ new WeakMap();
var getObservationIndex = function(observationTargets, target) {
  for (var i = 0; i < observationTargets.length; i += 1) {
    if (observationTargets[i].target === target) {
      return i;
    }
  }
  return -1;
};
var ResizeObserverController = function() {
  function ResizeObserverController2() {
  }
  ResizeObserverController2.connect = function(resizeObserver, callback) {
    var detail = new ResizeObserverDetail(resizeObserver, callback);
    observerMap.set(resizeObserver, detail);
  };
  ResizeObserverController2.observe = function(resizeObserver, target, options) {
    var detail = observerMap.get(resizeObserver);
    var firstObservation = detail.observationTargets.length === 0;
    if (getObservationIndex(detail.observationTargets, target) < 0) {
      firstObservation && resizeObservers.push(detail);
      detail.observationTargets.push(new ResizeObservation(target, options && options.box));
      updateCount(1);
      scheduler.schedule();
    }
  };
  ResizeObserverController2.unobserve = function(resizeObserver, target) {
    var detail = observerMap.get(resizeObserver);
    var index = getObservationIndex(detail.observationTargets, target);
    var lastObservation = detail.observationTargets.length === 1;
    if (index >= 0) {
      lastObservation && resizeObservers.splice(resizeObservers.indexOf(detail), 1);
      detail.observationTargets.splice(index, 1);
      updateCount(-1);
    }
  };
  ResizeObserverController2.disconnect = function(resizeObserver) {
    var _this = this;
    var detail = observerMap.get(resizeObserver);
    detail.observationTargets.slice().forEach(function(ot) {
      return _this.unobserve(resizeObserver, ot.target);
    });
    detail.activeTargets.splice(0, detail.activeTargets.length);
  };
  return ResizeObserverController2;
}();

// node_modules/@juggle/resize-observer/lib/ResizeObserver.js
var ResizeObserver2 = function() {
  function ResizeObserver3(callback) {
    if (arguments.length === 0) {
      throw new TypeError("Failed to construct 'ResizeObserver': 1 argument required, but only 0 present.");
    }
    if (typeof callback !== "function") {
      throw new TypeError("Failed to construct 'ResizeObserver': The callback provided as parameter 1 is not a function.");
    }
    ResizeObserverController.connect(this, callback);
  }
  ResizeObserver3.prototype.observe = function(target, options) {
    if (arguments.length === 0) {
      throw new TypeError("Failed to execute 'observe' on 'ResizeObserver': 1 argument required, but only 0 present.");
    }
    if (!isElement(target)) {
      throw new TypeError("Failed to execute 'observe' on 'ResizeObserver': parameter 1 is not of type 'Element");
    }
    ResizeObserverController.observe(this, target, options);
  };
  ResizeObserver3.prototype.unobserve = function(target) {
    if (arguments.length === 0) {
      throw new TypeError("Failed to execute 'unobserve' on 'ResizeObserver': 1 argument required, but only 0 present.");
    }
    if (!isElement(target)) {
      throw new TypeError("Failed to execute 'unobserve' on 'ResizeObserver': parameter 1 is not of type 'Element");
    }
    ResizeObserverController.unobserve(this, target);
  };
  ResizeObserver3.prototype.disconnect = function() {
    ResizeObserverController.disconnect(this);
  };
  ResizeObserver3.toString = function() {
    return "function ResizeObserver () { [polyfill code] }";
  };
  return ResizeObserver3;
}();

// node_modules/@formily/grid/esm/index.js
var __assign25 = function() {
  __assign25 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign25.apply(this, arguments);
};
var __read15 = function(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
};
var __spreadArray = function(to, from, pack) {
  if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
    if (ar || !(i in from)) {
      if (!ar) ar = Array.prototype.slice.call(from, 0, i);
      ar[i] = from[i];
    }
  }
  return to.concat(ar || Array.prototype.slice.call(from));
};
var SpanRegExp = /span\s*(\d+)/;
var isValid2 = function(value) {
  return value !== void 0 && value !== null;
};
var calcBreakpointIndex2 = function(breakpoints, width) {
  if (Array.isArray(breakpoints)) {
    for (var i = 0; i < breakpoints.length; i++) {
      if (width <= breakpoints[i]) {
        return i;
      }
    }
  }
  return -1;
};
var calcFactor2 = function(value, breakpointIndex) {
  var _a;
  if (Array.isArray(value)) {
    if (breakpointIndex === -1)
      return value[0];
    return (_a = value[breakpointIndex]) !== null && _a !== void 0 ? _a : value[value.length - 1];
  } else {
    return value;
  }
};
var parseGridNode = function(elements) {
  return Array.from(elements).reduce(function(buf, element, index) {
    var _a;
    var style = getComputedStyle(element);
    var visible = !(style.display === "none");
    var origin = element.getAttribute("data-grid-span");
    var span = (_a = parseSpan(style.gridColumnStart)) !== null && _a !== void 0 ? _a : 1;
    var originSpan = Number(origin !== null && origin !== void 0 ? origin : span);
    var node = {
      index,
      span,
      visible,
      originSpan,
      element
    };
    if (!origin) {
      element.setAttribute("data-grid-span", String(span));
    }
    return buf.concat(node);
  }, []);
};
var calcChildTotalColumns = function(nodes, shadow) {
  if (shadow === void 0) {
    shadow = false;
  }
  return nodes.reduce(function(buf, node) {
    var _a;
    if (!shadow) {
      if (!node.visible)
        return buf;
    }
    if (node.originSpan === -1)
      return buf + ((_a = node.span) !== null && _a !== void 0 ? _a : 1);
    return buf + node.span;
  }, 0);
};
var calcChildOriginTotalColumns = function(nodes, shadow) {
  if (shadow === void 0) {
    shadow = false;
  }
  return nodes.reduce(function(buf, node) {
    var _a;
    if (!shadow) {
      if (!node.visible)
        return buf;
    }
    if (node.originSpan === -1)
      return buf + ((_a = node.span) !== null && _a !== void 0 ? _a : 1);
    return buf + node.originSpan;
  }, 0);
};
var calcSatisfyColumns = function(width, maxColumns, minColumns, maxWidth, minWidth, gap) {
  var results = [];
  for (var columns = minColumns; columns <= maxColumns; columns++) {
    var innerWidth_1 = width - (columns - 1) * gap;
    var columnWidth = innerWidth_1 / columns;
    if (columnWidth >= minWidth && columnWidth <= maxWidth) {
      results.push(columns);
    } else if (columnWidth < minWidth) {
      results.push(Math.min(Math.floor(innerWidth_1 / minWidth), maxColumns));
    } else if (columnWidth > maxWidth) {
      results.push(Math.min(Math.floor(innerWidth_1 / maxWidth), maxColumns));
    }
  }
  return Math.max.apply(Math, __spreadArray([], __read15(results), false));
};
var parseSpan = function(gridColumnStart) {
  var _a, _b;
  return Number((_b = (_a = String(gridColumnStart).match(SpanRegExp)) === null || _a === void 0 ? void 0 : _a[1]) !== null && _b !== void 0 ? _b : 1);
};
var factor2 = function(value, grid) {
  return isValid2(value) ? calcFactor2(value, grid.breakpoint) : value;
};
var resolveChildren = function(grid) {
  var walked = 0, shadowWalked = 0, rowIndex = 0, shadowRowIndex = 0;
  if (!grid.ready)
    return;
  grid.children = grid.children.map(function(node) {
    var _a;
    var columnIndex = walked % grid.columns;
    var shadowColumnIndex = shadowWalked % grid.columns;
    var remainColumns = grid.columns - columnIndex;
    var originSpan = node.originSpan;
    var targetSpan = originSpan > grid.columns ? grid.columns : originSpan;
    var span = grid.options.strictAutoFit ? targetSpan : targetSpan > remainColumns ? remainColumns : targetSpan;
    var gridColumn = originSpan === -1 ? "span ".concat(remainColumns, " / -1") : "span ".concat(span, " / auto");
    if (node.element.style.gridColumn !== gridColumn) {
      node.element.style.gridColumn = gridColumn;
    }
    if (node.visible) {
      walked += span;
    }
    shadowWalked += span;
    if (columnIndex === 0) {
      rowIndex++;
    }
    if (shadowColumnIndex == 0) {
      shadowRowIndex++;
    }
    node.shadowRow = shadowRowIndex;
    node.shadowColumn = shadowColumnIndex + 1;
    if (node.visible) {
      node.row = rowIndex;
      node.column = columnIndex + 1;
    }
    if ((_a = grid.options) === null || _a === void 0 ? void 0 : _a.shouldVisible) {
      if (!grid.options.shouldVisible(node, grid)) {
        if (node.visible) {
          node.element.style.display = "none";
        }
        node.visible = false;
      } else {
        if (!node.visible) {
          node.element.style.display = "";
        }
        node.visible = true;
      }
    }
    return node;
  });
};
var nextTick = function(callback) {
  return Promise.resolve(0).then(callback);
};
var Grid = (
  /** @class */
  function() {
    function Grid2(options) {
      var _this = this;
      this.width = 0;
      this.height = 0;
      this.children = [];
      this.childTotalColumns = 0;
      this.shadowChildTotalColumns = 0;
      this.childOriginTotalColumns = 0;
      this.shadowChildOriginTotalColumns = 0;
      this.ready = false;
      this.connect = function(container) {
        if (container) {
          _this.container = container;
          var initialize = batch.bound(function() {
            digest_1();
            _this.ready = true;
          });
          var digest_1 = batch.bound(function() {
            _this.children = parseGridNode(_this.container.children);
            _this.childTotalColumns = calcChildTotalColumns(_this.children);
            _this.shadowChildTotalColumns = calcChildTotalColumns(_this.children, true);
            _this.childOriginTotalColumns = calcChildOriginTotalColumns(_this.children);
            _this.shadowChildOriginTotalColumns = calcChildOriginTotalColumns(_this.children, true);
            var rect = _this.container.getBoundingClientRect();
            if (rect.width && rect.height) {
              _this.width = rect.width;
              _this.height = rect.height;
            }
            resolveChildren(_this);
            nextTick(function() {
              var _a, _b;
              (_b = (_a = _this.options) === null || _a === void 0 ? void 0 : _a.onDigest) === null || _b === void 0 ? void 0 : _b.call(_a, _this);
            });
            if (!_this.ready) {
              nextTick(function() {
                var _a, _b;
                (_b = (_a = _this.options) === null || _a === void 0 ? void 0 : _a.onInitialized) === null || _b === void 0 ? void 0 : _b.call(_a, _this);
              });
            }
          });
          var mutationObserver_1 = new ChildListMutationObserver(digest_1);
          var smoothDigest = function() {
            requestAnimationFrame(function() {
              digest_1();
            });
          };
          var resizeObserver_1 = new ResizeObserver2(smoothDigest);
          var dispose_1 = reaction(function() {
            return __assign25({}, _this.options);
          }, digest_1);
          resizeObserver_1.observe(_this.container);
          mutationObserver_1.observe(_this.container, {
            attributeFilter: ["data-grid-span"],
            attributes: true
          });
          initialize();
          return function() {
            resizeObserver_1.unobserve(_this.container);
            resizeObserver_1.disconnect();
            mutationObserver_1.disconnect();
            dispose_1();
            _this.children = [];
          };
        }
        return function() {
        };
      };
      this.options = __assign25({ breakpoints: [720, 1280, 1920], columnGap: 8, rowGap: 4, minWidth: 100, colWrap: true, strictAutoFit: false }, options);
      define(this, {
        options: observable.shallow,
        width: observable.ref,
        height: observable.ref,
        ready: observable.ref,
        children: observable.ref,
        childOriginTotalColumns: observable.ref,
        shadowChildOriginTotalColumns: observable.ref,
        shadowChildTotalColumns: observable.ref,
        childTotalColumns: observable.ref,
        columns: observable.computed,
        templateColumns: observable.computed,
        gap: observable.computed,
        maxColumns: observable.computed,
        minColumns: observable.computed,
        maxWidth: observable.computed,
        minWidth: observable.computed,
        breakpoints: observable.computed,
        breakpoint: observable.computed,
        rowGap: observable.computed,
        columnGap: observable.computed,
        colWrap: observable.computed
      });
    }
    Object.defineProperty(Grid2.prototype, "breakpoints", {
      get: function() {
        return this.options.breakpoints;
      },
      set: function(breakpoints) {
        this.options.breakpoints = breakpoints;
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(Grid2.prototype, "breakpoint", {
      get: function() {
        return calcBreakpointIndex2(this.options.breakpoints, this.width);
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(Grid2.prototype, "maxWidth", {
      get: function() {
        var _a;
        return (_a = factor2(this.options.maxWidth, this)) !== null && _a !== void 0 ? _a : Infinity;
      },
      set: function(maxWidth) {
        this.options.maxWidth = maxWidth;
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(Grid2.prototype, "minWidth", {
      get: function() {
        var _a;
        return (_a = factor2(this.options.minWidth, this)) !== null && _a !== void 0 ? _a : 100;
      },
      set: function(minWidth) {
        this.options.minWidth = minWidth;
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(Grid2.prototype, "maxColumns", {
      get: function() {
        var _a;
        return (_a = factor2(this.options.maxColumns, this)) !== null && _a !== void 0 ? _a : Infinity;
      },
      set: function(maxColumns) {
        this.options.maxColumns = maxColumns;
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(Grid2.prototype, "maxRows", {
      get: function() {
        var _a;
        return (_a = this.options.maxRows) !== null && _a !== void 0 ? _a : Infinity;
      },
      set: function(maxRows) {
        this.options.maxRows = maxRows;
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(Grid2.prototype, "minColumns", {
      get: function() {
        var _a;
        return (_a = factor2(this.options.minColumns, this)) !== null && _a !== void 0 ? _a : 1;
      },
      set: function(minColumns) {
        this.options.minColumns = minColumns;
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(Grid2.prototype, "rowGap", {
      get: function() {
        var _a;
        return (_a = factor2(this.options.rowGap, this)) !== null && _a !== void 0 ? _a : 5;
      },
      set: function(rowGap) {
        this.options.rowGap = rowGap;
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(Grid2.prototype, "columnGap", {
      get: function() {
        var _a;
        return (_a = factor2(this.options.columnGap, this)) !== null && _a !== void 0 ? _a : 10;
      },
      set: function(columnGap) {
        this.options.columnGap = columnGap;
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(Grid2.prototype, "colWrap", {
      get: function() {
        var _a;
        return (_a = factor2(this.options.colWrap, this)) !== null && _a !== void 0 ? _a : true;
      },
      set: function(colWrap) {
        this.options.colWrap = colWrap;
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(Grid2.prototype, "columns", {
      get: function() {
        if (!this.ready)
          return 0;
        var originTotalColumns = this.childOriginTotalColumns;
        if (this.colWrap === false) {
          return originTotalColumns;
        }
        var baseColumns = this.childSize;
        var strictMaxWidthColumns = Math.round(this.width / (this.maxWidth + this.columnGap));
        var looseMaxWidthColumns = Math.min(originTotalColumns, strictMaxWidthColumns);
        var maxWidthColumns = this.options.strictAutoFit ? strictMaxWidthColumns : looseMaxWidthColumns;
        var strictMinWidthColumns = Math.round(this.width / (this.minWidth + this.columnGap));
        var looseMinWidthColumns = Math.min(originTotalColumns, strictMinWidthColumns);
        var minWidthColumns = this.options.strictAutoFit ? strictMinWidthColumns : looseMinWidthColumns;
        var minCalculatedColumns = Math.min(baseColumns, originTotalColumns, maxWidthColumns, minWidthColumns);
        var maxCalculatedColumns = Math.max(baseColumns, originTotalColumns, maxWidthColumns, minWidthColumns);
        var finalColumns = calcSatisfyColumns(this.width, maxCalculatedColumns, minCalculatedColumns, this.maxWidth, this.minWidth, this.columnGap);
        if (finalColumns >= this.maxColumns) {
          return this.maxColumns;
        }
        if (finalColumns <= this.minColumns) {
          return this.minColumns;
        }
        return finalColumns;
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(Grid2.prototype, "rows", {
      get: function() {
        return Math.ceil(this.childTotalColumns / this.columns);
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(Grid2.prototype, "shadowRows", {
      get: function() {
        return Math.ceil(this.shadowChildTotalColumns / this.columns);
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(Grid2.prototype, "templateColumns", {
      get: function() {
        if (!this.width)
          return "";
        if (this.maxWidth === Infinity) {
          return "repeat(".concat(this.columns, ",minmax(0,1fr))");
        }
        if (this.options.strictAutoFit !== true) {
          var columnWidth = (this.width - (this.columns - 1) * this.columnGap) / this.columns;
          if (columnWidth < this.minWidth || columnWidth > this.maxWidth) {
            return "repeat(".concat(this.columns, ",minmax(0,1fr))");
          }
        }
        return "repeat(".concat(this.columns, ",minmax(").concat(this.minWidth, "px,").concat(this.maxWidth, "px))");
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(Grid2.prototype, "gap", {
      get: function() {
        return "".concat(this.rowGap, "px ").concat(this.columnGap, "px");
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(Grid2.prototype, "childSize", {
      get: function() {
        return this.children.length;
      },
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(Grid2.prototype, "fullnessLastColumn", {
      get: function() {
        var _a;
        return this.columns === ((_a = this.children[this.childSize - 1]) === null || _a === void 0 ? void 0 : _a.span);
      },
      enumerable: false,
      configurable: true
    });
    Grid2.id = function(options) {
      if (options === void 0) {
        options = {};
      }
      return JSON.stringify([
        "maxRows",
        "maxColumns",
        "minColumns",
        "maxWidth",
        "minWidth",
        "breakpoints",
        "columnGap",
        "rowGap",
        "colWrap",
        "strictAutoFit"
      ].map(function(key2) {
        return options[key2];
      }));
    };
    return Grid2;
  }()
);

// node_modules/@formily/antd-v5/esm/form-grid/index.js
var import_classnames12 = __toESM(require_classnames());
var import_react48 = __toESM(require_react());

// node_modules/@formily/antd-v5/esm/form-grid/style.js
var style_default10 = genStyleHook("form-grid", function(token) {
  var _a;
  var componentCls = token.componentCls;
  return _a = {}, _a["".concat(componentCls, "-layout")] = {
    display: "grid"
  }, _a;
});

// node_modules/@formily/antd-v5/esm/form-grid/index.js
var __assign26 = function() {
  __assign26 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign26.apply(this, arguments);
};
var __rest10 = function(s, e) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
var __read16 = function(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
};
var FormGridContext = import_react48.default.createContext(null);
var createFormGrid = function(props) {
  return markRaw(new Grid(props));
};
var useFormGrid = function() {
  return (0, import_react48.useContext)(FormGridContext);
};
var InternalFormGrid = observer(function(_a) {
  var _b, _c;
  var children = _a.children, className = _a.className, style = _a.style, props = __rest10(_a, ["children", "className", "style"]);
  var layout = useFormLayout();
  var options = __assign26({ columnGap: (_b = layout === null || layout === void 0 ? void 0 : layout.gridColumnGap) !== null && _b !== void 0 ? _b : 8, rowGap: (_c = layout === null || layout === void 0 ? void 0 : layout.gridRowGap) !== null && _c !== void 0 ? _c : 4 }, props);
  var grid = (0, import_react48.useMemo)(function() {
    return markRaw((options === null || options === void 0 ? void 0 : options.grid) ? options.grid : new Grid(options));
  }, [Grid.id(options)]);
  var ref = (0, import_react48.useRef)(null);
  var prefixCls = usePrefixCls("formily-grid", props);
  var _d = __read16(style_default10(prefixCls), 2), wrapSSR = _d[0], hashId = _d[1];
  var dataProps = pickDataProps(props);
  (0, import_react48.useLayoutEffect)(function() {
    if (ref.current) {
      return grid.connect(ref.current);
    }
  }, [grid]);
  return import_react48.default.createElement(FormGridContext.Provider, { value: grid }, wrapSSR(import_react48.default.createElement("div", __assign26({}, dataProps, { className: (0, import_classnames12.default)("".concat(prefixCls, "-layout"), hashId, className), style: __assign26(__assign26({}, style), { gridTemplateColumns: grid.templateColumns, gap: grid.gap }), ref }), children)));
}, {
  forwardRef: true
});
var GridColumn = observer(function(_a) {
  var _b = _a.gridSpan, gridSpan = _b === void 0 ? 1 : _b, children = _a.children, props = __rest10(_a, ["gridSpan", "children"]);
  return import_react48.default.createElement("div", __assign26({}, props, { style: props.style, "data-grid-span": gridSpan }), children);
});
var FormGrid = Object.assign(InternalFormGrid, {
  createFormGrid,
  useFormGrid,
  GridColumn
});

// node_modules/@formily/antd-v5/esm/form-step/index.js
var import_classnames13 = __toESM(require_classnames());
var import_react50 = __toESM(require_react());
var __assign27 = function() {
  __assign27 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign27.apply(this, arguments);
};
var __awaiter6 = function(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var __generator6 = function(thisArg, body) {
  var _ = { label: 0, sent: function() {
    if (t[0] & 1) throw t[1];
    return t[1];
  }, trys: [], ops: [] }, f, y, t, g;
  return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() {
    return this;
  }), g;
  function verb(n) {
    return function(v) {
      return step([n, v]);
    };
  }
  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");
    while (g && (g = 0, op[0] && (_ = 0)), _) try {
      if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
      if (y = 0, t) op = [op[0] & 2, t.value];
      switch (op[0]) {
        case 0:
        case 1:
          t = op;
          break;
        case 4:
          _.label++;
          return { value: op[1], done: false };
        case 5:
          _.label++;
          y = op[1];
          op = [0];
          continue;
        case 7:
          op = _.ops.pop();
          _.trys.pop();
          continue;
        default:
          if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
            _ = 0;
            continue;
          }
          if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
            _.label = op[1];
            break;
          }
          if (op[0] === 6 && _.label < t[1]) {
            _.label = t[1];
            t = op;
            break;
          }
          if (t && _.label < t[2]) {
            _.label = t[2];
            _.ops.push(op);
            break;
          }
          if (t[2]) _.ops.pop();
          _.trys.pop();
          continue;
      }
      op = body.call(thisArg, _);
    } catch (e) {
      op = [6, e];
      y = 0;
    } finally {
      f = t = 0;
    }
    if (op[0] & 5) throw op[1];
    return { value: op[0] ? op[1] : void 0, done: true };
  }
};
var __rest11 = function(s, e) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
var parseSteps = function(schema) {
  var steps = [];
  schema.mapProperties(function(schema2, name) {
    var _a;
    if (((_a = schema2["x-component"]) === null || _a === void 0 ? void 0 : _a.indexOf("StepPane")) > -1) {
      steps.push({
        name,
        props: schema2["x-component-props"],
        schema: schema2
      });
    }
  });
  return steps;
};
var createFormStep = function(defaultCurrent) {
  var _a, _b, _c;
  if (defaultCurrent === void 0) {
    defaultCurrent = 0;
  }
  var env = define({
    form: null,
    field: null,
    steps: []
  }, {
    form: observable.ref,
    field: observable.ref,
    steps: observable.shallow
  });
  var setDisplay = (_a = action.bound) === null || _a === void 0 ? void 0 : _a.call(action, function(target) {
    var currentStep = env.steps[target];
    env.steps.forEach(function(_a2) {
      var _b2, _c2;
      var name = _a2.name;
      (_b2 = env.form) === null || _b2 === void 0 ? void 0 : _b2.query("".concat((_c2 = env.field) === null || _c2 === void 0 ? void 0 : _c2.address, ".").concat(name)).take(function(field) {
        if (name === currentStep.name) {
          field.setDisplay("visible");
        } else {
          field.setDisplay("hidden");
        }
      });
    });
  });
  var next = (_b = action.bound) === null || _b === void 0 ? void 0 : _b.call(action, function() {
    if (formStep.allowNext) {
      formStep.setCurrent(formStep.current + 1);
    }
  });
  var back = (_c = action.bound) === null || _c === void 0 ? void 0 : _c.call(action, function() {
    if (formStep.allowBack) {
      formStep.setCurrent(formStep.current - 1);
    }
  });
  var formStep = model({
    connect: function(steps, field) {
      env.steps = steps;
      env.form = field === null || field === void 0 ? void 0 : field.form;
      env.field = field;
    },
    current: defaultCurrent,
    setCurrent: function(key2) {
      setDisplay === null || setDisplay === void 0 ? void 0 : setDisplay(key2);
      formStep.current = key2;
    },
    get allowNext() {
      return formStep.current < env.steps.length - 1;
    },
    get allowBack() {
      return formStep.current > 0;
    },
    next: function() {
      var _a2, _b2;
      return __awaiter6(this, void 0, void 0, function() {
        var _c2;
        return __generator6(this, function(_d) {
          switch (_d.label) {
            case 0:
              _d.trys.push([0, 2, , 3]);
              return [4, (_a2 = env.form) === null || _a2 === void 0 ? void 0 : _a2.validate()];
            case 1:
              _d.sent();
              if ((_b2 = env.form) === null || _b2 === void 0 ? void 0 : _b2.valid) {
                next === null || next === void 0 ? void 0 : next();
              }
              return [3, 3];
            case 2:
              _c2 = _d.sent();
              return [3, 3];
            case 3:
              return [
                2
                /*return*/
              ];
          }
        });
      });
    },
    back: function() {
      return __awaiter6(this, void 0, void 0, function() {
        return __generator6(this, function(_a2) {
          back === null || back === void 0 ? void 0 : back();
          return [
            2
            /*return*/
          ];
        });
      });
    },
    submit: function(onSubmit) {
      var _a2, _b2;
      return __awaiter6(this, void 0, void 0, function() {
        return __generator6(this, function(_c2) {
          return [2, (_b2 = (_a2 = env.form) === null || _a2 === void 0 ? void 0 : _a2.submit) === null || _b2 === void 0 ? void 0 : _b2.call(_a2, onSubmit)];
        });
      });
    }
  });
  return markRaw(formStep);
};
var FormStep = connect(observer(function(_a) {
  var _b;
  var formStep = _a.formStep, className = _a.className, props = __rest11(_a, ["formStep", "className"]);
  var field = useField();
  var prefixCls = usePrefixCls("formily-step", props);
  var schema = useFieldSchema();
  var steps = parseSteps(schema);
  var current = props.current || (formStep === null || formStep === void 0 ? void 0 : formStep.current) || 0;
  (_b = formStep === null || formStep === void 0 ? void 0 : formStep.connect) === null || _b === void 0 ? void 0 : _b.call(formStep, steps, field);
  return import_react50.default.createElement(
    "div",
    { className: (0, import_classnames13.default)(prefixCls, className) },
    import_react50.default.createElement(steps_default, __assign27({}, props, { style: __assign27({ marginBottom: 10 }, props.style), current }), steps.map(function(_a2, key2) {
      var props2 = _a2.props;
      return import_react50.default.createElement(steps_default.Step, __assign27({}, props2, { key: key2 }));
    })),
    steps.map(function(_a2, key2) {
      var name = _a2.name, schema2 = _a2.schema;
      if (key2 !== current)
        return;
      return import_react50.default.createElement(RecursionField, { key: key2, name, schema: schema2 });
    })
  );
}));
var StepPane = function(_a) {
  var children = _a.children;
  return import_react50.default.createElement(import_react50.Fragment, null, children);
};
FormStep.StepPane = StepPane;
FormStep.createFormStep = createFormStep;

// node_modules/@formily/antd-v5/esm/form-tab/index.js
var import_classnames14 = __toESM(require_classnames());
var import_react52 = __toESM(require_react());
var __assign28 = function() {
  __assign28 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign28.apply(this, arguments);
};
var __rest12 = function(s, e) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
var useTabs = function() {
  var tabsField = useField();
  var schema = useFieldSchema();
  var tabs = [];
  schema.mapProperties(function(schema2, name) {
    var _a, _b, _c;
    var field = tabsField.query(tabsField.address.concat(name)).take();
    if ((field === null || field === void 0 ? void 0 : field.display) === "none" || (field === null || field === void 0 ? void 0 : field.display) === "hidden")
      return;
    if (((_a = schema2["x-component"]) === null || _a === void 0 ? void 0 : _a.indexOf("TabPane")) > -1) {
      var key2 = ((_b = field === null || field === void 0 ? void 0 : field.componentProps) === null || _b === void 0 ? void 0 : _b.key) || ((_c = schema2 === null || schema2 === void 0 ? void 0 : schema2["x-component-props"]) === null || _c === void 0 ? void 0 : _c.key) || name;
      tabs.push({
        name,
        props: __assign28(__assign28(__assign28({}, schema2 === null || schema2 === void 0 ? void 0 : schema2["x-component-props"]), field === null || field === void 0 ? void 0 : field.componentProps), { key: key2 }),
        schema: schema2
      });
    }
  });
  return tabs;
};
var FeedbackBadge2 = observer(function(props) {
  var field = useField();
  var errors = field.form.queryFeedbacks({
    type: "error",
    address: "".concat(field.address.concat(props.name), ".*")
  });
  if (errors.length) {
    return import_react52.default.createElement(badge_default, { size: "small", className: "errors-badge", count: errors.length }, props.tab);
  }
  return import_react52.default.createElement(import_react52.Fragment, null, props.tab);
});
var createFormTab = function(defaultActiveKey) {
  var formTab = model({
    activeKey: defaultActiveKey,
    setActiveKey: function(key2) {
      formTab.activeKey = key2;
    }
  });
  return markRaw(formTab);
};
var FormTab = observer(function(_a) {
  var formTab = _a.formTab, props = __rest12(_a, ["formTab"]);
  var tabs = useTabs();
  var _formTab = (0, import_react52.useMemo)(function() {
    return formTab ? formTab : createFormTab();
  }, []);
  var prefixCls = usePrefixCls("formily-tab", props);
  var activeKey = props.activeKey || (_formTab === null || _formTab === void 0 ? void 0 : _formTab.activeKey);
  return import_react52.default.createElement(tabs_default, __assign28({}, props, { className: (0, import_classnames14.default)(prefixCls, props.className), activeKey, onChange: function(key2) {
    var _a2, _b;
    (_a2 = props.onChange) === null || _a2 === void 0 ? void 0 : _a2.call(props, key2);
    (_b = _formTab === null || _formTab === void 0 ? void 0 : _formTab.setActiveKey) === null || _b === void 0 ? void 0 : _b.call(_formTab, key2);
  }, items: tabs.map(function(_a2, key2) {
    var props2 = _a2.props, schema = _a2.schema, name = _a2.name;
    return __assign28(__assign28({ key: key2, label: import_react52.default.createElement(FeedbackBadge2, { name, tab: props2.tab }) }, props2), { children: import_react52.default.createElement(RecursionField, { schema, name }) });
  }) }));
});
var TabPane = function(_a) {
  var children = _a.children;
  return import_react52.default.createElement(import_react52.Fragment, null, children);
};
FormTab.TabPane = TabPane;
FormTab.createFormTab = createFormTab;

// node_modules/@formily/antd-v5/esm/input/index.js
var import_react54 = __toESM(require_react());
var __assign29 = function() {
  __assign29 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign29.apply(this, arguments);
};
var InternalInput = connect(input_default, mapProps(function(props, field) {
  return __assign29(__assign29({}, props), { suffix: import_react54.default.createElement("span", null, (field === null || field === void 0 ? void 0 : field["loading"]) || (field === null || field === void 0 ? void 0 : field["validating"]) ? import_react54.default.createElement(LoadingOutlined_default, null) : props.suffix) });
}), mapReadPretty(PreviewText.Input));
var TextArea = connect(input_default.TextArea, mapReadPretty(PreviewText.Input));
var Input2 = Object.assign(InternalInput, {
  TextArea
});

// node_modules/@formily/antd-v5/esm/number-picker/index.js
var NumberPicker2 = connect(input_number_default, mapReadPretty(PreviewText.NumberPicker));

// node_modules/@formily/antd-v5/esm/password/index.js
var import_react58 = __toESM(require_react());

// node_modules/@formily/antd-v5/esm/password/PasswordStrength.js
var import_react56 = __toESM(require_react());
var isNum2 = function(c) {
  return c >= 48 && c <= 57;
};
var isLower = function(c) {
  return c >= 97 && c <= 122;
};
var isUpper = function(c) {
  return c >= 65 && c <= 90;
};
var isSymbol = function(c) {
  return !(isLower(c) || isUpper(c) || isNum2(c));
};
var isLetter = function(c) {
  return isLower(c) || isUpper(c);
};
var getStrength = function(val) {
  if (!val)
    return 0;
  var num = 0;
  var lower = 0;
  var upper = 0;
  var symbol = 0;
  var MNS = 0;
  var rep = 0;
  var repC = 0;
  var consecutive = 0;
  var sequential = 0;
  var len = function() {
    return num + lower + upper + symbol;
  };
  var callme = function() {
    var re = num > 0 ? 1 : 0;
    re += lower > 0 ? 1 : 0;
    re += upper > 0 ? 1 : 0;
    re += symbol > 0 ? 1 : 0;
    if (re > 2 && len() >= 8) {
      return re + 1;
    } else {
      return 0;
    }
  };
  for (var i = 0; i < val.length; i++) {
    var c = val.charCodeAt(i);
    if (isNum2(c)) {
      num++;
      if (i !== 0 && i !== val.length - 1) {
        MNS++;
      }
      if (i > 0 && isNum2(val.charCodeAt(i - 1))) {
        consecutive++;
      }
    } else if (isLower(c)) {
      lower++;
      if (i > 0 && isLower(val.charCodeAt(i - 1))) {
        consecutive++;
      }
    } else if (isUpper(c)) {
      upper++;
      if (i > 0 && isUpper(val.charCodeAt(i - 1))) {
        consecutive++;
      }
    } else {
      symbol++;
      if (i !== 0 && i !== val.length - 1) {
        MNS++;
      }
    }
    var exists = false;
    for (var j = 0; j < val.length; j++) {
      if (val[i] === val[j] && i !== j) {
        exists = true;
        repC += Math.abs(val.length / (j - i));
      }
    }
    if (exists) {
      rep++;
      var unique = val.length - rep;
      repC = unique ? Math.ceil(repC / unique) : Math.ceil(repC);
    }
    if (i > 1) {
      var last1 = val.charCodeAt(i - 1);
      var last2 = val.charCodeAt(i - 2);
      if (isLetter(c)) {
        if (isLetter(last1) && isLetter(last2)) {
          var v = val.toLowerCase();
          var vi = v.charCodeAt(i);
          var vi1 = v.charCodeAt(i - 1);
          var vi2 = v.charCodeAt(i - 2);
          if (vi - vi1 === vi1 - vi2 && Math.abs(vi - vi1) === 1) {
            sequential++;
          }
        }
      } else if (isNum2(c)) {
        if (isNum2(last1) && isNum2(last2)) {
          if (c - last1 === last1 - last2 && Math.abs(c - last1) === 1) {
            sequential++;
          }
        }
      } else {
        if (isSymbol(last1) && isSymbol(last2)) {
          if (c - last1 === last1 - last2 && Math.abs(c - last1) === 1) {
            sequential++;
          }
        }
      }
    }
  }
  var sum = 0;
  var length = len();
  sum += 4 * length;
  if (lower > 0) {
    sum += 2 * (length - lower);
  }
  if (upper > 0) {
    sum += 2 * (length - upper);
  }
  if (num !== length) {
    sum += 4 * num;
  }
  sum += 6 * symbol;
  sum += 2 * MNS;
  sum += 2 * callme();
  if (length === lower + upper) {
    sum -= length;
  }
  if (length === num) {
    sum -= num;
  }
  sum -= repC;
  sum -= 2 * consecutive;
  sum -= 3 * sequential;
  sum = sum < 0 ? 0 : sum;
  sum = sum > 100 ? 100 : sum;
  if (sum >= 80) {
    return 100;
  } else if (sum >= 60) {
    return 80;
  } else if (sum >= 40) {
    return 60;
  } else if (sum >= 20) {
    return 40;
  } else {
    return 20;
  }
};
var PasswordStrength = function(props) {
  if (isFn(props.children)) {
    return props.children(getStrength(String(props.value)));
  } else {
    return import_react56.default.createElement(import_react56.Fragment, null, props.children);
  }
};

// node_modules/@formily/antd-v5/esm/password/index.js
var __assign30 = function() {
  __assign30 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign30.apply(this, arguments);
};
var __rest13 = function(s, e) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
var Password = connect(function(props) {
  var value = props.value, className = props.className, checkStrength = props.checkStrength, others = __rest13(props, ["value", "className", "checkStrength"]);
  var blockStyle = {
    position: "absolute",
    zIndex: 1,
    height: 8,
    top: 0,
    background: "#fff",
    width: 1,
    transform: "translate(-50%, 0)"
  };
  return import_react58.default.createElement(
    "span",
    { className },
    import_react58.default.createElement(input_default.Password, __assign30({}, others, { value })),
    checkStrength && import_react58.default.createElement(PasswordStrength, { value: String(value) }, function(score) {
      return import_react58.default.createElement(
        "div",
        { style: {
          background: "#e0e0e0",
          marginBottom: 3,
          position: "relative"
        } },
        import_react58.default.createElement("div", { style: __assign30(__assign30({}, blockStyle), { left: "20%" }) }),
        import_react58.default.createElement("div", { style: __assign30(__assign30({}, blockStyle), { left: "40%" }) }),
        import_react58.default.createElement("div", { style: __assign30(__assign30({}, blockStyle), { left: "60%" }) }),
        import_react58.default.createElement("div", { style: __assign30(__assign30({}, blockStyle), { left: "80%" }) }),
        import_react58.default.createElement("div", { style: {
          position: "relative",
          backgroundImage: "-webkit-linear-gradient(left, #ff5500, #ff9300)",
          transition: "all 0.35s ease-in-out",
          height: 8,
          width: "100%",
          marginTop: 5,
          clipPath: "polygon(0 0,".concat(score, "% 0,").concat(score, "% 100%,0 100%)")
        } })
      );
    })
  );
}, mapReadPretty(PreviewText.Input));

// node_modules/@formily/antd-v5/esm/radio/index.js
var InternalRadio = connect(radio_default, mapProps({
  value: "checked",
  onInput: "onChange"
}));
var Group2 = connect(radio_default.Group, mapProps({
  dataSource: "options"
}), mapReadPretty(PreviewText.Select));
var Radio = Object.assign(InternalRadio, {
  __ANT_RADIO: true,
  Group: Group2
});

// node_modules/@formily/antd-v5/esm/reset/index.js
var import_react61 = __toESM(require_react());
var __assign31 = function() {
  __assign31 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign31.apply(this, arguments);
};
var __rest14 = function(s, e) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
var Reset = function(_a) {
  var forceClear = _a.forceClear, validate = _a.validate, onResetValidateSuccess = _a.onResetValidateSuccess, onResetValidateFailed = _a.onResetValidateFailed, props = __rest14(_a, ["forceClear", "validate", "onResetValidateSuccess", "onResetValidateFailed"]);
  var form = useParentForm();
  return import_react61.default.createElement(button_default, __assign31({}, props, { onClick: function(e) {
    if (isFn(props.onClick)) {
      if (props.onClick(e) === false)
        return;
    }
    form.reset("*", {
      forceClear,
      validate
    }).then(onResetValidateSuccess).catch(onResetValidateFailed);
  } }), props.children);
};

// node_modules/@formily/antd-v5/esm/select/index.js
var import_react63 = __toESM(require_react());
var __assign32 = function() {
  __assign32 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign32.apply(this, arguments);
};
var Select2 = connect(select_default, mapProps({
  dataSource: "options",
  loading: true
}, function(props, field) {
  return __assign32(__assign32({}, props), { suffixIcon: (field === null || field === void 0 ? void 0 : field["loading"]) || (field === null || field === void 0 ? void 0 : field["validating"]) ? import_react63.default.createElement(LoadingOutlined_default, null) : props.suffixIcon });
}), mapReadPretty(PreviewText.Select));

// node_modules/@formily/antd-v5/esm/select-table/index.js
var import_classnames15 = __toESM(require_classnames());
var import_react66 = __toESM(require_react());

// node_modules/@formily/antd-v5/esm/select-table/hooks/useFlatOptions.js
var __read17 = function(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
};
var __spreadArray2 = function(to, from, pack) {
  if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
    if (ar || !(i in from)) {
      if (!ar) ar = Array.prototype.slice.call(from, 0, i);
      ar[i] = from[i];
    }
  }
  return to.concat(ar || Array.prototype.slice.call(from));
};
var useFlatOptions = function(tree) {
  var flatData = function(data) {
    var list = [];
    data === null || data === void 0 ? void 0 : data.forEach(function(item) {
      var _a;
      list = __spreadArray2(__spreadArray2([], __read17(list), false), [item], false);
      if ((_a = item === null || item === void 0 ? void 0 : item.children) === null || _a === void 0 ? void 0 : _a.length) {
        list = __spreadArray2(__spreadArray2([], __read17(list), false), __read17(flatData(item.children)), false);
      }
    });
    return list;
  };
  return flatData(tree);
};

// node_modules/@formily/antd-v5/esm/select-table/utils.js
var __assign33 = function() {
  __assign33 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign33.apply(this, arguments);
};
var __read18 = function(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
};
var __spreadArray3 = function(to, from, pack) {
  if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
    if (ar || !(i in from)) {
      if (!ar) ar = Array.prototype.slice.call(from, 0, i);
      ar[i] = from[i];
    }
  }
  return to.concat(ar || Array.prototype.slice.call(from));
};
var getTreeKeys = function(tree, primaryKey) {
  if (primaryKey === void 0) {
    primaryKey = "";
  }
  return isArr(tree) ? tree.reduce(function(prev, current) {
    if (current === null || current === void 0 ? void 0 : current.disabled) {
      return prev;
    }
    return __spreadArray3(__spreadArray3(__spreadArray3([], __read18(prev), false), [
      current[primaryKey]
    ], false), __read18(getTreeKeys(current === null || current === void 0 ? void 0 : current.children, primaryKey)), false);
  }, []) : [];
};
var hasSelectedKey = function(tree, selected, primaryKey) {
  var keys = getTreeKeys(tree, primaryKey);
  var mergedKeys = __spreadArray3(__spreadArray3([], __read18(keys), false), __read18(selected), false);
  var validKeys = __spreadArray3([], __read18(new Set(mergedKeys)), false);
  return validKeys.length !== mergedKeys.length;
};
var isAllSelected = function(list, selected, primaryKey) {
  if (primaryKey === void 0) {
    primaryKey = "";
  }
  var validList = list.filter(function(item) {
    return !(item === null || item === void 0 ? void 0 : item.disabled);
  });
  var selectedList = validList.filter(function(item) {
    return selected === null || selected === void 0 ? void 0 : selected.includes(item[primaryKey]);
  });
  return selectedList.length === validList.length;
};
var completedKeys = function(flatDataSource, selected, primaryKey) {
  if (flatDataSource === void 0) {
    flatDataSource = [];
  }
  if (primaryKey === void 0) {
    primaryKey = "";
  }
  var allSelectedKeys = __spreadArray3([], __read18(selected), false);
  flatDataSource.forEach(function(item) {
    var _a;
    if ((_a = item.children) === null || _a === void 0 ? void 0 : _a.length) {
      allSelectedKeys = completedKeys(item.children, allSelectedKeys, primaryKey);
      if (isAllSelected(item.children, allSelectedKeys, primaryKey)) {
        if (!(item === null || item === void 0 ? void 0 : item.disabled)) {
          allSelectedKeys = __spreadArray3([], __read18(new Set(__spreadArray3(__spreadArray3([], __read18(allSelectedKeys), false), [item[primaryKey]], false))), false);
        }
      } else {
        allSelectedKeys = allSelectedKeys.filter(function(key2) {
          return key2 !== item[primaryKey];
        });
      }
    }
  });
  return allSelectedKeys;
};
var getSelectedPath = function(tree, selected, primaryKey) {
  if (tree === void 0) {
    tree = [];
  }
  var pathData = [];
  tree.forEach(function(item) {
    var validChildren = getSelectedPath(item.children, selected, primaryKey);
    if (validChildren.length || (selected === null || selected === void 0 ? void 0 : selected.includes(item[primaryKey]))) {
      pathData.push(__assign33(__assign33({}, item), validChildren.length ? { children: validChildren } : {}));
    }
  });
  return pathData;
};
var deleteTreeItem = function(tree, key2) {
  return tree.map(function(item) {
    var _a;
    var validItem = __assign33({}, item);
    delete validItem[key2];
    if ((_a = validItem.children) === null || _a === void 0 ? void 0 : _a.length) {
      validItem.children = deleteTreeItem(validItem.children, key2);
    }
    return validItem;
  });
};
var getOutputData = function(keys, options, dataSource, primaryKey, originalValueType, originalOptionAsValue, mode, checkStrictly) {
  if (primaryKey === void 0) {
    primaryKey = "";
  }
  if (originalValueType === void 0) {
    originalValueType = "";
  }
  if (originalOptionAsValue === void 0) {
    originalOptionAsValue = false;
  }
  if (mode === void 0) {
    mode = "";
  }
  var valueType = checkStrictly !== false ? "all" : originalValueType;
  var optionAsValue = valueType === "path" ? false : originalOptionAsValue;
  var outputValue = [];
  var outputOptions = [];
  if (valueType === "parent") {
    var childrenKeys_1 = [];
    options.forEach(function(option) {
      childrenKeys_1 = __spreadArray3(__spreadArray3([], __read18(childrenKeys_1), false), __read18(getTreeKeys(option.children, primaryKey)), false);
    });
    outputValue = keys.filter(function(key2) {
      return !childrenKeys_1.includes(key2);
    });
    outputOptions = options.filter(function(options2) {
      return outputValue.includes(options2[primaryKey]);
    });
  } else if (valueType === "child") {
    outputValue = __spreadArray3([], __read18(keys), false);
    outputOptions = __spreadArray3([], __read18(options), false);
    outputOptions.forEach(function(option) {
      if (hasSelectedKey(option.children, keys, primaryKey)) {
        outputValue = outputValue.filter(function(key2) {
          return key2 !== option[primaryKey];
        });
        outputOptions = outputOptions.filter(function(options2) {
          return options2[primaryKey] !== option[primaryKey];
        });
      }
    });
  } else if (valueType === "path") {
    outputValue = getSelectedPath(dataSource, keys, primaryKey);
    outputOptions = __spreadArray3([], __read18(options), false);
  } else {
    outputValue = __spreadArray3([], __read18(keys), false);
    outputOptions = __spreadArray3([], __read18(options), false);
  }
  outputOptions = deleteTreeItem(outputOptions, "__formily_key__");
  outputValue = optionAsValue && valueType !== "path" ? outputOptions : outputValue;
  if (mode === "single") {
    outputValue = outputValue[0];
    outputOptions = outputOptions[0];
  }
  return { outputValue, outputOptions };
};
var getUISelected = function(value, flatDataSource, primaryKey, originalValueType, originalOptionAsValue, mode, checkStrictly, rowKey) {
  var valueType = checkStrictly !== false ? "all" : originalValueType;
  var optionAsValue = valueType === "path" ? false : originalOptionAsValue;
  var keys = mode === "single" ? [value] : isArr(value) ? value : [];
  keys = optionAsValue && valueType !== "path" ? keys.map(function(record) {
    return isFn(rowKey) ? rowKey(record) : record === null || record === void 0 ? void 0 : record[primaryKey || ""];
  }) : keys;
  var newKeys = [];
  if (valueType === "parent") {
    var options = flatDataSource === null || flatDataSource === void 0 ? void 0 : flatDataSource.filter(function(item) {
      return keys.includes(item[primaryKey || ""]);
    });
    var childrenKeys_2 = [];
    options === null || options === void 0 ? void 0 : options.forEach(function(option) {
      childrenKeys_2 = __spreadArray3(__spreadArray3([], __read18(childrenKeys_2), false), __read18(getTreeKeys(option.children, primaryKey)), false);
    });
    newKeys = __spreadArray3([], __read18(new Set(__spreadArray3(__spreadArray3([], __read18(keys), false), __read18(childrenKeys_2), false))), false);
  } else if (valueType === "child") {
    newKeys = completedKeys(flatDataSource, keys, primaryKey);
  } else if (valueType === "path") {
    var pathKeys = useFlatOptions(keys).map(function(item) {
      return item[primaryKey || ""];
    });
    newKeys = completedKeys(flatDataSource, pathKeys, primaryKey);
  } else {
    newKeys = __spreadArray3([], __read18(keys), false);
  }
  return newKeys;
};
var getCompatibleAllSelected = function(selected, dataSource, usableKeys, checkStrictly, primaryKey) {
  if (!usableKeys.length) {
    return false;
  }
  var currentSelected = selected.filter(function(item) {
    return usableKeys.includes(item);
  });
  var validSelected = checkStrictly !== false ? currentSelected : completedKeys(dataSource, currentSelected, primaryKey);
  return validSelected.length === usableKeys.length;
};

// node_modules/@formily/antd-v5/esm/select-table/hooks/useCheckSlackly.js
var __read19 = function(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
};
var __spreadArray4 = function(to, from, pack) {
  if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
    if (ar || !(i in from)) {
      if (!ar) ar = Array.prototype.slice.call(from, 0, i);
      ar[i] = from[i];
    }
  }
  return to.concat(ar || Array.prototype.slice.call(from));
};
var getIndeterminate = function(record, flatDataSource, selected, primaryKey) {
  if (primaryKey === void 0) {
    primaryKey = "";
  }
  if (selected === null || selected === void 0 ? void 0 : selected.includes(record[primaryKey])) {
    return void 0;
  }
  var wholeRecord = flatDataSource.find(function(item) {
    return item[primaryKey] === record[primaryKey];
  });
  return hasSelectedKey(wholeRecord.children, selected, primaryKey) || void 0;
};
var useCheckSlackly = function(currentSelected, selected, flatDataSource, flatFilteredDataSource, primaryKey, checkStrictly) {
  if (primaryKey === void 0) {
    primaryKey = "";
  }
  var isSelected = currentSelected.length > selected.length;
  var currentKey = __spreadArray4(__spreadArray4([], __read19(currentSelected), false), __read19(selected), false).find(
    function(key2) {
      return !(currentSelected.includes(key2) && selected.includes(key2));
    }
    // 当前变化key不同时存在于两个selected
  );
  var currentRecord = flatFilteredDataSource.find(function(item) {
    return item[primaryKey] === currentKey;
  });
  var currentTreeKeys = getTreeKeys(currentRecord.children, primaryKey);
  if (getCompatibleAllSelected(selected, currentRecord.children, currentTreeKeys, checkStrictly, primaryKey)) {
    isSelected = false;
  }
  var newSelected = [];
  if (isSelected) {
    newSelected = __spreadArray4([], __read19(new Set(__spreadArray4(__spreadArray4(__spreadArray4([], __read19(selected), false), [currentKey], false), __read19(currentTreeKeys), false))), false);
  } else {
    newSelected = selected.filter(function(key2) {
      return !__spreadArray4([currentKey], __read19(currentTreeKeys), false).includes(key2);
    });
  }
  newSelected = completedKeys(flatDataSource, newSelected, primaryKey);
  return { selectedRowKeys: newSelected };
};

// node_modules/@formily/antd-v5/esm/select-table/hooks/useFilterOptions.js
var React32 = __toESM(require_react());
var __assign34 = function() {
  __assign34 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign34.apply(this, arguments);
};
function includes(test, search) {
  return toArray(test).join("").toUpperCase().includes(search);
}
function includesOption(option, search) {
  var searched = /* @__PURE__ */ new Set();
  var _includesOption = function(option2) {
    var keys = Object.keys(option2 || {});
    return keys.some(function(key2) {
      if (key2 === "__level") {
        return false;
      }
      var value = option2[key2];
      if (React32.isValidElement(value))
        return false;
      if (key2 !== "children" && !searched.has(value)) {
        if (typeof value === "object") {
          searched.add(value);
          return _includesOption(value);
        }
        return includes(value, search);
      }
      return false;
    });
  };
  return _includesOption(option);
}
function toArray(value) {
  if (isArr(value)) {
    return value;
  }
  return value !== void 0 ? [value] : [];
}
var useFilterOptions = function(options, searchValue, filterOption, checkStrictly) {
  return React32.useMemo(function() {
    if (!searchValue || filterOption === false) {
      return options;
    }
    var filterFunc = isFn(filterOption) ? filterOption : function(value, option) {
      return includesOption(option, value.toUpperCase());
    };
    var doFilter = function(arr) {
      var filterArr = [];
      arr === null || arr === void 0 ? void 0 : arr.forEach(function(item) {
        var _a;
        if ((_a = item === null || item === void 0 ? void 0 : item.children) === null || _a === void 0 ? void 0 : _a.length) {
          var filterChildren = doFilter(item.children);
          if (filterChildren.length) {
            filterArr.push(__assign34(__assign34({}, item), { children: filterChildren }));
          } else if (filterFunc(searchValue, item) && checkStrictly !== false) {
            filterArr.push(__assign34(__assign34({}, item), { children: [] }));
          }
        } else if (filterFunc(searchValue, item)) {
          filterArr.push(item);
        }
      });
      return filterArr;
    };
    return doFilter(options);
  }, [options, searchValue, filterOption]);
};

// node_modules/@formily/antd-v5/esm/select-table/hooks/useSize.js
var useSize = function(fieldSize, searchSize, tableSize) {
  if (fieldSize === void 0) {
    fieldSize = "middle";
  }
  var fieldSizeMap = {
    small: {
      searchSize: "small",
      tableSize: "small"
    },
    default: {
      searchSize: "middle",
      tableSize: "middle"
    },
    large: {
      searchSize: "large",
      tableSize: "default"
    }
  };
  var _a = fieldSizeMap[fieldSize] || {}, fieldSearchSize = _a.searchSize, fieldTableSize = _a.tableSize;
  return {
    searchSize: searchSize || fieldSearchSize,
    tableSize: tableSize || fieldTableSize
  };
};

// node_modules/@formily/antd-v5/esm/select-table/hooks/useTitleAddon.js
var import_react64 = __toESM(require_react());
var __read20 = function(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
};
var __spreadArray5 = function(to, from, pack) {
  if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
    if (ar || !(i in from)) {
      if (!ar) ar = Array.prototype.slice.call(from, 0, i);
      ar[i] = from[i];
    }
  }
  return to.concat(ar || Array.prototype.slice.call(from));
};
var newCheckbox = function(selected, flatDataSource, flatFilteredDataSource, primaryKey, disabled, readOnly, checkStrictly, onChange) {
  return function() {
    var checked = Boolean((selected === null || selected === void 0 ? void 0 : selected.length) && (selected === null || selected === void 0 ? void 0 : selected.length) === flatDataSource.filter(function(item) {
      return !item.disabled;
    }).length);
    var indeterminate = Boolean((selected === null || selected === void 0 ? void 0 : selected.length) && !checked);
    var onInnerChange = function(e) {
      if (!readOnly) {
        var isSelected = e.target.checked;
        var usableKeys_1 = flatFilteredDataSource.filter(function(item) {
          return !item.disabled;
        }).map(function(item) {
          return item === null || item === void 0 ? void 0 : item[primaryKey];
        });
        if (getCompatibleAllSelected(selected, flatFilteredDataSource, usableKeys_1, checkStrictly, primaryKey)) {
          isSelected = false;
        }
        var newSelected = [];
        if (isSelected) {
          newSelected = __spreadArray5([], __read20(new Set(__spreadArray5(__spreadArray5([], __read20(selected), false), __read20(usableKeys_1), false))), false);
        } else {
          newSelected = selected.filter(function(key2) {
            return !usableKeys_1.includes(key2);
          });
        }
        newSelected = completedKeys(flatDataSource, newSelected, primaryKey);
        onChange === null || onChange === void 0 ? void 0 : onChange(newSelected);
      }
    };
    return import_react64.default.createElement(checkbox_default, { key: "titleAddons", disabled, checked, indeterminate, onChange: onInnerChange });
  };
};
var useTitleAddon = function(selected, flatDataSource, flatFilteredDataSource, primaryKey, mode, disabled, readOnly, checkStrictly, onChange) {
  if (mode === "single") {
    return {};
  }
  return {
    columnTitle: newCheckbox(selected, flatDataSource, flatFilteredDataSource, primaryKey, disabled, readOnly, checkStrictly, onChange)
  };
};

// node_modules/@formily/antd-v5/esm/select-table/style.js
var style_default11 = genStyleHook("select-table", function(token) {
  var _a, _b;
  var componentCls = token.componentCls;
  return _a = {}, _a[componentCls] = (_b = {}, _b["".concat(componentCls, "-search")] = {
    marginBottom: 8
  }, _b), _a;
});

// node_modules/@formily/antd-v5/esm/select-table/index.js
var __assign35 = function() {
  __assign35 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign35.apply(this, arguments);
};
var __rest15 = function(s, e) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
var __read21 = function(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
};
var __spreadArray6 = function(to, from, pack) {
  if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
    if (ar || !(i in from)) {
      if (!ar) ar = Array.prototype.slice.call(from, 0, i);
      ar[i] = from[i];
    }
  }
  return to.concat(ar || Array.prototype.slice.call(from));
};
var Search = input_default.Search;
var isColumnComponent2 = function(schema) {
  var _a;
  return ((_a = schema["x-component"]) === null || _a === void 0 ? void 0 : _a.indexOf("Column")) > -1;
};
var useSources = function() {
  var arrayField = useField();
  var schema = useFieldSchema();
  var parseSources = function(schema2) {
    var _a, _b, _c;
    if (isColumnComponent2(schema2)) {
      if (!((_a = schema2["x-component-props"]) === null || _a === void 0 ? void 0 : _a["dataIndex"]) && !schema2["name"])
        return [];
      var name_1 = ((_b = schema2["x-component-props"]) === null || _b === void 0 ? void 0 : _b["dataIndex"]) || schema2["name"];
      var field = arrayField.query(arrayField.address.concat(name_1)).take();
      var columnProps = ((_c = field === null || field === void 0 ? void 0 : field.component) === null || _c === void 0 ? void 0 : _c[1]) || schema2["x-component-props"] || {};
      var display = (field === null || field === void 0 ? void 0 : field.display) || schema2["x-display"];
      return [
        {
          name: name_1,
          display,
          field,
          schema: schema2,
          columnProps: __assign35({ title: (field === null || field === void 0 ? void 0 : field.title) || columnProps.title }, columnProps)
        }
      ];
    } else if (schema2.properties) {
      return schema2.reduceProperties(function(buf, schema3) {
        return buf.concat(parseSources(schema3));
      }, []);
    }
    return [];
  };
  var parseArrayItems = function(schema2) {
    if (!schema2)
      return [];
    var sources = [];
    var items = isArr(schema2) ? schema2 : [schema2];
    return items.reduce(function(columns, schema3) {
      var item = parseSources(schema3);
      if (item) {
        return columns.concat(item);
      }
      return columns;
    }, sources);
  };
  var validSchema = (schema === null || schema === void 0 ? void 0 : schema.type) === "array" && (schema === null || schema === void 0 ? void 0 : schema.items) ? schema.items : schema;
  return parseArrayItems(validSchema);
};
var useColumns = function(sources) {
  return sources.reduce(function(buf, _a, key2) {
    var name = _a.name, columnProps = _a.columnProps, schema = _a.schema, display = _a.display;
    if (display !== "visible")
      return buf;
    if (!isColumnComponent2(schema))
      return buf;
    return buf.concat(__assign35(__assign35({}, columnProps), { key: key2, dataIndex: name }));
  }, []);
};
var addPrimaryKey = function(dataSource, rowKey, primaryKey) {
  return dataSource.map(function(item) {
    var _a;
    var children = isArr(item.children) ? addPrimaryKey(item.children, rowKey, primaryKey) : {};
    return __assign35(__assign35(__assign35({}, item), children), (_a = {}, _a[primaryKey] = rowKey(item), _a));
  });
};
var InternalSelectTable = observer(function(props) {
  var _a, _b;
  var _c = props.mode, mode = _c === void 0 ? "multiple" : _c, propsDataSource = props.dataSource, optionAsValue = props.optionAsValue, _d = props.valueType, valueType = _d === void 0 ? "all" : _d, _e = props.showSearch, showSearch = _e === void 0 ? false : _e, filterOption = props.filterOption, filterSort = props.filterSort, onSearch = props.onSearch, searchProps = props.searchProps, className = props.className, value = props.value, onChange = props.onChange, rowSelection = props.rowSelection, _f = props.primaryKey, rowKey = _f === void 0 ? "key" : _f, otherTableProps = __rest15(props, ["mode", "dataSource", "optionAsValue", "valueType", "showSearch", "filterOption", "filterSort", "onSearch", "searchProps", "className", "value", "onChange", "rowSelection", "primaryKey"]);
  var prefixCls = usePrefixCls("formily-select-table", props);
  var _g = __read21(style_default11(prefixCls), 2), wrapSSR = _g[0], hashId = _g[1];
  var _h = __read21((0, import_react66.useState)(), 2), searchValue = _h[0], setSearchValue = _h[1];
  var field = useField();
  var loading2 = isBool(props.loading) ? props.loading : field.loading;
  var disabled = field.disabled;
  var readOnly = field.readOnly;
  var readPretty = field.readPretty;
  var _j = useSize((_a = field.decoratorProps) === null || _a === void 0 ? void 0 : _a.size, searchProps === null || searchProps === void 0 ? void 0 : searchProps.size, props === null || props === void 0 ? void 0 : props.size), searchSize = _j.searchSize, tableSize = _j.tableSize;
  var primaryKey = isFn(rowKey) ? "__formily_key__" : rowKey;
  var sources = useSources();
  var columns = useColumns(sources);
  var dataSource = isArr(propsDataSource) ? propsDataSource : field.dataSource;
  dataSource = isFn(rowKey) ? addPrimaryKey(dataSource, rowKey, primaryKey) : dataSource;
  var filteredDataSource = useFilterOptions(dataSource, searchValue, filterOption, rowSelection === null || rowSelection === void 0 ? void 0 : rowSelection.checkStrictly);
  var orderedFilteredDataSource = (0, import_react66.useMemo)(function() {
    if (!filterSort) {
      return filteredDataSource;
    }
    return __spreadArray6([], __read21(filteredDataSource), false).sort(function(a, b) {
      return filterSort(a, b);
    });
  }, [filteredDataSource, filterSort]);
  var flatDataSource = useFlatOptions(dataSource);
  var flatFilteredDataSource = useFlatOptions(filteredDataSource);
  var getWholeDataSource = function() {
    if (optionAsValue && mode === "multiple" && (value === null || value === void 0 ? void 0 : value.length)) {
      var map_1 = /* @__PURE__ */ new Map();
      var arr = __spreadArray6(__spreadArray6([], __read21(flatDataSource), false), __read21(value), false);
      arr.forEach(function(item) {
        if (!primaryKey)
          return;
        if (!map_1.has(item[primaryKey])) {
          map_1.set(item[primaryKey], item);
        }
      });
      return __spreadArray6([], __read21(map_1.values()), false);
    }
    return flatDataSource;
  };
  var selected = getUISelected(value, flatDataSource, primaryKey, valueType, optionAsValue, mode, rowSelection === null || rowSelection === void 0 ? void 0 : rowSelection.checkStrictly, rowKey);
  var readPrettyDataSource = useFilterOptions(orderedFilteredDataSource, selected, function(value2, item) {
    return primaryKey && value2.includes(item[primaryKey]);
  });
  var onInnerSearch = function(searchText) {
    var formatted = (searchText || "").trim();
    setSearchValue(searchText);
    onSearch === null || onSearch === void 0 ? void 0 : onSearch(formatted);
  };
  var onInnerChange = function(selectedRowKeys) {
    if (readOnly) {
      return;
    }
    var wholeRecords = getWholeDataSource().filter(function(item) {
      return primaryKey && selectedRowKeys.includes(item === null || item === void 0 ? void 0 : item[primaryKey]);
    });
    var _a2 = getOutputData(selectedRowKeys, wholeRecords, dataSource, primaryKey, valueType, optionAsValue, mode, rowSelection === null || rowSelection === void 0 ? void 0 : rowSelection.checkStrictly), outputValue = _a2.outputValue, outputOptions = _a2.outputOptions;
    onChange === null || onChange === void 0 ? void 0 : onChange(outputValue, outputOptions);
  };
  var onRowClick = function(record) {
    if (readPretty || disabled || readOnly || (record === null || record === void 0 ? void 0 : record.disabled)) {
      return;
    }
    var selectedRowKey = primaryKey ? record === null || record === void 0 ? void 0 : record[primaryKey] : null;
    var isSelected = selectedRowKey ? selected === null || selected === void 0 ? void 0 : selected.includes(selectedRowKey) : false;
    var selectedRowKeys = [];
    if (mode === "single" && selectedRowKey) {
      selectedRowKeys = [selectedRowKey];
    } else {
      if (isSelected) {
        selectedRowKeys = selected.filter(function(item) {
          return item !== selectedRowKey;
        });
      } else {
        selectedRowKeys = __spreadArray6(__spreadArray6([], __read21(selected), false), [selectedRowKey], false);
      }
    }
    if ((rowSelection === null || rowSelection === void 0 ? void 0 : rowSelection.checkStrictly) !== false) {
      onInnerChange(selectedRowKeys);
    } else {
      onSlacklyChange(selectedRowKeys);
    }
  };
  var onSlacklyChange = function(currentSelected) {
    var selectedRowKeys = useCheckSlackly(currentSelected, selected, flatDataSource, flatFilteredDataSource, primaryKey, rowSelection === null || rowSelection === void 0 ? void 0 : rowSelection.checkStrictly).selectedRowKeys;
    onInnerChange(selectedRowKeys);
  };
  var titleAddon = useTitleAddon(selected, flatDataSource, flatFilteredDataSource, primaryKey, mode, disabled, readOnly, rowSelection === null || rowSelection === void 0 ? void 0 : rowSelection.checkStrictly, onInnerChange);
  var modeAsType = (_b = { multiple: "checkbox", single: "radio" }) === null || _b === void 0 ? void 0 : _b[mode || ""];
  return wrapSSR(import_react66.default.createElement(
    "div",
    { className: (0, import_classnames15.default)(prefixCls, hashId) },
    showSearch ? import_react66.default.createElement(Search, __assign35({}, searchProps, { className: (0, import_classnames15.default)("".concat(prefixCls, "-search"), searchProps === null || searchProps === void 0 ? void 0 : searchProps.className), style: __assign35({ width: "100%" }, searchProps === null || searchProps === void 0 ? void 0 : searchProps.style), onSearch: onInnerSearch, onChange: function(e) {
      return onInnerSearch(e.target.value);
    }, disabled, readOnly, size: searchSize, loading: loading2 })) : null,
    import_react66.default.createElement(table_default, __assign35({}, otherTableProps, { className: (0, import_classnames15.default)("".concat(prefixCls, "-table"), className), dataSource: readPretty ? readPrettyDataSource : orderedFilteredDataSource, rowSelection: readPretty ? void 0 : __assign35(__assign35(__assign35(__assign35(__assign35({}, rowSelection), titleAddon), { getCheckboxProps: function(record) {
      var _a2;
      return __assign35(__assign35({}, (_a2 = rowSelection === null || rowSelection === void 0 ? void 0 : rowSelection.getCheckboxProps) === null || _a2 === void 0 ? void 0 : _a2.call(rowSelection, record)), { disabled: disabled || (record === null || record === void 0 ? void 0 : record.disabled) });
    } }), (rowSelection === null || rowSelection === void 0 ? void 0 : rowSelection.checkStrictly) !== false ? {} : {
      renderCell: function(checked, record, index, originNode) {
        return import_react66.default.cloneElement(originNode, {
          indeterminate: getIndeterminate(record, flatDataSource, selected, primaryKey)
        });
      }
    }), { selectedRowKeys: selected, onChange: (rowSelection === null || rowSelection === void 0 ? void 0 : rowSelection.checkStrictly) !== false ? onInnerChange : onSlacklyChange, type: modeAsType, preserveSelectedRowKeys: true, checkStrictly: true }), columns: props.columns || columns, rowKey: primaryKey, loading: loading2, size: tableSize, onRow: function(record) {
      var _a2;
      var onRowResult = (_a2 = otherTableProps.onRow) === null || _a2 === void 0 ? void 0 : _a2.call(otherTableProps, record);
      return __assign35(__assign35({}, onRowResult), { onClick: function(e) {
        var _a3;
        (_a3 = onRowResult === null || onRowResult === void 0 ? void 0 : onRowResult.onClick) === null || _a3 === void 0 ? void 0 : _a3.call(onRowResult, e);
        onRowClick(record);
      } });
    } }), ""),
    sources.map(function(column, key2) {
      if (!isColumnComponent2(column.schema))
        return;
      return import_react66.default.createElement(RecursionField, {
        name: column.name,
        schema: column.schema,
        onlyRenderSelf: true,
        key: key2
      });
    })
  ));
});
var TableColumn = function() {
  return import_react66.default.createElement(import_react66.default.Fragment, null);
};
var SelectTable = Object.assign(InternalSelectTable, {
  Column: TableColumn
});

// node_modules/@formily/antd-v5/esm/space/index.js
var import_react67 = __toESM(require_react());
var __assign36 = function() {
  __assign36 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign36.apply(this, arguments);
};
var Space = function(props) {
  var _a;
  var layout = useFormLayout();
  return import_react67.default.createElement(space_default, __assign36({ size: (_a = props.size) !== null && _a !== void 0 ? _a : layout === null || layout === void 0 ? void 0 : layout.spaceGap }, props));
};

// node_modules/@formily/antd-v5/esm/submit/index.js
var import_react69 = __toESM(require_react());
var __assign37 = function() {
  __assign37 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign37.apply(this, arguments);
};
var __rest16 = function(s, e) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
var Submit = observer(function(_a) {
  var onSubmit = _a.onSubmit, onSubmitFailed = _a.onSubmitFailed, onSubmitSuccess = _a.onSubmitSuccess, props = __rest16(_a, ["onSubmit", "onSubmitFailed", "onSubmitSuccess"]);
  var form = useParentForm();
  return import_react69.default.createElement(button_default, __assign37({ htmlType: onSubmit ? "button" : "submit", type: "primary" }, props, { loading: props.loading !== void 0 ? props.loading : form.submitting, onClick: function(e) {
    if (props.onClick) {
      if (props.onClick(e) === false)
        return;
    }
    if (onSubmit) {
      form.submit(onSubmit).then(onSubmitSuccess).catch(onSubmitFailed);
    }
  } }), props.children);
}, {
  forwardRef: true
});

// node_modules/@formily/antd-v5/esm/switch/index.js
var __assign38 = function() {
  __assign38 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign38.apply(this, arguments);
};
var Switch = connect(switch_default, mapProps({
  value: "checked"
}, function(props) {
  delete props["value"];
  return __assign38({}, props);
}));

// node_modules/@formily/antd-v5/esm/time-picker/index.js
var __assign39 = function() {
  __assign39 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign39.apply(this, arguments);
};
var mapTimeFormat = function() {
  return function(props) {
    var format = props["format"] || "HH:mm:ss";
    var onChange = props.onChange;
    return __assign39(__assign39({}, props), { format, value: dayjsable(props.value, format), onChange: function(value) {
      if (onChange) {
        onChange(formatDayjsValue(value, format));
      }
    } });
  };
};
var InternalTimePicker = connect(time_picker_default, mapProps(mapTimeFormat()), mapReadPretty(PreviewText.TimePicker));
var RangePicker2 = connect(time_picker_default.RangePicker, mapProps(mapTimeFormat()), mapReadPretty(PreviewText.TimeRangePicker));
var TimePicker2 = Object.assign(InternalTimePicker, { RangePicker: RangePicker2 });

// node_modules/@formily/antd-v5/esm/transfer/index.js
var __assign40 = function() {
  __assign40 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign40.apply(this, arguments);
};
var renderTitle = function(item) {
  var _a;
  return (_a = item.title) !== null && _a !== void 0 ? _a : null;
};
var Transfer = connect(transfer_default, mapProps({
  value: "targetKeys"
}, function(props, field) {
  var _a;
  if (isVoidField(field))
    return props;
  return __assign40(__assign40({}, props), { render: props.render || renderTitle, dataSource: ((_a = field.dataSource) === null || _a === void 0 ? void 0 : _a.map(function(item) {
    return __assign40(__assign40({}, item), { title: item.title || item.label, key: item.key || item.value });
  })) || [] });
}));

// node_modules/@formily/antd-v5/esm/tree-select/index.js
var import_react74 = __toESM(require_react());
var __assign41 = function() {
  __assign41 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign41.apply(this, arguments);
};
var TreeSelect2 = connect(tree_select_default, mapProps({
  dataSource: "treeData"
}, function(props, field) {
  return __assign41(__assign41({}, props), { suffixIcon: (field === null || field === void 0 ? void 0 : field["loading"]) || (field === null || field === void 0 ? void 0 : field["validating"]) ? import_react74.default.createElement(LoadingOutlined_default, null) : props.suffixIcon });
}), mapReadPretty(PreviewText.TreeSelect));

// node_modules/@formily/antd-v5/esm/upload/index.js
var import_react75 = __toESM(require_react());

// node_modules/@formily/antd-v5/esm/upload/placeholder.js
var UPLOAD_PLACEHOLDER = [
  {
    ext: /\.docx$/i,
    icon: "//img.alicdn.com/tfs/TB1n8jfr1uSBuNjy1XcXXcYjFXa-200-200.png"
  },
  {
    ext: /\.pptx$/i,
    icon: "//img.alicdn.com/tfs/TB1ItgWr_tYBeNjy1XdXXXXyVXa-200-200.png"
  },
  {
    ext: /\.jpe?g$/i,
    icon: "//img.alicdn.com/tfs/TB1wrT5r9BYBeNjy0FeXXbnmFXa-200-200.png"
  },
  {
    ext: /\.pdf$/i,
    icon: "//img.alicdn.com/tfs/TB1GwD8r9BYBeNjy0FeXXbnmFXa-200-200.png"
  },
  {
    ext: /\.png$/i,
    icon: "//img.alicdn.com/tfs/TB1BHT5r9BYBeNjy0FeXXbnmFXa-200-200.png"
  },
  {
    ext: /\.eps$/i,
    icon: "//img.alicdn.com/tfs/TB1G_iGrVOWBuNjy0FiXXXFxVXa-200-200.png"
  },
  {
    ext: /\.ai$/i,
    icon: "//img.alicdn.com/tfs/TB1B2cVr_tYBeNjy1XdXXXXyVXa-200-200.png"
  },
  {
    ext: /\.gif$/i,
    icon: "//img.alicdn.com/tfs/TB1DTiGrVOWBuNjy0FiXXXFxVXa-200-200.png"
  },
  {
    ext: /\.svg$/i,
    icon: "//img.alicdn.com/tfs/TB1uUm9rY9YBuNjy0FgXXcxcXXa-200-200.png"
  },
  {
    ext: /\.xlsx?$/i,
    icon: "//img.alicdn.com/tfs/TB1any1r1OSBuNjy0FdXXbDnVXa-200-200.png"
  },
  {
    ext: /\.psd?$/i,
    icon: "//img.alicdn.com/tfs/TB1_nu1r1OSBuNjy0FdXXbDnVXa-200-200.png"
  },
  {
    ext: /\.(wav|aif|aiff|au|mp1|mp2|mp3|ra|rm|ram|mid|rmi)$/i,
    icon: "//img.alicdn.com/tfs/TB1jPvwr49YBuNjy0FfXXXIsVXa-200-200.png"
  },
  {
    ext: /\.(avi|wmv|mpg|mpeg|vob|dat|3gp|mp4|mkv|rm|rmvb|mov|flv)/i,
    icon: "//img.alicdn.com/tfs/TB1FrT5r9BYBeNjy0FeXXbnmFXa-200-200.png"
  },
  {
    ext: /\.(zip|rar|arj|z|gz|iso|jar|ace|tar|uue|dmg|pkg|lzh|cab)$/i,
    icon: "//img.alicdn.com/tfs/TB10jmfr29TBuNjy0FcXXbeiFXa-200-200.png"
  },
  {
    ext: /\.[^.]+/i,
    icon: "//img.alicdn.com/tfs/TB10.R4r3mTBuNjy1XbXXaMrVXa-200-200.png"
  }
];

// node_modules/@formily/antd-v5/esm/upload/index.js
var __assign42 = function() {
  __assign42 = Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
        t[p] = s[p];
    }
    return t;
  };
  return __assign42.apply(this, arguments);
};
var __rest17 = function(s, e) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
var __read22 = function(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m) return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"])) m.call(i);
    } finally {
      if (e) throw e.error;
    }
  }
  return ar;
};
var __spreadArray7 = function(to, from, pack) {
  if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
    if (ar || !(i in from)) {
      if (!ar) ar = Array.prototype.slice.call(from, 0, i);
      ar[i] = from[i];
    }
  }
  return to.concat(ar || Array.prototype.slice.call(from));
};
var testOpts = function(ext, options) {
  if (options && isArr(options.include)) {
    return options.include.some(function(url) {
      return ext.test(url);
    });
  }
  if (options && isArr(options.exclude)) {
    return !options.exclude.some(function(url) {
      return ext.test(url);
    });
  }
  return true;
};
var getImageByUrl = function(url, options) {
  for (var i = 0; i < UPLOAD_PLACEHOLDER.length; i++) {
    if (UPLOAD_PLACEHOLDER[i].ext.test(url) && testOpts(UPLOAD_PLACEHOLDER[i].ext, options)) {
      return UPLOAD_PLACEHOLDER[i].icon || url;
    }
  }
  return url;
};
var getURL = function(target) {
  return (target === null || target === void 0 ? void 0 : target["url"]) || (target === null || target === void 0 ? void 0 : target["downloadURL"]) || (target === null || target === void 0 ? void 0 : target["imgURL"]);
};
var getThumbURL = function(target) {
  return (target === null || target === void 0 ? void 0 : target["thumbUrl"]) || (target === null || target === void 0 ? void 0 : target["url"]) || (target === null || target === void 0 ? void 0 : target["downloadURL"]) || (target === null || target === void 0 ? void 0 : target["imgURL"]);
};
var getErrorMessage = function(target) {
  return (target === null || target === void 0 ? void 0 : target.errorMessage) || (target === null || target === void 0 ? void 0 : target.errMsg) || (target === null || target === void 0 ? void 0 : target.errorMsg) || (target === null || target === void 0 ? void 0 : target.message) || (typeof (target === null || target === void 0 ? void 0 : target.error) === "string" ? target.error : "");
};
var getState = function(target) {
  if ((target === null || target === void 0 ? void 0 : target.success) === false)
    return "error";
  if ((target === null || target === void 0 ? void 0 : target.failed) === true)
    return "error";
  if (target === null || target === void 0 ? void 0 : target.error)
    return "error";
  return (target === null || target === void 0 ? void 0 : target.state) || (target === null || target === void 0 ? void 0 : target.status);
};
var normalizeFileList = function(fileList) {
  if (fileList && fileList.length) {
    return fileList.map(function(file, index) {
      return __assign42(__assign42({}, file), { uid: file.uid || "".concat(index), status: getState(file.response) || getState(file), url: getURL(file) || getURL(file === null || file === void 0 ? void 0 : file.response), thumbUrl: getImageByUrl(getThumbURL(file) || getThumbURL(file === null || file === void 0 ? void 0 : file.response), {
        exclude: [
          ".png",
          ".jpg",
          ".jpeg",
          ".gif",
          ".svg",
          ".webp",
          ".ico",
          ".icon",
          ".tif",
          ".tiff"
        ]
      }) });
    });
  }
  return [];
};
var useValidator = function(validator) {
  var field = useField();
  (0, import_react75.useEffect)(function() {
    var dispose = reaction(function() {
      return field.value;
    }, function(value) {
      var message = validator(value);
      field.setFeedback({
        type: "error",
        code: "UploadError",
        messages: message ? [message] : []
      });
    });
    return function() {
      dispose();
    };
  }, []);
};
var useUploadValidator = function(serviceErrorMessage) {
  if (serviceErrorMessage === void 0) {
    serviceErrorMessage = "Upload Service Error";
  }
  useValidator(function(value) {
    var _a, _b;
    var list = toArr(value);
    for (var i = 0; i < list.length; i++) {
      if (((_a = list[i]) === null || _a === void 0 ? void 0 : _a.status) === "error") {
        return getErrorMessage((_b = list[i]) === null || _b === void 0 ? void 0 : _b.response) || getErrorMessage(list[i]) || serviceErrorMessage;
      }
    }
  });
};
function useUploadProps(_a) {
  var serviceErrorMessage = _a.serviceErrorMessage, props = __rest17(_a, ["serviceErrorMessage"]);
  useUploadValidator(serviceErrorMessage);
  var onChange = function(param) {
    var _a2;
    (_a2 = props.onChange) === null || _a2 === void 0 ? void 0 : _a2.call(props, normalizeFileList(__spreadArray7([], __read22(param.fileList), false)));
  };
  return __assign42(__assign42({}, props), { fileList: normalizeFileList(props.fileList), onChange });
}
var getPlaceholder = function(props) {
  if (props.listType !== "picture-card") {
    return import_react75.default.createElement(
      button_default,
      null,
      import_react75.default.createElement(UploadOutlined_default, null),
      props.textContent
    );
  }
  return null;
};
var Upload = connect(function(props) {
  return import_react75.default.createElement(upload_default, __assign42({}, useUploadProps(props)), props.children || getPlaceholder(props));
}, mapProps({
  value: "fileList"
}));
var Dragger = connect(function(props) {
  return import_react75.default.createElement(
    "div",
    { className: usePrefixCls("upload-dragger") },
    import_react75.default.createElement(upload_default.Dragger, __assign42({}, useUploadProps(props)), props.children || import_react75.default.createElement(
      import_react75.default.Fragment,
      null,
      import_react75.default.createElement(
        "p",
        { className: "ant-upload-drag-icon" },
        import_react75.default.createElement(InboxOutlined_default, null)
      ),
      props.textContent && import_react75.default.createElement("p", { className: "ant-upload-text" }, props.textContent)
    ))
  );
}, mapProps({
  value: "fileList"
}));
Upload.Dragger = Dragger;
export {
  ArrayBase,
  ArrayCards,
  ArrayCollapse,
  ArrayItems,
  ArrayTable,
  ArrayTabs,
  BaseItem,
  Cascader2 as Cascader,
  Checkbox,
  DatePicker2 as DatePicker,
  Editable,
  Form,
  FormButtonGroup,
  FormCollapse,
  FormDialog,
  FormDrawer,
  FormGrid,
  FormItem,
  FormLayout,
  FormLayoutDeepContext,
  FormLayoutShallowContext,
  FormStep,
  FormTab,
  GridColumn,
  Input2 as Input,
  InternalArrayCards,
  InternalRadio,
  NumberPicker2 as NumberPicker,
  Password,
  Placeholder,
  PlaceholderContext,
  PreviewText,
  Radio,
  Reset,
  Select2 as Select,
  SelectTable,
  Space,
  Submit,
  Switch,
  TimePicker2 as TimePicker,
  Transfer,
  TreeSelect2 as TreeSelect,
  Upload,
  createFormGrid,
  useFormDeepLayout,
  useFormGrid,
  useFormItemLayout,
  useFormLayout,
  useFormShallowLayout,
  useOverflow,
  usePlaceholder
};
//# sourceMappingURL=@formily_antd-v5.js.map
