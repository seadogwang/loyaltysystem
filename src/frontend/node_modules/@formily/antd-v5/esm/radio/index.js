import { connect, mapProps, mapReadPretty } from '@formily/react';
import { Radio as AntdRadio } from 'antd';
import { PreviewText } from '../preview-text';
export var InternalRadio = connect(AntdRadio, mapProps({
    value: 'checked',
    onInput: 'onChange',
}));
var Group = connect(AntdRadio.Group, mapProps({
    dataSource: 'options',
}), mapReadPretty(PreviewText.Select));
export var Radio = Object.assign(InternalRadio, {
    __ANT_RADIO: true,
    Group: Group,
});
export default Radio;
//# sourceMappingURL=index.js.map