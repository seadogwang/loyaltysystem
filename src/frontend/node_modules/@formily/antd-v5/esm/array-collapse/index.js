var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
import { observer, RecursionField, useField, useFieldSchema, } from '@formily/react';
import { toArr } from '@formily/shared';
import { Badge, Card, Collapse, Empty, } from 'antd';
import cls from 'classnames';
import React, { Fragment, useEffect, useState } from 'react';
import ArrayBase from '../array-base';
import { usePrefixCls } from '../__builtins__';
import useStyle from './style';
var isAdditionComponent = function (schema) {
    var _a, _b;
    return ((_b = (_a = schema['x-component']) === null || _a === void 0 ? void 0 : _a.indexOf) === null || _b === void 0 ? void 0 : _b.call(_a, 'Addition')) > -1;
};
var isIndexComponent = function (schema) {
    var _a, _b;
    return ((_b = (_a = schema['x-component']) === null || _a === void 0 ? void 0 : _a.indexOf) === null || _b === void 0 ? void 0 : _b.call(_a, 'Index')) > -1;
};
var isRemoveComponent = function (schema) {
    var _a, _b;
    return ((_b = (_a = schema['x-component']) === null || _a === void 0 ? void 0 : _a.indexOf) === null || _b === void 0 ? void 0 : _b.call(_a, 'Remove')) > -1;
};
var isMoveUpComponent = function (schema) {
    var _a, _b;
    return ((_b = (_a = schema['x-component']) === null || _a === void 0 ? void 0 : _a.indexOf) === null || _b === void 0 ? void 0 : _b.call(_a, 'MoveUp')) > -1;
};
var isMoveDownComponent = function (schema) {
    var _a, _b;
    return ((_b = (_a = schema['x-component']) === null || _a === void 0 ? void 0 : _a.indexOf) === null || _b === void 0 ? void 0 : _b.call(_a, 'MoveDown')) > -1;
};
var isOperationComponent = function (schema) {
    return (isAdditionComponent(schema) ||
        isRemoveComponent(schema) ||
        isMoveDownComponent(schema) ||
        isMoveUpComponent(schema));
};
var range = function (count) { return Array.from({ length: count }).map(function (_, i) { return i; }); };
var takeDefaultActiveKeys = function (dataSourceLength, defaultOpenPanelCount) {
    if (dataSourceLength < defaultOpenPanelCount)
        return range(dataSourceLength);
    return range(defaultOpenPanelCount);
};
var insertActiveKeys = function (activeKeys, index) {
    if (activeKeys.length <= index)
        return activeKeys.concat(index);
    return activeKeys.reduce(function (buf, key) {
        if (key < index)
            return buf.concat(key);
        if (key === index)
            return buf.concat([key, key + 1]);
        return buf.concat(key + 1);
    }, []);
};
var InternalArrayCollapse = observer(function (props) {
    var _a;
    var field = useField();
    var dataSource = Array.isArray(field.value) ? field.value : [];
    var _b = __read(useState(takeDefaultActiveKeys(dataSource.length, (_a = props.defaultOpenPanelCount) !== null && _a !== void 0 ? _a : 5)), 2), activeKeys = _b[0], setActiveKeys = _b[1];
    var schema = useFieldSchema();
    var prefixCls = usePrefixCls('formily-array-collapse', props);
    var _c = __read(useStyle(prefixCls), 2), wrapSSR = _c[0], hashId = _c[1];
    useEffect(function () {
        var _a;
        if (!field.modified && dataSource.length) {
            setActiveKeys(takeDefaultActiveKeys(dataSource.length, (_a = props.defaultOpenPanelCount) !== null && _a !== void 0 ? _a : 5));
        }
    }, [dataSource.length, field]);
    if (!schema)
        throw new Error('can not found schema object');
    var renderAddition = function () {
        return schema.reduceProperties(function (addition, schema, key) {
            if (isAdditionComponent(schema)) {
                return React.createElement(RecursionField, { schema: schema, name: key });
            }
            return addition;
        }, null);
    };
    var renderEmpty = function () {
        if (dataSource.length)
            return;
        return (React.createElement(Card, { className: cls("".concat(prefixCls, "-item"), hashId, props.className) },
            React.createElement(Empty, null)));
    };
    var renderItems = function () {
        return (React.createElement(Collapse, __assign({}, props, { activeKey: activeKeys, onChange: function (keys) {
                setActiveKeys(toArr(keys).map(Number));
            }, className: cls("".concat(prefixCls, "-item"), hashId, props.className) }), dataSource.map(function (item, index) {
            var items = Array.isArray(schema.items)
                ? schema.items[index] || schema.items[0]
                : schema.items;
            if (!items)
                return null;
            var panelProps = field
                .query("".concat(field.address, ".").concat(index))
                .get('componentProps');
            var props = items['x-component-props'];
            var header = function () {
                var header = (panelProps === null || panelProps === void 0 ? void 0 : panelProps.header) || props.header || field.title;
                var path = field.address.concat(index);
                var errors = field.form.queryFeedbacks({
                    type: 'error',
                    address: "".concat(path, ".**"),
                });
                return (React.createElement(ArrayBase.Item, { index: index, record: function () { var _a; return (_a = field.value) === null || _a === void 0 ? void 0 : _a[index]; } },
                    React.createElement(RecursionField, { schema: items, name: index, filterProperties: function (schema) {
                            if (!isIndexComponent(schema))
                                return false;
                            return true;
                        }, onlyRenderProperties: true }),
                    errors.length ? (React.createElement(Badge, { size: "small", className: "errors-badge", count: errors.length }, header)) : (header)));
            };
            var extra = (React.createElement(ArrayBase.Item, { index: index, record: item }, panelProps === null || panelProps === void 0 ? void 0 :
                panelProps.extra,
                React.createElement(RecursionField, { schema: items, name: index, filterProperties: function (schema) {
                        if (!isOperationComponent(schema))
                            return false;
                        return true;
                    }, onlyRenderProperties: true })));
            var content = (React.createElement(RecursionField, { schema: items, name: index, filterProperties: function (schema) {
                    if (isIndexComponent(schema))
                        return false;
                    if (isOperationComponent(schema))
                        return false;
                    return true;
                } }));
            return (React.createElement(Collapse.Panel, __assign({}, props, panelProps, { forceRender: true, key: index, header: header(), extra: extra }),
                React.createElement(ArrayBase.Item, { index: index, key: index, record: item }, content)));
        })));
    };
    return wrapSSR(React.createElement(ArrayBase, { onAdd: function (index) {
            setActiveKeys(insertActiveKeys(activeKeys, index));
        } },
        renderEmpty(),
        renderItems(),
        renderAddition()));
});
var CollapsePanel = function (_a) {
    var children = _a.children;
    return React.createElement(Fragment, null, children);
};
CollapsePanel.displayName = 'CollapsePanel';
export var ArrayCollapse = Object.assign(ArrayBase.mixin(InternalArrayCollapse), {
    CollapsePanel: CollapsePanel,
});
ArrayCollapse.displayName = 'ArrayCollapse';
export default ArrayCollapse;
//# sourceMappingURL=index.js.map