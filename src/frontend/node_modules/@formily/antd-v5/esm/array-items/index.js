var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
import { observer, RecursionField, useField, useFieldSchema, } from '@formily/react';
import cls from 'classnames';
import React from 'react';
import { ArrayBase } from '../array-base';
import { SortableContainer, SortableElement, usePrefixCls, } from '../__builtins__';
import useStyle from './style';
var SortableItem = SortableElement(function (props) {
    var prefixCls = usePrefixCls('formily-array-items');
    var _a = __read(useStyle(prefixCls), 2), wrapSSR = _a[0], hashId = _a[1];
    return wrapSSR(React.createElement("div", __assign({}, props, { className: cls("".concat(prefixCls, "-item"), hashId, props.className) }), props.children));
});
var SortableList = SortableContainer(function (props) {
    var prefixCls = usePrefixCls('formily-array-items');
    var _a = __read(useStyle(prefixCls), 2), wrapSSR = _a[0], hashId = _a[1];
    return wrapSSR(React.createElement("div", __assign({}, props, { className: cls("".concat(prefixCls, "-list"), hashId, props.className) }), props.children));
});
var isAdditionComponent = function (schema) {
    var _a;
    return ((_a = schema['x-component']) === null || _a === void 0 ? void 0 : _a.indexOf('Addition')) > -1;
};
var useAddition = function () {
    var schema = useFieldSchema();
    return schema.reduceProperties(function (addition, schema, key) {
        if (isAdditionComponent(schema)) {
            return React.createElement(RecursionField, { schema: schema, name: key });
        }
        return addition;
    }, null);
};
var InternalArrayItems = observer(function (props) {
    var field = useField();
    var prefixCls = usePrefixCls('formily-array-items');
    var _a = __read(useStyle(prefixCls), 2), wrapSSR = _a[0], hashId = _a[1];
    var schema = useFieldSchema();
    var addition = useAddition();
    var dataSource = Array.isArray(field.value) ? field.value : [];
    if (!schema)
        throw new Error('can not found schema object');
    return wrapSSR(React.createElement(ArrayBase, null,
        React.createElement("div", __assign({}, props, { onChange: function () { }, className: cls(prefixCls, hashId, props.className) }),
            React.createElement(SortableList, { list: dataSource.slice(), className: "".concat(prefixCls, "-sort-helper"), onSortEnd: function (event) {
                    var oldIndex = event.oldIndex, newIndex = event.newIndex;
                    field.move(oldIndex, newIndex);
                } }, dataSource === null || dataSource === void 0 ? void 0 : dataSource.map(function (item, index) {
                var items = Array.isArray(schema.items)
                    ? schema.items[index] || schema.items[0]
                    : schema.items;
                return (React.createElement(ArrayBase.Item, { key: index, index: index, record: function () { var _a; return (_a = field.value) === null || _a === void 0 ? void 0 : _a[index]; } },
                    React.createElement(SortableItem, { key: "item-".concat(index), lockAxis: "y", index: index },
                        React.createElement("div", { className: "".concat(prefixCls, "-item-inner") }, items ? (React.createElement(RecursionField, { schema: items, name: index })) : null))));
            })),
            addition)));
});
var Item = function (props) {
    var prefixCls = usePrefixCls('formily-array-items');
    var _a = __read(useStyle(prefixCls), 2), wrapSSR = _a[0], hashId = _a[1];
    return wrapSSR(React.createElement("div", __assign({}, props, { onChange: function () { }, className: cls("".concat(prefixCls, "-").concat(props.type || 'card'), hashId, props.className) }), props.children));
};
export var ArrayItems = Object.assign(ArrayBase.mixin(InternalArrayItems), {
    Item: Item,
});
ArrayItems.displayName = 'ArrayItems';
export default ArrayItems;
//# sourceMappingURL=index.js.map