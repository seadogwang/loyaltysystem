import { connect, mapProps, mapReadPretty } from '@formily/react';
import { Checkbox as AntdCheckbox } from 'antd';
import { PreviewText } from '../preview-text';
var InternalCheckbox = connect(AntdCheckbox, mapProps({
    value: 'checked',
    onInput: 'onChange',
}));
var Group = connect(AntdCheckbox.Group, mapProps({
    dataSource: 'options',
}), mapReadPretty(PreviewText.Select, {
    mode: 'tags',
}));
export var Checkbox = Object.assign(InternalCheckbox, {
    __ANT_CHECKBOX: true,
    Group: Group,
});
export default Checkbox;
//# sourceMappingURL=index.js.map